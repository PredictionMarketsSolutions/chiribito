---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tools/table-3d/m9-determinism.mjs
  - tools/table-3d/m7-bloom-assert.mjs
  - tools/table-3d/m1-m2-m12.mjs
  - tools/table-3d/integrity.test.mjs
  - docs/table-3d/METRICS_ADMISSION_INTEGRITY.md
autonomous: true
requirements: [TP0b-1, TP0b-3]
must_haves:
  truths:
    - "M9 determinism proves two consecutive captures under &spin=off are byte-identical (md5 equal) — D-06, D-14"
    - "M7 bloom-absence is a code assert: grep of frontend/src/lab finds zero Bloom/EffectComposer/postprocessing AND no large bright halo in the histogram — D-06"
    - "M12 regional MSE compares a region between two frames; zero-visual-change is provable as regional MSE ~= 0 on must-not-change regions vs HEAD — D-01, D-06"
    - "M1 (px-height >= 22px + manual confirm) and M2 (cards-vs-chips area >= 2.0x, manual polygon fallback) are implemented as semi-automated metrics with an operator-confirm seam — D-06"
  artifacts:
    - path: "tools/table-3d/m9-determinism.mjs"
      provides: "md5 byte-identity check over two captures (M9)"
      contains: "md5"
    - path: "tools/table-3d/m7-bloom-assert.mjs"
      provides: "Code-assert: no Bloom/EffectComposer/postprocessing mounted (M7) + histogram halo check"
      contains: "postprocessing"
    - path: "tools/table-3d/m1-m2-m12.mjs"
      provides: "M1 px-height bbox, M2 region-area ratio, M12 regional MSE (sharp)"
      contains: "MSE"
  key_links:
    - from: "tools/table-3d/m7-bloom-assert.mjs"
      to: "frontend/src/lab"
      via: "grep for Bloom|EffectComposer|postprocessing -> 0 matches"
      pattern: "Bloom|EffectComposer|postprocessing"
    - from: "tools/table-3d/m9-determinism.mjs"
      to: "crypto.createHash('md5')"
      via: "byte-identity of two PNGs"
      pattern: "createHash"
---

<objective>
Build the integrity + identity-anchor metrics: M9 determinism (byte-identical md5 of two consecutive captures), M7 bloom-absence (a CODE assert that no `Bloom`/`EffectComposer`/`postprocessing` is mounted, plus a histogram halo check), M12 regional MSE (the zero-visual-change / regression anchor), and M1/M2 as semi-automated metrics with an operator-confirm seam (M1 rank-glyph bbox px-height >= 22px + manual; M2 cards-vs-chips area >= 2.0x with the SSOT-sanctioned manual polygon fallback). Run the meta-gate on the automatable ones and prove zero-visual-change via M12 regional MSE ~= 0 on must-not-change regions vs HEAD.

Purpose: M9 + M7 are T1 must-pass gates (determinism + anti-casino, SSOT §4.5/§5.4); M12 is the regression anchor that proves TP0 changed zero pixels and that later TPs change ONLY what they intend; M1/M2 protect the protagonist read. These complement plan 03's T1 colour metrics (separate files -> same wave). Bloom is a hard-NO (SSOT §5.4) and M7 asserts its absence.
Output: `m9-determinism.mjs`, `m7-bloom-assert.mjs`, `m1-m2-m12.mjs` (+ tests) in the tracked `tools/table-3d/`, and the M9/M7/M12 rows appended to `METRICS_ADMISSION_INTEGRITY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement M9 (determinism md5), M7 (bloom-absence code assert), and prove zero-visual-change via M12 regional MSE vs HEAD</name>
  <read_first>
    - 01-RESEARCH.md §"Code Examples" (M9 = `createHash('md5')` byte-identity of two captures; M7 = grep `src/lab` for `/Bloom|EffectComposer|postprocessing/` -> 0 matches, none installed, + histogram halo check), §"Q4" M12 row (sharp raw buffers of the legibility region + felt-mark identity region, MSE below a churn threshold; whole-frame-vs-reference = operator A/B informational only).
    - SSOT §4.5 M9 (byte-identical md5 under spin=off + reduced-motion), M7 (no Bloom mounted code assert + no large connected bright halo), M12 (regional MSE vs previous-phase tag; whole-frame informational), §5.4 (anti-casino: NO bloom ever), §5.6 (apples-to-apples determinism).
    - 01-RESEARCH.md §"Pitfall 2" (M9 fails if autoRotate/micro-motion live — harness auto-applies `&spin=off`; there is NO `useFrame` animation in the lab today — keep it that way at TP0).
  </read_first>
  <behavior>
    - Test 1 (M9): two identical PNG files -> md5 equal -> PASS; a 1-byte-altered copy -> md5 differs -> FAIL.
    - Test 2 (M7 code assert): a grep of `frontend/src/lab` for `Bloom|EffectComposer|postprocessing` returns 0 matches -> PASS; a fixture string containing `EffectComposer` -> the assert FAILS (proves the grep catches a real mount).
    - Test 3 (M12): MSE of an image against itself is 0 (-> zero-visual-change PASS); MSE against a visibly different crop is >> 0 (-> change detected).
  </behavior>
  <action>
    Create `tools/table-3d/m9-determinism.mjs` (tracked): captures the same `?cam` twice via the harness (dev server from plan 01, `&spin=off` auto) to two scratch PNGs and asserts `md5(a)===md5(b)` using `crypto.createHash('md5')`. Export a pure `m9(aPath,bPath)` for the unit test.
    Create `tools/table-3d/m7-bloom-assert.mjs` (tracked): the code assert greps `frontend/src/lab` for `/Bloom|EffectComposer|postprocessing/` and PASSES iff 0 matches (postprocessing is NOT installed — verified; TP6 adds it, not TP0). Add the secondary sharp histogram check for a large connected bright halo. Export a pure asserter for the unit test (point it at a fixture dir for the negative case).
    Create `tools/table-3d/m1-m2-m12.mjs` (tracked): implement `m12RegionalMSE(frameA, frameB, rect)` using sharp `.extract(rect).raw()` -> mean squared error over the region; and stub `m1Legibility` (rank-glyph bbox px-height, returns the measured px + a `requiresOperatorConfirm:true` flag) and `m2CardsVsChips` (region-area ratio with a manual-polygon fallback config). Bake the thresholds: M1 >= 22px, M2 >= 2.0x, M12 churn threshold (Claude's discretion for the exact MSE floor, documented).
    Prove ZERO visual change: capture the default scene (no `?stats`) at HEAD and compare must-not-change regions (felt-mark identity region + the card legibility region) with `m12RegionalMSE` -> MSE ~= 0 (within the churn floor). Since TP0 edits no scene pixels, this must hold; record the proof.
    Add `tools/table-3d/integrity.test.mjs` (`node --test`, run from ROOT) covering the 3 behaviors. Commit via `git commit -F <msgfile>`.
  </action>
  <verify>
    <automated>node --test tools/table-3d/integrity.test.mjs</automated>
    Also the M7 code-assert against the real tree: `grep -rIl "Bloom\|EffectComposer\|postprocessing" frontend/src/lab; echo "exit $? (1 = zero matches = M7 PASS)"` returns exit 1 (no matches). And the zero-visual-change M12 run prints MSE within the churn floor on the must-not-change regions.
  </verify>
  <acceptance_criteria>
    - `node --test tools/table-3d/integrity.test.mjs` passes M9 (identical PASS / 1-byte FAIL), M7 (0-match PASS / fixture-with-EffectComposer FAIL), M12 (self-MSE 0 / different crop >> 0).
    - M7 code assert on the real `frontend/src/lab` finds ZERO `Bloom|EffectComposer|postprocessing` matches (grep exit 1) -> M7 PASS; bloom is provably not mounted (anti-casino, SSOT §5.4).
    - M9 uses `crypto.createHash('md5')` byte-identity (grep finds `createHash`).
    - M12 regional MSE on must-not-change regions vs HEAD is ~= 0 (within the documented churn floor) -> zero-visual-change for TP0 proven.
    - M1 (>=22px) and M2 (>=2.0x) thresholds baked; M1 exposes a `requiresOperatorConfirm` seam (no automated OCR hard gate).
  </acceptance_criteria>
  <done>M9 determinism, M7 bloom-absence code assert, and M12 regional MSE exist and pass; zero-visual-change proven; M1/M2 stubbed with the operator-confirm seam and baked thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Run the meta-gate on M7/M9/M12 (and M1/M2 where automatable) and append the admission verdicts</name>
  <read_first>
    - 01-RESEARCH.md §"Q4 -> The meta-gate" (M9: two identical files PASS, a 1-byte-altered copy FAIL; M7: a synthetic bright-halo frame must FAIL the histogram check, normal PASS), and the note that M1 is px-check + manual, M2/M12 allow a manual-polygon/region fallback.
    - SSOT §4.5 (admit only after positive+negative control) + §8 DoD #13 (metric integrity validated against control frames).
    - 01-VALIDATION.md §"Validation Sign-Off" (M9 byte-identical; M7 code assert green; zero-visual-change via M12 regional MSE ~= 0 — these are the phase sign-off items this plan must satisfy).
  </read_first>
  <action>
    Construct the positive+negative control frames for the automatable integrity metrics and store them DOWNSCALED in `docs/table-3d/anchors/controls/` (reuse the dir created in plan 03): M9-good = two byte-identical files (PASS), M9-bad = a 1-byte-altered copy (FAIL); M7-good = the normal frame (PASS histogram), M7-bad = a synthetic bright-halo frame (FAIL); M12-good = a frame vs itself (MSE ~0 PASS), M12-bad = a frame vs a visibly different crop (MSE high FAIL).
    Run the meta-gate and APPEND the verdicts to `docs/table-3d/METRICS_ADMISSION_INTEGRITY.md` (its own integrity admission table; the T1 colour-metric table is plan 03 in METRICS_ADMISSION.md): for M7/M9/M12 record good=PASS, bad=FAIL, ADMITTED. For M1 record `informational + operator-confirm` (px-height is necessary-not-sufficient; manual confirm is plan 05). For M2 record its admission status honestly: if the automated region-area is unreliable, mark it `informational (manual polygon fallback)` per the SSOT — never silently admit a metric that can't pass its controls.
    Commit control frames + the appended admission rows via `git commit -F <msgfile>`.
  </action>
  <verify>
    <automated>node tools/table-3d/m9-determinism.mjs --meta-gate docs/table-3d/anchors/controls && node tools/table-3d/m7-bloom-assert.mjs --meta-gate docs/table-3d/anchors/controls</automated>
    Each meta-gate runner exits 0 only if its metric PASSED the good control AND FAILED the bad control. Also: `grep -n "M7\|M9\|M12" docs/table-3d/METRICS_ADMISSION_INTEGRITY.md` shows the three integrity rows with ADMITTED; `grep -n "M1\b" docs/table-3d/METRICS_ADMISSION_INTEGRITY.md` shows `operator-confirm`/`informational`.
  </verify>
  <acceptance_criteria>
    - M9 meta-gate: identical files PASS, 1-byte-altered FAIL -> ADMITTED.
    - M7 meta-gate: normal frame PASS, synthetic bright-halo FAIL -> ADMITTED (bloom would be caught).
    - M12 meta-gate: self-compare MSE ~0 PASS, different-crop MSE high FAIL -> ADMITTED.
    - M1 recorded `informational + operator-confirm` (manual legibility is plan 05); M2 recorded honestly (admitted only if its controls pass, else `informational (manual polygon fallback)`).
    - All integrity control frames committed downscaled under `docs/table-3d/anchors/controls/`.
  </acceptance_criteria>
  <done>M7/M9/M12 admitted via passing positive + failing negative controls; M1/M2 recorded with their operator/manual seam; verdicts appended to the admission doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture pipeline -> determinism gate | Non-deterministic captures would invalidate every later apples-to-apples comparison (M9). |
| lab scene code -> anti-casino invariant | A future Bloom/postprocessing mount must be caught before it ships (M7). |
| TP0 edits -> regression anchor | Any unintended pixel change must be detectable (M12 zero-visual-change). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | non-deterministic capture | mitigate | M9 byte-identity (md5) under `&spin=off` + no `useFrame` motion; meta-gated (identical PASS / 1-byte FAIL). |
| T-04-02 | Elevation (casino-drift) | a Bloom/EffectComposer mount | mitigate | M7 code assert greps `src/lab` -> 0 matches; meta-gated with a synthetic-halo negative control (SSOT §5.4 hard-NO bloom). |
| T-04-03 | Spoofing (false-pass) | M7/M9/M12 false-passing | mitigate | Each admitted via a passing positive AND a failing negative control (SSOT §4.5, §8 DoD #13). |
| T-04-04 | Tampering | undetected silent pixel change in TP0 | mitigate | M12 regional MSE ~= 0 on must-not-change regions vs HEAD proves TP0 changed zero scene pixels. |
</threat_model>

<verification>
- `node --test tools/table-3d/integrity.test.mjs` green (M9/M7/M12 good+bad).
- M7 code assert on real `frontend/src/lab` -> 0 matches (grep exit 1).
- M12 regional MSE ~= 0 on must-not-change regions vs HEAD (zero visual change).
- M9/M7/M12 meta-gates exit 0; admission verdicts recorded; M1/M2 carry the operator/manual seam.
</verification>

<success_criteria>
- M9 determinism, M7 bloom-absence, M12 regional MSE implemented and admitted via control frames.
- Zero-visual-change for TP0 proven (M12 ~= 0 on must-not-change regions).
- M1/M2 implemented with the operator-confirm / manual-polygon seam and baked thresholds.
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-04-SUMMARY.md` when done.
</output>
