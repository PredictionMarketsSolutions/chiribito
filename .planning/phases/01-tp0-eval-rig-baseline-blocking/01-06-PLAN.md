---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 06
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - docs/table-3d/anchors/head/.gitkeep
  - docs/table-3d/anchors/reference-tag/.gitkeep
  - tools/table-3d/region-rects.json
  - docs/table-3d/SCORECARD_TABLE_3D.md
  - docs/table-3d/TP0_BASELINE.md
autonomous: false
requirements: [TP0a-4, TP0a-5, TP0a-6]
must_haves:
  truths:
    - "The baseline corpus is captured at the worktree HEAD (frozen rig, post operator gate) AND at the protected tag table-3d-premium-reference-2026-06-04 (d17df37) via a throwaway detached worktree removed cleanly — the protected tag is never mutated — D-13, D-20, D-21"
    - "All anchors (HEAD x3, tag x3) are persisted DOWNSCALED to the tracked docs/table-3d/anchors/ (survives a clean checkout; corpus kept small to avoid repo bloat) — D-10, D-22"
    - "The 15-element scorecard baseline scores are filled by the operator and the draw-call/frame-time baseline is recorded in TP0_BASELINE.md — D-04, D-05"
    - "The metric region rects are finalized against the FINAL blessed frame (the POV fov the operator locked) so every admitted metric reads the correct pixels — D-06, D-23"
  artifacts:
    - path: "docs/table-3d/anchors/head/.gitkeep"
      provides: "Baseline corpus at the frozen-rig worktree HEAD (hero/card/macro, downscaled)"
    - path: "docs/table-3d/anchors/reference-tag/.gitkeep"
      provides: "Protected-tag corpus (felt/rail/chip/lighting reference; the tag has no cards)"
    - path: "tools/table-3d/region-rects.json"
      provides: "Finalized fixed pixel rects for every metric, authored on the blessed frame"
  key_links:
    - from: "docs/table-3d/anchors/reference-tag/"
      to: "table-3d-premium-reference-2026-06-04 (d17df37)"
      via: "git worktree add --detach <tag> -> capture -> git worktree remove (tag never mutated)"
      pattern: "worktree"
    - from: "tools/table-3d/region-rects.json"
      to: "docs/table-3d/anchors/head/card.png"
      via: "rects authored against the final blessed POV frame"
      pattern: "card"
---

<objective>
The IRREVERSIBLE final step of TP0a — gated on the operator blessing (plan 05). Capture the complete baseline corpus at the frozen-rig worktree HEAD AND at the protected reference tag (via a throwaway detached `git worktree`, removed cleanly so the immutable tag is never mutated), downscale and persist ALL anchors to the tracked `docs/table-3d/anchors/`, finalize the metric region rects against the FINAL blessed frame, fill the 15-element scorecard baseline scores (operator), and record the draw-call/frame-time baseline. After this the rig + baseline are LOCKED.

Purpose: SSOT §TP0 acceptance (baseline captured at HEAD + tag, persisted to a tracked location; scorecard baseline filled; draw/frame-time recorded) + §4.3 (corpus persistence — `.dev-stack/` is gitignored, anchors MUST be tracked) + §5.2 (reference never degraded). This is sequenced LAST because the baseline is irreversible and may only be captured AFTER the operator locks the POV fov (the ordering constraint, 01-CONTEXT.md / Pitfall 1).
Output: `docs/table-3d/anchors/{head,reference-tag}/{hero,card,macro}.png` (downscaled), `tools/table-3d/region-rects.json`, the filled scorecard baseline column, and the completed `TP0_BASELINE.md` draw/frame-time section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
@docs/table-3d/TP0_BASELINE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the baseline at the frozen-rig HEAD + at the protected tag (throwaway worktree), downscale, finalize region rects</name>
  <files>docs/table-3d/anchors/head/, docs/table-3d/anchors/reference-tag/, tools/table-3d/region-rects.json, docs/table-3d/TP0_BASELINE.md</files>
  <read_first>
    - 01-RESEARCH.md §"Q5 — Baseline capture at HEAD + protected tag" (the SAFE mechanism: `git worktree add --detach ../tp0-ref-tag table-3d-premium-reference-2026-06-04` -> `cd ../tp0-ref-tag/frontend && npm install` -> serve on a FREE port e.g. 5174 -> capture from root -> `git worktree remove ../tp0-ref-tag --force`; the tag is uncheckedout elsewhere so this never moves the main worktree; the `../tp0-ref-tag` path is verified FREE) + the caveat (the tag d17df37 has NO cards -> its POV is a felt/rail/chip/lighting reference, cards compare vs M1 per SSOT TP9 DoD #2).
    - 01-RESEARCH.md §"Pitfall 5" (REFERENCE frames are 2.4-4.9 MB each at 2880x1800 -> downscale committed anchors to ~1280-wide / re-encode; target the tracked corpus < ~10 MB; M1 itself uses a 1080p POV downscale so a downscaled POV anchor is on-spec) + §"Q3" (run captures from repo ROOT so playwright resolves).
    - SSOT §4.3 (persist to `docs/table-3d/anchors/`), §5.2 (protected tag immutable — never degraded), §11.6 (PowerShell quote gotcha -> `git -F`).
    - `docs/table-3d/TP0_OPERATOR_GATE.md` (plan 05): the LOCKED POV fov decision (40 or 37) — capture HEAD against whatever was locked.
  </read_first>
  <action>
    Confirm plan 05's operator gate cleared (M1 PASS + 3 shots blessed + POV fov locked) by reading `docs/table-3d/TP0_OPERATOR_GATE.md`. If the POV fov was set to 37, the edit is already on HEAD (plan 05); capture against current HEAD as-is.
    HEAD corpus: with the dev server up (`cd frontend && npm run dev`, 5173), capture the 3 money shots from the repo ROOT to a scratch dir, then DOWNSCALE (sharp, ~1280-wide PNG re-encode) and write to `docs/table-3d/anchors/head/{hero,card,macro}.png`. Record the exact captured HEAD SHA in `TP0_BASELINE.md` (do not hardcode a stale one — the live worktree HEAD has advanced past the research's bab28af).
    Protected-tag corpus (SAFE, non-mutating): `git worktree add --detach ../tp0-ref-tag table-3d-premium-reference-2026-06-04`; `cd ../tp0-ref-tag/frontend && npm install`; serve on a FREE port (5174); capture the 3 shots from the repo ROOT against `http://localhost:5174/table-lab.html?cam=...`; downscale -> `docs/table-3d/anchors/reference-tag/{hero,card,macro}.png`; then `git worktree remove ../tp0-ref-tag --force`. The tag is NEVER checked out in the main worktree and NEVER mutated (reference-never-degraded). Note in the doc that the tag has no cards -> reference-tag anchors are the felt/rail/chip/lighting basis.
    Finalize `tools/table-3d/region-rects.json`: author the fixed pixel rects for every metric (felt sample rect for M3/M5/+B, brass rect for M4, under-card/under-stack + open-felt rects for M6, corner+center rects for M8/+A, the legibility region + felt-mark identity region for M1/M12) AGAINST the FINAL blessed head/{card,hero,macro}.png (the POV fov is now locked, so the rects are stable — RESEARCH A3: author rects AFTER the gate). Re-run the admitted metrics over `docs/table-3d/anchors/head` with the finalized rects to confirm they still pass their controls.
    Commit anchors + rects via `git commit -F <msgfile>`.
  </action>
  <verify>
    <automated>node -e "const s=require('sharp');(async()=>{for(const f of ['head/hero','head/card','head/macro','reference-tag/hero','reference-tag/card','reference-tag/macro']){const m=await s('docs/table-3d/anchors/'+f+'.png').metadata();if(!m.width||m.width>1600){console.error('FAIL '+f+' missing or not downscaled');process.exit(1)}}console.log('PASS: 6 anchors present and downscaled <=1600w')})()"</automated>
    Also: `git worktree list` shows NO `tp0-ref-tag` entry (removed cleanly); `git rev-list -1 table-3d-premium-reference-2026-06-04` still equals d17df37 (tag unmutated); `tools/table-3d/region-rects.json` exists; re-running the admitted metrics over `docs/table-3d/anchors/head` still passes their controls.
  </verify>
  <acceptance_criteria>
    - 6 anchors exist (head x3 + reference-tag x3), all DOWNSCALED (<=1600w; tracked corpus small), under `docs/table-3d/anchors/` (NOT gitignored).
    - The protected tag was captured via a throwaway detached worktree that is REMOVED (`git worktree list` clean) and the tag SHA is still d17df37 (never mutated — reference-never-degraded, SSOT §5.2).
    - The exact captured HEAD SHA is recorded in `TP0_BASELINE.md` (live, not the stale research SHA).
    - `tools/table-3d/region-rects.json` finalized against the blessed frame; admitted metrics re-run over `head/` still pass their controls.
    - Anchors committed via `git -F` (no PowerShell quote gotcha).
  </acceptance_criteria>
  <done>The baseline corpus (HEAD + protected tag) is captured, downscaled, and persisted to the tracked anchors dir; the tag is untouched; region rects are finalized against the locked frame.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator fills the 15-element scorecard baseline scores + record the draw/frame-time baseline; final freeze lock</name>
  <files>docs/table-3d/SCORECARD_TABLE_3D.md, docs/table-3d/TP0_BASELINE.md</files>
  <read_first>
    - SSOT §4.4 (15-element scorecard, baseline scored at TP0, each level anchored to a concrete capture; green = >=4/5 at TP9) + §TP0 acceptance ("scorecard baseline filled; draw/frame-time baseline recorded").
    - 01-RESEARCH.md §"Q6 — 15-element scorecard" (the row-per-element format; baseline column filled by the operator at TP0) + the M10/M11 readings recorded by plan 05 (`TP0_OPERATOR_GATE.md`).
    - `docs/table-3d/SCORECARD_TABLE_3D.md` (plan 02 authored it with `_TP0_` placeholders) + `docs/table-3d/anchors/head/*` (task 1 — the frames the operator scores against).
  </read_first>
  <what-built>
    Task 1 produced the downscaled HEAD + reference-tag anchor corpus and finalized the region rects. Plan 05 recorded the M10 draw-call + M11 frame-time readings. This checkpoint asks the operator to assign the baseline 0-5 scores against the frozen frames and confirm the recorded perf numbers, then lock the rig.
  </what-built>
  <action>
    Orchestrate the baseline scoring + freeze lock: present the committed `docs/table-3d/anchors/head/*` (and the reference-tag set for shared elements) to the operator and PAUSE for the 0-5 scores. Replace every `_TP0_` placeholder in `docs/table-3d/SCORECARD_TABLE_3D.md` with the operator's numeric baseline score, transcribe the M10/M11 numbers from `TP0_OPERATOR_GATE.md` into the "Draw-call / frame-time baseline" section of `docs/table-3d/TP0_BASELINE.md`, and on operator sign-off mark the rig frozen. Commit via `git commit -F`. No scene/code change.
  </action>
  <how-to-verify>
    1. View the committed anchors `docs/table-3d/anchors/head/{hero,card,macro}.png` (and the reference-tag set for the shared elements).
    2. For each of the 15 scorecard elements (felt, cards, chips, leather rail, wood coaming, brass, body/contour, lighting, shadows, depth, composition, cameras, tactility, social-read, premium-overall) assign a baseline 0-5 score in `docs/table-3d/SCORECARD_TABLE_3D.md`, replacing the `_TP0_` placeholders.
    3. Transcribe the draw-call (M10) + frame-time (M11) baseline numbers from `TP0_OPERATOR_GATE.md` into the "Draw-call / frame-time baseline" section of `docs/table-3d/TP0_BASELINE.md` (HERO/POV/MACRO draw calls; HERO median frame-time @ RTX 4060 vsync OFF).
    4. Confirm the rig is frozen: presets verbatim, scene verbatim, anchors persisted, metrics admitted, scorecard baselined — then sign off.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/table-3d/SCORECARD_TABLE_3D.md','utf8');const n=(s.match(/_TP0_/g)||[]).length;if(n>0){console.error('FAIL: '+n+' unscored placeholders remain');process.exit(1)}console.log('PASS: all scorecard baseline scores filled (0 placeholders)')"</automated>
    Operator-perceptual scoring itself is MISSING by design (SSOT §4.4 — a human 0-5 read); the automated check above proves the operator completed the scoring (no `_TP0_` left) and the TP0_BASELINE.md perf section is filled.
  </verify>
  <acceptance_criteria>
    - All 15 scorecard rows have a numeric baseline score (no remaining `_TP0_` placeholder) — the automated check returns 0 placeholders.
    - `docs/table-3d/TP0_BASELINE.md` "Draw-call / frame-time baseline" section is filled with the M10 draw-call counts + the M11 HERO frame-time (from plan 05's on-device read).
    - The operator signs off that the rig is frozen + the baseline is complete (this is the TP0a freeze lock).
  </acceptance_criteria>
  <done>The 15-element scorecard is baselined (no placeholders left), the perf baseline is recorded, and the operator has signed off the rig freeze.</done>
  <resume-signal>Type "baseline locked" (scorecard scored + perf baseline recorded + rig frozen) or "revise: <reason>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture against protected tag -> the immutable reference | The tag must never be mutated while capturing its anchors. |
| anchor corpus -> git history | Full-res frames (2.4-4.9 MB) would permanently bloat the repo; corpus must be downscaled. |
| irreversible freeze -> the whole program | Freezing before the operator locked the fov, or against the wrong frame, invalidates every later gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | protected tag table-3d-premium-reference-2026-06-04 (d17df37) | mitigate | Captured via a throwaway `git worktree add --detach` removed with `git worktree remove --force`; the tag is never checked out in the main worktree and its SHA is re-asserted == d17df37 (reference-never-degraded, SSOT §5.2). |
| T-06-02 | Tampering | git repo size / anchor corpus | mitigate | All 6 anchors downscaled (~1280w, verify <=1600w) before commit; tracked corpus kept < ~10 MB (Pitfall 5); full-res stays in gitignored `.dev-stack/`. |
| T-06-03 | Tampering | premature/wrong-frame baseline freeze | mitigate | This plan is wave 4, gated on plan 05's operator fov-lock; region rects authored AFTER the gate against the blessed frame (ordering constraint, Pitfall 1). |
| T-06-04 | Information Disclosure | secrets in committed PNG metadata | accept | Anchors are pure render output (no backend/secrets in the lab — HANDOFF); PNGs carry no sensitive metadata. |
</threat_model>

<verification>
- 6 downscaled anchors (head x3 + reference-tag x3) persisted to the tracked `docs/table-3d/anchors/`.
- Protected-tag worktree removed cleanly; tag SHA still d17df37 (never mutated).
- Region rects finalized against the blessed frame; admitted metrics still pass their controls over `head/`.
- Scorecard baseline scores filled (no `_TP0_` left); draw/frame-time baseline recorded; rig frozen + signed off.
</verification>

<success_criteria>
- The complete baseline (HEAD + protected tag) is captured, downscaled, and persisted to a tracked location that survives a clean checkout.
- The protected reference is provably never degraded.
- The 15-element scorecard is baselined and the perf baseline recorded — the rig + baseline are LOCKED.
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-06-SUMMARY.md` when done.
</output>
