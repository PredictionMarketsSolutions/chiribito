---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tools/table-3d/metrics.mjs
  - tools/table-3d/run-metrics.mjs
  - tools/table-3d/metrics.test.mjs
  - docs/table-3d/anchors/controls/m3-felt-good.png
  - docs/table-3d/anchors/controls/m3-felt-bad.png
  - docs/table-3d/METRICS_ADMISSION.md
autonomous: true
requirements: [TP0b-1, TP0b-2]
must_haves:
  truths:
    - "The pure-pixel T1 metrics M3, M4, M5, M6, M8, +A, +B are implemented with the exact §4.5 thresholds and run from the repo ROOT on sharp — D-05, D-06"
    - "Each metric is admitted to the gate-set ONLY after it PASSES a known-good control frame AND FAILS a known-bad control frame (the red-team meta-gate); un-validated metrics are recorded informational-only — D-07"
    - "M10 draw-call ceiling reads gl.info.render.calls via ?stats (<150 hero/POV/macro; ?chips=full <220) — D-05"
    - "The metric instrument code is written into the TRACKED dir tools/table-3d/ (created in plan 01) so it survives a clean checkout, not in gitignored .dev-stack/ — D-10"
  artifacts:
    - path: "tools/table-3d/metrics.mjs"
      provides: "sharp-based region math for M3/M4/M5/M6/M8/+A/+B (luma, HSV, Lab ΔE, region sampling)"
      contains: "sharp"
    - path: "tools/table-3d/run-metrics.mjs"
      provides: "Runs the admitted metric set over a capture dir, prints a PASS/FAIL/info table"
    - path: "docs/table-3d/METRICS_ADMISSION.md"
      provides: "Per-metric: threshold, control-frame verdicts (good=PASS, bad=FAIL), admitted|informational"
      contains: "informational"
  key_links:
    - from: "tools/table-3d/metrics.mjs"
      to: "docs/table-3d/anchors/controls/"
      via: "reads positive + negative control PNGs per metric"
      pattern: "controls"
    - from: "tools/table-3d/run-metrics.mjs"
      to: "window.__labStats.calls"
      via: "M10 reads draw calls captured via ?stats"
      pattern: "calls"
---

<objective>
Build the TIER-1 pure-pixel metric kit (M3 felt-hue ΔE, M4 brass-not-gold, M5 highlight-clip, M6 contact-shadow, M8 vignette hierarchy, +A warm-corner floor, +B felt-specular extent) plus M10 (draw-call via `?stats`), all run from the repo ROOT on the pre-installed `sharp`. Each metric is gated by the red-team META-GATE: it is admitted to the gate-set ONLY after producing the expected verdict on BOTH a positive (known-good) and a negative (known-bad) control frame; otherwise it is recorded informational-only. Construct the control frames cheaply (lab debug flags + sharp tints) and record every admission verdict.

Purpose: These are the objective regression gates every later TP (TP1-TP9) runs at each money shot (SSOT §4.5). Admission via control frames is the red-team requirement that a metric cannot false-pass (SSOT §4.5 meta-gate, §8 DoD #13). Instrument code is written into the TRACKED `tools/table-3d/` dir (created in plan 01) so the rig is reproducible after a clean checkout (the same trap the anchors avoid — D-10).
Output: `tools/table-3d/metrics.mjs` + `run-metrics.mjs` (+ tests) written into the pre-existing tracked dir, the control-frame corpus written into `docs/table-3d/anchors/controls/` (the dir created in plan 01), and `docs/table-3d/METRICS_ADMISSION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the T1 pure-pixel metrics (M3/M4/M5/M6/M8/+A/+B) + M10, with the exact §4.5 thresholds, written into the tracked tools/table-3d/ created in plan 01</name>
  <read_first>
    - 01-RESEARCH.md §"Q4 — Metric tooling" (the per-metric table + sharp sketches: `.extract(rect).raw()` for region rects, `.stats()` for channel means), §"Code Examples" (M3 felt-hue ΔE via RGB->Lab; capture invocation contract), §"Don't Hand-Roll" (use sharp `.extract()`/`.raw()`/`.stats()`, NOT a hand-rolled PNG decoder).
    - 01-CONTEXT.md D-10 (instrument code persists to the TRACKED dir tools/table-3d/, NOT gitignored .dev-stack/) — plan 01 already created `tools/table-3d/` with a `.gitkeep`; this task only WRITES INTO it.
    - SSOT §4.5 table — the EXACT thresholds: M3 mean Lab ΔE < 12 from nearest of #1f9163/#147a51/#0a4a33; M4 HSV H 35-48° · S <= 0.55 · V <= 0.80; M5 luma>250 < 0.5% of felt · < 1.5% whole frame; M6 under-card/under-stack luma >= 12% darker than adjacent open felt; M8 corner luma 8-20% below center; M10 < 150 hero/POV/macro and `?chips=full` < 220; +A corner luma >= floor AND corner hue warm; +B bright-sheen fraction below a small cap AND no continuous specular sweep across felt.
    - 01-RESEARCH.md note: the M3 anchors #1f9163/#147a51/#0a4a33 ARE the literal felt gradient stops in textures.ts:437-439.
  </read_first>
  <behavior>
    - Test 1 (M3): a sharp-synthesized solid `#147a51` swatch -> M3 returns ΔE ~0 -> PASS; a solid magenta swatch -> ΔE >> 12 -> FAIL.
    - Test 2 (M4): a swatch inside H 35-48 / S<=0.55 / V<=0.80 -> PASS; a high-sat high-val gold swatch (e.g. S~0.9 V~0.95) -> FAIL.
    - Test 3 (M5): a mostly mid-luma swatch -> highlight fraction < threshold -> PASS; an all-white (luma 255) swatch -> fraction > threshold -> FAIL.
    - Test 4 (M8): a frame whose corners are 12% darker than center -> in 8-20% band -> PASS; a flat uniform frame -> < 8% -> FAIL (no framing).
    - Test 5 (region helper): `.extract(rect)` over a known synthetic image returns the expected mean RGB (the region-sampling primitive is correct).
  </behavior>
  <action>
    Write the instrument code into the TRACKED `tools/table-3d/` dir that plan 01 already created with a `.gitkeep` (NOT `.dev-stack/`, which is gitignored — D-10: the instrument code must survive a clean checkout; do NOT re-create the dir, just write the files into it). Implement `tools/table-3d/metrics.mjs` exporting one pure function per metric over a PNG path + a region-rect config: `m3FeltHue`, `m4BrassNotGold`, `m5HighlightClip`, `m6ContactShadow`, `m8Vignette`, `aWarmCorner`, `bFeltSpecular`, plus an `m10DrawCall` reader (consumes the `window.__labStats.calls` value captured via `?stats`). Use `sharp(path).extract({left,top,width,height}).raw().toBuffer({resolveWithObject:true})` for region buffers and a small RGB->HSV + RGB->Lab + ΔE helper (~20 lines; or `.stats()` for channel means). Bake the EXACT §4.5 thresholds as named constants. Region rects are fixed pixel boxes on the 2880x1800 frame (or the 1080p downscale for M1's region) — define placeholders now; they are authored precisely against the FINAL frozen frame in plan 06 (after the operator picks the POV fov), so keep rects in a single config object that plan 06 can finalize.
    Add `tools/table-3d/metrics.test.mjs` (run with `node --test`, or vitest from root config — Claude's discretion; it must run from ROOT where sharp resolves) covering the 5 behaviors using sharp-synthesized swatches (no GPU capture needed for the unit tests).
    Create `tools/table-3d/run-metrics.mjs`: takes a capture dir, runs the ADMITTED metric set, prints a PASS/FAIL/informational table. Do NOT install sharp/playwright (already at root). Run everything with cwd = repo root.
  </action>
  <verify>
    <automated>node --test tools/table-3d/metrics.test.mjs</automated>
    Also: `node -e "require.resolve('sharp')"` from root exits 0 (metrics resolve sharp at root); `tools/table-3d/metrics.mjs` and `run-metrics.mjs` exist; `git check-ignore tools/table-3d/metrics.mjs` returns exit 1 (TRACKED, not gitignored).
  </verify>
  <acceptance_criteria>
    - `node --test tools/table-3d/metrics.test.mjs` passes all 5 behaviors (M3/M4/M5/M8 PASS-on-good + FAIL-on-bad, region helper correct).
    - Each metric encodes the EXACT §4.5 threshold as a named constant (grep finds `12` for M3 ΔE, `35`/`48`/`0.55`/`0.80` for M4, `250`/`0.5`/`1.5` for M5, `8`/`20` for M8, `150`/`220` for M10).
    - Metrics resolve `sharp` from the repo ROOT; no `npm install sharp` anywhere (grep package.json files -> no new sharp entry).
    - `tools/table-3d/` is TRACKED (git check-ignore exit 1) — instrument written into the pre-existing wave-1 dir; survives clean checkout.
  </acceptance_criteria>
  <done>The T1 pure-pixel metrics + M10 reader are implemented from root on sharp, written into the pre-existing tracked tools/table-3d/ dir, with the exact baked thresholds and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Construct positive+negative control frames per metric and run the meta-gate (admit or mark informational)</name>
  <read_first>
    - 01-RESEARCH.md §"Q4 -> The meta-gate (positive + negative CONTROL FRAME validation)" — the cheap construction recipes: M3 `?felt=magenta` (TableLab.tsx:365) renders pure magenta felt -> M3 must FAIL, normal felt -> PASS; M4 a sharp-tinted high-sat high-val gold crop of the brass rect -> FAIL, real brass -> PASS; M5 a sharp `.linear()` over-exposed crop -> FAIL, normal -> PASS; M8 normal frame in-band -> PASS, a flattened frame -> FAIL.
    - SSOT §4.5 ("A metric is admitted to the gate-set ONLY after it produces the expected result on a known-good AND a known-bad control frame ... until validated it is informational") + §8 DoD #13 (metric integrity).
    - 01-RESEARCH.md §"Pitfall 5" (downscale committed control frames — full-res is 2.4-4.9 MB each; keep the tracked corpus small).
  </read_first>
  <action>
    For EACH T1 metric, construct a positive (known-good) and a negative (known-bad) control frame and store them DOWNSCALED in the TRACKED `docs/table-3d/anchors/controls/` dir (created in plan 01 — write into it, do NOT re-create it; e.g. `m3-felt-good.png`/`m3-felt-bad.png`, `m4-brass-good.png`/`m4-brass-bad.png`, ...). Construct cheaply:
    - Positive controls: the real frozen baseline frame (or an in-spec crop) captured via the harness (dev server from plan 01).
    - Negative controls: lab debug flags where available (`?felt=magenta` for M3's bad felt) + sharp transforms for the rest (`.tint()` to gold for M4-bad; `.linear(a,b)` to overexpose for M5-bad; flatten corners for M8-bad; a synthetic bright halo for +A/+B bad cases as applicable).
    Run the meta-gate: for every metric assert `metric(positive)==PASS AND metric(negative)==FAIL`. Record the result per metric in `docs/table-3d/METRICS_ADMISSION.md` as a table: metric | §4.5 threshold | good-frame verdict | bad-frame verdict | ADMITTED | informational. A metric that does NOT produce PASS-on-good + FAIL-on-bad is marked **informational-only** (NOT admitted to the gate-set) — never silently dropped.
    Commit control frames + the admission doc via `git commit -F <msgfile>`. Keep the tracked controls corpus small (downscaled).
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs --meta-gate docs/table-3d/anchors/controls</automated>
    The meta-gate runner must exit 0 only when every metric it admits passed its positive control AND failed its negative control; it prints `ADMITTED` or `informational` per metric. Also: `grep -c "ADMITTED\|informational" docs/table-3d/METRICS_ADMISSION.md` has a row per T1 metric (>=8: M3,M4,M5,M6,M8,+A,+B,M10); each control PNG exists under `docs/table-3d/anchors/controls/`.
  </verify>
  <acceptance_criteria>
    - Every T1 metric has BOTH a positive and a negative control frame committed (downscaled) under `docs/table-3d/anchors/controls/` (the pre-existing wave-1 dir).
    - The meta-gate run proves: each admitted metric PASSES its good frame AND FAILS its bad frame.
    - `METRICS_ADMISSION.md` records per-metric verdicts; any metric failing the meta-gate is marked `informational` (not admitted), never dropped silently.
    - The negative control for M3 uses `?felt=magenta` (a real lab hook), proving the gate catches a genuine out-of-palette felt.
    - Control corpus is downscaled/small (no 2880x1800 raw frames committed).
  </acceptance_criteria>
  <done>Every T1 metric is admitted (or explicitly marked informational) via a passing positive control + a failing negative control; the admission verdicts are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metric script -> gate decision | A metric that false-passes would let a regression (or a casino-drift) ship undetected at a later TP. |
| control-frame corpus -> git history | Control PNGs must be small (downscaled) or they bloat the repo. |
| instrument code location | If placed in gitignored `.dev-stack/` the gate is unreproducible after a clean checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing (false-pass) | each §4.5 metric | mitigate | Task 2 meta-gate: a metric is admitted ONLY if it PASSES a known-good AND FAILS a known-bad control frame; failures -> informational-only (SSOT §4.5, §8 DoD #13). |
| T-03-02 | Tampering | git repo size / control corpus | mitigate | Control frames committed DOWNSCALED to `docs/table-3d/anchors/controls/` (Pitfall 5); no full-res 2880x1800 frames. |
| T-03-03 | Repudiation | unreproducible instrument | mitigate | Instrument code written into the TRACKED `tools/table-3d/` dir created in plan 01 (git check-ignore exit 1), not gitignored `.dev-stack/` (D-10). |
| T-03-04 | Tampering | duplicate/typosquat tooling install | mitigate | No new package installed — sharp resolves at root (pre-existing, audited in 01-RESEARCH Package Legitimacy Audit); scripts run with cwd=root. |
</threat_model>

<verification>
- `node --test tools/table-3d/metrics.test.mjs` green (M3/M4/M5/M8 good+bad + region helper).
- `node tools/table-3d/run-metrics.mjs --meta-gate ...` exits 0; every admitted metric passed good + failed bad.
- `METRICS_ADMISSION.md` records admitted vs informational per metric.
- `tools/table-3d/` is tracked (dir created in plan 01); no sharp install added.
</verification>

<success_criteria>
- T1 pure-pixel metrics + M10 implemented with exact §4.5 thresholds, running from root on sharp.
- Every T1 metric admitted (or marked informational) via the positive+negative control-frame meta-gate.
- Instrument code reproducible from a clean checkout (written into the tracked tools/table-3d/ dir).
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-03-SUMMARY.md` when done.
</output>
</content>
