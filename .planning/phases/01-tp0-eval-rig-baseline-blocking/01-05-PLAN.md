---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - docs/table-3d/TP0_OPERATOR_GATE.md
  - frontend/src/lab/TableLab.tsx
autonomous: false
requirements: [TP0.0, TP0a-2, TP0a-3]
must_haves:
  truths:
    - "The operator confirms on-device that the cards read as the unmistakable protagonist (M1 precondition) BEFORE any TP1+ materiality work — this BLOCKING gate must PASS or the program halts — D-17, D-18"
    - "The operator blesses the 3 money shots as canonical AND decides the POV fov (40 vs 37) — the ONE allowed TP0 refinement, applied as the LAST reversible edit before the freeze — D-02, D-19"
    - "The operator confirms M1 legibility (hole-card rank glyphs razor-legible at POV @1080p, >= 22px) — D-06"
    - "The operator-assisted M11 frame-time read on the real RTX 4060 (vsync OFF, ?stats) is < 8 ms at HERO — D-06"
  artifacts:
    - path: "docs/table-3d/TP0_OPERATOR_GATE.md"
      provides: "The operator gate record: M1 verdict, money-shot blessing, POV fov decision, M1-legibility + M11 readings"
      contains: "POV"
  key_links:
    - from: "docs/table-3d/TP0_OPERATOR_GATE.md"
      to: "frontend/src/lab/TableLab.tsx"
      via: "the POV fov decision (40 keep, or apply 37 at line 618) recorded then enacted before the freeze"
      pattern: "fov"
---

<objective>
The TP0 OPERATOR PERCEPTUAL GATE — the BLOCKING human checkpoints that cannot be automated. The operator, on-device on the real RTX 4060: (1) confirms the M1 cards-as-protagonist read (TP0.0 — the program's blocking precondition); (2) blesses the 3 money shots as canonical and decides the POV fov (keep 40 OR apply the 37 tighten — the ONE allowed TP0 refinement); (3) confirms M1 legibility at POV; (4) gives the operator-assisted M11 frame-time read. This plan PAUSES for the operator at each checkpoint; its outcome unlocks the irreversible baseline freeze (plan 06).

Purpose: SSOT §TP0 perceptual gate + TP0.0 precondition (red-team #12) + §5.7 stop-on-ambiguous. The fov decision is the LAST reversible edit before the irreversible freeze (the ordering constraint, 01-CONTEXT.md). M1 (legibility) and M11 (on-device frame-time) are the two metrics the SSOT explicitly routes through the operator (px-height is necessary-not-sufficient; headless frame-time is only indicative).
Output: `docs/table-3d/TP0_OPERATOR_GATE.md` recording every verdict, and — IF the operator chooses 37 — the single `TableLab.tsx:618` fov edit applied (reversible, pre-freeze).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
@frontend/src/lab/TableLab.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: TP0.0 — operator confirms the M1 cards-as-protagonist read on-device (BLOCKING precondition)</name>
  <files>docs/table-3d/TP0_OPERATOR_GATE.md</files>
  <read_first>
    - SSOT §TP0 "TP0.0 — M1 precondition gate (red-team #12): operator confirms the M1 cards-as-protagonist read on-device BEFORE any materiality work. If it fails -> stop before TP1." + §3 (cards = absolute protagonist) + §5.4 (anti-casino identity).
    - 01-VALIDATION.md §"Manual-Only Verifications" (TP0.0 — subjective on-device perceptual judgement, the program's BLOCKING precondition).
    - 01-RESEARCH.md §"Q1" (the lab default view is `?cam=card` POV — the M1 shot; `TableLab.tsx:615` default).
  </read_first>
  <what-built>
    The frozen lab scene at the player-POV money shot, captured deterministically (plans 02-04 produced the harness, the `?stats` overlay, and the POV captures). This checkpoint does NOT build pixels — it asks the operator to render a perceptual verdict on the CURRENT table before any materiality work begins.
  </what-built>
  <action>
    Orchestrate the TP0.0 BLOCKING precondition gate: ensure the dev server is up, present the live lab to the operator on-device, and PAUSE for the operator's M1 cards-as-protagonist verdict. Record the PASS/FAIL verdict + a one-line reason in `docs/table-3d/TP0_OPERATOR_GATE.md` (create it if absent). On FAIL, HALT the program before TP1 (do not proceed to any later plan or materiality work); on PASS, allow the gate sequence to continue. This task writes only the gate record — no scene/code change.
  </action>
  <how-to-verify>
    1. With the dev server running (`cd frontend && npm run dev`), open `http://localhost:5173/table-lab.html?cam=card` on-device on the RTX 4060 (live, not a screenshot — the IDE preview cannot render WebGL anyway).
    2. Also open `?cam=hero` and `?cam=macro` for context.
    3. Judge ONE perceptual question: do the cards (la Perla [10O,7O] hole + the [1E,12C,11B] board) read as the UNMISTAKABLE protagonist of the table — the eye lands on the cards first, they dominate, they are razor-legible — WITHOUT any casino/Vegas energy?
    4. Record PASS or FAIL with a one-line reason in `docs/table-3d/TP0_OPERATOR_GATE.md`.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator on-device perceptual gate, by design (SSOT §TP0 TP0.0). Validated by the recorded PASS/FAIL verdict in docs/table-3d/TP0_OPERATOR_GATE.md (grep for "M1: PASS" or "M1: FAIL").</automated>
  </verify>
  <acceptance_criteria>
    - The operator records an explicit PASS or FAIL for the M1 cards-as-protagonist read in `TP0_OPERATOR_GATE.md`.
    - On FAIL: the program HALTS here (BLOCKING — never proceed to TP1 on a failed M1; the fix is to refine the M1 cards, surfaced to the operator, NOT to push forward). No baseline freeze, no TP1.
    - On PASS: the gate is cleared and plan continues to the money-shot blessing.
  </acceptance_criteria>
  <done>The operator's M1 cards-as-protagonist verdict is recorded; on PASS the gate is cleared, on FAIL the program is halted before TP1.</done>
  <resume-signal>Type "M1 approved" (cards are the clear protagonist) or "M1 failed: <reason>" (halts the program before TP1).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator blesses the 3 money shots + decides the POV fov (keep 40 OR apply 37) — the ONE allowed TP0 refinement</name>
  <files>docs/table-3d/TP0_OPERATOR_GATE.md, frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - SSOT §4.1 (POV "fov tightened 40->37 ... the operator may revert to 40 as the ONE allowed TP0 preset refinement, before any baseline is captured") + §TP0 perceptual gate ("operator blesses the 3 money shots ... incl. POV fov 37 vs 40. One refinement allowed, then locked").
    - 01-CONTEXT.md §"Ordering constraint" (the fov refinement is the LAST reversible edit BEFORE the irreversible freeze; the SSOT allows it ONLY before any baseline is captured).
    - 01-RESEARCH.md §"Q1" (POV/card fov is **40** in code at `TableLab.tsx:618` — the 37 is UN-APPLIED; HERO 32 @ 620, MACRO 26 @ 623 are already correct).
  </read_first>
  <decision>
    Are the 3 money shots (HERO fov 32 / POV fov ? / MACRO fov 26) blessed as the canonical, frozen-forever views for the whole program — and is the POV fov kept at 40 or tightened to 37?
  </decision>
  <context>
    Cameras are frozen at TP0 and reused verbatim at every gate (TP1-TP9) and at the final TP9 verdict — there is no mid-program re-baseline (SSOT §4 note). This is the last chance to set framing. The POV fov is the only allowed refinement: 40 (current code, wider) vs 37 (tighter, less near-card stretch, protagonist still large). Whatever is chosen is enacted NOW and then locked; the baseline freeze (plan 06) captures against it.
  </context>
  <options>
    <option id="keep-40">
      <name>Keep POV fov 40 (current code, no edit)</name>
      <pros>Zero edit; the scene stays exactly as captured in plans 02-04; widest protagonist context.</pros>
      <cons>Slightly more near-card perspective stretch at the grazing POV angle.</cons>
    </option>
    <option id="apply-37">
      <name>Tighten POV fov 40 -> 37 (single edit at TableLab.tsx:618)</name>
      <pros>Reduces near-card stretch while keeping the protagonist large (the SSOT's recommended candidate).</pros>
      <cons>One reversible edit; the plans 02-04 POV captures must be re-taken at 37 before the freeze (handled in plan 06).</cons>
    </option>
  </options>
  <action>
    Present both POV framings to the operator on-device (capture `?cam=card` at 40 and, if requested, temporarily at 37 to compare). Record the blessing of all 3 shots + the POV fov decision in `docs/table-3d/TP0_OPERATOR_GATE.md`. IF the operator chooses 37: apply the single edit `fov: 40 -> 37` on `TableLab.tsx:618` (the `card` preset only — do NOT touch hero/macro), commit it reversibly (`git commit -F`), and note that plan 06 must re-capture the POV anchor at 37. IF keep-40: no code edit; record "POV locked at 40." Either way the POV fov is now LOCKED — no further camera change for the rest of the program.
  </action>
  <verify>
    <automated>MISSING — operator canonical-view choice, by design (SSOT §4.1). Validated by the recorded fov decision in docs/table-3d/TP0_OPERATOR_GATE.md AND `grep -n "fov: 37\|fov: 40" frontend/src/lab/TableLab.tsx` matching the recorded decision on the card preset (line ~618), with hero(32)/macro(26) unchanged.</automated>
  </verify>
  <acceptance_criteria>
    - `TP0_OPERATOR_GATE.md` records: the 3-shot blessing + the explicit POV fov decision (40 kept or 37 applied).
    - IF 37 chosen: `grep -n "fov: 37" frontend/src/lab/TableLab.tsx` shows the `card` preset at 37 (line ~618) and hero(32)/macro(26) are UNCHANGED; the edit is a single reversible commit.
    - IF 40 kept: `TableLab.tsx:618` still reads `fov: 40` and the doc records "POV locked at 40, no edit."
    - The decision is recorded as FINAL — cameras are frozen for TP1-TP9 (no mid-program re-baseline).
  </acceptance_criteria>
  <done>The 3 money shots are blessed and the POV fov is locked (40 kept or 37 applied at line 618); the decision is recorded and final.</done>
  <resume-signal>Select: "keep 40" or "apply 37" (then the 3 shots are blessed and the POV fov is locked).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms M1 legibility (POV @1080p, >= 22px) + gives the M11 on-device frame-time read</name>
  <files>docs/table-3d/TP0_OPERATOR_GATE.md</files>
  <read_first>
    - SSOT §4.5 M1 (rank-glyph bbox >= 22px on a 1080p downscale of POV + operator manual "legible" confirm — NO automated OCR hard gate) and M11 (median frame-time < 8 ms at HERO on the RTX 4060, vsync OFF, full `?fx`; the on-device read IS the gate).
    - 01-RESEARCH.md §"Pattern 1 -> vsync caveat" (headless frame-time is indicative; the authoritative M11 read is the operator running the live lab with `?stats` on-device) + §"Q4" M11 row (operator-assisted).
    - 01-VALIDATION.md §"Manual-Only Verifications" (M1 legibility confirm; M11 frame-time on the real RTX 4060).
  </read_first>
  <what-built>
    The `?stats` overlay (plan 02) exposes `window.__labStats` (draw calls + frame-time) without drawing on-canvas; M1's px-height stub (plan 04) measures the rank-glyph bbox. This checkpoint collects the two readings the SSOT routes through the operator.
  </what-built>
  <action>
    Orchestrate the operator-assisted M1-legibility + M11 read: ensure the dev server + `?stats` are available on the RTX 4060, present the POV (M1) and HERO+`?stats` (M11) views, and PAUSE for the operator's readings. Record M1 legibility PASS/FAIL + the measured px-height, the M11 HERO median frame-time, and (for completeness) the automatable M10 draw-call counts in `docs/table-3d/TP0_OPERATOR_GATE.md`. Flag any M1 fail (BLOCKING) or M11 over-budget (perf-guardrail). No scene/code change in this task.
  </action>
  <how-to-verify>
    1. M1 legibility: view `?cam=card` at the final blessed POV fov on-device; confirm the hole-card rank glyphs (Sota 10O + 7O) are razor-legible at a 1080p POV downscale. Cross-check the px-height stub reports >= 22px. Record PASS/FAIL + the measured px.
    2. M11 frame-time: open `?cam=hero` with `?stats` on the live RTX 4060 (vsync OFF); read the median frame-time from `window.__labStats` (DevTools console: `window.__labStats`). Record the value; PASS if < 8 ms.
    3. M10 cross-check (automatable, recorded here for completeness): the draw-call count from `?stats` at HERO/POV/MACRO < 150 (and `?chips=full` < 220).
    4. Record all readings in `docs/table-3d/TP0_OPERATOR_GATE.md`.
  </how-to-verify>
  <verify>
    <automated>MISSING — M1 manual confirm + M11 on-device RTX 4060 read, by design (SSOT §4.5). The M10 portion IS automatable: `node tools/table-3d/run-metrics.mjs` reading window.__labStats.calls returns < 150 at HERO/POV/MACRO; M1/M11 validated by the recorded readings in docs/table-3d/TP0_OPERATOR_GATE.md.</automated>
  </verify>
  <acceptance_criteria>
    - `TP0_OPERATOR_GATE.md` records: M1 legibility PASS/FAIL + measured px-height (>= 22px expected); M11 median frame-time at HERO (< 8 ms expected) read on the real RTX 4060 with `?stats`, vsync OFF.
    - M10 draw-call readings recorded (< 150 hero/POV/macro; `?chips=full` < 220) — this one IS automatable via `?stats` and can be re-run headless.
    - On a clear M1-legibility FAIL or an M11 over-budget: flag it in the gate record (M1 fail is BLOCKING per TP0.0 spirit; M11 over-budget is a perf-guardrail flag).
  </acceptance_criteria>
  <done>M1 legibility confirmed (>=22px + manual) and M11 frame-time + M10 draw-calls recorded on the real GPU; any failure flagged.</done>
  <resume-signal>Type "legibility + frametime approved" with the recorded px-height + ms, or "flagged: <reason>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator perceptual judgement -> irreversible freeze | A wrong/ambiguous gate verdict would lock a bad baseline forever (or skip the M1 precondition). |
| POV fov decision -> frozen cameras | The fov is the last allowed edit; getting it wrong contaminates every TP1-TP9 capture. |
| headless frame-time -> M11 gate | Headless frame-time is only indicative; the real-GPU read is the honest gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | un-recorded perceptual verdicts | mitigate | Every verdict (M1, money-shot blessing, fov, M1-legibility, M11) is written to `TP0_OPERATOR_GATE.md` with a reason — an auditable record, stop-on-ambiguous (SSOT §5.7). |
| T-05-02 | Tampering | premature/irreversible baseline | mitigate | This gate is sequenced BEFORE plan 06's freeze; the fov edit (if any) is a reversible single commit applied here, then locked (ordering constraint). |
| T-05-03 | Spoofing (false-pass) | M11 read from headless instead of real GPU | mitigate | M11 gate is the operator's on-device RTX 4060 `?stats` read (vsync OFF), not the indicative headless number (SSOT §4.5). |
| T-05-04 | Elevation | proceeding past a failed M1 precondition | mitigate | M1 FAIL halts the program (BLOCKING, SSOT §TP0 TP0.0) — no freeze, no TP1; escalates to refine M1 cards. |
</threat_model>

<verification>
- M1 precondition: operator PASS recorded (or program halted on FAIL).
- 3 money shots blessed; POV fov decision recorded + enacted (40 kept or 37 applied at line 618), then locked.
- M1 legibility confirmed (>= 22px + manual); M11 on-device frame-time recorded (< 8 ms); M10 draw-calls recorded (< 150).
- All verdicts in `docs/table-3d/TP0_OPERATOR_GATE.md`.
</verification>

<success_criteria>
- The BLOCKING operator gate is cleared: M1 cards-as-protagonist PASS, 3 shots blessed, POV fov locked, M1-legibility + M11 read on the real GPU.
- The irreversible baseline freeze (plan 06) is now unlocked.
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-05-SUMMARY.md` when done.
</output>
