---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/src/lab/TableLab.tsx
  - frontend/src/lab/StatsProbe.tsx
  - frontend/src/lab/StatsProbe.test.ts
  - docs/table-3d/SCORECARD_TABLE_3D.md
  - docs/table-3d/TP0_BASELINE.md
autonomous: true
requirements: [TP0a-2, TP0a-3, TP0a-5]
must_haves:
  truths:
    - "A ?stats overlay reads gl.info.render.calls + frame-time and writes them to window.__labStats while rendering NOTHING on-canvas (zero pixels changed) — D-03, D-11"
    - "When ?stats is absent the scene is byte-for-byte unchanged vs HEAD (M9 holds, zero visual change) — D-01, D-12"
    - "The 3 frozen presets are recorded verbatim (HERO fov 32 / POV/card fov 40 with the 37 candidate flagged as an operator-gate refinement / MACRO fov 26) + the Perla hand + demoted pot — D-02"
    - "The 15-element AAA scorecard exists with the rubric (0-5) and a baseline column awaiting operator scores — D-04"
  artifacts:
    - path: "frontend/src/lab/StatsProbe.tsx"
      provides: "Dependency-free useThree+useFrame draw-call + frame-time reader; returns null"
      contains: "window.__labStats"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "15-element 0-5 rubric + baseline column + anchor-shot mapping"
      contains: "premium-overall"
    - path: "docs/table-3d/TP0_BASELINE.md"
      provides: "Frozen presets recorded verbatim (the 'Frozen presets' section filled)"
  key_links:
    - from: "frontend/src/lab/StatsProbe.tsx"
      to: "window.__labStats"
      via: "useFrame writes gl.info.render.calls + median frame-time"
      pattern: "__labStats"
    - from: "frontend/src/lab/TableLab.tsx"
      to: "StatsProbe"
      via: "mount only when qp('stats') !== null"
      pattern: "qp\\(\"stats\"\\)"
---

<objective>
Build the `?stats` instrumentation and freeze the rig's human-readable record — both ZERO visual change. Add a dependency-free `StatsProbe` that reads `gl.info.render.calls` (M10) + a frame-time sample (M11) and writes them to `window.__labStats` WITHOUT drawing anything on-canvas (renders `null`), mounted ONLY when `?stats` is present. Record the 3 frozen presets verbatim (flagging the POV fov-40-vs-37 operator-gate refinement, NOT silently baking 37) and author the 15-element AAA scorecard with an empty baseline column for the operator.

Purpose: M10/M11 need a draw-call + frame-time read that does not perturb the frame (drei `<Stats>` draws a visible widget -> breaks M9 / zero-visual-change). The frozen-preset record + scorecard are TP0a deliverables (SSOT §4.4, §TP0 acceptance) that everything downstream measures against.
Output: `StatsProbe.tsx` (+ test), a `?stats`-gated mount in `TableLab.tsx`, the filled "Frozen presets" section of `TP0_BASELINE.md`, and `SCORECARD_TABLE_3D.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
@frontend/src/lab/TableLab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dependency-free ?stats StatsProbe (renders null) and a ?stats-gated mount</name>
  <read_first>
    - 01-RESEARCH.md §"Pattern 1: dependency-free ?stats overlay" + §"Q2 — ?stats overlay" (use `useThree((s)=>s.gl)` + `useFrame`; read `gl.info.render.calls` INSIDE useFrame because `gl.info.autoReset` defaults true; write to `window.__labStats`; return `null`; the harness reads it via `page.evaluate`). NOT drei `<Stats>` (draws a visible FPS widget -> breaks M9).
    - frontend/src/lab/TableLab.tsx: the `qp()` helper (lines 63-68), the `<Canvas>` + `<Scene>` mount (lines 757-768), `<Lights>` (548-578). The renderer `gl` is reachable in any Canvas child via `useThree`.
    - SSOT §4.5 M10 (draw-call ceiling <150 hero; `?chips=full` <220), M11 (frame-time <8ms HERO RTX4060 vsync OFF), §5.1 (measure via renderer.info + a frame-time read).
  </read_first>
  <behavior>
    - Test 1 (zero-visual-change contract): StatsProbe's React output is `null` (it renders no element / no mesh) — assert the component returns null so no pixel is drawn.
    - Test 2 (data contract): after a simulated frame, `window.__labStats` is an object exposing a numeric `calls` field (draw calls) and a frame-time field (e.g. `medianFrameMs` / `frameMs`).
    - Test 3 (gating): `TableLab` mounts `<StatsProbe/>` only when `qp("stats") !== null`; with `?stats` absent the scene tree contains no StatsProbe.
  </behavior>
  <action>
    Create `frontend/src/lab/StatsProbe.tsx`: a component that calls `useThree((s)=>s.gl)`, and inside `useFrame` reads `gl.info.render.calls` and samples `performance.now()` deltas across a small rolling window to compute a median/last frame-time, writing `{ calls, medianFrameMs }` (names at Claude's discretion, but include draw-calls + a frame-time) to `window.__labStats`. It MUST `return null` — render NOTHING on-canvas (Pitfall 3: any on-canvas overlay contaminates the capture + breaks M9). Do NOT use drei `<Stats>`/`<PerfMonitor>`.
    Wire it into `TableLab.tsx`: mount `<StatsProbe/>` inside the Canvas/Scene ONLY when `qp("stats") !== null`. The default (captured) scene — `?stats` absent — must be byte-for-byte identical to HEAD; do not move/alter any existing element. Add NO geometry, NO light, NO material (anti-casino + zero-visual-change). Keep `StatsProbe` rendering null so there is no felt-y=0 coplanar geometry risk (Pitfall: z-fight sunburst).
    Add `frontend/src/lab/StatsProbe.test.ts` (vitest) covering the 3 behaviors above (null render, the `window.__labStats` shape, the `?stats` gating). Mock `useThree`/`useFrame` from `@react-three/fiber` as needed (happy-dom env per vite.config.ts).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/StatsProbe.test.ts</automated>
    Also: `grep -n "return null" frontend/src/lab/StatsProbe.tsx` matches (probe renders nothing); `grep -c "Stats\b\|PerfMonitor" frontend/src/lab/StatsProbe.tsx` via `grep -v '^//'` is 0 (drei widget not used); `grep -n "qp(\"stats\")" frontend/src/lab/TableLab.tsx` shows the gated mount.
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lab/StatsProbe.test.ts` passes all 3 behaviors.
    - `StatsProbe.tsx` returns `null` (proven by grep + the null-render test) — zero pixels drawn.
    - `window.__labStats` exposes a numeric draw-call field + a frame-time field.
    - `StatsProbe` mounts ONLY when `qp("stats") !== null`; no drei `<Stats>`/`<PerfMonitor>` import (grep, comments stripped, == 0).
    - No new geometry/light/material added to the default scene; no `postprocessing`/`Bloom` import introduced (M7 stays green).
  </acceptance_criteria>
  <done>A `?stats`-gated, null-rendering draw-call + frame-time reader exists; the default captured scene is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove zero-visual-change for ?stats vs default + record the frozen presets verbatim + author the 15-element scorecard</name>
  <read_first>
    - 01-RESEARCH.md §"Q1 — Lab scene reality" (HERO fov 32 @ line 620, POV/card fov **40** @ line 618 — the 37 is an UN-APPLIED candidate, MACRO fov 26 @ line 623; `LAB_COMMUNITY=[1E,12C,11B]`, `LAB_HOLE=[10O,7O]` @ lines 60-61; demoted pot `group[2.7,0,1.5] scale 0.66` @ 700-708) and §"Q6 — 15-element scorecard" (the proposed `SCORECARD_TABLE_3D.md` skeleton).
    - SSOT §4.1 (the 3 money shots; POV "tightened 40->37 ... operator may revert to 40 as the ONE allowed TP0 refinement, before any baseline is captured"), §4.2 (frozen scene + "no face-down card added to the Perla hand"), §4.4 (15-element scorecard, baseline scored at TP0, green = >=4/5 at TP9).
    - 01-CONTEXT.md §"Ordering constraint" (the fov refinement is an OPERATOR decision applied as the LAST reversible edit before the irreversible freeze — do NOT bake 37 here).
  </read_first>
  <action>
    Prove the `?stats` overlay changes zero pixels: with the dev server up (plan 01), capture `?cam=card` WITHOUT `?stats` and WITH `?stats` to two gitignored scratch PNGs, then md5-compare them (`crypto.createHash('md5')` from root) — they MUST be byte-identical (the StatsProbe renders null, so the captured frame is unchanged; this is the M9 / zero-visual-change proof for the overlay). If they differ, the probe is drawing something — fix it to render null. Capture twice each and confirm determinism too (`&spin=off` auto-applied by the harness).
    Fill the "Frozen presets (verbatim)" section of `docs/table-3d/TP0_BASELINE.md` EXACTLY as the code is today: HERO `?cam=hero` pos[1.2,5.0,8.2] target[0,0.5,0] **fov 32**; POV `?cam=card` pos[0,4.7,10.6] target[0,0.25,1.2] **fov 40 (code today)** — explicitly note "candidate refinement to fov 37 is an OPERATOR-gate decision (plan 05), applied as the LAST reversible edit before the baseline freeze; do NOT bake 37 now"; MACRO `?cam=macro` pos[-1.7,1.7,2.4] target[-1.55,0.05,1.05] **fov 26**. Record the frozen scene verbatim: `LAB_COMMUNITY=[1E,12C,11B]`, `LAB_HOLE=[10O,7O]` (la Perla, all 5 FACE-UP), demoted pot `group[2.7,0,1.5] scale 0.66`, and the invariant "no face-down/6th card added to the Perla hand."
    Author `docs/table-3d/SCORECARD_TABLE_3D.md`: the 15 elements (felt, cards, chips, leather rail, wood coaming, brass, body/contour, lighting, shadows, depth, composition, cameras, tactility, social-read, premium-overall), each a row with a 0-5 rubric (0=absent / 3=acceptable / 5=AAA anchored to a concrete capture description), a BASELINE column left as `_TP0_` placeholders for the operator to fill (plan 05/06), a TARGET column `>=4`, and an anchor-shot mapping (hero/card/macro). Reference the anchor paths `docs/table-3d/anchors/head/{hero,card,macro}.png`.
    Commit docs via `git commit -F <msgfile>` (no double-quotes-in-PowerShell-here-string gotcha).
  </action>
  <verify>
    <automated>node -e "const{createHash}=require('crypto'),{readFileSync}=require('fs');const m=p=>createHash('md5').update(readFileSync(p)).digest('hex');const a=m(process.argv[1]),b=m(process.argv[2]);if(a!==b){console.error('FAIL: ?stats changed pixels');process.exit(1)}console.log('PASS: ?stats zero-visual-change (md5 identical)')" .dev-stack/diag/table-3d/tp0-smoke/card-nostats.png .dev-stack/diag/table-3d/tp0-smoke/card-stats.png</automated>
    Also: `grep -c "^|" docs/table-3d/SCORECARD_TABLE_3D.md` shows >=15 element rows; `grep -n "fov 32\|fov 40\|fov 26" docs/table-3d/TP0_BASELINE.md` shows all three recorded; `grep -n "37" docs/table-3d/TP0_BASELINE.md` shows 37 is flagged as an operator-gate candidate (not baked into code).
  </verify>
  <acceptance_criteria>
    - md5(`?cam=card` without `?stats`) == md5(`?cam=card` with `?stats`) -> the overlay changes ZERO pixels (M9 / zero-visual-change proven for the StatsProbe).
    - `TP0_BASELINE.md` records all 3 presets verbatim incl. POV **fov 40 (code)** with the fov-37 candidate explicitly flagged as the operator's LAST reversible refinement (not baked).
    - `TP0_BASELINE.md` records the frozen scene (community [1E,12C,11B], hole [10O,7O] face-up, demoted pot group[2.7,0,1.5] scale 0.66) + the no-6th-card invariant.
    - `SCORECARD_TABLE_3D.md` has all 15 elements, a 0-5 rubric, a baseline column of `_TP0_` placeholders, target `>=4`, and the anchor-shot mapping.
  </acceptance_criteria>
  <done>`?stats` proven zero-pixel; the 3 presets frozen verbatim (37 left as an operator decision); the 15-element scorecard authored with an empty baseline column.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?stats` instrumentation -> captured frame | The probe must add zero pixels or it contaminates anchors + breaks M9. |
| TableLab default scene -> regression baseline | Any incidental edit to the default view would silently break the zero-visual-change invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `StatsProbe.tsx` rendering into the canvas | mitigate | Probe returns `null` (grep + null-render test); Task 2 md5-proves `?stats` vs default are byte-identical -> zero pixels added. |
| T-02-02 | Tampering | `TableLab.tsx` default scene | mitigate | StatsProbe mounted ONLY under `qp("stats")`; no geometry/light/material added; M9 byte-identity confirms the default frame is unchanged vs HEAD. |
| T-02-03 | Spoofing (false-pass) | a metric reading a contaminated frame | mitigate | The overlay's zero-pixel proof (md5 identical) guarantees later metric reads operate on an uncontaminated frame. |
| T-02-04 | Information Disclosure | drei `<Stats>` widget leaking into a shipped/captured image | mitigate | drei `<Stats>`/`<PerfMonitor>` explicitly NOT used (grep == 0); dependency-free `gl.info` read instead. |
</threat_model>

<verification>
- `npx vitest run src/lab/StatsProbe.test.ts` green (null render + data shape + gating).
- md5(`?cam=card` no-stats) == md5(`?cam=card` stats) -> overlay is zero-pixel.
- 3 presets recorded verbatim in TP0_BASELINE.md (POV fov 40 + 37-candidate flag).
- 15-element scorecard authored with empty baseline column.
</verification>

<success_criteria>
- A dependency-free, null-rendering `?stats` reader exposes draw-calls + frame-time on `window.__labStats`.
- The default captured scene is byte-identical with vs without `?stats` (zero visual change proven).
- Frozen presets + frozen scene recorded verbatim; fov-37 left as an operator-gate decision.
- The 15-element AAA scorecard exists awaiting baseline scores.
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-02-SUMMARY.md` when done.
</output>
