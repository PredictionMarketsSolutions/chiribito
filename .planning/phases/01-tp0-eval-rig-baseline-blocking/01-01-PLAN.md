---
phase: 01-tp0-eval-rig-baseline-blocking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/node_modules/.package-lock.json
  - .dev-stack/lab-shot.mjs
  - docs/table-3d/anchors/.gitkeep
  - docs/table-3d/anchors/controls/.gitkeep
  - tools/table-3d/.gitkeep
  - docs/table-3d/TP0_BASELINE.md
autonomous: true
requirements: [TP0a-1]
must_haves:
  truths:
    - "The Vite dev server starts and serves /table-lab.html (frontend deps installed) — D-08"
    - "The Playwright harness captures the 3 money shots on the real D3D11 GPU with ERRORS [] and a non-empty 2880x1800 PNG — D-09, D-13"
    - "A per-phase rollback tag tp0-before-rig exists at the pre-edit HEAD — D-15"
    - "The lab is provably NOT in the prod build (no table-lab input in vite.config.ts) — D-16"
    - "The TRACKED corpus + instrument dirs (docs/table-3d/anchors/, docs/table-3d/anchors/controls/, tools/table-3d/) all exist with a .gitkeep so wave-3 metric plans WRITE INTO pre-existing dirs (no parallel creator->writer race) — D-10"
  artifacts:
    - path: ".dev-stack/lab-shot.mjs"
      provides: "Restored capture harness (Playwright, D3D11) — gitignored, lives in worktree"
    - path: "docs/table-3d/TP0_BASELINE.md"
      provides: "The frozen-rig record skeleton (env facts, harness contract, isolation proof, rollback tag)"
    - path: "docs/table-3d/anchors/.gitkeep"
      provides: "Tracked anchor corpus directory (NOT gitignored)"
    - path: "docs/table-3d/anchors/controls/.gitkeep"
      provides: "Tracked control-frame corpus directory (NOT gitignored) — wave-3 plans 03/04 write good/bad control PNGs here"
    - path: "tools/table-3d/.gitkeep"
      provides: "Tracked metric-instrument directory (NOT gitignored) — wave-3 plans 03/04 write metric .mjs here (survives a clean checkout)"
  key_links:
    - from: ".dev-stack/lab-shot.mjs"
      to: "http://localhost:5173/table-lab.html"
      via: "LAB_URL env -> Playwright page.goto -> page.screenshot"
      pattern: "LAB_URL"
---

<objective>
TP0a Wave-0 foundation: make the eval rig RUNNABLE and reversible before any instrument code or capture exists. Install the frontend deps so the Vite dev server boots, restore the gitignored Playwright capture harness into the worktree, smoke-verify it captures the 3 money shots on the real GPU, create the TRACKED corpus + instrument directories that the wave-3 metric plans WRITE INTO, cut the per-phase rollback tag, and re-assert the lab stays OUT of the prod build. ZERO visual change — this plan adds no scene code and captures nothing to the tracked anchor corpus yet.

Purpose: Every later TP0 task (StatsProbe, metrics, baseline freeze) depends on a booting dev server + a working harness. This closes the Wave-0 gaps RESEARCH flagged (01-RESEARCH.md "Wave 0 Gaps") and establishes reversibility (SSOT §5.3, rollback disposition = BLOCKING for TP0). It also owns the creation of ALL tracked rig directories (`docs/table-3d/anchors/`, `docs/table-3d/anchors/controls/`, `tools/table-3d/`) so the parallel wave-3 metric plans (03 ‖ 04) only WRITE INTO pre-existing dirs and never race on directory creation (D-10).
Output: Booting dev server, restored `.dev-stack/lab-shot.mjs`, the `docs/table-3d/anchors/` + `docs/table-3d/anchors/controls/` + `tools/table-3d/` tracked dirs, the `TP0_BASELINE.md` record skeleton, and the `tp0-before-rig` rollback tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-CONTEXT.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-RESEARCH.md
@.planning/phases/01-tp0-eval-rig-baseline-blocking/01-VALIDATION.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install frontend deps + restore the capture harness + create the tracked rig directories (anchors, anchors/controls, tools)</name>
  <read_first>
    - 01-RESEARCH.md §"Runtime State Inventory", §"Environment Availability", §"Q3 — Capture harness internals" (the harness is ONE file: `.dev-stack/lab-shot.mjs`, imports playwright + node builtins only).
    - SSOT §4.3 (capture pipeline + corpus persistence) and §11.3 (capture commands).
    - CLAUDE.md (repo layout — game root vs frontend; the lab lives in frontend/src/lab/).
    - 01-CONTEXT.md D-10 (anchor corpus + instrument code persist to TRACKED locations docs/table-3d/anchors/ + tools/table-3d/, NOT gitignored .dev-stack/).
  </read_first>
  <action>
    Run `cd frontend && npm install` (frontend/node_modules exists but is STALE/EMPTY — three/r3f/drei/react/vitest do NOT resolve; the dev server will not start otherwise). Do NOT add `sharp` or `playwright` to frontend — both already resolve from the repo ROOT (verified 0.34.5 + ^1.60.0); metric scripts and the harness run from root cwd.
    Restore the harness: copy `.dev-stack/lab-shot.mjs` from the MAIN checkout (`C:\Users\Usuario\Documents\CHIRIBITO\chiri-infrastructure\chiri-app\.dev-stack\lab-shot.mjs`) into this worktree's `.dev-stack/lab-shot.mjs`. It is gitignored (intentional — stays in the worktree, never committed). If the copy fails, the documented fallback is to run all captures from the main checkout instead.
    Create ALL THREE TRACKED rig directories, each with a `.gitkeep` (this plan OWNS directory creation so the parallel wave-3 metric plans 03 ‖ 04 only WRITE INTO pre-existing dirs — D-10): (a) the anchor corpus dir `docs/table-3d/anchors/`; (b) the control-frame corpus dir `docs/table-3d/anchors/controls/` (plans 03 + 04 write good/bad control PNGs here); (c) the metric-instrument dir `tools/table-3d/` (plans 03 + 04 write metric .mjs here). Verify each is NOT gitignored (`git check-ignore` returns exit 1 for all three). These are where the corpus + instruments will be committed later (NOT `.dev-stack/`, which is gitignored).
    Do NOT capture or commit any PNG in this task — the baseline freeze is sequenced LAST (plan 06), after the operator gate.
  </action>
  <verify>
    <automated>cd frontend && node -e "require.resolve('three');require.resolve('@react-three/fiber');require.resolve('react');console.log('frontend deps OK')"</automated>
    Also assert from repo ROOT: `node -e "require.resolve('sharp');require.resolve('playwright');console.log('root tooling OK')"` exits 0; `.dev-stack/lab-shot.mjs` exists; all three of `docs/table-3d/anchors/.gitkeep`, `docs/table-3d/anchors/controls/.gitkeep`, `tools/table-3d/.gitkeep` exist and each returns exit 1 from `git check-ignore` (NOT ignored).
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm install` completes; three/@react-three/fiber/react resolve inside frontend/node_modules.
    - sharp + playwright resolve from repo ROOT (no new install added anywhere).
    - `.dev-stack/lab-shot.mjs` present in the worktree (restored).
    - All three tracked rig dirs exist with a `.gitkeep` and are NOT gitignored (exit 1 from git check-ignore): `docs/table-3d/anchors/`, `docs/table-3d/anchors/controls/`, `tools/table-3d/`.
    - No new `sharp`/`playwright` entry added to frontend/package.json (grep finds none).
  </acceptance_criteria>
  <done>Dev server can boot, harness restored, all three tracked rig dirs created (so wave-3 plans write into pre-existing dirs), no duplicate tooling installed.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-verify the harness on the 3 money shots + cut the rollback tag + re-assert prod-build isolation</name>
  <read_first>
    - 01-RESEARCH.md §"Q3 — Capture harness internals" (flags `--use-angle=d3d11 --enable-gpu`, viewport 1440x900 DPR2 -> 2880x1800, auto `&spin=off`, prints CANVAS/GPU/ERRORS, `preserveDrawingBuffer:true` at TableLab.tsx:762 makes the canvas readable).
    - SSOT §5.3 (per-phase rollback tag BEFORE edits; TP0 disposition = BLOCKING) and §5.8 + §11.6 (lab stays out of prod build; PowerShell here-string quote gotcha -> use `git -F` / git-bash).
    - frontend/vite.config.ts (verified: NO `rollupOptions.input` / no `table-lab` build entry — isolation holds; re-assert it stays true).
  </read_first>
  <action>
    Start the dev server (`cd frontend && npm run dev`; Vite on 5173, 5174 if busy) in the background. From the repo ROOT (so Node resolves root's playwright), smoke-capture the 3 money shots to the GITIGNORED scratch dir `.dev-stack/diag/table-3d/tp0-smoke/` (NOT the tracked anchors dir — this is a throwaway verification, not the baseline):
    `LAB_URL="http://localhost:5173/table-lab.html?cam=hero" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp0-smoke/hero.png` and likewise `?cam=card` -> pov.png and `?cam=macro` -> macro.png.
    Assert each run prints `ERRORS []` (the harness captures console + pageerror via `page.on`) and the output PNG is 2880x1800 and non-empty. The IDE/preview screenshot tool CANNOT capture live WebGL (times out) — only this Playwright path works.
    Cut the per-phase rollback tag at the current pre-edit HEAD: `git tag tp0-before-rig` (SSOT §5.3). Do NOT push it.
    Re-assert isolation: grep `frontend/vite.config.ts` for `table-lab` / `rollupOptions` / `input` -> there must be ZERO lab build input (the lab is dev-server-only; the game's vanilla entries are the only prod build). Record the proof in `docs/table-3d/TP0_BASELINE.md`.
    Create `docs/table-3d/TP0_BASELINE.md` as the frozen-rig record skeleton: capture the worktree HEAD SHA (record it, do not hardcode a stale one), the protected tag `table-3d-premium-reference-2026-06-04` -> `d17df37`, the harness contract (URL + flags + 2880x1800), the isolation proof, and a placeholder section "Frozen presets (verbatim)" + "Draw-call / frame-time baseline" to be filled by plans 02 and 06. Commit doc + tag via `git commit -F <msgfile>` (NEVER double quotes inside a PowerShell here-string).
  </action>
  <verify>
    <automated>cd frontend && grep -L "table-lab\|rollupOptions\|input" vite.config.ts && echo "ISOLATION OK: no lab build input"</automated>
    Also: each smoke PNG is 2880x1800 (read dimensions via `node -e` + sharp metadata from root, or `Read` the PNG); the harness printed `ERRORS []` for all three; `git tag -l tp0-before-rig` lists the tag; `docs/table-3d/TP0_BASELINE.md` exists with the isolation proof + recorded HEAD SHA.
  </verify>
  <acceptance_criteria>
    - All 3 smoke captures exit 0, print `ERRORS []`, and produce 2880x1800 non-empty PNGs on the D3D11 GPU.
    - `grep -L` confirms `frontend/vite.config.ts` contains NONE of `table-lab` / `rollupOptions` / `input` -> lab provably out of the prod build.
    - `git tag -l tp0-before-rig` returns the tag (rollback point established).
    - `docs/table-3d/TP0_BASELINE.md` records: worktree HEAD SHA, protected tag SHA d17df37, harness contract, isolation proof.
    - Smoke PNGs live ONLY in gitignored `.dev-stack/` (NOT in `docs/table-3d/anchors/`) — no anchor committed yet.
  </acceptance_criteria>
  <done>Harness proven on the real GPU at all 3 shots, rollback tag cut, prod-build isolation re-asserted and recorded, baseline record skeleton committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> git history | Committing artifacts: risk of bloating the repo or committing the gitignored harness/scratch frames. |
| lab dev code -> prod `vite build` | The lab must never leak into the shippable bundle (pre-release design exposure). |
| this worktree -> protected reference tag | The immutable tag must never be mutated; this plan only READS its SHA. |
| wave-1 dir creator -> wave-3 metric writers | This plan creates the tracked dirs the parallel wave-3 plans (03 ‖ 04) write into; missing dirs would race under parallel execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `frontend/vite.config.ts` | mitigate | Task 2 grep-asserts no `table-lab`/`rollupOptions`/`input` -> lab stays dev-server-only, out of the prod build (SSOT §5.8). |
| T-01-02 | Tampering | git repo size / `.dev-stack/` scratch | mitigate | Smoke PNGs written ONLY to gitignored `.dev-stack/`; no PNG committed in this plan; the three tracked rig dirs get only a `.gitkeep` each. |
| T-01-03 | Tampering | protected tag `d17df37` | accept | This plan only reads the tag SHA (`git rev-list`); no checkout/mutation. Reference-never-degraded (SSOT §5.2) preserved by construction. |
| T-01-04 | Tampering | wave-3 parallel directory race | mitigate | This plan (wave 1) creates ALL tracked rig dirs (`docs/table-3d/anchors/`, `.../controls/`, `tools/table-3d/`) with a `.gitkeep`; the parallel wave-3 plans 03 ‖ 04 only WRITE INTO pre-existing dirs -> no creator->writer ordering assumption between same-wave plans (D-10). |
| T-01-SC | Tampering | npm install (`cd frontend && npm install`) | mitigate | No NEW package introduced — installs the pre-existing, committed frontend/package.json deps only (slopcheck N/A per 01-RESEARCH Package Legitimacy Audit). sharp/playwright NOT added (already at root). |
</threat_model>

<verification>
- Dev server boots and serves `/table-lab.html`; harness captures 3 shots with `ERRORS []`, 2880x1800.
- All three tracked rig dirs (`docs/table-3d/anchors/`, `docs/table-3d/anchors/controls/`, `tools/table-3d/`) exist with a `.gitkeep` and are NOT gitignored.
- `tp0-before-rig` rollback tag exists at pre-edit HEAD.
- Prod-build isolation re-asserted (no `table-lab` input in vite.config.ts) and recorded in TP0_BASELINE.md.
- Zero tracked PNG committed (baseline freeze deferred to plan 06, post operator gate).
</verification>

<success_criteria>
- `frontend` deps resolve; sharp/playwright resolve at ROOT; no duplicate tooling installed.
- `.dev-stack/lab-shot.mjs` restored; all three tracked rig dirs created (anchors, anchors/controls, tools/table-3d) so wave-3 plans write into pre-existing dirs.
- 3 money shots smoke-captured cleanly on the D3D11 GPU.
- `tp0-before-rig` tag cut; isolation proof + HEAD SHA recorded in `docs/table-3d/TP0_BASELINE.md`.
</success_criteria>

<output>
Create `.planning/phases/01-tp0-eval-rig-baseline-blocking/01-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
