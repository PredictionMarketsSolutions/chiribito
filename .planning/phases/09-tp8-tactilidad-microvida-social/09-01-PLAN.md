---
phase: 09-tp8-tactilidad-microvida-social
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP8 — sub-threshold micro-motion on hero objects only (hole cards + top chip): amplitude < 0.01wu / < 0.5°, settle 0.2–0.4s, idle 6–12s"
  - "SSOT §TP8 — ALL micro-motion freezes under prefers-reduced-motion AND harness spin=off → M9 determinism HARD gate"
  - "SSOT §TP8 — no flip/spin/glow/bouncy easing (no-FX); restrained stillness beats visible motion"
  - "SSOT §TP8 — additive over TP2 dealt-variance base pose (store restY at mount); no base-pose overwrite"

must_haves:
  truths:
    - "TableLab.tsx declares four named constants: MICRO_AMPLITUDE_Y = 0.003, MICRO_AMPLITUDE_ROT = 0.004, MICRO_IDLE_PERIOD = 9.0, MICRO_SETTLE_TAU = 0.25"
    - "A HeroMotion component exists in TableLab.tsx using a single useFrame; returns null (no geometry)"
    - "motionFrozen = isFrozen || reducedMotion is declared in Scene() and passed as the frozen prop to HeroMotion"
    - "reducedMotion reads matchMedia('(prefers-reduced-motion: reduce)').matches once at mount (mount-static, not a listener)"
    - "The useFrame body begins with 'if (frozen) return' — no position mutation when frozen"
    - "restY is captured for each ref in a useEffect at mount so breathing is additive over the TP2 base pose"
    - "M9 PASS: two consecutive captures with spin=off are byte-identical (md5 match)"
    - "cd frontend && npx vitest run src/lab/ exits 0 (all existing tests green)"
    - "cd frontend && npx tsc --noEmit exits 0 (no new type errors under src/lab)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "HeroMotion component + four MICRO_* named constants + dual freeze guard (isFrozen || reducedMotion)"
      contains: "MICRO_AMPLITUDE_Y"
  key_links:
    - from: "HeroMotion (TableLab.tsx)"
      to: "holeCardGroupRef + topChipGroupRef (TableLab.tsx)"
      via: "useFrame mutates position.y and rotation.x via exponential decay settle"
      pattern: "MICRO_AMPLITUDE_Y"
    - from: "motionFrozen (Scene() in TableLab.tsx)"
      to: "HeroMotion frozen prop"
      via: "isFrozen || reducedMotion — M9 determinism gate"
      pattern: "motionFrozen"
---

<objective>
Implement sub-threshold micro-life on the two hero objects (hole-card group + demoted-pot group)
via a single HeroMotion component in TableLab.tsx. The motion is a breathing stillness — a very-low-
frequency, near-zero-amplitude sine-on-idle with exponential-decay settle — that sells weight without
ever being consciously perceived as motion. It is gated behind a hard dual freeze: prefers-reduced-motion
AND the harness spin=off capture flag, making M9 byte-identical captures the hard gate.

Purpose: TP8 is the FEEL phase. The table is geometrically and materially complete from TP1-TP7.
This plan adds the one remaining ingredient: the micro-life that transforms the scene from a still-life
photograph into a table caught mid-play. It is the last code change in TP8 — plans 09-02 and 09-03
only audit and gate what this plan ships.

Output: TableLab.tsx with HeroMotion (four named constants + dual freeze + additive breathing);
M9 PASS (frozen captures byte-identical); vitest green; tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-CONTEXT.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HeroMotion component + dual freeze guard to TableLab.tsx</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx — Read these regions before writing:
        (a) Lines 1-30: existing imports (THREE, useFrame, useRef, useEffect, React) — confirm which
            are already imported so no duplicate import is added.
        (b) Lines 1065-1085: the isFly / isFrozen declarations in Scene() — confirm the exact variable
            names and the qp("spin") call form. isFrozen MUST already be in scope; do not redeclare it.
        (c) Lines 1185-1210: the demoted accent pot group — confirm the exact position prop and that
            no ref prop is currently attached. The target is the group at position=[3.0, 0, 1.5] (or
            similar per source) with scale={0.5} containing the InstancedChipStack columns.
        (d) Lines 1240-1260: the hole-card CardGroup — confirm the exact JSX form so the wrapping
            group ref is attached correctly without disrupting the CardGroup props.
        (e) Lines 1290-1310: the location just before EffectComposer (or at the end of Scene JSX
            content) — this is where to mount HeroMotion.
        (f) tools/table-3d/grep-check-tp7-08.cjs lines 1-40 — confirm the stripComments() convention
            (lines starting with "//") so the four constants and the freeze guard are written outside
            comments and will pass the grep-check authored in plan 09-02.
  </read_first>
  <action>
    The entire implementation is a single logical block. Work top-to-bottom in TableLab.tsx:

    STEP 1 — Add the four named MICRO_* constants (at module scope, above Scene(), alongside
    existing top-level constants such as FELT_REST_Y, CHIP_ROT_SEED, etc.):

      const MICRO_AMPLITUDE_Y   = 0.003;  // world units — SSOT < 0.01wu; err LESS (30% of ceiling)
      const MICRO_AMPLITUDE_ROT = 0.004;  // radians    — SSOT < 0.00873 rad (0.5°); err LESS (46%)
      const MICRO_IDLE_PERIOD   = 9.0;    // seconds    — SSOT 6-12s idle; midpoint
      const MICRO_SETTLE_TAU    = 0.25;   // seconds    — SSOT 0.2-0.4s settle
      const CHIP_PHASE_OFFSET   = Math.PI * 0.7; // non-harmonic offset between the two hero objects

    These five constants are the ONLY new module-level additions from TP8. They must appear outside
    any function, comment block, or JSX — plain top-level const declarations.

    STEP 2 — Add the applyMicroBreath helper function (at module scope, below the constants):

    function applyMicroBreath(
      obj:      THREE.Group | null,
      t:        number,
      delta:    number,
      phase:    number,
      restYVal: number,
    ): void {
      if (!obj) return;
      const cycle    = (2 * Math.PI * t) / MICRO_IDLE_PERIOD;
      const targetY  = restYVal + MICRO_AMPLITUDE_Y   * Math.sin(cycle + phase);
      const targetRot = MICRO_AMPLITUDE_ROT * Math.sin(cycle + phase + 0.6);
      // Exponential settle — frame-rate-independent; zero overshoot (no bouncy easing).
      // Equivalent to maath/easing.damp but inline — no new package.
      const alpha = 1 - Math.exp(-delta / MICRO_SETTLE_TAU);
      obj.position.y += (targetY  - obj.position.y) * alpha;
      obj.rotation.x += (targetRot - obj.rotation.x) * alpha;
    }

    This function uses += (additive) — it does NOT overwrite position.y or rotation.x
    to an absolute value. This is the critical difference that preserves the TP2 dealt-variance
    base pose (Pitfall 6 in RESEARCH.md). The targetY formula includes restYVal (the card's
    actual rest Y, not 0) so the breathing bob is centred on the real base position.

    STEP 3 — Add the HeroMotion interface and component (at module scope, below applyMicroBreath):

    interface HeroMotionProps {
      holeRef:    React.RefObject<THREE.Group | null>;
      topChipRef: React.RefObject<THREE.Group | null>;
      frozen:     boolean; // isFrozen || reducedMotion — M9 HARD gate
    }

    function HeroMotion({ holeRef, topChipRef, frozen }: HeroMotionProps) {
      // restY stores the mount-time Y position of each hero object so breathing is additive.
      const restY = useRef<{ hole: number; chip: number }>({ hole: 0, chip: 0 });

      useEffect(() => {
        if (holeRef.current)    restY.current.hole = holeRef.current.position.y;
        if (topChipRef.current) restY.current.chip = topChipRef.current.position.y;
      }, []); // eslint-disable-line react-hooks/exhaustive-deps

      useFrame(({ clock }, delta) => {
        if (frozen) return; // HARD dual freeze — M9 determinism gate; no mutation when frozen
        const t = clock.getElapsedTime();
        applyMicroBreath(holeRef.current, t, delta, 0, restY.current.hole);
        applyMicroBreath(topChipRef.current, t, delta, CHIP_PHASE_OFFSET, restY.current.chip);
      });

      return null; // no geometry — pure side-effect component
    }

    Notes:
    - HeroMotion registers ONE useFrame for TWO objects. Per-frame cost: one callback, two ref
      reads, four arithmetic operations. This is the correct pattern (not per-component useFrame).
    - return null is intentional — HeroMotion contributes no JSX to the scene graph.
    - The eslint-disable comment on the useEffect dep array is correct — refs are stable and
      the effect must run only once at mount.

    STEP 4 — In Scene() function, ADD alongside the existing isFrozen declaration:

      const reducedMotion = typeof window !== "undefined"
        && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      const motionFrozen = isFrozen || reducedMotion;

    The existing isFrozen = qp("spin") === "off" line remains unchanged. Add reducedMotion and
    motionFrozen immediately after it (same block). Do NOT create a useState or useEffect for
    reducedMotion — mount-static read is the correct pattern (matching the qp() convention already
    used in the file). typeof window guard covers any SSR/test context.

    STEP 5 — Declare the two hero refs in Scene() (alongside existing refs such as flyCamRef):

      const holeCardGroupRef = useRef<THREE.Group>(null);
      const topChipGroupRef  = useRef<THREE.Group>(null);

    STEP 6 — Attach refs in the Scene JSX:

    (a) Hole-card group: The existing CardGroup for the hole cards (LAB_HAND_IDS hole pair —
        the Perla, Sota de Oros + 7 de Oros) is currently rendered without an outer group ref.
        Wrap it in a plain group with the ref:
          <group ref={holeCardGroupRef}>
            <CardGroup kit={cardKit} faces={cardFaces} poses={hole} />
          </group>
        This is a ref attachment only — no key change, no new geometry, no prop change to CardGroup.

    (b) Demoted accent pot group: The pot group at position=[3.0, 0, 1.5] scale={0.5} already
        exists. Add ref={topChipGroupRef} to that group's props. Do not change any other prop.
        If the exact position values differ in the source (e.g., the pot lives at a slightly
        different coordinate), attach the ref to whatever group wraps the three InstancedChipStack
        columns that form the accent pot — it is the logical "top chip group."

    STEP 7 — Mount HeroMotion in the Scene JSX, after all scene content and before any
    EffectComposer (or at the very end of the Canvas children if EffectComposer is not present):

      <HeroMotion
        holeRef={holeCardGroupRef}
        topChipRef={topChipGroupRef}
        frozen={motionFrozen}
      />

    AMPLITUDE CALIBRATION NOTE (for the executor — not a scope change):
    The constants in STEP 1 are START values. If during M9 or a quick live check the motion is
    visually perceptible (you can describe "the card is moving"), HALVE MICRO_AMPLITUDE_Y and
    MICRO_AMPLITUDE_ROT before running M9. If still visible after halving, set both to 0 and
    record as "motion removed — restrained stillness beats visible motion" in the SUMMARY.
    The grep-check in plan 09-02 asserts val <= threshold, so any value in (0, ceiling] passes.

    STEP 8 — Commit:
    Stage only frontend/src/lab/TableLab.tsx.
    Commit message: "feat(09-01): TP8 HeroMotion -- sub-threshold micro-life on hole cards + accent pot"
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep "src/lab" | head -20</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node tools/table-3d/grep-check-tp7-08.cjs</automated>
  </verify>
  <done>
    TableLab.tsx compiles clean (tsc --noEmit 0 errors under src/lab); vitest run src/lab/ green;
    grep-check-tp7-08.cjs exits 0 (11/11 — all prior invariants intact); the four MICRO_* constants
    are at module scope; HeroMotion is mounted in Scene JSX with frozen={motionFrozen}; both refs
    are attached; the useFrame body has "if (frozen) return" as its first statement. LOCAL only.
  </done>
</task>

<task type="auto">
  <name>Task 2: M9 double-capture — frozen byte-identical HARD gate</name>
  <files>
    docs/table-3d/anchors/tp8-gate/m9-hole-a.png
    docs/table-3d/anchors/tp8-gate/m9-hole-b.png
  </files>
  <read_first>
    - .dev-stack/lab-shot.mjs — confirm the exact CLI form: LAB_URL env var + output path arg;
      confirm the script appends spin=off automatically before navigating. Confirm the 2500ms settle
      window (the scene must be frozen by the time the screenshot fires).
    - tools/table-3d/m9-determinism.mjs — confirm invocation: --shot / --port / --out args and
      the exact output filenames it writes (m9-hero-a/b or a different naming scheme). If the script
      does not accept a --shot=hole variant, run two manual lab-shot.mjs captures instead and md5
      them manually (see action below).
  </read_first>
  <action>
    PREREQUISITE: Vite dev server must be running on port 5181. Confirm with a curl or check before
    proceeding:
      curl -s -o /dev/null -w "%{http_code}" http://localhost:5181/table-lab.html
    Expected: 200. If not running: cd frontend && npm run dev -- --port 5181 (in a separate terminal,
    or confirm it is already up from Task 1 work). Do NOT proceed with M9 if the server is not up.

    STEP 1 — Create the tp8-gate anchor directory:
      mkdir -p docs/table-3d/anchors/tp8-gate

    STEP 2 — M9 double-capture via m9-determinism.mjs (preferred):
      node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp8-gate
    This captures m9-hero-a.png and m9-hero-b.png (or equivalent names) and compares their md5.
    Expected: "M9: PASS (byte-identical)".
    Record the md5 hash in the SUMMARY.

    FALLBACK (if m9-determinism.mjs does not support the --out flag or different output names):
    Run two manual captures with the hero preset and spin=off:
      LAB_URL="http://localhost:5181/table-lab.html?cam=hero&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp8-gate/m9-hole-a.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=hero&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp8-gate/m9-hole-b.png
    Then compare md5:
      node -e "const crypto=require('crypto'),fs=require('fs'); const a=crypto.createHash('md5').update(fs.readFileSync('docs/table-3d/anchors/tp8-gate/m9-hole-a.png')).digest('hex'); const b=crypto.createHash('md5').update(fs.readFileSync('docs/table-3d/anchors/tp8-gate/m9-hole-b.png')).digest('hex'); console.log(a===b?'M9: PASS (byte-identical) '+a:'M9: FAIL -- a='+a+' b='+b); if(a!==b)process.exit(1);"
    Expected: "M9: PASS (byte-identical) <hash>".

    M9 FAIL PROTOCOL (applies to both paths):
    If M9 fails after the HeroMotion dual-freeze implementation:
    (a) Confirm that isFrozen = qp("spin") === "off" is mount-static in Scene() (not inside useFrame).
    (b) Confirm that motionFrozen is passed to HeroMotion and the useFrame body's FIRST statement
        is "if (frozen) return".
    (c) Check for any OTHER useFrame in TableLab.tsx that is not guarded by the spin=off flag.
        The flythrough useFrame from TP7 is already guarded by isFrozen — confirm no regression.
    (d) Confirm the harness appends spin=off BEFORE the page load (read .dev-stack/lab-shot.mjs
        line 13 as noted in RESEARCH.md). If the URL does not contain spin=off, the freeze guard
        never activates.
    (e) If after all checks M9 still fails: STOP. Record cause in SUMMARY. Do NOT proceed to 09-02.
    M9 is a HARD gate; if it fails the implementation is not shippable.

    STEP 3 — Verify grep-check-tp7-08.cjs still exits 0 (sanity check after Task 1 commit):
      node tools/table-3d/grep-check-tp7-08.cjs
    Expected: exit 0, "OK — grep-check-tp7-08: all 11 TP7 camera invariants hold".
    If it fails (unlikely — Task 1 does not touch cameras/fov/autoRotate): fix the regression before
    proceeding to 09-02.

    STEP 4 — Commit the tp8-gate anchor captures:
    Stage docs/table-3d/anchors/tp8-gate/m9-hole-a.png and m9-hole-b.png (or equivalent names).
    Commit message: "chore(09-01): TP8 M9 double-capture -- frozen byte-identical PASS on hero+spin=off"
  </action>
  <verify>
    <automated>node -e "const crypto=require('crypto'),fs=require('fs'); const a=crypto.createHash('md5').update(fs.readFileSync('docs/table-3d/anchors/tp8-gate/m9-hole-a.png')).digest('hex'); const b=crypto.createHash('md5').update(fs.readFileSync('docs/table-3d/anchors/tp8-gate/m9-hole-b.png')).digest('hex'); if(a!==b){console.error('M9 FAIL -- not byte-identical');process.exit(1);}console.log('M9 PASS -- byte-identical: '+a);"</automated>
  </verify>
  <done>
    docs/table-3d/anchors/tp8-gate/m9-hole-a.png and m9-hole-b.png committed; md5 hashes are
    identical (M9 PASS); grep-check-tp7-08.cjs exits 0 after Task 1 changes. SUMMARY records the
    exact md5 hash. LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render and local tooling only. No auth, no API, not in the prod build.

| Boundary | Description |
|----------|-------------|
| HeroMotion useFrame -> scene object refs | The frozen guard is the only seam; if isFrozen/reducedMotion are stale or computed inside useFrame the M9 gate fails |
| mount-static reducedMotion -> prefers-reduced-motion compliance | A live listener would be more compliant for a real app; mount-static is correct for this lab (consistent with qp() pattern) |
| TP2 dealt-variance base pose -> additive breathing | If applyMicroBreath overwrites position.y instead of adding, the TP2 variance is silently destroyed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01a | Tampering | Breathing overwrites TP2 dealt-variance base pose | mitigate | applyMicroBreath uses += and targetY includes restYVal (captured in useEffect at mount); action block explicitly calls out Pitfall 6 |
| T-09-01b | Tampering | reducedMotion computed inside useFrame (stale URL) | mitigate | reducedMotion must be declared as a mount-static const in Scene(), not inside the useFrame callback; action block specifies the exact placement (STEP 4) |
| T-09-01c | Tampering | A second useFrame registered on each Card/Chip component | mitigate | HeroMotion is ONE component with ONE useFrame for both objects; action block states "not per-component useFrame" |
| T-09-01d | Tampering | CHIP_PHASE_OFFSET as a runtime random value (breaks M9) | mitigate | CHIP_PHASE_OFFSET = Math.PI * 0.7 is a fixed constant, not computed from Math.random() or Date.now() |
| T-09-01e | Tampering | gsap.to() created inside useFrame | mitigate | Implementation uses pure arithmetic (Math.sin + exponential decay); no gsap call inside useFrame; action block names this as forbidden |
| T-09-01f | Repudiation | M9 md5 hash not recorded | mitigate | Task 2 action block explicitly records the md5 hash in SUMMARY |
| T-09-01g | Tampering | Bouncy easing (Elastic/Bounce/Back) introduced | mitigate | Only Math.exp(-delta/tau) is used; no gsap ease name referenced in applyMicroBreath; grep-check-tp8-09 CHECK 5 (plan 09-02) asserts no forbidden ease names |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages — maath is NOT installed; gsap is already present; this plan touches only TableLab.tsx |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit exits 0 (no new errors under src/lab).
- cd frontend && npx vitest run src/lab/ exits 0 (all prior tests green; no new tests expected — the HeroMotion side-effect logic is not unit-testable via JSDOM).
- node tools/table-3d/grep-check-tp7-08.cjs exits 0 (11/11 — all TP7 camera invariants intact).
- M9 PASS: docs/table-3d/anchors/tp8-gate/m9-hole-a.png and m9-hole-b.png are byte-identical (md5 match).
- TableLab.tsx has MICRO_AMPLITUDE_Y, MICRO_AMPLITUDE_ROT, MICRO_IDLE_PERIOD, MICRO_SETTLE_TAU at module scope (grep-able, non-comment).
- HeroMotion useFrame body begins with "if (frozen) return".
- motionFrozen = isFrozen || reducedMotion declared in Scene() as mount-static consts.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- tsc clean; vitest green; grep-check-tp7-08.cjs 11/11.
- M9 PASS (byte-identical double-capture, md5 hash recorded in SUMMARY).
- Four MICRO_* constants at module scope in TableLab.tsx (non-comment, grep-checkable).
- HeroMotion mounted with frozen={motionFrozen}; dual freeze guard active.
- Breathing is additive over the TP2 base pose (restY captured at mount).
- No new packages installed; no geometry/material/camera/postprocessing changed.
- LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/09-tp8-tactilidad-microvida-social/09-01-SUMMARY.md` when done.
Record: the exact five constant values written (MICRO_AMPLITUDE_Y, MICRO_AMPLITUDE_ROT,
MICRO_IDLE_PERIOD, MICRO_SETTLE_TAU, CHIP_PHASE_OFFSET); M9 md5 hash + PASS/FAIL verdict;
tsc/vitest results; grep-check-tp7-08 11-check verdict; the exact JSX lines where HeroMotion is
mounted and where the two refs are attached; any amplitude calibration applied (if constants were
halved or zeroed during the visible-wobble check). Plan 09-02 reads this SUMMARY as the basis for
the constant values to check.
</output>
