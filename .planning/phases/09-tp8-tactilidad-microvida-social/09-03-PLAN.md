---
phase: 09-tp8-tactilidad-microvida-social
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - docs/table-3d/TP8_OPERATOR_GATE.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - "SSOT §TP8 — perceptual gate (LIVE view): does it feel ALIVE + weighty + read as a shared mid-play game with NO motion consciously noticeable?"
  - "SSOT §TP8 — NOT auto-approvable; live motion-feel is a LIVE-VIEW judgment the orchestrator cannot assess from frozen captures"
  - "SSOT §TP8 — stop-on-ambiguous: any visible wobble -> HALVE; if still seen -> REMOVE; restrained stillness beats visible motion"
  - "SSOT §TP8 — gate record: docs/table-3d/TP8_OPERATOR_GATE.md + SCORECARD tactility + social-read rows updated"
  - "SSOT §TP8 — non-blocking rollback: keeping the static-but-complete table is a valid outcome"

must_haves:
  truths:
    - "The orchestrator has auto-approved: grep-check-tp8-09.cjs 18/18 PASS; M9 byte-identical PASS; M2 >= 2x PASS; social-read COMPLETE (no new center objects)"
    - "The live motion-feel judgment is FLAGGED for operator batch review — the orchestrator cannot assess this from frozen captures"
    - "The stop-on-ambiguous rule is presented and enforced: visible wobble -> HALVE -> if still seen REMOVE"
    - "On verdict: docs/table-3d/TP8_OPERATOR_GATE.md records verdict + scorecard rows + HARD gate values"
    - "On verdict: SCORECARD_TABLE_3D.md tactility/micro-life row and social-read row updated"
    - "On verdict: .planning/STATE.md and .planning/ROADMAP.md updated (Phase 9 / TP8 closed)"
  artifacts:
    - path: "docs/table-3d/TP8_OPERATOR_GATE.md"
      provides: "Recorded operator verdict (motion feel + social read), HARD gate values from 09-02-SUMMARY, stop-on-ambiguous disposition, non-blocking rollback status, TP9 note"
      contains: "Verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP8 score update for tactility/micro-life row and social-read row"
      contains: "TP8"
  key_links:
    - from: "docs/table-3d/TP8_OPERATOR_GATE.md"
      to: "docs/table-3d/anchors/tp8-gate/{hero,card,macro}.png"
      via: "Operator reviews frozen captures as the static scene record alongside the LIVE motion feel"
      pattern: "tp8-gate"
    - from: "docs/table-3d/TP8_OPERATOR_GATE.md"
      to: "docs/ROADMAP_TABLE_3D_PERFECTION.md §TP8"
      via: "Gate record confirms the SSOT acceptance criterion: breathing sub-threshold, social read complete, ready for TP9"
      pattern: "TP8_OPERATOR_GATE"
---

<objective>
The TP8 operator perceptual gate — the human seam for the tactility, micro-vida, and social-read
phase. The orchestrator presents the pre-gate record (HARD gates already passed: grep-check 18/18,
M9 byte-identical, M2 >= 2x, social-read complete), then FLAGS the live motion-feel for the
operator's batch review. The operator answers one question while viewing the table LIVE:
"Does it feel alive and weighty, and read as a shared mid-play game, with NO motion consciously
noticeable?"

Purpose: The orchestrator auto-approves every gate that can be verified from frozen captures or
static code inspection. The live motion-feel is the one dimension that cannot be assessed without
seeing the scene animate. It is explicitly FLAGGED for the operator's batch review rather than
auto-approved. The stop-on-ambiguous rule is REMOVE, not KEEP (opposite of the TP7 camera gate):
restrained stillness beats visible motion, and the non-blocking rollback (keep the static table)
is a fully valid outcome.

Output: docs/table-3d/TP8_OPERATOR_GATE.md (verdict + HARD gate values + stop-on-ambiguous
disposition + non-blocking rollback status); SCORECARD_TABLE_3D.md tactility + social-read rows
updated; STATE.md + ROADMAP.md closed for Phase 9 / TP8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-CONTEXT.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-01-SUMMARY.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-02-SUMMARY.md
@docs/table-3d/TP7_OPERATOR_GATE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator gate — "Does it feel ALIVE + weighty + read as a shared mid-play game, with NO motion consciously noticeable?"</name>
  <files>docs/table-3d/TP8_OPERATOR_GATE.md, docs/table-3d/SCORECARD_TABLE_3D.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/09-tp8-tactilidad-microvida-social/09-01-SUMMARY.md — the HeroMotion
      implementation record: exact MICRO_* constants written (including any calibration applied),
      M9 md5 hash, vitest/tsc results, JSX mount location. Restate all values at the gate.
    - .planning/phases/09-tp8-tactilidad-microvida-social/09-02-SUMMARY.md — the full pre-gate
      record: grep-check-tp8-09 18-check verdict with per-check breakdown, grep-check-tp7-08 11-check
      verdict, three anchor capture paths, social-read completeness audit table (4 items), M2
      standing (ratio + source). Restate all values at the gate presentation.
    - docs/table-3d/TP7_OPERATOR_GATE.md — gate record structure to mirror for TP8_OPERATOR_GATE.md.
    - docs/table-3d/SCORECARD_TABLE_3D.md — current state of tactility and social-read rows to update
      after the gate; must not regress from TP7 column.
  </read_first>
  <action>
    ORCHESTRATOR NOTE: This gate has TWO distinct layers with different verification authority.
    Layer 1 (auto-approved by the orchestrator — present as CONFIRMED, not as pending):
      - grep-check-tp8-09.cjs: 18/18 PASS — all amplitude bounds, no-FX, freeze guard confirmed.
      - M9: byte-identical double-capture PASS — harness captures are deterministic.
      - M2: >= 2x PASS (3.66x/2.60x or re-run value from 09-02-SUMMARY) — no new center objects.
      - Social read: COMPLETE — CenterGameState + staged Perla + community board + demoted pot
        all unconditionally mounted; no new objects added in TP8.
    Layer 2 (FLAGGED for operator batch review — cannot be assessed from frozen captures):
      - The LIVE motion-feel: "does it breathe with sub-conscious weight without being consciously
        noticed?" This requires the operator to view the table in motion (dev server).

    PRESENT THE GATE as follows:

    First, restate the auto-approved HARD gates from 09-01-SUMMARY and 09-02-SUMMARY:
    - MICRO_AMPLITUDE_Y: [value from 09-01-SUMMARY]wu — CHECK 1 <= 0.01wu PASS
    - MICRO_AMPLITUDE_ROT: [value]rad — CHECK 2 <= 0.00873 rad (0.5°) PASS
    - MICRO_IDLE_PERIOD: [value]s — CHECK 3 in [6, 12]s PASS
    - MICRO_SETTLE_TAU: [value]s — CHECK 4 in [0.2, 0.4]s PASS
    - No-FX (no Elastic/Bounce/Back/Flip/Glow): CHECK 5 PASS
    - Freeze guard (motionFrozen): CHECK 6 PASS
    - reducedMotion: CHECK 7 PASS
    - All 11 TP7 forward-carried invariants: CHECKS 8-18 PASS
    - M9: [md5 hash] — byte-identical PASS
    - M2: [ratio] — >= 2x PASS
    - Social read: COMPLETE (CenterGameState + staged Perla + community board + demoted pot)
    These are CONFIRMED by static analysis and frozen captures. The orchestrator presents these
    as green — the operator does not need to re-verify them.

    Then PRESENT THE LIVE MOTION-FEEL JUDGMENT (the operator's exclusive domain):
    Ask the operator to:
    1. Start the dev server if not already running:
         cd frontend && npm run dev -- --port 5181
    2. Open the live table WITHOUT spin=off (so HeroMotion is active):
         http://localhost:5181/table-lab.html?cam=hero&fx
    3. Observe for 15-30 seconds (one to two full breathing cycles at 9s period).

    Gate questions for the operator:
    (a) WEIGHT + LIFE: "Does the table feel like a real physical object caught mid-play —
        a quality you would describe as 'weight' or 'presence' rather than 'animation'?"
    (b) SUB-THRESHOLD: "Is the motion consciously noticeable? Can you describe it ('the card
        is moving')? If YES: the motion is above threshold. If you only notice it BECAUSE you
        are looking for it and then barely see it: PASS."
    (c) SOCIAL READ: "Does the scene read as a shared, mid-play game — a table that someone
        was just playing at? Not just a still life of objects."
    (d) NO-FX: "Is there any bounce, snap, spin, or glow? Any motion that reads as a UI
        animation rather than a physical object settling?"

    Also ask the operator to check the card preset:
         http://localhost:5181/table-lab.html?cam=card&fx
    Gate question: "Are the hole cards (Sota de Oros + 7 de Oros) still dominant and legible
    — the micro-motion does not visually undermine the protagonist read?"

    STOP-ON-AMBIGUOUS RULE (the rollback rule — different from TP7):
    The stop-on-ambiguous rule for TP8 motion is REDUCE, not KEEP:
    - If the operator can describe the motion ("the card is moving", "it wobbles", "it's distracting"):
        VISIBLE WOBBLE. Action: HALVE MICRO_AMPLITUDE_Y and MICRO_AMPLITUDE_ROT in TableLab.tsx.
        Re-run grep-check-tp8-09 (CHECK 1-2 will still pass at the halved value as long as it's
        below the ceiling). Re-present live for operator review.
    - If after halving the motion is still described as visible:
        REMOVE the motion. Set MICRO_AMPLITUDE_Y = 0 and MICRO_AMPLITUDE_ROT = 0 (or remove
        the HeroMotion mount). The non-blocking rollback is in effect — the static-but-complete
        table is the TP8 deliverable. Record in gate doc: "motion removed per stop-on-ambiguous
        — restrained stillness beats visible motion; table complete as static."
    - "I can't tell if it's moving" = PASS. Sub-threshold = not consciously noticeable.
    - "It feels alive/weighted but I can't describe motion" = PASS. This is the target.
    Do NOT reduce the idle period or change the easing type — only the amplitude is the dial.

    NON-BLOCKING ROLLBACK NOTE:
    TP8 delivers the motion as additive over the static complete table. If the motion is removed,
    the table is NOT degraded — it is the same TP1-TP7 result with the social-read complete. The
    rollback is non-blocking for TP9. Record this status in TP8_OPERATOR_GATE.md regardless of
    the motion verdict.

    On operator verdict, record docs/table-3d/TP8_OPERATOR_GATE.md (mirror TP7_OPERATOR_GATE.md
    structure, adapted for TP8):
    - The verdict string (MOTION APPROVED / MOTION HALVED + APPROVED / MOTION REMOVED).
    - The HARD gate summary (all 7 new + 11 forward-carried confirmed; M9 hash; M2 ratio; social-read).
    - The stop-on-ambiguous disposition applied (NONE / HALVED / REMOVED + amplitude values before/after).
    - The non-blocking rollback status (motion present and approved / motion removed — table complete as static).
    - The final MICRO_AMPLITUDE_Y and MICRO_AMPLITUDE_ROT values shipped (even if 0).
    - The note: "TP8 delivers sub-conscious weight + social read via table-state; TP9 is the final
      scorecard + AAA unification + new protected reference tag."
    - The chips scorecard note from TP3: "Chips count operator-gated at 3 per stack (TP3 record);
      AAA(5) deferred to TP5/6." (Carried forward from TP3 gate record as an invariant for TP9.)

    Update SCORECARD_TABLE_3D.md:
    - Tactility / micro-life row: add TP8 result column. Value depends on verdict:
        MOTION APPROVED: 4/5 (breathing sub-threshold, weight + social read)
        MOTION REMOVED: 3/5 (static-but-complete, social read via table-state; motion deferred)
    - Social read row: add TP8 result. Value: 4/5 (center table-state complete; static social read;
      no per-seat figures; M2 >= 2x).
    - Must not regress any prior TP column.

    Update .planning/STATE.md and .planning/ROADMAP.md: mark Phase 9 / TP8 COMPLETE.

    Do NOT change any geometry, materials, lighting, cameras, or postprocessing. LOCAL only — no
    push/deploy/merge.
  </action>
  <what-built>
    TP8 executed two automated plans before this gate:

    (A) HEROMOION IMPLEMENTATION — 09-01:
    TableLab.tsx modified:
      - Four named constants at module scope: MICRO_AMPLITUDE_Y, MICRO_AMPLITUDE_ROT,
        MICRO_IDLE_PERIOD, MICRO_SETTLE_TAU (within SSOT sub-threshold bounds).
      - CHIP_PHASE_OFFSET = Math.PI * 0.7 (non-harmonic out-of-phase offset).
      - reducedMotion (mount-static matchMedia read) + motionFrozen = isFrozen || reducedMotion.
      - Two hero refs: holeCardGroupRef (wraps hole CardGroup), topChipGroupRef (demoted pot group).
      - HeroMotion component: ONE useFrame, TWO objects, hard dual freeze "if (frozen) return".
      - restY captured at mount via useEffect — breathing is additive over TP2 base pose.
      - Exponential decay settle (no bouncy easing; no gsap; no maath).
    M9 PASS: double-capture byte-identical under spin=off (md5: [from 09-01-SUMMARY]).
    vitest green. tsc clean. grep-check-tp7-08 11/11 PASS.

    (B) STATIC CODE ASSERTION + GATE ANCHORS — 09-02:
    tools/table-3d/grep-check-tp8-09.cjs authored (18 checks, exits 0):
      CHECK 1: MICRO_AMPLITUDE_Y [value] <= 0.01wu PASS
      CHECK 2: MICRO_AMPLITUDE_ROT [value] <= 0.00873 rad (0.5°) PASS
      CHECK 3: MICRO_IDLE_PERIOD [value] in [6, 12]s PASS
      CHECK 4: MICRO_SETTLE_TAU [value] in [0.2, 0.4]s PASS
      CHECK 5: No Elastic/Bounce/Back/Flip/Glow in lab source PASS
      CHECK 6: Freeze guard (motionFrozen) present PASS
      CHECK 7: reducedMotion present PASS
      CHECKS 8-18: all 11 TP7 forward-carried invariants PASS
    grep-check-tp7-08.cjs: 11/11 PASS (backward compatibility).
    Three gate anchor captures committed (spin=off, ?fx, frozen scene):
      docs/table-3d/anchors/tp8-gate/hero.png
      docs/table-3d/anchors/tp8-gate/card.png
      docs/table-3d/anchors/tp8-gate/macro.png
    Social read COMPLETE (CenterGameState + staged Perla + community board + demoted pot — all
    unconditionally mounted; no new objects added in TP8).
    M2 standing: [ratio from 09-02-SUMMARY] >= 2x PASS.

    ORCHESTRATOR AUTO-APPROVED (from frozen captures + static analysis):
    All amplitude bounds, no-FX, freeze guard, reducedMotion, M9 determinism, M2 floor,
    social-read completeness — these are GREEN.

    FLAGGED FOR OPERATOR BATCH REVIEW (cannot be judged from frozen captures):
    LIVE motion-feel: does the table breathe with sub-conscious weight — alive and weighty,
    with NO motion consciously noticeable? The motion is imperceptible by SSOT design; the
    operator's live-view judgment is the only instrument that can confirm it.
  </what-built>
  <how-to-verify>
    The operator judges the LIVE motion feel and the social read. HARD gates are already green.

    1. Start the dev server if not running:
         cd frontend && npm run dev -- --port 5181

    2. LIVE HERO VIEW (primary motion-feel gate):
       Open: http://localhost:5181/table-lab.html?cam=hero&fx
       Observe for 15-30 seconds (1-2 breathing cycles at 9s).
       Compare against the frozen anchor: docs/table-3d/anchors/tp8-gate/hero.png

       Motion-feel questions:
       (a) "Is the table ALIVE — does it feel like a quality physical object caught mid-play,
           not a photograph of one?"
       (b) "Is the motion consciously NOTICEABLE? Can you describe it ('the card bobs')?"
           If NO, or "barely, only because I was looking" → PASS (sub-threshold confirmed).
           If YES, clearly → visible wobble → HALVE.
       (c) "Does it read as a SHARED mid-play game — someone was just playing here?"
       (d) Is there any bounce, snap, or animation-like motion? → REMOVE if yes.

    3. LIVE CARD VIEW (legibility + protagonist read under motion):
       Open: http://localhost:5181/table-lab.html?cam=card&fx
       Gate question: "Are the Sota de Oros + 7 de Oros still dominant and legible —
       does the micro-motion help (weight) or hurt (distraction) the protagonist read?"

    4. FROZEN ANCHOR REVIEW (for the gate record — compare static vs. live feel):
       Review docs/table-3d/anchors/tp8-gate/hero.png, card.png, macro.png.
       The frozen captures show the scene with motion OFF — they are the permanent record of
       the geometric and material state. Use them to confirm: "Is the static scene sufficient
       (the non-blocking rollback) if the motion is removed?"

    5. HARD GATES (already CONFIRMED — no re-run required unless operator requests):
       grep-check-tp8-09.cjs: 18/18 (constants within SSOT, no-FX, freeze guards present)
       M9: byte-identical (motion freezes under spin=off — the capture determinism gate)
       M2: >= 2x (social read geometry unchanged)
       Social read: COMPLETE (four scene items unconditionally mounted)
       If operator wants to confirm live:
         node tools/table-3d/grep-check-tp8-09.cjs     → exits 0
         node tools/table-3d/grep-check-tp7-08.cjs     → exits 0

    STOP-ON-AMBIGUOUS RULE:
    - "I can describe the motion" → VISIBLE WOBBLE → HALVE amplitudes in TableLab.tsx,
      re-run grep-check-tp8-09 (CHECK 1-2 pass at any value <= ceiling), re-present live.
    - After halving, still visible → REMOVE (set MICRO_AMPLITUDE_Y = MICRO_AMPLITUDE_ROT = 0
      or remove HeroMotion mount). Record as "motion removed — restrained stillness beats motion."
    - "I notice it only because I'm looking, and even then barely" → PASS.
    - "It feels alive/weighted" → PASS. This is the target.
    The non-blocking rollback is valid and honourable: the static table is complete and ready for TP9.

  </how-to-verify>
  <resume-signal>
    Operator types:
    - "approved" — motion feel PASS; table breathes with sub-conscious weight; social read confirmed;
      no visible wobble. Execute: write TP8_OPERATOR_GATE.md (verdict=MOTION APPROVED); update
      SCORECARD_TABLE_3D.md (tactility 4/5, social-read 4/5); update STATE.md + ROADMAP.md (Phase 9
      TP8 COMPLETE); commit all four files.
      Commit message: "docs(09-03): TP8 operator gate APPROVED -- micro-life sub-threshold + social read complete"
    - "approved — note [observation]" — ship with the note recorded in the gate doc; same action as approved.
    - "halve" — VISIBLE WOBBLE detected; executor halves MICRO_AMPLITUDE_Y and MICRO_AMPLITUDE_ROT
      in TableLab.tsx (divide each by 2); re-runs grep-check-tp8-09 (must still exit 0); re-presents
      the live hero view for operator review. Awaits re-approval or "remove" signal.
    - "remove" — motion is gimmicky even at halved amplitude; executor sets MICRO_AMPLITUDE_Y = 0
      and MICRO_AMPLITUDE_ROT = 0 in TableLab.tsx (or removes HeroMotion mount entirely); writes
      TP8_OPERATOR_GATE.md (verdict=MOTION REMOVED — non-blocking rollback); updates SCORECARD
      (tactility 3/5 — static-but-complete; social-read 4/5); updates STATE + ROADMAP; commits.
      Commit message: "docs(09-03): TP8 operator gate -- motion removed (restrained stillness); table complete"
    - "approved — halved" — operator observed that halving was applied and approves the halved
      version; same action as approved but records the halved amplitude values.
    Do NOT auto-approve. The live motion-feel judgment belongs to the operator.
  </resume-signal>
  <done>
    The operator has reviewed the table LIVE and rendered a verdict on the motion feel + social read.
    HARD gates (grep-check 18/18, M9, M2, social-read) restated from 09-02-SUMMARY. Stop-on-ambiguous
    rule applied (HALVE → REMOVE if still visible). docs/table-3d/TP8_OPERATOR_GATE.md written with
    verdict + HARD gate values + stop-on-ambiguous disposition + non-blocking rollback status + TP9 note.
    SCORECARD_TABLE_3D.md tactility/micro-life row and social-read row updated. .planning/STATE.md and
    .planning/ROADMAP.md updated (Phase 9 / TP8 COMPLETE). No geometry/materials/lighting/cameras
    changed (or amplitudes halved/zeroed per stop-on-ambiguous, recorded). LOCAL only — no push/deploy/merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, dev-server-only.

| Boundary | Description |
|----------|-------------|
| Frozen captures -> live motion feel | Frozen captures cannot represent the live motion; the operator's live-view is the only legitimate instrument for this dimension |
| stop-on-ambiguous REMOVE rule -> non-blocking rollback | The rollback must not degrade the table; motion is additive, so removing it restores the TP1-TP7 state exactly |
| SCORECARD update -> TP9 basis | The tactility and social-read rows updated here are part of the input the TP9 scorecard sign-off reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03a | Tampering | Auto-approve circumventing the live-motion-feel gate | mitigate | autonomous:false; this checkpoint cannot be auto-approved; the orchestrator's standing "auto-approve (0 paradas)" directive explicitly carves out ambiguous/unverifiable items as FLAGGED, not auto-approved |
| T-09-03b | Tampering | Stop-on-ambiguous REMOVE rule not applied (wobble kept) | mitigate | Resume-signal provides explicit "halve" and "remove" paths; action block specifies: "If the operator can describe the motion → HALVE. If still visible after halving → REMOVE." |
| T-09-03c | Repudiation | Gate verdict not written to TP8_OPERATOR_GATE.md | mitigate | Resume-signal action block explicitly writes TP8_OPERATOR_GATE.md on EVERY outcome path (approved / halved / removed); commit anchors the record |
| T-09-03d | Tampering | SCORECARD tactility row regresses from TP7 column | mitigate | read_first reads current SCORECARD_TABLE_3D.md; action block states "must not regress any prior TP column" |
| T-09-03e | Tampering | Motion halving bypasses grep-check CHECK 1-2 re-run | mitigate | Resume-signal "halve" path explicitly requires re-running grep-check-tp8-09 after the amplitude change |
| T-09-03f | Tampering | Geometry/materials changed at the gate (scope creep) | mitigate | Action block explicitly prohibits: "Do NOT change any geometry, materials, lighting, cameras, or postprocessing." |
| T-09-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- Operator has reviewed the table LIVE at ?cam=hero&fx (without spin=off) for 15-30 seconds.
- HARD gates from 09-01-SUMMARY and 09-02-SUMMARY restated: grep-check-tp8-09 18/18, M9 byte-identical, M2 >= 2x, social-read COMPLETE.
- Stop-on-ambiguous rule applied: visible wobble -> HALVE -> if still seen REMOVE; restrained stillness beats motion.
- docs/table-3d/TP8_OPERATOR_GATE.md written: verdict + HARD gate values + stop-on-ambiguous disposition + non-blocking rollback status + TP9 note.
- docs/table-3d/SCORECARD_TABLE_3D.md tactility/micro-life row and social-read row updated; no TP column regressed.
- .planning/STATE.md and .planning/ROADMAP.md updated: Phase 9 / TP8 COMPLETE.
- No geometry, materials, lighting, cameras, or postprocessing changed.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator has rendered a live-view verdict (APPROVED / HALVED + APPROVED / REMOVED).
- TP8_OPERATOR_GATE.md written with full verdict record.
- SCORECARD updated (tactility + social-read rows; no regression).
- STATE.md + ROADMAP.md closed for TP8.
- Non-blocking rollback status documented regardless of verdict.
- TP9 note recorded: final scorecard + AAA unification + new protected reference tag.
- LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/09-tp8-tactilidad-microvida-social/09-03-SUMMARY.md` when done,
and write the verdict to `docs/table-3d/TP8_OPERATOR_GATE.md`. Update the tactility/micro-life
row and social-read row in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP8 scores (must not
regress from TP7 column; note if final AAA(5) awaits TP9 sign-off). The SUMMARY records the full
gate disposition: verdict, motion amplitude shipped (or 0 if removed), stop-on-ambiguous outcome,
non-blocking rollback status, HARD gate values as restated, and the TP9 advancement note.
</output>
