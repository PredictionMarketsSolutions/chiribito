---
phase: 09-tp8-tactilidad-microvida-social
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tools/table-3d/grep-check-tp8-09.cjs
  - docs/table-3d/anchors/tp8-gate/hero.png
  - docs/table-3d/anchors/tp8-gate/card.png
  - docs/table-3d/anchors/tp8-gate/macro.png
autonomous: true
requirements:
  - "SSOT §TP8 — static CODE assertion that easing constants <= documented thresholds (since frozen captures cannot measure live motion)"
  - "SSOT §TP8 — no flip/spin/glow/bouncy easing in lab source (no-FX assertion)"
  - "SSOT §TP8 — freeze guard present (motionFrozen or equivalent guards the useFrame)"
  - "SSOT §TP8 — social read via CENTER table-STATE ONLY — NO new objects; M2 >= 2x holds"

must_haves:
  truths:
    - "tools/table-3d/grep-check-tp8-09.cjs exits 0 (18 checks: 7 new + 11 forward-carried from tp7-08)"
    - "tools/table-3d/grep-check-tp7-08.cjs still exits 0 (11 checks — backward compatibility)"
    - "CHECK 1: MICRO_AMPLITUDE_Y value extracted and <= 0.01"
    - "CHECK 2: MICRO_AMPLITUDE_ROT value extracted and <= 0.00873"
    - "CHECK 3: MICRO_IDLE_PERIOD value extracted and in [6, 12]"
    - "CHECK 4: MICRO_SETTLE_TAU value extracted and in [0.2, 0.4]"
    - "CHECK 5: no Elastic/Bounce/Back/Flip/Spin/Glow in non-comment lab source"
    - "CHECK 6: freeze guard (motionFrozen or frozen) present in TableLab.tsx non-comment code"
    - "CHECK 7: reducedMotion (or prefers-reduced-motion) string present in TableLab.tsx"
    - "CHECKS 8-18: all 11 forward-carried TP7 invariants pass"
    - "Social read audit documented in SUMMARY: CenterGameState + staged hand + demoted pot = COMPLETE; no new objects added in TP8"
    - "M2 documented: no new scene objects => M2 unchanged at >= 2x (last recorded value from TP7-08: 3.66x hero / 2.60x card)"
    - "Three canonical tp8-gate anchor captures committed: hero.png, card.png, macro.png (spin=off, with ?fx)"
  artifacts:
    - path: "tools/table-3d/grep-check-tp8-09.cjs"
      provides: "TP8 invariant checker — 18 checks (7 new: 4 constant-value + no-FX + freeze-guard + reducedMotion; 11 forward-carried from tp7-08); exit 0/exit 1 discipline; Node stdlib only"
      contains: "grep-check-tp8-09"
    - path: "docs/table-3d/anchors/tp8-gate/hero.png"
      provides: "TP8 gate anchor capture at hero preset with fx and spin=off (frozen scene)"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp8-gate/card.png"
      provides: "TP8 gate anchor capture at card preset with fx and spin=off (frozen scene)"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp8-gate/macro.png"
      provides: "TP8 gate anchor capture at macro preset with fx and spin=off (frozen scene)"
      min_lines: 1
  key_links:
    - from: "tools/table-3d/grep-check-tp8-09.cjs"
      to: "frontend/src/lab/TableLab.tsx"
      via: "Reads TableLab.tsx non-comment code; extracts MICRO_* constant values via regex; asserts freeze guard + reducedMotion + no-FX present"
      pattern: "grep-check-tp8-09"
    - from: "docs/table-3d/anchors/tp8-gate/{hero,card,macro}.png"
      to: "TP8 Operator Gate (plan 09-03)"
      via: "The three frozen captures (motion OFF under spin=off) are the visual record the operator reviews alongside the live motion feel"
      pattern: "tp8-gate"
---

<objective>
Author tools/table-3d/grep-check-tp8-09.cjs (7 new checks + 11 forward-carried from tp7-08),
capture the three canonical TP8 gate anchors (hero/card/macro with ?fx and spin=off), and document
the social-read completeness audit and M2 standing.

Purpose: Because the harness captures the scene FROZEN (motion zeroed under spin=off), the harness
cannot verify that the micro-motion amplitude is sub-threshold. The static grep-check is the only
orchestrator-verifiable bound on the amplitude constants. It also asserts the no-FX invariant and
the presence of the freeze guard, covering all three dimensions of the motion specification without
requiring a live render. The anchor captures and social-read audit complete the pre-gate record for
the operator gate in plan 09-03.

Output: grep-check-tp8-09.cjs exits 0 (18 checks); anchor captures committed; social-read and M2
documented in SUMMARY. The operator gate plan reads this SUMMARY as its basis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-CONTEXT.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-RESEARCH.md
@.planning/phases/09-tp8-tactilidad-microvida-social/09-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tools/table-3d/grep-check-tp8-09.cjs (18 checks: 7 new + 11 forward-carried)</name>
  <files>tools/table-3d/grep-check-tp8-09.cjs</files>
  <read_first>
    - tools/table-3d/grep-check-tp7-08.cjs — READ THE FULL FILE. Extract:
        (a) The exact "use strict" + require block + readFile/fail/stripComments helper implementations
            — copy byte-for-byte; do not paraphrase.
        (b) All 11 check bodies (CHECK 1 through CHECK 11) and their exact regex patterns,
            fail messages, and variable names — these become CHECKS 8-18 (forward-carried, verbatim).
        (c) The one-line OK exit 0 format (console.log with multi-line template literal listing
            all check names).
    - frontend/src/lab/TableLab.tsx — Read the actual constant values and token forms AFTER the
      09-01 implementation. Specifically:
        (a) The five MICRO_* constant declarations (to confirm the exact token form the regex must
            match: "const MICRO_AMPLITUDE_Y   = 0.003;" — confirm spacing around = and the exact
            decimal notation; the regex must tolerate 1-3 spaces before =).
        (b) The motionFrozen declaration and the frozen prop on HeroMotion (to confirm the exact
            variable names used — the grep-check CHECK 6 string search must match what is actually
            in the file).
        (c) The reducedMotion declaration (to confirm the exact string the CHECK 7 search targets).
    - .planning/phases/09-tp8-tactilidad-microvida-social/09-01-SUMMARY.md — Read the actual
      constant values written by plan 09-01 (in case the executor calibrated them; e.g., MICRO_AMPLITUDE_Y
      may have been halved to 0.0015). The grep-check threshold is the SSOT ceiling (<=0.01), not
      the exact value from RESEARCH.md — any value in (0, ceiling] passes CHECK 1.
  </read_first>
  <action>
    Author tools/table-3d/grep-check-tp8-09.cjs as a CommonJS module. Mirror grep-check-tp7-08.cjs
    exactly in structure ("use strict", require block, helpers, per-check blocks, one-line OK exit 0).

    STRUCTURE REQUIREMENTS:
    - Node stdlib only (fs, path). Zero new packages.
    - const TABLE_LAB = path.join("frontend", "src", "lab", "TableLab.tsx");
    - const LAB_DIR   = path.join("frontend", "src", "lab");
    - Helper functions identical to tp7-08: readFile(filePath), fail(message), stripComments(src).
      stripComments(src) removes lines starting with "//" (the existing convention).
    - Read TableLab.tsx once into `const src = readFile(TABLE_LAB)` and `const nc = stripComments(src)`.
    - Run all 18 checks in order; fail() on any; exit 0 with one-line OK message listing all check names.

    CHECKS 1-7 (NEW — TP8 micro-motion invariants):

    CHECK 1 — MICRO_AMPLITUDE_Y declared and value <= 0.01:
      const m1 = nc.match(/MICRO_AMPLITUDE_Y\s*=\s*([0-9.]+)/);
      const v1 = m1 ? parseFloat(m1[1]) : NaN;
      if (isNaN(v1) || v1 > 0.01)
        fail("CHECK 1 FAILED — MICRO_AMPLITUDE_Y not found or value > 0.01wu in TableLab.tsx.\n" +
          "  SSOT TP8 threshold: amplitude < 0.01 world units. Current value: " + v1 + "\n" +
          "  Named constant required so this check can verify the sub-threshold bound.\n" +
          "  Restrained stillness beats visible motion — err LESS, not more.");

    CHECK 2 — MICRO_AMPLITUDE_ROT declared and value <= 0.00873 (0.5° in radians):
      const m2 = nc.match(/MICRO_AMPLITUDE_ROT\s*=\s*([0-9.]+)/);
      const v2 = m2 ? parseFloat(m2[1]) : NaN;
      if (isNaN(v2) || v2 > 0.00873)
        fail("CHECK 2 FAILED — MICRO_AMPLITUDE_ROT not found or value > 0.00873 rad (0.5°).\n" +
          "  SSOT TP8 threshold: rotation amplitude < 0.5° = 0.00873 radians. Current: " + v2 + "\n" +
          "  Named constant required for static amplitude verification.");

    CHECK 3 — MICRO_IDLE_PERIOD declared and value in [6, 12]:
      const m3 = nc.match(/MICRO_IDLE_PERIOD\s*=\s*([0-9.]+)/);
      const v3 = m3 ? parseFloat(m3[1]) : NaN;
      if (isNaN(v3) || v3 < 6 || v3 > 12)
        fail("CHECK 3 FAILED — MICRO_IDLE_PERIOD not found or value outside [6, 12]s.\n" +
          "  SSOT TP8: idle period must be 6–12s near-zero. Current: " + v3);

    CHECK 4 — MICRO_SETTLE_TAU declared and value in [0.2, 0.4]:
      const m4 = nc.match(/MICRO_SETTLE_TAU\s*=\s*([0-9.]+)/);
      const v4 = m4 ? parseFloat(m4[1]) : NaN;
      if (isNaN(v4) || v4 < 0.2 || v4 > 0.4)
        fail("CHECK 4 FAILED — MICRO_SETTLE_TAU not found or value outside [0.2, 0.4]s.\n" +
          "  SSOT TP8: settle tau must be 0.2–0.4s. Current: " + v4);

    CHECK 5 — no bouncy/flip/spin/glow easing in lab source (no-FX assertion):
      Scan all .tsx and .ts files in LAB_DIR. For each file, strip comments, then search for
      the forbidden pattern. Forbidden tokens: Elastic, Bounce, Back, Flip, Spin, Glow.
      Note: "Spin" matches the gimmicky easing name, NOT the QP parameter "spin=off". Use
      /\b(Elastic|Bounce|Back\.out|Back\.in|Flip|Glow)\b/i on non-comment code only.
      (The string "spin" alone without a capital B would match too broadly — use the token form.)
      If any match found: fail with the filename and matched token.
      fail("CHECK 5 FAILED — forbidden easing token found in " + file + ": " + match + "\n" +
        "  SSOT TP8 no-FX assertion: no Elastic / Bounce / Back / Flip / Glow easing.\n" +
        "  Use exponential decay only: v += (target - v) * (1 - Math.exp(-delta / tau)).");

    CHECK 6 — freeze guard present in TableLab.tsx (motionFrozen or frozen prop guards useFrame):
      Search nc for the string "motionFrozen" OR "frozen" used as a guard. The check is a
      string presence check — it confirms the freeze guard variable name is in the non-comment
      code. Use:
        const hasFreezeGuard = nc.includes("motionFrozen") || nc.includes("if (frozen)");
      If false:
        fail("CHECK 6 FAILED — freeze guard (motionFrozen / frozen) not found in non-comment code.\n" +
          "  M9 HARD GATE: the HeroMotion useFrame must begin with 'if (frozen) return' where\n" +
          "  frozen = isFrozen || reducedMotion. Without this guard, captures are non-deterministic.");

    CHECK 7 — reducedMotion (prefers-reduced-motion compliance) present:
      const hasReducedMotion = nc.includes("reducedMotion") || nc.includes("prefers-reduced-motion");
      if (!hasReducedMotion)
        fail("CHECK 7 FAILED — reducedMotion / prefers-reduced-motion not found in non-comment code.\n" +
          "  SSOT TP8: micro-motion MUST freeze under prefers-reduced-motion as well as spin=off.\n" +
          "  Declare: const reducedMotion = typeof window !== 'undefined' &&\n" +
          "    window.matchMedia('(prefers-reduced-motion: reduce)').matches;");

    CHECKS 8-18 (FORWARD-CARRIED — verbatim from grep-check-tp7-08.cjs):
    Copy CHECK 1 through CHECK 11 from grep-check-tp7-08.cjs VERBATIM as CHECKS 8-18. Do not
    simplify, rewrite, or omit any of the 11 checks. The forward-carry is the backward-compatibility
    contract — if TP7 invariants regress, this script catches them.

    ONE-LINE OK (exit 0):
    console.log(
      "OK — grep-check-tp8-09: all 18 TP8 micro-motion + TP7 camera invariants hold\n" +
      "  CHECK 1: MICRO_AMPLITUDE_Y <= 0.01wu — TP8 Y-amplitude sub-threshold\n" +
      "  CHECK 2: MICRO_AMPLITUDE_ROT <= 0.00873 rad (0.5°) — TP8 rotation sub-threshold\n" +
      "  CHECK 3: MICRO_IDLE_PERIOD in [6, 12]s — TP8 idle period SSOT range\n" +
      "  CHECK 4: MICRO_SETTLE_TAU in [0.2, 0.4]s — TP8 settle time SSOT range\n" +
      "  CHECK 5: No Elastic/Bounce/Back/Flip/Glow in lab source — TP8 no-FX assertion\n" +
      "  CHECK 6: Freeze guard (motionFrozen/frozen) present — M9 determinism gate\n" +
      "  CHECK 7: reducedMotion/prefers-reduced-motion present — accessibility freeze\n" +
      "  CHECK 8: EffectComposer present behind ?fx (TP6)\n" +
      "  CHECK 9: N8AO present — crevice darkening, M6 satisfier (TP6)\n" +
      "  CHECK 10: DepthOfField present (or cut — M1 HARD gate; disposition in 07-03-SUMMARY) (TP6)\n" +
      "  CHECK 11: Vignette present — frame darkening, M8 gate (TP6)\n" +
      "  CHECK 12: No Bloom in frontend/src/lab/ — M7 HARD GATE (permanently banned) (TP5)\n" +
      "  CHECK 13: brassMat roughness 0.42-0.45 — TP4-locked brass aging pass\n" +
      "  CHECK 14: SoftShadows unconditional in Scene — TP5 PCSS grounding\n" +
      "  CHECK 15: ContactShadows frames={1} — TP5 baked once at mount\n" +
      "  CHECK 16: autoRotate={false} hardcoded — M9 determinism HARD GATE (TP7)\n" +
      "  CHECK 17: fov: 32 / fov: 40 / fov: 26 all present — TP0-frozen money-shot presets (TP7)\n" +
      "  CHECK 18: no second makeDefault camera — flythrough uses useFrame only (TP7)"
    );
    process.exit(0);

    Commit message: "chore(09-02): add grep-check-tp8-09.cjs — 18 checks (7 TP8 micro-motion + 11 forward-carry)"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp8-09.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp7-08.cjs</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep "src/lab" | head -10</automated>
  </verify>
  <done>
    tools/table-3d/grep-check-tp8-09.cjs exits 0 with 18-check OK message; grep-check-tp7-08.cjs
    still exits 0 (11/11 — backward compatibility); vitest green; tsc clean under src/lab.
    LOCAL only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture TP8 gate anchors + document social-read audit and M2 standing</name>
  <files>
    docs/table-3d/anchors/tp8-gate/hero.png
    docs/table-3d/anchors/tp8-gate/card.png
    docs/table-3d/anchors/tp8-gate/macro.png
  </files>
  <read_first>
    - .dev-stack/lab-shot.mjs — confirm CLI signature (LAB_URL + output path arg; spin=off appended
      automatically). These captures are with ?fx (the intended final look) and spin=off (frozen).
    - docs/table-3d/TP7_OPERATOR_GATE.md — the recorded M2 values from TP7 (3.66x hero / 2.60x
      card) that become the M2 standing for TP8. Also confirm the M2 floor (>= 2x).
    - frontend/src/lab/TableLab.tsx — after 09-01 implementation, confirm that no new JSX objects
      were added to the center table-state beyond HeroMotion (which returns null). Any new non-null
      JSX would require re-running M2 via m1-m2-m12.mjs to confirm the 2x floor still holds.
  </read_first>
  <action>
    PREREQUISITE: Vite dev server running on port 5181. Confirm before proceeding.

    STEP 1 — Confirm docs/table-3d/anchors/tp8-gate/ directory exists (created in plan 09-01
    Task 2). If not present: mkdir -p docs/table-3d/anchors/tp8-gate.

    STEP 2 — Capture the three canonical money shots (frozen, with ?fx):
      LAB_URL="http://localhost:5181/table-lab.html?cam=hero&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp8-gate/hero.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=card&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp8-gate/card.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=macro&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp8-gate/macro.png

    These captures are with spin=off (the harness appends it automatically). The HeroMotion
    useFrame is frozen — the scene is identical to the pre-TP8 table for capture purposes. This is
    intentional: the captures are the FROZEN scene record; the live motion is judged by the operator
    in plan 09-03.

    STEP 3 — Social-read completeness audit (document in SUMMARY — no code changes):
    Confirm by reading frontend/src/lab/TableLab.tsx that the following are unconditionally mounted
    in Scene() (no conditional prop hiding any of these):
      (a) CenterGameState — deck stub + dealer button (TP6)
      (b) The staged Perla hole pair (Sota de Oros + 7 de Oros) via LAB_HAND_IDS
      (c) The 5-card community board (from LAB_HAND_IDS)
      (d) The demoted accent pot group (InstancedChipStack, scale={0.5}, position=[3.0, 0, 1.5]
          or confirmed source coordinates)
    Document each item as PRESENT / CONDITIONAL / MISSING in the SUMMARY.
    EXPECTED: all four PRESENT unconditionally. If any is CONDITIONAL, document the condition
    and whether the default URL (no query params) shows it.
    VERDICT: "Social read COMPLETE — CenterGameState + staged Perla hand + community board +
    demoted pot all unconditionally mounted; no new center objects added in TP8; center-only."

    STEP 4 — M2 standing (document in SUMMARY — no new metric run required if no new geometry):
    TP8 adds only HeroMotion (returns null, no geometry). No new scene objects were added. Therefore:
    - M2 is unchanged from the TP7 recorded value: 3.66x (hero) / 2.60x (card) per TP7_OPERATOR_GATE.md.
    - The 2x floor is unambiguously met. No new metric run is needed.
    Record in SUMMARY: "M2 standing: 3.66x / 2.60x (TP7 value, unchanged — TP8 adds no scene
    geometry; HeroMotion returns null). M2 floor >= 2x: PASS."
    EXCEPTION: If the 09-01 executor added any new scene object (which they should not have),
    the SUMMARY from 09-01 will note it. In that case, re-run:
      node tools/table-3d/m1-m2-m12.mjs
    and record the new M2 value. If M2 < 2x: STOP and report — this is a scope violation that
    must be resolved before the operator gate.

    STEP 5 — Run the final pre-gate check battery:
      node tools/table-3d/grep-check-tp8-09.cjs
      node tools/table-3d/grep-check-tp7-08.cjs
    Both must exit 0. Record in SUMMARY.

    STEP 6 — Commit the three anchor captures:
    Stage: docs/table-3d/anchors/tp8-gate/hero.png, card.png, macro.png.
    Commit message: "chore(09-02): TP8 gate anchors -- 3 frozen money shots (spin=off, fx)"
  </action>
  <verify>
    <automated>node -e "const f=require('fs');['docs/table-3d/anchors/tp8-gate/hero.png','docs/table-3d/anchors/tp8-gate/card.png','docs/table-3d/anchors/tp8-gate/macro.png'].forEach(p=>{if(!f.existsSync(p)){console.error('MISSING: '+p);process.exit(1);}});console.log('OK — 3 tp8-gate anchor captures present');"</automated>
    <automated>node tools/table-3d/grep-check-tp8-09.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp7-08.cjs</automated>
  </verify>
  <done>
    hero.png, card.png, macro.png committed to docs/table-3d/anchors/tp8-gate/; grep-check-tp8-09.cjs
    exits 0 (18/18); grep-check-tp7-08.cjs exits 0 (11/11); social-read audit documented in SUMMARY
    (all four scene items PRESENT unconditionally); M2 standing documented (3.66x/2.60x or re-run
    value, >= 2x floor PASS). LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — local tooling and lab render. No auth, no API, not in the prod build.

| Boundary | Description |
|----------|-------------|
| Regex extraction -> amplitude bound | The regex must match the exact token form written in 09-01; a mismatch returns NaN, which fails CHECK 1-4 correctly |
| Frozen anchor captures -> operator gate basis | The three stills show the scene with motion OFF; the operator must view the LIVE scene to judge the motion feel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02a | Tampering | Regex matches a comment constant value (e.g., // MICRO_AMPLITUDE_Y = 0.003) | mitigate | All checks run on nc = stripComments(src) which removes // lines; consistent with tp7-08 convention |
| T-09-02b | Tampering | CHECK 5 "Spin" token matches the legitimate qp("spin") string in non-bounce context | mitigate | CHECK 5 uses /\b(Elastic|Bounce|Back\.out|Back\.in|Flip|Glow)\b/i — "Spin" the easing name is capitalized as a proper gsap ease class, unlike "spin" in qp; the pattern is case-insensitive but requires word-boundary context |
| T-09-02c | Tampering | Forward-carried checks paraphrased rather than verbatim (silent regex regression) | mitigate | read_first mandates reading the full grep-check-tp7-08.cjs before writing; action block says "Copy CHECK 1 through CHECK 11 VERBATIM" |
| T-09-02d | Repudiation | Social-read audit not documented (gate has no basis for "no new objects" claim) | mitigate | Task 2 STEP 3 documents each of the four scene items explicitly in SUMMARY; plan 09-03 reads this SUMMARY |
| T-09-02e | Tampering | M2 re-run skipped when 09-01 actually added a new scene object | mitigate | Task 2 STEP 4 includes the exception path: "If the 09-01 executor added any new scene object, re-run m1-m2-m12.mjs" |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | grep-check-tp8-09.cjs uses Node stdlib only; no packages installed in this plan |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp8-09.cjs exits 0 (18 checks: MICRO_AMPLITUDE_Y/ROT/PERIOD/TAU within bounds + no-FX + freeze-guard + reducedMotion + 11 forward-carried TP7 invariants).
- node tools/table-3d/grep-check-tp7-08.cjs exits 0 (11/11 — backward compatibility).
- docs/table-3d/anchors/tp8-gate/ contains hero.png, card.png, macro.png (committed).
- cd frontend && npx vitest run src/lab/ green.
- cd frontend && npx tsc --noEmit clean under src/lab.
- Social-read audit in SUMMARY: CenterGameState + staged Perla + community board + demoted pot = PRESENT unconditionally; no new objects.
- M2 standing in SUMMARY: >= 2x (documented; re-run if new geometry was added in 09-01).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- grep-check-tp8-09.cjs exits 0 with 18-check OK; grep-check-tp7-08.cjs exits 0.
- Three tp8-gate anchor captures committed.
- Social-read and M2 documented in SUMMARY.
- No new packages; no geometry/material/camera changes.
- LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/09-tp8-tactilidad-microvida-social/09-02-SUMMARY.md` when done.
Record: grep-check-tp8-09.cjs 18-check verdict (pass/fail per check) and exit code;
grep-check-tp7-08.cjs verdict; the exact MICRO_* constant values extracted by the regexes
(confirms the 09-01 calibration); social-read completeness audit table (4 items, each PRESENT/
CONDITIONAL/MISSING); M2 standing (ratio and source: TP7 carry-forward or fresh run);
three anchor capture paths. Plan 09-03 reads this SUMMARY as the full pre-gate record.
</output>
