---
phase: 06-tp5-iluminacion-sombras-warm-light
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - docs/table-3d/anchors/tp5-gate/rail-shaped.png
  - docs/table-3d/anchors/tp5-gate/rail-base.png
autonomous: true
requirements:
  - "SSOT §TP5 — felt green-bounce verification: hemisphere ground tint #0d3d24 (from 06-02) produces a subtle warm-green GI on object undersides (chips, cards, body apron underside) — not a lime wash, not a green spotlight"
  - "SSOT §TP5 — apron/body volume read: the table body outer wall must read as a volume (top-face lighter, underside green-warm tinted) — resolves 'table floats' (SSOT §TP5 red-team #7). Lighting fix only; the PointLight at [0,-0.25,11] already rakes the body fascia."
  - "SSOT §TP5 — no new light added: the green-bounce is the hemisphere ground tint from 06-02 (zero new draw calls); the body volume is the shaped hemisphere + the existing pointLight (no new shadow-caster)"
  - "SSOT §TP5 — ?cam=rail captures: verify body top-face reads warmer/lighter than underside; confirm no green spotlight read on chip undersides; the green should be a subtle warm tint, not dominant"

must_haves:
  truths:
    - "The ?cam=rail shot with shaped path shows the body outer wall reading as a volume (top lighter/warmer, underside with a warm-green tint from the hemisphere ground)"
    - "No 'green spotlight' read: chip undersides show a subtle warm-green tint (not a saturated lime spotlight)"
    - "The body volume effect (top vs underside) is present with ?light= default (shaped) and absent or subtler with ?light=base"
    - "M6 PASS holds at the rail shot (under-rail shadow still present)"
    - "No new light sources added to the scene (zero new draw calls)"
  artifacts:
    - path: "docs/table-3d/anchors/tp5-gate/rail-shaped.png"
      provides: "Capture confirming green-bounce + body volume read at ?cam=rail with shaped path"
      contains: ""
    - path: "docs/table-3d/anchors/tp5-gate/rail-base.png"
      provides: "Capture confirming prior flat-warm key read at ?cam=rail with ?light=base"
      contains: ""
  key_links:
    - from: "hemisphereLight ground #0d3d24 (06-02)"
      to: "body apron underside GI tint + chip underside GI tint"
      via: "HemisphereLight ground color illuminates surfaces facing downward — the physically-motivated mechanism for felt green-bounce"
      pattern: "#0d3d24"
---

<objective>
Verify the felt green-bounce and body volume read that were delivered mechanically in 06-02
(hemisphere ground tint #0d3d24) and 06-03 (body specular recedes). No new code changes are
needed — this plan confirms the perceptual outcomes with ?cam=rail captures and runs the
metric gates on those captures.

Purpose: The SSOT §TP5 specifies "felt green-bounce onto object undersides" and "apron/rail
outer wall as a volume" as distinct perceptual goals. Both are mechanically delivered by the
hemisphere ground tint (06-02) and the body envMapIntensity/clearcoat reduction (06-03).
This plan produces the authoritative rail-angle captures that the operator gate (06-06) will
use to judge these two effects, and confirms no "green spotlight" artifact from the tint.

Output: docs/table-3d/anchors/tp5-gate/{rail-shaped,rail-base}.png committed; M6 confirmed
at the rail shot; green-bounce and body volume perceptual check recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-CONTEXT.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-RESEARCH.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-02-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture ?cam=rail shaped vs base + run M6 at rail + record green-bounce verdict</name>
  <files>docs/table-3d/anchors/tp5-gate/rail-shaped.png, docs/table-3d/anchors/tp5-gate/rail-base.png</files>
  <read_first>
    - 06-RESEARCH.md Pattern 3 (green-bounce mechanism — hemisphere ground tint; why NOT a
      separate fill light; the "green spotlight" anti-pattern warning); Pattern 4 (apron/body
      volume lighting — the existing pointLight at [0,-0.25,11] already rakes the body fascia;
      the hemisphere ground tint completes the top-key/bottom-bounce volume read).
    - 06-RESEARCH.md Assumption A3 (hemisphere ground #0d3d24 saturation: if too saturated →
      lime wash → lower saturation toward #0a2f1a or remove entirely) and A6 (fallback: if
      the tint is too uniform, a low-intensity green pointLight above felt level could be added
      — but only as a fallback, not the default).
    - .dev-stack/lab-shot.mjs — the LAB_URL pattern for ?cam=rail captures.
    - 06-02-SUMMARY.md — the exact shaped/base hemisphere ground values as committed (to confirm
      they match the RESEARCH spec #0d3d24 / #1a0f08).
  </read_first>
  <action>
    STEP 1 — Ensure dev server is running.

    STEP 2 — Capture the rail shot with the shaped path (default, no ?light= param):
      LAB_URL="http://localhost:5173/table-lab.html?cam=rail" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/rail-shaped.png

    STEP 3 — Capture the rail shot with the base path (?light=base — prior flat-warm key):
      LAB_URL="http://localhost:5173/table-lab.html?cam=rail&light=base" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/rail-base.png

    STEP 4 — Run the metric suite on all captures in the tp5-gate directory:
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate
    Confirm M6 PASS at the rail shot (under-rail/body shadow area >=12% darker than adjacent
    felt). This confirms the grounding from 06-01 holds at the rail camera angle.

    STEP 5 — Green-bounce perceptual check (pixel-based proxy):
    Using node, compare the mean luma and hue of a region at the body underside in
    rail-shaped.png vs rail-base.png. The underside region should show a higher green-channel
    contribution in rail-shaped.png vs rail-base.png (green-warm tint present). This is the
    hemisphere ground tint effect. If the green-channel delta between shaped/base at the
    underside region is < 2 luma points (imperceptible): note in the record that the effect is
    present but very subtle — this is acceptable (the SSOT says "subtle GI"). If > 30 luma points
    higher in green: the tint is too saturated (lime-wash risk). In that case, lower hemisphere
    ground from #0d3d24 to #0a2f1a in TableLab.tsx, rebuild, recapture, rerun.

    STEP 6 — Body volume read proxy:
    Compare the luma of the top-face of the body (the horizontal ledge visible at ?cam=rail)
    vs the luma of the underside fascia in rail-shaped.png. The top-face should read warmer/
    brighter than the underside (the key light hits the top-face; the hemisphere ground green
    tints the underside). If both read at the same luma (no volume read): the body pointLight
    at [0,-0.25,11] is the primary actor; verify it is present and intensity=0.5 (unchanged
    from baseline). If the volume read is flat, note it — do NOT add a new light (SSOT hard gate:
    no new shadow-casting light). Record in the SUMMARY as "volume read present/absent/subtle".

    STEP 7 — M7 confirm (no postprocessing introduced):
      node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab

    Commit: "chore(06-04): TP5 green-bounce + body volume rail captures + M6/M7 gates"
  </action>
  <verify>
    <automated>node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab</automated>
    <automated>node -e "const fs=require('fs'); ['rail-shaped','rail-base'].forEach(s=>{const p='docs/table-3d/anchors/tp5-gate/'+s+'.png'; if(!fs.existsSync(p)){console.error('MISSING: '+p); process.exit(1);}}); console.log('OK — rail captures present');"</automated>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate</automated>
  </verify>
  <done>docs/table-3d/anchors/tp5-gate/{rail-shaped,rail-base}.png committed; M6 PASS confirmed at rail shot; M7 PASS confirmed; green-bounce perceptual read recorded (subtle warm-green on underside, not a lime wash); body volume read recorded (top-face vs underside luma delta); any hemisphere ground tuning (if lime-wash triggered) committed and documented; no new lights added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build.

| Boundary | Description |
|----------|-------------|
| Green-bounce saturation | Hemisphere ground #0d3d24 must produce a subtle tint, not a lime wash; A3 assumption is checked at this capture step |
| No new lights | Body volume read relies on the existing pointLight at [0,-0.25,11]; if absent, note but do NOT add a new shadow-casting light |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04a | Tampering | Green-bounce lime wash (hemisphere tint too saturated) | mitigate | Capture at ?cam=rail; if green-channel delta >30 points → lower ground to #0a2f1a; recheck; the effect should be "subtle GI" not "green lamp" |
| T-06-04b | Tampering | New shadow-casting light added for body volume | mitigate | No new lights in this plan; body volume is the hemisphere+pointLight combination; SSOT hard gate enforced |
| T-06-SC | Tampering | npm installs | accept | No package installs; capture-only plan |
</threat_model>

<verification>
- docs/table-3d/anchors/tp5-gate/{rail-shaped,rail-base}.png present and committed.
- run-metrics.mjs exits 0 with M6 PASS at rail shot.
- m7-bloom-assert exits 0 (M7 PASS).
- Green-bounce verdict recorded: subtle warm-green on undersides (not lime-wash, not absent).
- Body volume read verdict recorded: top-face vs underside luma delta (present/absent/subtle).
- No code changes to TableLab.tsx in this plan (unless hemisphere tint correction was needed).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Rail-angle captures committed for both shaped and base paths.
- M6 PASS at rail shot (under-body shadow grounding holds).
- M7 PASS (no postprocessing).
- Green-bounce verdict: "subtle warm-green GI on undersides" (or lime-wash correction documented).
- Body volume verdict: "top-face lighter/warmer than underside" or "minimal but present" recorded.
- LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-04-SUMMARY.md` when done,
recording the M6 value at the rail shot (under-body shadow percentage), the green-bounce
verdict (green-channel delta between shaped/base at underside; tuning if applied), the body
volume read verdict (top-face vs underside luma delta), and the final hemisphere ground color
(#0d3d24 as planned or adjusted to #0a2f1a if lime-wash was triggered).
</output>
