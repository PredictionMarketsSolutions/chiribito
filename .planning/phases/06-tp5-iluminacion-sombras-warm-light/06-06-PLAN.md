---
phase: 06-tp5-iluminacion-sombras-warm-light
plan: 06
type: execute
wave: 6
depends_on:
  - 06-05
files_modified:
  - docs/table-3d/TP5_OPERATOR_GATE.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
autonomous: false
requirements:
  - "SSOT §TP5 perceptual gate — operator A/B at all 3 frozen money shots: 'every material under ONE warm motivated light, honest grounding, restrained highlights, no casino harshness / cold void?'"
  - "SSOT §TP5 — stop-on-ambiguous: pool reads as spotlight → STOP; pull back intensity/penumbra/clearcoat; OR keep prior flat-warm key (?light=base) + ship grounding; grounding (SoftShadows + ContactShadows) ALWAYS ships"
  - "SSOT §TP5 — NOT auto-approvable; autonomous: false; this is the operator human seam"
  - "SSOT §TP5 — gate record: docs/table-3d/TP5_OPERATOR_GATE.md records verdict + per-element disposition + HARD gate confirmation + ?light= flag map + non-blocking rollback disposition"
  - "SSOT §TP5 — SCORECARD update: lighting + shadows rows for all materials if shipped; must not regress from TP4 column"

must_haves:
  truths:
    - "The operator has reviewed all 3 frozen money shots at shaped path (default) and ?light=base path, with A/B captures from the tp5-gate anchor dir available"
    - "The HARD gates are confirmed: M4 PASS (H/S/V from 06-05); M5 PASS; M6 PASS; +A PASS; M7 PASS; M10 <=106 HERO (from 06-05)"
    - "The non-blocking rollback disposition is presented: grounding (SoftShadows + ContactShadows + shadow frustum) ALWAYS ships; ?light= shaped key is the separable workstream"
    - "On approval: docs/table-3d/TP5_OPERATOR_GATE.md records verdict + A/B basis + per-element disposition + HARD-gate confirmation + ?light= flag map"
    - "On approval: SCORECARD_TABLE_3D.md lighting/shadows rows updated (must not regress from TP4 column)"
    - "On stop-on-ambiguous on key: ?light=base becomes the shipped default; grounding ships; key-reshaping deferred or dropped; TP5_OPERATOR_GATE.md records the stop reason"
  artifacts:
    - path: "docs/table-3d/TP5_OPERATOR_GATE.md"
      provides: "Recorded operator verdict, A/B basis (vs tp5-pre captures), per-element disposition, HARD gate confirmation (M4/M5/M6/+A/M7/M10), ?light= flag map, non-blocking rollback disposition"
      contains: "Verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP5 score update for lighting/shadows rows — must not regress from TP4 column"
      contains: "lighting"
  key_links:
    - from: "docs/table-3d/TP5_OPERATOR_GATE.md"
      to: "docs/table-3d/anchors/tp5-gate/{hero-final,card,macro,rail-shaped,hero-base,hero-shaped}.png"
      via: "The A/B compares shaped path (tp5 default) against base path (?light=base = prior flat-warm key); grounding always-on in both paths"
      pattern: "tp5-gate"
    - from: "?light= flag map"
      to: "non-blocking rollback disposition"
      via: "?light=base restores prior flat-warm key while keeping SoftShadows + ContactShadows frames=1; grounding ships regardless of key verdict"
      pattern: "light=base"
---

<objective>
The TP5 operator perceptual gate — the GSD / Chiribito human seam. The operator reviews all
three frozen money shots (hero, card, macro) comparing the shaped path (default, new warm
gradient + green-bounce + PCSS grounding) against the base path (?light=base, prior flat-warm
key with the same grounding). The gate question: "every material under ONE warm motivated light,
honest grounding, restrained highlights, no casino harshness / cold void?"

Purpose: Automated metrics (M4/M5/M6/+A/M7/M10) and grep-check-tp5-06.cjs are necessary-not-
sufficient. Only the operator can judge whether the scene reads as ONE coherent shaped warm light
(not a casino cone, not a flat wash) with honest contact grounding (not hovering) and restrained
per-material highlights (not clipping, not wet, not gold). This gate cannot be auto-approved
(autonomous:false; auto_advance does not apply).

Output: docs/table-3d/TP5_OPERATOR_GATE.md recording verdict + per-element disposition + HARD-
gate confirmation + ?light= flag map; SCORECARD_TABLE_3D.md updated if shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-CONTEXT.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-01-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-02-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-03-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-04-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-05-SUMMARY.md
@docs/table-3d/TP4_OPERATOR_GATE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — "every material under ONE warm motivated light, honest grounding, restrained highlights, no casino harshness / cold void?" at HERO + CARD + MACRO</name>
  <files>docs/table-3d/TP5_OPERATOR_GATE.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - .planning/phases/06-tp5-iluminacion-sombras-warm-light/06-05-SUMMARY.md — the complete
      pre-gate metric table (M4 H/S/V, M5, M6 per shot, +A, M7, M10 draw count). These are
      the HARD gate values to restate at the gate presentation.
    - .planning/phases/06-tp5-iluminacion-sombras-warm-light/06-02-SUMMARY.md — the ?light= flag
      map and the shaped path verdict (key ratio, A/B captures).
    - .planning/phases/06-tp5-iluminacion-sombras-warm-light/06-03-SUMMARY.md — the per-material
      specular table (what changed and what was locked).
    - .planning/phases/06-tp5-iluminacion-sombras-warm-light/06-04-SUMMARY.md — the green-bounce
      and body volume verdicts.
    - docs/table-3d/TP4_OPERATOR_GATE.md — the gate record template shape (mirror this structure).
    - docs/table-3d/SCORECARD_TABLE_3D.md — current lighting/shadows rows to update if approved.
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP5 — the SSOT gate question, stop-on-ambiguous rule,
      and rollback disposition (L380-388).
  </read_first>
  <action>
    Present the A/B to the operator using the consolidated captures from 06-01 through 06-05
    (live lab preferred; static captures as fallback), following the how-to-verify protocol.
    Do NOT auto-approve — this is a perceptual gate (autonomous:false; auto_advance does not apply).

    First restate the HARD gates from the 06-05-SUMMARY:
    - M4 brass: H [measured] / S [measured] / V [measured] — within H 35-48° / S<=0.55 / V<=0.80
    - M5 highlight-clip: [measured %] felt area — below 0.5% threshold
    - M6 contact shadow: [measured %] under objects — above 12% threshold (all shots)
    - +A warm corner floor: luma [measured] / hue [measured] — luma >=18 / warm hue PASS
    - M7 bloom: 0 forbidden tokens in frontend/src/lab/ — PASS
    - M10 draw count: [measured] at HERO — <=106 (TP4 baseline; improved if lower)
    - grep-check-tp5-06.cjs: exits 0 (all 6 structural invariants)

    Then present the per-element disposition from the TP5 summaries:
    - GROUNDING (always-on): SoftShadows PCSS [params]; ContactShadows frames=1 [params];
      shadow-normalBias + shadow-camera-near/far on key
    - KEY RESHAPING (?light= separable): shaped path [angle/ratio] vs base path [prior values]
    - PER-MATERIAL SPECULAR: [changes per 06-03-SUMMARY — wood/body/card/chip]
    - GREEN-BOUNCE: hemisphere ground [color] — [verdict from 06-04-SUMMARY]
    - BODY VOLUME: [verdict from 06-04-SUMMARY]
    - BRASS: UNCHANGED (TP4-locked roughness 0.42 / envMapIntensity 0.45)

    On operator verdict, record into docs/table-3d/TP5_OPERATOR_GATE.md (mirror
    TP4_OPERATOR_GATE.md structure):
    - The verdict string.
    - The A/B basis (shaped path vs ?light=base; captures in tp5-gate/).
    - The per-element disposition (grounding: always-ships; key-reshaping: shaped/base/reverted;
      per-material specular: shipped/reverted; green-bounce: shipped/dropped).
    - The HARD-gate confirmation (M4/M5/M6/+A/M7/M10 with measured values).
    - The ?light= flag map (all values and their meaning — see below).
    - The non-blocking rollback disposition (grounding ships regardless; key is separable).

    ?light= FLAG MAP to document:
      (default, no ?light= param)  = shaped key (wider angle 0.72, green-bounce hemisphere
                                      ground #0d3d24, key/fill ratio 2.75x, ratio ceiling 3.5x)
      ?light=base                  = prior flat-warm key (angle 0.62, hemisphere ground #1a0f08,
                                      key/fill ratio 2.86x, baseline intensities unchanged)
      Note: SoftShadows PCSS + ContactShadows frames={1} + warm shadow floor (#1a0e06) +
      shadow-normalBias + shadow-camera-near/far are ALWAYS ON in both paths.

    On UNAMBIGUOUS APPROVAL: update SCORECARD_TABLE_3D.md lighting/shadows + grounding rows
    with the post-TP5 score. Score must not regress from the TP4 column. Note if AAA(5)
    awaits TP6 for the AO/depth pass.

    On STOP-ON-AMBIGUOUS (key reads as spotlight / a specific specular clips / brass drifts):
    STOP. Identify the offending element via its corresponding flag or parameter. Execute the
    appropriate correction ONLY:
      - Key reads as spotlight / too tight: set ?light=base as the shipped default (keep prior
        flat-warm key); grounding still ships; note in TP5_OPERATOR_GATE.md as "key-reshaping
        deferred — grounding shipped; prior flat-warm key is default."
      - Specular clips on wood (M5 FAIL): raise wood clearcoatRoughness by 0.03 (from the
        06-03 value); recapture; rerun M5; re-present.
      - M4 FAIL at gate (brass drifts gold): this should have been caught in 06-03; if triggered
        at gate, lower the highest chip/wood clearcoat (the most likely env contributor); rerun M4.
      - Any single element reads wrong: isolate it, correct, recapture, re-present that shot only.

    DEFAULT STOP (overall ambiguous): keep grounding (always-ships); revert key to ?light=base;
    ship the per-material specular changes from 06-03 that individually passed M4/M5; record the
    stop reason as "shaped key read ambiguous — grounding + per-material specular ship; key
    deferred."

    Do NOT add postprocessing. Do NOT add new lights. Do NOT change geometry or cameras.
    LOCAL only — no push / deploy / merge.
  </action>
  <what-built>
    TP5 executed four implementation workstreams before this gate, all locally only:

    (A) GROUNDING — 06-01 (ALWAYS-ON, the must-ship deliverable):
    SoftShadows (PCSS, size=30 samples=16 focus=0) injected unconditionally in Scene.
    ContactShadows upgraded: frames={1} (baked once at mount — M11 improvement), opacity=0.35
    (anti-double-darken), color="#1a0e06" (warm near-black — +A warm floor), far=5, blur=2.0,
    scale=FELT_R*3.5. Key spotLight: shadow-normalBias=0.02 (peter-pan), shadow-camera-near=8,
    shadow-camera-far=28 (frustum tightened). M6 PASS + +A PASS + M10 <=106 HERO confirmed.

    (B) KEY RESHAPING — 06-02 (behind ?light= flag; separately revertible):
    Shaped path (default): key angle=0.72 (from 0.62), intensity=2.2, fill=0.8 (from 0.7),
    rim=0.22, hemisphere ground="#0d3d24" (dark felt-green, green-bounce GI on undersides).
    KEY_TO_FILL_RATIO_CEILING = 3.5 constant; shaped ratio 2.75x; base ratio 2.86x.
    Base path (?light=base): exact prior flat-warm key restored (angle=0.62, intensity=2.0,
    fill=0.7, hemisphere ground="#1a0f08").
    Shaped path verdict from captures: [INSERT FROM 06-02-SUMMARY].

    (C) PER-MATERIAL SPECULAR — 06-03 (in safer direction; brass UNCHANGED):
    Wood: roughness 0.42, clearcoat 0.68, clearcoatRoughness 0.25, envMapIntensity 0.55.
    Body: roughness 0.52, clearcoat 0.44, clearcoatRoughness 0.35, envMapIntensity 0.4.
    Card stock: roughness 0.60, clearcoatRoughness 0.50.
    Card face: roughness 0.50.
    Chip pre-dv: roughness 0.52, clearcoat 0.38, clearcoatRoughness 0.50, sheen 0.4.
    Brass: UNCHANGED (roughness 0.42, envMapIntensity 0.45 — TP4-locked).
    M4 re-run at END: PASS (H/S/V: [INSERT FROM 06-03-SUMMARY]). M5 PASS.

    (D) GREEN-BOUNCE + BODY VOLUME — 06-04 (verification):
    Green-bounce verdict: [INSERT FROM 06-04-SUMMARY — subtle warm-green on undersides].
    Body volume verdict: [INSERT FROM 06-04-SUMMARY — top-face vs underside luma delta].

    HARD GATES (confirmed in 06-05):
    grep-check-tp5-06.cjs: exits 0 (6/6 structural invariants).
    M4 H [value] / S [value] / V [value] — PASS.
    M5 [%] felt area — PASS.
    M6 [%] under objects — PASS at all shots.
    +A luma [value] hue [value] — PASS.
    M7: 0 postprocessing tokens — PASS.
    M10 HERO draw count: [count] — <=106 (TP4 baseline).
  </what-built>
  <how-to-verify>
    Judge the LIGHTING COHERENCE + GROUNDING at the three frozen money shots.

    1. Bring the dev server up if needed (cd frontend && npm run dev) and open the lab live, OR
       review the still captures in docs/table-3d/anchors/tp5-gate/.

    2. HERO shot (fov32 — the full table read, the definitive money shot):
       Open http://localhost:5173/table-lab.html?cam=hero (shaped path, default).
       Then open http://localhost:5173/table-lab.html?cam=hero&light=base (prior flat-warm key).
       Compare against the base captures (hero-base.png in tp5-gate/).

       Gate questions for HERO:
       (a) "Does the table read under ONE coherent shaped warm light — warm gradient pooling
           GENTLY toward the rail center, not a tight casino cone over dark corners?"
       (b) "Does the contact grounding read honest — chips and cards sit ON the felt, not
           hovering 1-2 px above it? Is the contact shadow warm (not cold void / not
           crushed black)?"
       (c) "Do per-material highlights read crisp but SMALL — the wood varnish catches a
           restrained glint, chip edges catch light, brass reads aged? None of these clips?"
       (d) "Are the corners/edges warm (not cold void) — the +A warm floor is present?"

    3. CARD shot (?cam=card — the close-up card/felt/chip read):
       Open http://localhost:5173/table-lab.html?cam=card.
       Gate questions for CARD:
       (a) "Do the card edges catch a crisp small highlight under the warm key?"
       (b) "Is the felt-to-card contact shadow present (not floating)?"
       (c) "Does the clearcoat on the card backing read as a whisper coat, not a glass slab?"

    4. MACRO shot (?cam=macro — the close-up chip/wood/brass read):
       Open http://localhost:5173/table-lab.html?cam=macro.
       Gate questions for MACRO:
       (a) "Do chips read as matte clay with a restrained sealed highlight? Not casino gloss?"
       (b) "Does the wood coaming read as varnished wood — a thin highlight band, not a
           mirror-wet chrome surface?"
       (c) "Does the brass reveal read AGED (warm, subdued) — not new / not gold)?"

    5. RAIL shot (?cam=rail if available — body volume + green-bounce read):
       Open http://localhost:5173/table-lab.html?cam=rail.
       Gate questions for RAIL:
       (a) "Does the body outer wall read as a VOLUME — top-face lighter/warmer, underside
           tinted with a subtle warm-green (not a lime spotlight)?"
       (b) "Does the table read as SITTING on the floor (contact shadow present at body base),
           not floating?"

    6. The ?light= flag map (for A/B isolation):
       ?light=base          = prior flat-warm key (angle 0.62, fill 0.7, hemisphere ground near-black)
       (default, no param)  = shaped key (angle 0.72, fill 0.8, green-bounce, ratio 2.75x ceiling)
       SoftShadows + ContactShadows + warm shadow floor: ALWAYS ON in both paths.
       Use ?light=base to confirm "does the grounding read honest with the prior key too?"

    7. HARD gates (confirmed in 06-05; re-confirm live if requested):
       M4 PASS (H 35-48°/S<=0.55/V<=0.80); M5 PASS; M6 PASS; +A PASS; M7 PASS; M10 <=106.
       node tools/table-3d/grep-check-tp5-06.cjs → must exit 0.
       These are non-negotiable; the operator cannot approve if any metric FAILS.

    8. Stop-on-ambiguous protocol:
       - Key reads as a spotlight (tight pool, dark corners): STOP. Name "key-reshaping". Execute:
         set ?light=base as default by swapping the shaped/base conditional (flip the shaped logic
         to use base values as default); ship grounding only; re-present.
       - Specular clips on a specific material: STOP. Name the material. Raise its clearcoatRoughness
         by 0.03; recapture; rerun M5; re-present that shot.
       - Body/chip highlights read too strong overall: STOP. Lower the material's clearcoat by 0.04
         (within M5 bounds); re-present.
       - Overall lighting reads as casino / unnatural: DEFAULT STOP. Keep grounding (always ships).
         Revert to ?light=base as default. Ship only the grounding + per-material specular changes
         that passed their individual gates. Record the stop reason.
       DEFAULT: if the overall light read is ambiguous or "not clearly better" → DEFAULT STOP.
       Grounding still ships. Key-reshaping is the separable workstream.

    DISPOSITION (record in docs/table-3d/TP5_OPERATOR_GATE.md):
    - UNAMBIGUOUS YES (ONE coherent warm light, honest grounding, restrained highlights, no casino
      harshness / cold void; all HARD gates green): TP5 SHIPS (grounding + key-reshaping + specular).
      Update SCORECARD_TABLE_3D.md (must not regress; note if AAA(5) awaits TP6 for AO/depth).
    - STOP-ON-AMBIGUOUS on key (spotlight read): ship grounding + specular; revert key to ?light=base
      default; record "key-reshaping deferred: grounding + specular ship; prior flat-warm key as default."
    - STOP-ON-AMBIGUOUS on a specific specular: fix + re-present; if re-present passes → ship that fix.
    - DEFAULT STOP (overall ambiguous): ship grounding; ship specular changes that individually passed;
      revert key to ?light=base default. Record the stop reason.
  </how-to-verify>
  <resume-signal>
    Operator types:
    - "approved" — TP5 ships (grounding + shaped key + specular + green-bounce); update scorecard.
    - "approved — grounding only" — grounding ships (SoftShadows + ContactShadows + shadow frustum
      + warm floor); key stays at ?light=base as default; specular ships if M4/M5 passed.
      Record as "key-reshaping deferred — grounding + per-material specular ship."
    - "revert key" + reason (e.g. "reads as spotlight") — executor sets ?light=base as default;
      ships grounding + specular; re-presents (or records if a single re-present is not needed).
    - "revert [material] specular" + reason (e.g. "wood reads wet") — executor raises that
      material's clearcoatRoughness by 0.03; recaptures; re-presents that shot.
    - "stop" — DEFAULT STOP; ship grounding; key at ?light=base default; specular as-passed;
      record the stop reason.
    Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has reviewed all 3 frozen money shots (hero + card + macro, shaped vs base),
    with the HARD gates (M4/M5/M6/+A/M7/M10) restated from 06-05-SUMMARY. The verdict + per-
    element disposition (grounding: always-ships; key-reshaping: shaped/base/deferred; per-
    material specular: shipped/reverted; green-bounce: shipped/dropped) + the HARD-gate
    confirmation + the ?light= flag map are recorded in docs/table-3d/TP5_OPERATOR_GATE.md.
    SCORECARD_TABLE_3D.md lighting/shadows rows updated if shipped (must not regress from TP4
    column; note if AAA(5) awaits TP6 for AO/depth). No postprocessing added. No new lights.
    No geometry/camera changes. LOCAL only — no push/deploy/merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build. /table-lab.html is dev-server-only.
At this gate the boundaries are the perceptual judgement seam, the HARD metric gates (M4/M5/M6/+A/M7/M10),
and the non-blocking rollback disposition.

| Boundary | Description |
|----------|-------------|
| automated pre-gate metrics -> human perceptual gate | M4/M5/M6/+A/M7/M10/grep-check are necessary-not-sufficient; the operator judges "one coherent warm light, honest grounding, restrained highlights, no casino harshness" |
| key-reshaping -> grounding (non-blocking split) | Key-reshaping (?light= shaped) is separable from grounding (SoftShadows + ContactShadows); grounding ships regardless of key verdict |
| TP5 changes -> felt/cards/chips/cameras | This gate changes only lighting/shadows/specular disposition; no geometry, no cameras, no felt/card/chip identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06a | Tampering | HARD gates (M4/M5/M6/+A/M7/M10) | mitigate | Restated at gate from 06-05-SUMMARY; operator confirms all PASS before approving any element; if any FAIL → fix before gate |
| T-06-06b | Tampering | Casino cone read (key too tight) | mitigate | KEY_TO_FILL_RATIO_CEILING=3.5 is the pre-gate structural guard; shaped path 2.75x; operator names it explicitly if the read looks tight; fix: revert to ?light=base |
| T-06-06c | Tampering | Auto-approve circumventing the operator gate | mitigate | autonomous:false; this checkpoint cannot be auto-approved; auto_advance does not apply |
| T-06-06d | Repudiation | Gate decision not recorded | mitigate | docs/table-3d/TP5_OPERATOR_GATE.md records verdict + per-element disposition + HARD-gate confirmation + flag map (non-auto-approvable; operator-signed) |
| T-06-06e | Tampering | Postprocessing added as "just one more" fix at gate | mitigate | TP5 HARD gate: no EffectComposer/Bloom/SSAO; grep-check CHECK 5 enforces; any postprocessing fix is explicitly deferred to TP6 |
| T-06-SC | Tampering | npm installs | accept | No package installs in TP5 gate plan |
</threat_model>

<verification>
- The operator has reviewed HERO (?cam=hero default + ?cam=hero&light=base) + CARD (?cam=card) + MACRO (?cam=macro), with the shaped vs base A/B, the flag map, and the HARD gates presented.
- The HARD gates from 06-05-SUMMARY are restated: M4 PASS (H/S/V); M5 PASS; M6 PASS; +A PASS; M7 PASS; M10 <=106; grep-check exits 0.
- docs/table-3d/TP5_OPERATOR_GATE.md records: verdict, A/B basis (tp5-gate/ captures), per-element disposition, HARD-gate confirmation, ?light= flag map, rollback disposition.
- docs/table-3d/SCORECARD_TABLE_3D.md lighting/shadows rows updated if shipped (must not regress from TP4 column).
- No postprocessing. No new lights. No geometry/camera changes. bodyProfile() + felt/cards/cameras untouched.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at HERO + CARD + MACRO (+ RAIL for body volume); verdict recorded.
- Unambiguous yes: TP5 ships (grounding + key-reshaping + specular + green-bounce; scorecard not regressed).
- OR: grounding ships; key reverted to ?light=base default; specular ships; verdict recorded.
- OR: DEFAULT STOP with explicit reason; grounding ships; per-element disposition documented.
- The non-blocking split is explicit in TP5_OPERATOR_GATE.md (grounding always-ships; key is separable).
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-06-SUMMARY.md` when done,
and write the verdict to `docs/table-3d/TP5_OPERATOR_GATE.md`. Update the lighting/shadows rows
in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP5 scores if shipped (must not regress;
note if AAA(5) awaits TP6 for AO/depth/postprocessing pass). The SUMMARY records the full
gate disposition: verdict, which elements shipped / were deferred / were dropped, the ?light=
default path as confirmed, and the final flag map.
</output>
