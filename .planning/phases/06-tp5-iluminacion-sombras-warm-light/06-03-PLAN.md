---
phase: 06-tp5-iluminacion-sombras-warm-light
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP5 — per-material specular tuning ONLY (NOT global IBL/exposure): wood roughness 0.38->0.42 clearcoat 0.72->0.68 clearcoatRoughness 0.2->0.25 envMapIntensity 0.65->0.55; body roughness 0.48->0.52 clearcoat 0.5->0.44 clearcoatRoughness 0.3->0.35 envMapIntensity 0.5->0.4; card stock roughness 0.62->0.60 clearcoatRoughness 0.55->0.50; chip pre-dv roughness 0.5->0.52 clearcoat 0.42->0.38 clearcoatRoughness 0.46->0.50 sheen 0.5->0.4"
  - "SSOT §TP5 — BRASS UNCHANGED: brassMat roughness stays at 0.42 (TP4-locked); envMapIntensity stays at 0.45 (TP4-locked). DO NOT touch brassMat at all."
  - "SSOT §TP5 — M4 brass PASS at END: after all specular changes, node tools/table-3d/run-metrics.mjs must return H in [35,48] S<=0.55 V<=0.80 on brass. M5 highlight-clip PASS (no new clipping from wood/card changes)."
  - "SSOT §TP5 — de-Vegas chip path (?chips=dv): roughness 0.72 clearcoat 0.32 clearcoatRoughness 0.50 — ALL UNCHANGED (already tuned at TP3). Do NOT change the dv path."
  - "SSOT §TP5 — felt/leather/chip-dv/brass — no specular changes on these materials in this plan"

must_haves:
  truths:
    - "Wood coaming: roughness raised to 0.42 (anti-wet direction), clearcoat lowered to 0.68, clearcoatRoughness raised to 0.25, envMapIntensity lowered to 0.55"
    - "Body/apron material: roughness 0.52, clearcoat 0.44, clearcoatRoughness 0.35, envMapIntensity 0.4 (body recedes behind rail)"
    - "Card stock material: roughness 0.60, clearcoatRoughness 0.50 (crisp card-edge highlight under warm key)"
    - "Card face material: roughness 0.50 (slight tighten from 0.52)"
    - "Chip pre-dv body: roughness 0.52, clearcoat 0.38, clearcoatRoughness 0.50, sheen 0.4 (matte clay read, anti-casino)"
    - "Chip dv path (?chips=dv): roughness 0.72, clearcoat 0.32, clearcoatRoughness 0.50 — ALL UNCHANGED"
    - "brassMat: roughness 0.42, envMapIntensity 0.45 — UNCHANGED from TP4 lock"
    - "feltMat: all props unchanged (felt is the bounce source, not the recipient)"
    - "M4 PASS after all changes: brass HSV H in [35,48] S<=0.55 V<=0.80"
    - "M5 PASS: highlight-clip not introduced by wood/card changes (luma>250 < 0.5% felt area)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "Per-material specular deltas for wood/body/card/chip applied; felt/leather/brass/dv-chip unchanged"
      contains: "clearcoatRoughness"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx woodMat useMemo"
      to: "tools/table-3d/run-metrics.mjs M5 gate"
      via: "wood roughness 0.42 + clearcoat 0.68 must not cause highlight clip at ?cam=rail; M5 run after wood change"
      pattern: "roughness.*0\\.42"
    - from: "frontend/src/lab/TableLab.tsx brassMat"
      to: "tools/table-3d/run-metrics.mjs M4 gate"
      via: "brassMat is NOT changed; M4 re-run at END confirms neighbor-specular changes did not drift brass HSV"
      pattern: "roughness.*0\\.42"
---

<objective>
Apply per-material specular deltas to wood, body, card stock, card face, and chip pre-dv
materials — in the safer direction (more matte, less casino) for every change. Brass is TP4-locked
and untouched. Felt, leather, and chip-dv are unchanged. Run M4 (brass drift check) and M5
(highlight-clip) after all changes land.

Purpose: Each material should feel like what it is under a warm motivated key — varnished wood
with a restrained glint (not wet), matte clay chips with a whisper clearcoat seal (not casino),
crisp card-edge highlights (small, not clipping). The changes are all strictly in the anti-casino
direction: roughness raised, clearcoat lowered, envMapIntensity lowered. M4 re-run at end is
the brass drift guard (neighbor specular changes must not push brass to gold via env contribution).

Output: TableLab.tsx with per-material specular deltas applied; M4 and M5 PASS confirmed on
captures that include the shaped-path key from 06-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-CONTEXT.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-RESEARCH.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-01-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply wood / body / card / chip pre-dv specular deltas (BRASS UNCHANGED)</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx: read the material useMemo blocks for woodMat, bodyMat (or
      apronMat — find the body/apron MeshPhysicalMaterial), the card stock material (the one
      with roughness ~0.62 and clearcoat 0.12 for the card backing), the card face material
      (faceMat, roughness ~0.52), and the chip body material pre-dv path. Read the chip dv-path
      conditional to confirm the dv props (roughness 0.72, clearcoat 0.32) — these MUST stay
      exactly as-is. Read the brassMat block (roughness 0.42, envMapIntensity 0.45) to confirm
      it will be left completely untouched. Read the feltMat block to confirm it is untouched.
    - 06-RESEARCH.md "Per-Material Specular — Current Baseline vs TP5 Proposed" — the exact
      before/after table for each material; Pitfall 4 (wood wet read — never lower roughness
      below 0.38, never raise clearcoat above 0.72); Pitfall 5 (brass goes gold — do not touch
      envMapIntensity 0.45); Pitfall 3 (anti-double-darken — already handled in 06-01).
  </read_first>
  <action>
    Apply the following exact deltas (from RESEARCH.md "Per-Material Specular" table).
    Each change is in the anti-casino, anti-wet, anti-gold direction. Make changes per material;
    do not combine into a single edit pass that might miss a material or touch a wrong one.

    WOOD COAMING (MeshPhysicalMaterial, the rail outer wall with clearcoat ~0.72):
      roughness:           0.38 -> 0.42   (raise: varnished wood not wet; M5 guard)
      clearcoat:           0.72 -> 0.68   (lower: less casino-polish; retain varnish hint)
      clearcoatRoughness:  0.2  -> 0.25   (raise: softer clearcoat lobe; anti-mirror-wet)
      envMapIntensity:     0.65 -> 0.55   (lower: wood recedes behind cards)
    Leave color, normalMap, normalScale, sheen, sheenColor untouched.

    BODY / APRON (MeshPhysicalMaterial, the vertical fascia below the rail):
      roughness:           0.48 -> 0.52   (raise: body in shadow; less specular)
      clearcoat:           0.5  -> 0.44   (lower: less contrast with the rail; body recedes)
      clearcoatRoughness:  0.3  -> 0.35   (raise: softer body clearcoat lobe)
      envMapIntensity:     0.5  -> 0.4    (lower: body recedes; less env punch)
    Leave color and other props untouched.

    CARD STOCK material (MeshPhysicalMaterial backing, roughness ~0.62, clearcoat 0.12):
      roughness:           0.62 -> 0.60   (slight tighten: crisp small highlight on card-edge)
      clearcoatRoughness:  0.55 -> 0.50   (slight tighten: allows a crisp clearcoat highlight)
    Do NOT change clearcoat (keep 0.12; hard cap is 0.18 — do not approach it), sheen (0.35),
    sheenColor, or color.

    CARD FACE material (faceMat, MeshPhysicalMaterial, roughness ~0.52, clearcoat 0.12):
      roughness:           0.52 -> 0.50   (slight tighten: allow slight highlight on face surface)
    Do NOT change clearcoat (keep 0.12), sheen, or color.

    CHIP BODY pre-dv path (MeshPhysicalMaterial, the isDeVegas=false branch, roughness ~0.5):
      roughness:           0.5  -> 0.52   (raise: matte clay; suppress highlight extent)
      clearcoat:           0.42 -> 0.38   (lower: reduce casino highlight; retain clay seal)
      clearcoatRoughness:  0.46 -> 0.50   (raise: softer clearcoat lobe; matte seal read)
      sheen:               0.5  -> 0.4    (lower: reduce sheen extent under the new key)
    Leave color, metalness (0), and all other props untouched.

    UNTOUCHED MATERIALS (verify these are NOT changed):
      brassMat:     roughness 0.42, envMapIntensity 0.45, metalness 1 — EXACTLY AS-IS (TP4-locked)
      feltMat:      roughness 0.93, sheen 0.70, sheenColor "#2aad7a" — ALL UNCHANGED
      leatherMat:   roughness 0.64, clearcoat 0.08, sheen 0.4 — ALL UNCHANGED
      chip dv-path: roughness 0.72, clearcoat 0.32, clearcoatRoughness 0.50 — ALL UNCHANGED
      Environment:  the three Lightformers — completely untouched (baked frames={1})

    Commit: "feat(06-03): TP5 per-material specular — wood/body/card/chip deltas (brass UNCHANGED)"
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8'); const nc=src.split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); const hasWood042=/roughness\s*:\s*0\.42/.test(nc); const hasBody052=/roughness\s*:\s*0\.52/.test(nc); const hasBrassLocked=/roughness\s*:\s*0\.42/.test(nc); if(!hasWood042)process.exit(1); console.log('OK — per-material specular deltas present (spot-check roughness 0.42 for wood, 0.52 for body/chip)');"</automated>
  </verify>
  <done>Wood: roughness 0.42, clearcoat 0.68, clearcoatRoughness 0.25, envMapIntensity 0.55. Body: roughness 0.52, clearcoat 0.44, clearcoatRoughness 0.35, envMapIntensity 0.4. Card stock: roughness 0.60, clearcoatRoughness 0.50. Card face: roughness 0.50. Chip pre-dv: roughness 0.52, clearcoat 0.38, clearcoatRoughness 0.50, sheen 0.4. Brass, felt, leather, chip-dv — confirmed untouched. tsc clean; vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Run M4 (brass drift check) + M5 (highlight-clip) + capture macro for wood/brass read</name>
  <files>docs/table-3d/anchors/tp5-gate/macro-specular.png, docs/table-3d/anchors/tp5-gate/hero-specular.png</files>
  <read_first>
    - 06-RESEARCH.md "Metric Gates for TP5" — exact M4/M5 commands and thresholds: M4 H in
      [35,48] S<=0.55 V<=0.80; M5 luma>250 < 0.5% felt area. Also the Pitfall 5 warning signs
      (M4 V approaching 0.75 = stop; lower wood/chip specular if brass V drifts).
    - tools/table-3d/run-metrics.mjs — first 30 lines for the exact CLI invocation.
  </read_first>
  <action>
    STEP 1 — Capture two diagnostic shots with the shaped key (default, no ?light= param):
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/macro-specular.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero"  node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/hero-specular.png

    STEP 2 — Run the full metric suite including M4 and M5:
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate
    Record the exact M4 output: H (degrees), S (0-1), V (0-1).
    Record the M5 output: percentage of luma>250 pixels in the felt area and in the frame.

    STEP 3 — M4 brass gate evaluation:
    PASS criterion: H in [35, 48], S <= 0.55, V <= 0.80.
    If PASS: record values and proceed. The new specular changes did not drift brass.
    If V > 0.78 (warning — approaching 0.80 ceiling): lower wood clearcoat to 0.65 (one step
    from 0.68) and/or lower chip clearcoat to 0.35 (from 0.38); re-run M4. Record change.
    If V > 0.80 (FAIL): lower wood envMapIntensity to 0.50 (from 0.55); re-run M4. If still
    FAIL, lower chip clearcoat to 0.32 (approaching the dv value); re-run M4. Do NOT lower
    wood roughness below 0.42 (wet risk) or touch brassMat at all.

    STEP 4 — M5 highlight-clip gate evaluation:
    PASS criterion: luma>250 < 0.5% of felt area AND < 1.5% of frame.
    If PASS: record and proceed.
    If FAIL at the wood clearcoat highlight: raise wood clearcoatRoughness to 0.28 (from 0.25)
    to soften the clearcoat lobe; recapture and rerun M5. If still FAIL, lower wood clearcoat
    to 0.65 (from 0.68); recapture. Stay above 0.60 clearcoat (below which wood loses the
    varnish read entirely).
    If FAIL at card highlight: raise card stock roughness to 0.62 (revert to baseline); recapture.

    STEP 5 — Commit with metric values:
    Commit: "chore(06-03): TP5 per-material specular — M4 PASS (H/S/V) M5 PASS, captures committed"
    Include the exact M4 H/S/V values in the commit message.
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate</automated>
    <automated>node -e "const fs=require('fs'); ['macro-specular','hero-specular'].forEach(s=>{const p='docs/table-3d/anchors/tp5-gate/'+s+'.png'; if(!fs.existsSync(p)){console.error('MISSING: '+p); process.exit(1);}}); console.log('OK — specular gate captures present');"</automated>
  </verify>
  <done>M4 PASS confirmed (H in [35,48], S<=0.55, V<=0.80) — brass did not drift despite neighbor specular changes; M5 PASS confirmed (no highlight clip introduced by wood/card changes); macro-specular.png and hero-specular.png committed to tp5-gate anchor dir; exact M4 H/S/V values recorded in commit message; any tuning changes documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build.

| Boundary | Description |
|----------|-------------|
| brassMat protection | Roughness 0.42 and envMapIntensity 0.45 are TP4-locked; M4 re-run at END is the drift guard after all neighbor changes |
| wood wet-read boundary | roughness must not drop below 0.38; clearcoat must not rise above 0.72 — both are respected (0.42 and 0.68) |
| global IBL prohibition | Per-material envMapIntensity changes ONLY (lowering, not raising); no global IBL, no global exposure bump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03a | Tampering | Brass gold drift via neighbor specular (M4 fail) | mitigate | brassMat untouched; M4 run at END of all changes; if drift detected → lower wood/chip envMapIntensity (not brass) |
| T-06-03b | Tampering | Wood wet read (clearcoat + roughness tip) | mitigate | Wood roughness raised (0.38→0.42) and clearcoat lowered (0.72→0.68) — both in safer direction; M5 guards highlight clip |
| T-06-03c | Tampering | Global IBL/exposure accidentally raised | mitigate | All envMapIntensity changes are downward (0.65→0.55, 0.5→0.4); no toneMappingExposure or IBL intensity change |
| T-06-03d | Tampering | Chip dv-path accidentally changed | mitigate | Read the dv-path conditional before editing; dv roughness 0.72 clearcoat 0.32 clearcoatRoughness 0.50 confirmed untouched |
| T-06-SC | Tampering | npm installs | accept | No package installs; all changes in TableLab.tsx |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- M4 PASS: node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate exits 0 with H in [35,48], S<=0.55, V<=0.80.
- M5 PASS: highlight-clip < 0.5% felt area (no new clipping from wood/card specular changes).
- brassMat: roughness 0.42 + envMapIntensity 0.45 confirmed unchanged (TP4-locked).
- feltMat, leatherMat, chip-dv path: confirmed unchanged.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Per-material specular deltas applied: wood (0.42/0.68/0.25/0.55), body (0.52/0.44/0.35/0.4), card stock (0.60/0.50), card face (0.50), chip pre-dv (0.52/0.38/0.50 sheen 0.4).
- Brass UNCHANGED (roughness 0.42, envMapIntensity 0.45 — TP4-locked).
- M4 PASS confirmed after all changes (brass drift guard).
- M5 PASS confirmed (no highlight clip introduced).
- tsc clean; vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-03-SUMMARY.md` when done,
recording the before/after specular table for all changed materials (and confirming unchanged
materials), the M4 measured values (H/S/V — must be in range), the M5 result (percentage
highlight-clip), any tuning applied beyond the plan defaults (e.g. if wood clearcoat went to
0.65 instead of 0.68 to pass M4), and confirmation that brass/felt/leather/dv-chip are untouched.
</output>
