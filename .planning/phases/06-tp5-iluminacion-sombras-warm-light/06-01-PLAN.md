---
phase: 06-tp5-iluminacion-sombras-warm-light
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP5 — grounding (must-ship): SoftShadows PCSS size=30 samples=16 focus=0 injected ONCE unconditionally in Scene above Lights; ContactShadows frames={1} opacity=0.35 color='#1a0e06' far=5 blur=2.0 scale=FELT_R*3.5; shadow-camera-near=8 shadow-camera-far=28 shadow-normalBias=0.02 on key spotLight"
  - "SSOT §TP5 — warm crushed-black lift: ContactShadows color='#1a0e06' (not '#000000') for the +A metric warm-corner floor"
  - "SSOT §TP5 — M6 contact-shadow presence PASS under every object; +A warm shadow floor PASS; M10 draw count not regressed vs TP4 baseline (<=106 HERO); M7 NO bloom (no postprocessing introduced)"
  - "SSOT §TP5 — no new shadow-casting light; SoftShadows injected ONCE (not behind a condition, not per-frame toggled)"

must_haves:
  truths:
    - "SoftShadows (PCSS) is in Scene unconditionally — not inside a conditional block, not inside Lights"
    - "ContactShadows has frames={1} wired in non-comment code (baked once at mount)"
    - "ContactShadows opacity is 0.35 (lowered from 0.55 to prevent double-darkening with SoftShadows)"
    - "ContactShadows color is '#1a0e06' (warm near-black, not '#000000' — lifts the warm shadow floor)"
    - "Key spotLight has shadow-normalBias={0.02} — cards and chips do not float on the felt at ?cam=macro"
    - "Key spotLight has shadow-camera-near={8} and shadow-camera-far={28} explicitly set"
    - "M6 PASS: under-object area is >=12% darker than adjacent felt in hero/card/macro captures"
    - "+A PASS: corner luma >=18 with warm hue H in [15,75] degrees (the warm shadow floor)"
    - "M10 (draw count proxy for M11) at hero does NOT exceed 106 (TP4 baseline)"
    - "M7: zero Bloom/EffectComposer/postprocessing tokens in frontend/src/lab/ source"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "SoftShadows unconditional in Scene; ContactShadows frames=1 opacity=0.35 color=#1a0e06; key spotLight shadow-normalBias + near/far tuned"
      contains: "SoftShadows"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx SoftShadows"
      to: "@react-three/drei SoftShadows (installed 9.114.0)"
      via: "import { SoftShadows } from '@react-three/drei'; placed above <Lights /> in Scene JSX"
      pattern: "SoftShadows"
    - from: "frontend/src/lab/TableLab.tsx ContactShadows frames"
      to: "M10 draw count (stats-read.mjs)"
      via: "frames={1} eliminates per-frame scene re-render in drei ContactShadows; M10 must not regress"
      pattern: "frames=\\{1\\}"
---

<objective>
Upgrade shadow grounding: inject SoftShadows (PCSS) once in Scene, flip ContactShadows to
frames={1} with warm shadow color and lower opacity, and tune the key spotLight shadow frustum
and normalBias. This is the must-ship grounding deliverable that ships regardless of the
key-reshaping outcome in 06-02.

Purpose: Per-frame ContactShadows re-render is the M11 driver; frames={1} bakes once at mount.
SoftShadows (PCSS) replaces the uniform-blur PCF with contact-hard-near/soft-far penumbra.
The warm ContactShadows color lifts the crushed-black corner into a warm graded shadow floor
(+A metric). shadow-normalBias stops the peter-pan float on the felt plane.

Output: TableLab.tsx with SoftShadows + upgraded ContactShadows + tuned key shadow frustum.
All four changes always-on (not behind any flag) — grounding is unconditional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-CONTEXT.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject SoftShadows unconditionally + upgrade ContactShadows + tune key shadow frustum</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx: read the Scene function JSX (the block containing ContactShadows
      at ~L1086-1097 and the Lights usage) — extract the exact JSX structure around ContactShadows
      and where the Scene function returns its JSX, to know exactly where to insert SoftShadows above
      the Lights element. Also read the spotLight KEY block (~L856-867) for the existing shadow props
      (shadow-bias=-0.0003, shadow-mapSize, shadow-radius, castShadow). Read the imports section
      (~L1-20) to confirm whether SoftShadows is already imported from drei.
    - 06-RESEARCH.md Pattern 1 (SoftShadows exact props and the unconditional injection rule) and
      Pattern 2 shadow-camera-near/far reasoning; Pitfall 2 (double-recompile if conditional) and
      Pitfall 3 (ContactShadows double-darken); the ContactShadows exact prop table.
  </read_first>
  <action>
    STEP 1 — Add SoftShadows to the drei import if not already present.
    In the existing `import { ..., ContactShadows, ... } from "@react-three/drei"` line, add
    `SoftShadows` to the named imports. Do not add a new import line; extend the existing drei import.

    STEP 2 — Inject SoftShadows ONCE, unconditionally, in the Scene JSX return, ABOVE the
    Lights component call. The critical constraint: do NOT place it inside a conditional
    expression (`{condition && <SoftShadows .../>}`). It must be a bare, unconditional element.
    Place it immediately above `<Lights .../>` in the Scene JSX. Props: size={30} samples={16}
    focus={0}.

    STEP 3 — Upgrade ContactShadows (the existing block around L1086-1097). Change only these
    five props; leave position, resolution, and any other props untouched:
      frames={1}             (add — was missing; bakes once at mount)
      opacity={0.35}         (lower from 0.55 — anti-double-darken with SoftShadows)
      far={5}                (raise from 4 — extends to cover rail + body base)
      blur={2.0}             (tighten from 2.8 — SoftShadows owns the far-penumbra now)
      color="#1a0e06"        (change from "#000000" — warm near-black lifts +A floor)
    Also change scale from FELT_R * 3 to FELT_R * 3.5 to widen coverage to the body apron.
    Do NOT wrap ContactShadows in a new conditional; keep the existing `qp("cs") !== "off"` guard
    if it already exists, otherwise leave it as-is.

    STEP 4 — On the key spotLight (the one with castShadow, position [1.2, 15, 2]), add three
    new props alongside the existing shadow-bias and shadow-mapSize:
      shadow-normalBias={0.02}       (peter-pan prevention on the flat felt plane — NEW)
      shadow-camera-near={8}         (tighten frustum near clip — was default 0.5)
      shadow-camera-far={28}         (explicit far clip — was default 500)
    Do NOT change shadow-bias (keep existing -0.0003). Do NOT change shadow-mapSize (keep 2048).
    Do NOT change castShadow, position, angle, penumbra, intensity, decay, or color on this light.
    Do NOT touch any other light (fill spotLight, rim directionalLight, body pointLight, ambient,
    hemisphere).

    STEP 5 — Verify no postprocessing / Bloom / EffectComposer tokens were accidentally introduced.
    Run a mental grep over the changes and confirm the diff is limited to: one import addition
    (SoftShadows), one new JSX element (SoftShadows), five ContactShadows prop changes (frames,
    opacity, far, blur, color) + one scale change, three new props on the key spotLight
    (shadow-normalBias, shadow-camera-near, shadow-camera-far). Nothing else.

    Commit: "feat(06-01): TP5 grounding — SoftShadows PCSS + ContactShadows frames=1 warm + shadow frustum"
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8'); const nc=src.split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); const hasSS=/SoftShadows/.test(nc); const hasCSnear=/shadow-camera-near=\{8\}/.test(nc); const hasCSnormalBias=/shadow-normalBias=\{0\.02\}/.test(nc); const hasFrames=/frames=\{1\}/.test(nc); const hasWarmColor=/#1a0e06/.test(nc); const hasOpacity035=/opacity=\{0\.35\}/.test(nc); const noBloom=!/Bloom|EffectComposer/.test(nc); if(!hasSS)process.exit(1); if(!hasCSnear)process.exit(1); if(!hasCSnormalBias)process.exit(1); if(!hasFrames)process.exit(1); if(!hasWarmColor)process.exit(1); if(!hasOpacity035)process.exit(1); if(!noBloom)process.exit(1); console.log('OK — TP5 grounding invariants present in TableLab.tsx');"</automated>
    <automated>node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab</automated>
  </verify>
  <done>SoftShadows with size={30} samples={16} focus={0} is unconditionally present in Scene JSX above Lights; ContactShadows has frames={1}, opacity={0.35}, color="#1a0e06", far={5}, blur={2.0}, scale=FELT_R*3.5; key spotLight has shadow-normalBias={0.02}, shadow-camera-near={8}, shadow-camera-far={28}; tsc clean; vitest green; m7-bloom-assert exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Capture all three money shots + run M6 / +A / M10 / M7 metric gates</name>
  <files>docs/table-3d/anchors/tp5-gate/hero.png, docs/table-3d/anchors/tp5-gate/card.png, docs/table-3d/anchors/tp5-gate/macro.png</files>
  <read_first>
    - 06-RESEARCH.md "Metric Gates for TP5" — the exact commands for M6, +A, M10 (stats-read M10
      draw count <= 106), M7; the anchor directory path docs/table-3d/anchors/tp5-gate/;
      the LAB_URL pattern for lab-shot.mjs captures.
    - tools/table-3d/run-metrics.mjs and tools/table-3d/stats-read.mjs — confirm the CLI flags
      (--frame vs positional arg, --cam vs the URL pattern). Read the first 40 lines of each to
      determine the exact invocation.
    - .dev-stack/lab-shot.mjs — confirm the LAB_URL environment variable pattern.
  </read_first>
  <action>
    STEP 1 — Ensure the dev server is running (cd frontend && npm run dev if needed; confirm
    http://localhost:5173 is live).

    STEP 2 — Create the anchor directory if absent:
    mkdir -p docs/table-3d/anchors/tp5-gate

    STEP 3 — Capture all three frozen money shots (default lighting, no flags):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero"  node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/hero.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=card"  node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/card.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/macro.png

    STEP 4 — Run the metric suite against the captures:
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate
    Confirm M6 PASS (under-object area >=12% darker than adjacent felt) at all three shots.
    Confirm +A PASS (corner luma >=18; hue warm H in [15,75] degrees) — the warm shadow floor.
    Record the exact M6 and +A values (before/after) in the commit message for traceability.

    STEP 5 — Run M7 code gate:
      node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab
    Expected: exits 0 (zero postprocessing tokens in lab source).

    STEP 6 — Run M10 draw-count gate (M11 proxy):
      node tools/table-3d/stats-read.mjs http://localhost:5173 hero
    Record the returned draw count. Must be <=106 (the TP4 HERO baseline — not regressed).
    If the count is lower than 106, record the improvement (ContactShadows frames={1} effect).
    If higher than 106, investigate before proceeding — do NOT pass if M10 regressed.

    Commit: "chore(06-01): TP5 grounding captures + M6/+A/M10/M7 metric gates"
  </action>
  <verify>
    <automated>node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab</automated>
    <automated>node -e "const fs=require('fs'); ['hero','card','macro'].forEach(shot=>{const p='docs/table-3d/anchors/tp5-gate/'+shot+'.png'; if(!fs.existsSync(p)){console.error('MISSING capture: '+p); process.exit(1);}}); console.log('OK — all 3 tp5-gate captures present');"</automated>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate</automated>
  </verify>
  <done>docs/table-3d/anchors/tp5-gate/{hero,card,macro}.png captured; run-metrics.mjs exits 0 with M6 PASS and +A PASS for all three shots; m7-bloom-assert exits 0; M10 draw count confirmed <=106 (M11 not regressed; improvement noted if lower); metric values recorded in commit message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build. /table-lab.html is dev-server-only.

| Boundary | Description |
|----------|-------------|
| SoftShadows unconditional placement | Must be outside any condition; inside a condition = double-recompile stutter on flag change |
| ContactShadows + SoftShadows opacity | double-darken: SoftShadows already darkens under objects; ContactShadows at 0.55 would stack → lower to 0.35 |
| shadow-normalBias value | Too high = peter-pan detach; too low = acne; 0.02 is the calibrated start; tune if artefacts appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01a | Tampering | SoftShadows inside conditional (shader recompile storm) | mitigate | SoftShadows placed unconditionally in Scene JSX above Lights; never behind `{flag && ...}` |
| T-06-01b | Tampering | ContactShadows + SoftShadows double-darken | mitigate | ContactShadows opacity lowered to 0.35 (from 0.55); +A gate confirms floor not crushed |
| T-06-01c | Tampering | Peter-pan card/chip float on felt | mitigate | shadow-normalBias={0.02} added to key spotLight; M6 capture at ?cam=macro confirms no float |
| T-06-01d | Tampering | Shadow frustum waste (mushy shadows) | mitigate | shadow-camera-near={8} / shadow-camera-far={28} tightens frustum to scene volume (key at y=15, felt at y=0, floor at y=-1.5) |
| T-06-01e | Tampering | M10 regression from SoftShadows | mitigate | SoftShadows adds PCSS per-fragment cost but no extra draw calls; M10 draw count verified <=106; if regressed investigate shader cost |
| T-06-01f | Tampering | Postprocessing accidentally introduced | mitigate | m7-bloom-assert.mjs exits 0; no EffectComposer/Bloom touched in this plan |
| T-06-SC | Tampering | npm installs | accept | No package installs; SoftShadows from installed @react-three/drei@9.114.0 |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit clean under frontend/src/lab/.
- cd frontend && npx vitest run src/lab/ green.
- node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab exits 0 (M7 PASS — no postprocessing).
- node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate exits 0 with M6 PASS + +A PASS at all three shots.
- node tools/table-3d/stats-read.mjs http://localhost:5173 hero returns draw count <=106.
- docs/table-3d/anchors/tp5-gate/{hero,card,macro}.png committed.
- No changes to geometry, cameras, material specular, or any light other than the key spotLight shadow props.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- SoftShadows size={30} samples={16} focus={0} unconditionally in Scene (not behind a flag).
- ContactShadows: frames={1}, opacity={0.35}, color="#1a0e06", far={5}, blur={2.0}, scale=FELT_R*3.5.
- Key spotLight: shadow-normalBias={0.02}, shadow-camera-near={8}, shadow-camera-far={28} added.
- M6 PASS, +A PASS, M7 PASS, M10 <=106 HERO confirmed.
- tsc clean, vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-01-SUMMARY.md` when done,
recording the SoftShadows props used, the ContactShadows before/after props diff, the shadow
frustum before/after props, the M6 values (per shot), the +A values (per shot), the M10 draw
count (before/after TP4 baseline 106), and M7 status.
</output>
