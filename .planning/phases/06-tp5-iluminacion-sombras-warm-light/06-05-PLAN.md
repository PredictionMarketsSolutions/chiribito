---
phase: 06-tp5-iluminacion-sombras-warm-light
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - tools/table-3d/grep-check-tp5-06.cjs
autonomous: true
requirements:
  - "SSOT §TP5 — grep-check-tp5-06.cjs: 6 structural invariant checks covering SoftShadows wired + ContactShadows frames=1 + KEY_TO_FILL_RATIO_CEILING constant + shadow-normalBias + M7 no-bloom code assert + brassMat roughness 0.42-0.45 (TP4-locked held)"
  - "SSOT §TP5 — full metric suite final run: M4 PASS (brass) + M5 PASS (no highlight-clip) + M6 PASS (contact shadow under all objects) + +A PASS (warm corner floor) + M7 PASS (no bloom) + M10 <=106 HERO (draw count not regressed)"
  - "SSOT §TP5 — grep-check uses plain Node stdlib only (no new packages); run from repo root; exit 0 on all checks passing; exit 1 with descriptive message on any failure"
  - "SSOT §TP5 — grep-check mirrors grep-check-tp4-05.cjs pattern: stripComments helper; extractFunction helper; flexible whitespace regex; one-line OK on exit 0"

must_haves:
  truths:
    - "tools/table-3d/grep-check-tp5-06.cjs exists and exits 0 from repo root"
    - "CHECK 1: SoftShadows present in TableLab.tsx non-comment code (confirming unconditional injection)"
    - "CHECK 2: frames={1} present in TableLab.tsx non-comment code (ContactShadows baked once)"
    - "CHECK 3: KEY_TO_FILL_RATIO_CEILING present in TableLab.tsx non-comment code (anti-casino sentinel)"
    - "CHECK 4: shadow-normalBias present in TableLab.tsx non-comment code (peter-pan guard)"
    - "CHECK 5: No Bloom / EffectComposer / postprocessing tokens in frontend/src/lab/ (M7 code assert)"
    - "CHECK 6: brassMat roughness 0.42-0.45 present in TableLab.tsx non-comment code (TP4-locked held)"
    - "Full metric suite exits 0: M4/M5/M6/+A all PASS on the tp5-gate anchor captures"
    - "M7: m7-bloom-assert exits 0 (zero forbidden tokens in frontend/src/lab/)"
    - "M10: stats-read.mjs HERO draw count <=106 (not regressed from TP4 baseline)"
  artifacts:
    - path: "tools/table-3d/grep-check-tp5-06.cjs"
      provides: "TP5 structural invariant checker — 6 checks; exit 0 / exit 1 discipline; Node stdlib only"
      contains: "KEY_TO_FILL_RATIO_CEILING"
  key_links:
    - from: "tools/table-3d/grep-check-tp5-06.cjs"
      to: "frontend/src/lab/TableLab.tsx"
      via: "Reads TableLab.tsx and frontend/src/lab/ to assert 6 TP5 invariants; exit 0 = pre-gate structural proof"
      pattern: "grep-check-tp5"
---

<objective>
Author tools/table-3d/grep-check-tp5-06.cjs with 6 structural invariant checks, then run the
full metric suite as the final automated pre-gate confirmation. The grep-check is the machine-
readable proof that all TP5 structural commitments are present before the operator reviews.

Purpose: The operator gate (06-06) is the human seam. The automated suite (grep-check + metrics)
is the necessary-not-sufficient pre-condition. Authoring grep-check in a dedicated plan (rather
than inline in 06-01) mirrors the established TP3/TP4 pattern (grep-check-tp3-03*.cjs /
grep-check-tp4-05.cjs), where the checker is authored after the implementation is stable.

Output: tools/table-3d/grep-check-tp5-06.cjs (exits 0); final metric suite run (M4/M5/M6/+A
PASS; M7 PASS; M10 <=106).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-CONTEXT.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-RESEARCH.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-01-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-02-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-03-SUMMARY.md
@.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tools/table-3d/grep-check-tp5-06.cjs (6 checks, Node stdlib, exit-code discipline)</name>
  <files>tools/table-3d/grep-check-tp5-06.cjs</files>
  <read_first>
    - tools/table-3d/grep-check-tp4-05.cjs — the EXACT canonical pattern to mirror:
      "use strict"; const fs/path requires; readFile(filePath) helper with fs.existsSync abort;
      fail(message) -> console.error + process.exit(1); stripComments(src) -> filter lines
      starting with "//"; extractFunction(src, name) for function-scoped checks; read the two
      source files into const vars; run checks in order; exit 0 one-line OK at the end listing
      all check names. Mirror this structure exactly.
    - 06-RESEARCH.md "grep-check-tp5-06.cjs skeleton" — the 6 check descriptions and the
      regex patterns for each check. Also re-read the Pattern 1 comment about the
      unconditional-SoftShadows check caveat (checking for <SoftShadows in non-comment code
      is sufficient; the truly unconditional check would need AST parsing, so a simpler proxy
      — presence in non-comment code — is used).
    - frontend/src/lab/TableLab.tsx current state (non-comment source) — confirm the exact
      tokens that each check should match, so the regexes are correct. Read the SoftShadows
      import/usage line, the frames={1} ContactShadows line, the KEY_TO_FILL_RATIO_CEILING
      const, the shadow-normalBias prop, and the brassMat roughness value.
  </read_first>
  <action>
    Author tools/table-3d/grep-check-tp5-06.cjs as a CommonJS module. Mirror grep-check-tp4-05.cjs
    exactly in structure (header comment, "use strict", helpers, per-check blocks, one-line OK).

    The file must:
    - Read from repo root (path.join("frontend", "src", "lab", "TableLab.tsx") and the src/lab/ dir).
    - Use only fs, path from Node stdlib. Zero new packages.
    - Provide readFile(filePath), fail(message), and stripComments(src) helpers identical to tp4-05.
    - Run 6 checks in order; fail() on any; exit 0 with one-line OK listing all check names.

    CHECK 1 — SOFTSHADOWS WIRED (unconditional injection):
      Pattern: /<SoftShadows/.test(tableNonComment)
      Fail message: "CHECK 1 FAILED — TableLab.tsx does not contain <SoftShadows in non-comment code.\n
        Expected: <SoftShadows size={30} samples={16} focus={0} /> unconditionally in Scene, above Lights.\n
        TP5 pitfall: SoftShadows inside a conditional causes shader recompile storm on flag change."

    CHECK 2 — CONTACTSHADOWS FRAMES=1 WIRED:
      Pattern: /frames=\{1\}/.test(tableNonComment)
      Fail message: "CHECK 2 FAILED — TableLab.tsx does not contain frames={1} in non-comment code.\n
        Expected: ContactShadows frames={1} baked once at mount (eliminates per-frame scene re-render → M11)."

    CHECK 3 — KEY_TO_FILL_RATIO_CEILING CONSTANT PRESENT:
      Pattern: /KEY_TO_FILL_RATIO_CEILING/.test(tableNonComment)
      Fail message: "CHECK 3 FAILED — TableLab.tsx does not contain KEY_TO_FILL_RATIO_CEILING in non-comment code.\n
        Expected: const KEY_TO_FILL_RATIO_CEILING = 3.5 in Lights component.\n
        Anti-casino sentinel: key intensity / fill intensity must stay <= 3.5x (current 2.75x shaped / 2.86x base)."

    CHECK 4 — SHADOW-NORMALBIAS ON KEY SPOTLIGHT:
      Pattern: /shadow-normalBias/.test(tableNonComment)
      Fail message: "CHECK 4 FAILED — TableLab.tsx does not contain shadow-normalBias in non-comment code.\n
        Expected: shadow-normalBias={0.02} on the key spotLight (peter-pan prevention on the flat felt plane)."

    CHECK 5 — NO BLOOM / EFFECTCOMPOSER / POSTPROCESSING IN LAB SOURCE (M7 code assert):
      Read all .tsx and .ts files in frontend/src/lab/ (non-comment lines).
      Pattern: check that NONE contain any of these tokens: Bloom, EffectComposer, SSAOEffect,
        N8AOEffect (reserved for TP6), postprocessing, vignette (as a postprocess import — not
        as a variable name; use /import.*postprocessing/ or /EffectComposer/ as the proxy).
      Use: /Bloom|EffectComposer/.test(labSrcCombined) as the primary check.
      Fail message: "CHECK 5 FAILED — frontend/src/lab/ contains Bloom or EffectComposer in non-comment code.\n
        TP5 hard gate: ALL postprocessing is deferred to TP6. No bloom, no SSAO, no EffectComposer here.\n
        Offending file: [list first match]."

    CHECK 6 — BRASSMAT ROUGHNESS IN TP4-LOCKED RANGE (0.42-0.45):
      Pattern: /roughness\s*:\s*0\.4[2-5]/.test(tableNonComment)
      This regex tolerates whitespace around the colon and matches 0.42 / 0.43 / 0.44 / 0.45.
      Fail message: "CHECK 6 FAILED — TableLab.tsx does not contain brassMat roughness 0.42-0.45 in non-comment code.\n
        TP4-locked: brassMat roughness must remain 0.42 (Lever D). TP5 per-material specular must not have changed this.\n
        M4 guards against brass-gold drift — but the roughness is the primary protection."

    ONE-LINE OK (exit 0):
      console.log(
        "OK — grep-check-tp5-06: all 6 TP5 lighting invariants hold\n" +
        "  (1) SoftShadows unconditional in Scene (PCSS injected once; no per-flag toggle)\n" +
        "  (2) ContactShadows frames={1} (baked once; M11 improvement)\n" +
        "  (3) KEY_TO_FILL_RATIO_CEILING = 3.5 (anti-casino sentinel; shaped 2.75x / base 2.86x)\n" +
        "  (4) shadow-normalBias present on key spotLight (peter-pan prevention)\n" +
        "  (5) No Bloom/EffectComposer in frontend/src/lab/ (M7 PASS — all postprocessing deferred to TP6)\n" +
        "  (6) brassMat roughness 0.42-0.45 (TP4-locked; brass aging pass held through TP5 specular changes)"
      );
      process.exit(0);

    Commit: "chore(06-05): add grep-check-tp5-06.cjs — 6 TP5 structural invariant checks"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
  </verify>
  <done>tools/table-3d/grep-check-tp5-06.cjs exits 0 with one-line OK listing all 6 checks; tsc clean; vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Final pre-gate metric suite — M4/M5/M6/+A/M7/M10 all confirmed PASS</name>
  <files>docs/table-3d/anchors/tp5-gate/hero-final.png</files>
  <read_first>
    - tools/table-3d/stats-read.mjs — first 30 lines to confirm exact CLI invocation for M10
      draw-count at hero shot.
    - tools/table-3d/run-metrics.mjs — first 30 lines to confirm the output format (which
      metrics it prints and how to read M4/M5/M6/+A values).
    - 06-RESEARCH.md "Metric Gates for TP5" — the full gate table with thresholds; and the M11
      note (stats-read draw count is the M11 proxy; M11 formal metric does not exist in
      metrics.mjs — draw count <=106 is the gate).
  </read_first>
  <action>
    STEP 1 — Ensure dev server is running with all TP5 changes active.

    STEP 2 — Take a clean final hero capture (with shaped path, the TP5 default):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp5-gate/hero-final.png

    STEP 3 — Run the full metric suite on the complete tp5-gate anchor directory:
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate
    Confirm and record each gate:
      M4: brass H in [35,48], S <=0.55, V <=0.80 — PASS (already confirmed in 06-03; re-confirm)
      M5: highlight-clip < 0.5% felt / < 1.5% frame — PASS
      M6: under-object >=12% darker than adjacent felt — PASS (all three shots: hero, card, macro)
      +A: corner luma >=18, warm hue H in [15,75] — PASS (warm shadow floor)

    STEP 4 — Run M7 code gate:
      node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab
    Expected: exits 0 (M7 PASS).

    STEP 5 — Run M10 draw-count gate (M11 proxy):
      node tools/table-3d/stats-read.mjs http://localhost:5173 hero
    Expected: <=106 draws at hero (TP4 baseline; ContactShadows frames={1} may have improved
    this). Record the exact count. If improved below 106, note as M11 improvement.

    STEP 6 — Run grep-check one final time to confirm structural integrity after all plans:
      node tools/table-3d/grep-check-tp5-06.cjs

    STEP 7 — Record all metric values. The 06-05-SUMMARY must contain the full pre-gate
    metric table (M4 H/S/V, M5 %, M6 % per shot, +A luma/hue, M7 status, M10 draw count).

    Commit: "chore(06-05): TP5 pre-gate — all metrics PASS (M4/M5/M6/+A/M7/M10), grep-check exits 0"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab</automated>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate</automated>
    <automated>node -e "const fs=require('fs'); const p='docs/table-3d/anchors/tp5-gate/hero-final.png'; if(!fs.existsSync(p)){console.error('MISSING: '+p); process.exit(1);} console.log('OK — hero-final capture present');"</automated>
  </verify>
  <done>grep-check-tp5-06.cjs exits 0 (all 6 structural invariants); run-metrics exits 0 with M4/M5/M6/+A all PASS; m7-bloom-assert exits 0 (M7 PASS); stats-read returns HERO draw count <=106 (M10/M11 not regressed); hero-final.png committed; complete pre-gate metric table recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build.

| Boundary | Description |
|----------|-------------|
| grep-check regex accuracy | Flexible whitespace patterns needed (roughness\s*:\s*0\.4[2-5]) to handle different formatting; test each regex in isolation |
| CHECK 5 scope | Reads all .tsx/.ts files in frontend/src/lab/; must not false-positive on TP6 comment references in documentation strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05a | Tampering | grep-check false negative (regex too strict) | mitigate | Test each regex pattern against the actual source before finalizing; use stripComments helper consistently |
| T-06-05b | Tampering | CHECK 5 false positive (TP6 comment in source) | mitigate | Match EffectComposer or Bloom as JSX import/element (not in comment lines — stripComments applied) |
| T-06-05c | Tampering | M10 regression not caught | mitigate | stats-read.mjs run explicitly; gate is <=106; if exceeded investigate SoftShadows fragment cost before operator gate |
| T-06-SC | Tampering | npm installs | accept | grep-check-tp5-06.cjs uses Node stdlib only; no packages installed |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp5-06.cjs exits 0 (all 6 TP5 structural invariants hold).
- node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp5-gate exits 0 (M4/M5/M6/+A all PASS).
- node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab exits 0 (M7 PASS).
- node tools/table-3d/stats-read.mjs http://localhost:5173 hero returns <=106 (M10 not regressed).
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- tools/table-3d/grep-check-tp5-06.cjs exits 0 with 6-check OK message.
- Full metric suite (M4/M5/M6/+A/M7/M10) all PASS — values recorded in SUMMARY.
- tsc clean; vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/06-tp5-iluminacion-sombras-warm-light/06-05-SUMMARY.md` when done,
containing the complete pre-gate metric table: M4 (H/S/V exact values), M5 (highlight-clip %),
M6 (shadow % per shot: hero/card/macro/rail-shaped), +A (corner luma + hue), M7 (PASS/FAIL),
M10 draw count (exact; vs TP4 baseline 106; improved if lower), and the grep-check-tp5-06.cjs
6-check verdict. This SUMMARY is what the operator gate (06-06) reads before presenting the A/B.
</output>
