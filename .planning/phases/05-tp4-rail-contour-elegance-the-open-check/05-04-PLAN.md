---
phase: 05-tp4-rail-contour-elegance-the-open-check
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - docs/table-3d/TP4_OPERATOR_GATE.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
requirements:
  - "SSOT §TP4 perceptual gate — operator verdict at HERO + rail/eye view: 'recovered edge elegance WITHOUT losing material/mass? Craft details read as restrained craft, not overworked?'"
  - "SSOT §TP4 — stop-on-ambiguous: DEFAULT STOP + KEEP current validated contour; ship only the independent craft upgrades that passed"
  - "SSOT §TP4 — rollback non-blocking: keep current contour; ship only the craft levers that passed independently"
  - "SSOT §TP4 — NOT auto-approvable; autonomous: false; this is the operator human seam"
autonomous: false
must_haves:
  truths:
    - "The operator has reviewed HERO (?cam=hero, default + ?rail=craft) and rail/eye (?cam=rail, default + ?rail=craft), with the verdict captures and the tp4-base corpus available for the side-by-side"
    - "The HARD gates are confirmed at the gate: M4 PASS (H 35-48°/S<=0.55/V<=0.80) from 05-03 measurement; M10 no-regression (<150 HERO) from 05-03 measurement; edge-thickness ratio before (0.087) recorded in 05-01"
    - "The SPLIT disposition is explicitly confirmed: the slim (?rail=slim, if applied) and each craft lever are independently separable; the current validated contour is the default fallback on ambiguous"
    - "The per-lever ?rail= flag map is presented so the operator can isolate any single lever (welt, normals, brass, slim) if a specific element needs a verdict"
    - "On operator approval: docs/table-3d/TP4_OPERATOR_GATE.md records the verdict, the A/B basis (vs tp4-base), the per-lever disposition, the HARD-gate confirmation, and the ?rail= flag map; the SCORECARD_TABLE_3D.md rail+coaming+brass rows are updated"
    - "On stop-on-ambiguous: the offending lever(s) are dropped (reverted to default), the current contour is kept, only the confirmed-passing levers ship; TP4_OPERATOR_GATE.md records the stop reason"
  artifacts:
    - path: "docs/table-3d/TP4_OPERATOR_GATE.md"
      provides: "The recorded operator verdict, the A/B basis (vs tp4-base), the per-lever disposition (ship/drop), the HARD-gate confirmation (M4 + M10 + edge-thickness ratio), and the ?rail= flag map"
      contains: "Verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP4 score update for rail/leather row, wood-coaming row, and brass row (must not regress from TP3 column)"
      contains: "rail"
  key_links:
    - from: "docs/table-3d/TP4_OPERATOR_GATE.md"
      to: "docs/table-3d/anchors/tp4-base/{hero,rail}.png + ?rail= flags"
      via: "The A/B compares the craft stack (?rail=craft) against the pre-change baseline (tp4-base corpus); each lever isolatable via its sub-flag"
      pattern: "tp4-base"
    - from: "the operator stop-on-ambiguous verdict"
      to: "the per-lever disposition (slim: surgical-or-keep; craft: ship-per-lever)"
      via: "Default STOP keeps the current validated contour; only confirmed-passing levers ship"
      pattern: "stop-on-ambiguous"
---

<objective>
The TP4 operator perceptual gate — the GSD / Chiribito human seam (SSOT §TP4 perceptual gate). The
operator reviews HERO + rail/eye view comparing the craft stack (?rail=craft, and ?rail=slim if verdict
was "lost") against the pre-change baseline (docs/table-3d/anchors/tp4-base/). The gate question:
"Recovered edge elegance WITHOUT losing material/mass? Craft details read as restrained craft,
not overworked?"

Purpose: Automated metrics (M4 brass PASS, M10 no-regression, grep-check structural invariants from
05-03) are necessary-not-sufficient. Only the operator can judge whether the rail now reads as a refined
poker-table edge with restrained craft, vs overworked texture / too-slim thin-disc / decorative welt.
This gate cannot be auto-approved (autonomous:false; auto_advance does not apply). Stop-on-ambiguous
keeps the current validated contour and ships only the independently-confirmed craft levers.

Output: docs/table-3d/TP4_OPERATOR_GATE.md recording the verdict, per-lever disposition, HARD-gate
confirmation, and ?rail= flag map; the rail/coaming/brass scorecard delta if shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-CONTEXT.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-01-SUMMARY.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-02-SUMMARY.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-03-SUMMARY.md
@docs/table-3d/TP4_VERDICT.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — "recovered edge elegance WITHOUT losing material/mass? Craft details read as restrained craft, not overworked?" at HERO + rail/eye</name>
  <files>docs/table-3d/TP4_OPERATOR_GATE.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - docs/table-3d/TP4_VERDICT.md (from 05-01) — the elegance verdict (lost/acceptable/lost-in-specific-respect), the edge-thickness ratio (0.087), the anchor provenance note (elev/ frames absent).
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-02-SUMMARY.md — the slim disposition: was the slim applied (and to which profile), or was the plan skipped (verdict=acceptable)?
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-03-SUMMARY.md — the per-lever pass/drop record (Levers A-F) and the ?rail= flag map (which flags are available and what they activate); the M4 measured values (H/S/V); the M10 draw count before/after.
    - docs/table-3d/anchors/tp4-base/{hero.png, rail.png} — the pre-change TP4 baseline (the "before" for the gate comparison).
    - docs/table-3d/SCORECARD_TABLE_3D.md — the current rail/leather row, wood-coaming row, and brass row scores (must not regress).
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP4 lines 334-357 — the SSOT gate question, stop-on-ambiguous rule, and the rollback (non-blocking) disposition.
    - docs/table-3d/TP3_OPERATOR_AB.md — the structural template for this gate plan (same checkpoint shape: what-built, how-to-verify, resume-signal, per-workstream disposition, HARD gates restated, no-auto-approve).
  </read_first>
  <action>
    Present the A/B to the operator using the consolidated captures from 05-01/05-02/05-03 (and the
    live lab if requested), following the <how-to-verify> protocol. Do NOT auto-approve — this is a
    perceptual gate (autonomous:false; auto_advance does not apply).

    First restate the HARD gates from the 05-03 summary:
    - M4 brass PASS: H [measured]/S [measured]/V [measured] — within H 35-48°/S<=0.55/V<=0.80.
    - M10 no-regression: HERO draw count [before] -> [after] — must be <150.
    - Edge-thickness ratio: 0.087 (before); [after slim value if 05-02 ran] (after).
    - grep-check-tp4-05.cjs: exits 0 (all structural invariants hold).

    Then present the per-lever disposition from 05-03-SUMMARY (which levers shipped, which were
    dropped and why). The operator must judge the PERCEPTUAL WHOLE at HERO + rail/eye.

    On operator verdict, record into docs/table-3d/TP4_OPERATOR_GATE.md:
    - The verdict string.
    - The A/B basis (craft stack vs tp4-base corpus).
    - The per-lever disposition (slim: kept/reverted; each craft lever: shipped/dropped).
    - The HARD-gate confirmation (M4 measured H/S/V; M10 draw count; edge-thickness ratio before/after).
    - The ?rail= flag map (all values and their meaning).
    - The stop-on-ambiguous handling if triggered.

    On UNAMBIGUOUS APPROVAL: update SCORECARD_TABLE_3D.md rail/leather + wood-coaming + brass rows
    with the post-TP4 score. Score must not regress from the TP3 column. Note if AAA(5) awaits TP5.

    On STOP-ON-AMBIGUOUS (specific lever reads overworked / the welt is too prominent / the contour
    is a thin disc / the wood reads noisy-plastic):
    - Identify the offending element via its ?rail= flag (e.g. ?rail=welt, ?rail=normals, ?rail=slim).
    - Revert ONLY that element (the flag system makes each one independently reverable).
    - Re-capture HERO + rail at the reduced stack; re-present.
    - Record the stop reason and the revised lever disposition in TP4_OPERATOR_GATE.md.
    - DEFAULT: if the elegance read is ambiguous overall -> STOP; keep the current validated
      contour; ship only the independently-confirmed craft levers. Never auto-revert to the slim
      rail wholesale (that discards validated mass).

    Do NOT add furniture mass. Do NOT touch felt / cards / chips / cameras. LOCAL only — no push /
    deploy / merge.
  </action>
  <what-built>
    TP4 executed three workstreams behind ?rail= flags on the post-TP3 scene, following the VERDICT
    FIRST protocol:

    (A) VERDICT (05-01): The elegance question was adjudicated by comparing HERO + rail/eye captures
    at HEAD against the available anchor corpus (head/, tp3-base/, tp4-base/). The SSOT-named slim/heavy
    reference frames (elev/ subdirectory) were absent from disk — this was recorded explicitly in
    TP4_VERDICT.md. The verdict was: [INSERT FROM 05-01-SUMMARY].

    (B) SURGICAL SLIM (05-02, ?rail=slim): [IF VERDICT=LOST: The named profile dimension was changed
    (INSERT FROM 05-02-SUMMARY: which profile, before/after constants, thin-disc check). / IF SKIPPED:
    This plan was skipped — verdict was acceptable; the current validated contour is the baseline.]

    (C) CRAFT LEVERS (05-03, ?rail= flags): Six independent levers were evaluated in order D->C->B+F->A->E:
    - Lever D (brass: roughness 0.34->0.42, envMapIntensity 0.45): [PASS/DROP + reason from 05-03-SUMMARY]
    - Lever C (leather normalMap, bumpMap->normalMap via shared Sobel helper): [PASS/DROP]
    - Lever B+F (wood normalMap freq=12 + cross-profile gradient for volume read): [PASS/DROP]
    - Lever A (welt at FELT_R*0.960, tube 0.012, y=0.022, color #2a1208): [PASS/DROP]
    - Lever E (per-arc-length UV remap): [PASS/DROP]
    The HARD gates were confirmed: M4 PASS (H/S/V from 05-03); M10 <150 HERO (draw count from 05-03).
    grep-check-tp4-05.cjs exits 0.
    The ?rail= flag map: (default) = pre-change baseline; ?rail=brass = Lever D only; ?rail=normals =
    Levers B+C+F only; ?rail=welt = Lever A only; ?rail=slim = surgical slim only (if applied);
    ?rail=craft = all passing levers.
  </what-built>
  <how-to-verify>
    Judge the RAIL ELEGANCE + CRAFT RESTRAINT at the two telling views.

    1. Bring the dev server up if needed (cd frontend && npm run dev) and open the lab live, OR
       review the still captures in .dev-stack/diag/table-3d/tp4/gate/.

    2. HERO read (the elegance / materiality shot, fov32):
       Open http://localhost:5173/table-lab.html?cam=hero and then with ?cam=hero&rail=craft.
       Compare against docs/table-3d/anchors/tp4-base/hero.png (the pre-change "before").
       Questions:
       (a) Does the rail read as a REFINED POKER-TABLE EDGE — or does it still read as heavy/clunky?
       (b) Do the craft details (the welt seam, the normalMap grain on wood+leather, the aged-brass
           tone) read as RESTRAINED CRAFT — or as overworked / noisy / decorative?
       (c) Does the wood coaming read as a CURVED VOLUME (top-highlight, underside-shadow) — or still
           a flat black band?
       (d) Does the brass read as AGED (not showroom-shiny, not court-card-gold)?

    3. RAIL/EYE read (the contour / seam detail shot, fov32):
       Open http://localhost:5173/table-lab.html?cam=rail and then ?cam=rail&rail=craft.
       Compare against docs/table-3d/anchors/tp4-base/rail.png (the pre-change "before").
       Questions:
       (a) Does the felt-to-leather seam read as a TIGHT PHYSICAL JOIN (not a gap, not a decorative
           stripe)?
       (b) Does the leather grain read as tooled-not-printed at this angle?
       (c) If slim was applied: does the contour read as more elegant WITHOUT reading as a thin disc?
           The leather+wood material story (grain, stitch) must still be readable.

    4. Isolate levers via the ?rail= flags (the flag map is in the 05-03-SUMMARY):
       ?rail=welt  — welt only (must read as shadow crease, NOT a color stripe)
       ?rail=normals — wood+leather normalMaps only (must not read noisy/chattering at HERO)
       ?rail=brass — brass tune only (must read aged, not new/gold)
       ?rail=slim  — slim only (if applied; must read as more elegant without thin-disc risk)
       This lets any suspect element be named.

    5. HARD gates (already proven in 05-03 summaries — re-confirm live if requested):
       M4 PASS (H 35-48°/S<=0.55/V<=0.80); M10 <150 HERO. These are non-negotiable.
       node tools/table-3d/grep-check-tp4-05.cjs → must exit 0.

    6. Stop-on-ambiguous: if the rail reads as overworked, or the welt reads as a stripe, or the wood
       reads noisy, or the slim made the table look like a thin disc → STOP. Name the offending element
       via its ?rail= flag. The executor isolates and reverts ONLY that element, re-captures, re-presents.
       DEFAULT: if the overall elegance read is ambiguous → STOP; keep the current validated contour;
       ship only the confirmed-passing craft levers. Never auto-revert the slim rail wholesale.

    DISPOSITION (record in docs/table-3d/TP4_OPERATOR_GATE.md):
    - UNAMBIGUOUS YES (elegance recovered or consciously kept-with-craft; craft details read restrained;
      HARD gates green; material/mass not lost): TP4 SHIPS. Update SCORECARD_TABLE_3D.md rail/leather +
      wood-coaming + brass rows (must not regress; note if AAA(5) awaits TP5).
    - STOP-ON-AMBIGUOUS on a SPECIFIC LEVER (welt reads as stripe / wood reads noisy / brass reads gold):
      STOP. Executor reverts ONLY the named lever (remove from ?rail=craft accumulator), re-captures,
      re-presents. The other passing levers are unaffected.
    - STOP-ON-AMBIGUOUS on SLIM (thin disc / material story lost): STOP. Executor reverts the slim
      (restore original profile constants), re-presents craft-only stack. Record the revert reason.
    - STOP-ON-AMBIGUOUS on the OVERALL ELEGANCE (ambiguous / read still heavy without a clear fix):
      DEFAULT STOP. Keep the current validated contour. Ship only the craft levers that passed
      independently. Record the elegance-check outcome as "keep-as-is with craft upgrades."
  </how-to-verify>
  <resume-signal>
    Operator types:
    - "approved" — TP4 ships (slim + all passing craft levers); update scorecard.
    - "approved — keep contour" — craft levers ship; the current contour is consciously kept (elegance
      check concluded "acceptable-with-craft"); no slim; update scorecard.
    - "revert welt" + reason (e.g. "reads as stripe") — executor drops Lever A; re-presents.
    - "revert normals" + reason (e.g. "wood reads noisy") — executor drops Lever B/C/F; re-presents.
    - "revert slim" + reason (e.g. "thin disc") — executor reverts 05-02 slim; re-presents craft-only.
    - "revert brass" + reason (e.g. "reads gold") — executor drops Lever D; re-presents.
    - "stop" — DEFAULT STOP; keep current validated contour; ship only confirmed-passing levers; record.
    Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has reviewed HERO (fov32) + rail/eye (?cam=rail) against the tp4-base corpus, with the
    HARD gates (M4 + M10) restated and each lever isolatable via its ?rail= flag. The verdict + per-lever
    disposition (slim: kept/reverted; each craft lever: ship/drop) + the HARD-gate confirmation + the
    ?rail= flag map are recorded in docs/table-3d/TP4_OPERATOR_GATE.md. SCORECARD_TABLE_3D.md rail/leather
    + wood-coaming + brass rows are updated if shipped (must not regress). No push/deploy/merge; no
    furniture mass added; felt/cards/cameras unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in the prod build. /table-lab.html is dev-server-only
and never in the production Vite build (SSOT §5.8). No auth / network / data / user-input surface.
At this gate the boundaries are the perceptual judgement seam, the HARD metric gates (M4 + M10), and
the SPLIT lever disposition.

| Boundary | Description |
|----------|-------------|
| automated pre-gate metrics -> human perceptual gate | M4 / M10 / grep-check are necessary-not-sufficient; the operator is the sole judge of "recovered elegance without losing material/mass" and "restrained craft vs overworked" |
| per-lever disposition -> overall verdict | The SPLIT disposition — each lever can be independently reverted without blocking the others; the current validated contour is always the default fallback |
| TP4 craft changes -> the felt / cards / chips / cameras | This gate changes only the rail/contour/brass disposition; no felt/card/chip/camera change; no furniture mass added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04a | Tampering | M4 brass / M10 draw count (HARD gates) | mitigate | Restated at the gate from the 05-03 summary; operator confirms M4 PASS + M10 <150 before approving any lever |
| T-05-04b | Tampering | thin-disc read if slim is kept | mitigate | Operator reviews rail/eye at ?rail=slim; stop-on-ambiguous triggers slim revert (restore profile constants); the 05-02 arithmetic check (rOut floor) was the pre-gate guard |
| T-05-04c | Tampering | overworked / noisy craft (welt stripe, wood noise, gold drift) | mitigate | Per-lever ?rail= flags let the operator isolate and revert each element independently; the DEFAULT is keep-current-contour |
| T-05-04d | Repudiation | the gate decision | mitigate | Verdict + per-lever disposition + HARD-gate confirmation + ?rail= flag map recorded in docs/table-3d/TP4_OPERATOR_GATE.md (non-auto-approvable; autonomous:false) |
| T-05-04e | Tampering | auto-revert to the old slim rail (discards validated mass) | mitigate | The stop-on-ambiguous-on-slim procedure reverts ONLY the 05-02 surgical delta; the old pre-TP2 slim rail is never restored wholesale |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP4 |
</threat_model>

<verification>
- The operator has reviewed HERO (?cam=hero default + ?cam=hero&rail=craft) + rail/eye (?cam=rail default + ?cam=rail&rail=craft) against the tp4-base corpus, with each lever isolatable via its ?rail= sub-flag.
- The HARD gates (M4 PASS: H 35-48°/S<=0.55/V<=0.80; M10 <150 HERO; grep-check exits 0) from the 05-03 summary are restated and confirmed.
- docs/table-3d/TP4_OPERATOR_GATE.md records: verdict, A/B basis (vs tp4-base), per-lever disposition (slim: kept/reverted; each craft lever: ship/drop), HARD-gate confirmation, and ?rail= flag map.
- docs/table-3d/SCORECARD_TABLE_3D.md rail/leather + wood-coaming + brass rows updated if shipped (must not regress from TP3 column).
- No furniture mass added; bodyProfile() unchanged; felt/cards/chips/cameras untouched.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at HERO + rail/eye; verdict + per-lever disposition recorded; HARD gates (M4 + M10) confirmed.
- Unambiguous yes -> TP4 ships (slim if applied + passing craft levers; scorecard not regressed); or per-lever selective reverts with re-present; or DEFAULT STOP (keep current contour + passing crafts).
- The SPLIT lever disposition is explicit in the TP4_OPERATOR_GATE.md record.
- The elegance-check may legitimately conclude "keep-as-is with craft" — this is a valid and recorded outcome.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-04-SUMMARY.md` when done,
and write the verdict to `docs/table-3d/TP4_OPERATOR_GATE.md`. Update the rail/leather, wood-coaming,
and brass rows in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP4 scores if shipped (must not
regress; note if AAA(5) awaits TP5 per the brass lighting pass deferred to TP5 in the SSOT).
</output>
