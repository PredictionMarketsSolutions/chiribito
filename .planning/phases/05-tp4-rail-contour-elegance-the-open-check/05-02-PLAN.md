---
phase: 05-tp4-rail-contour-elegance-the-open-check
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP4 — if verdict=lost: surgically slim leatherProfile / woodCoamingProfile toward edge thinness WITHOUT deleting the leather+wood material story or the mass; reversible"
  - "SSOT §TP4 — auto-reverting to the slim rail is FORBIDDEN (discards validated mass); one profile dimension at a time; stop-on-ambiguous -> REVERT immediately"
  - "SSOT §TP4 — thin-disc invariant: woodCoamingProfile rOut must always be > bodyProfile fascia by at least 2% of FELT_R (>=0.13 world units); leatherProfile rOut must never go below FELT_R*1.058"
  - "SSOT §TP4 — ?rail=slim isolates the contour change; NEVER combined with ?rail=craft in captures"
must_haves:
  truths:
    - "This plan runs ONLY if docs/table-3d/TP4_VERDICT.md contains verdict=lost or verdict=lost-in-specific-respect; if verdict=acceptable this plan is a no-op and must be recorded as skipped"
    - "Exactly ONE profile dimension is changed per iteration (woodCoamingProfile yTop OR leatherProfile rOut — never both in the same change); the slim is behind ?rail=slim for isolation"
    - "The thin-disc invariant holds after any slim: woodCoamingProfile rOut > FELT_R*1.14 + 0.13 (i.e. rOut > 7.475); leatherProfile rOut never drops below FELT_R*1.058 (6.877)"
    - "A HERO + rail capture at ?rail=slim is produced and compared against the tp4-base corpus; the read is recorded as 'better-without-losing-material' or 'reverted'"
    - "The leather+wood material story (the saddle-stitch seam, the pebble grain) is visible at the MACRO shot after any slim; material story not deleted"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "The slim profile dimension change behind ?rail=slim (if verdict=lost); unchanged if verdict=acceptable"
      contains: "rail"
  key_links:
    - from: "docs/table-3d/TP4_VERDICT.md"
      to: "frontend/src/lab/TableLab.tsx leatherProfile / woodCoamingProfile"
      via: "The verdict names which profile to target; the plan changes that profile only, behind ?rail=slim"
      pattern: "slim"
    - from: "?rail=slim capture"
      to: "thin-disc invariant check"
      via: "After the slim: rOut arithmetic check ensures the table does not read as a thin disc"
      pattern: "FELT_R"
---

<objective>
Apply the surgical edge-slim IF AND ONLY IF the 05-01 verdict is "lost" or "lost-in-specific-respect."
One profile dimension is changed (woodCoamingProfile yTop 0.34->0.28 OR leatherProfile rOut
FELT_R*1.072->FELT_R*1.060, per the verdict's target), placed behind ?rail=slim for apples-to-apples
isolation from the craft levers, captured and evaluated before commit.

Purpose: SSOT §TP4 prohibits auto-reverting to the slim rail (that discards validated mass) but permits
surgical slimming when the verdict provides evidence. The slim is reversible: ?rail=slim is an additive
flag path; removing it restores the current contour. If the slim reads as "better-without-losing-material"
it is committed; if it reads as a thin disc or loses the leather+wood story it is REVERTED immediately and
recorded as such. Stop-on-ambiguous -> REVERT; the current validated contour is always the default.

If verdict=acceptable (from 05-01): this plan is a no-op. The executor records "skipped — verdict:
acceptable" in the SUMMARY and proceeds directly to 05-03.

Output: frontend/src/lab/TableLab.tsx with the ?rail=slim flag path implementing the surgical slim (or
unchanged if reverted/skipped); the slim verdict recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-CONTEXT.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-01-SUMMARY.md
@docs/table-3d/TP4_VERDICT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the 05-01 verdict; if acceptable, record skip and exit. If lost, wire ?rail=slim in TableLab.tsx and apply the named profile dimension change</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - docs/table-3d/TP4_VERDICT.md — the verdict string and the ACTION TRIGGERED field. This is the gate: if verdict=acceptable, output a no-op SUMMARY ("skipped — verdict: acceptable") and do not touch TableLab.tsx. Only proceed with code changes if verdict=lost or verdict=lost-in-specific-respect.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Surgical Slim Recipe" — the tunable constants:
        woodCoamingProfile rOut = FELT_R * 1.17 → slim to FELT_R * 1.14 if verdict=lost (or yTop 0.34 → 0.28)
        leatherProfile rOut = FELT_R * 1.072 → slim to FELT_R * 1.060 if the roll is too proud
      Only the dimension named in the ACTION TRIGGERED field is changed — never both in one iteration.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "?rail= Flag Design" — the qp("rail") pattern, the isSlim conditional (railFlag === "slim"), and the critical rule that ?rail=slim is NEVER combined with ?rail=craft in captures.
    - frontend/src/lab/TableLab.tsx ~L107-173 — leatherProfile(), woodCoamingProfile(), bodyProfile() definitions. Read the exact current constants (FELT_R, the rOut / yTop literals) before editing. The ?rail= flag is read via the existing qp() helper already used for ?chips= and ?card= flags.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Common Pitfalls" Pitfall 6 (adding geometry mass — bodyProfile is READ-ONLY in TP4) and Pitfall 7 (?rail=slim must never be combined with ?rail=craft in gate captures).
    - Thin-disc invariant (SSOT + RESEARCH): woodCoamingProfile rOut must always be > bodyProfile fascia (FELT_R*1.135 apron outer = ~7.378) by at least 0.13 world units. Minimum safe rOut = 7.378 + 0.13 = 7.508, which is FELT_R*1.155. The Research recipe slim target FELT_R*1.14 = 7.41 is close to this floor — apply FELT_R*1.155 instead if FELT_R*1.14 violates the invariant. Record the arithmetic.
  </read_first>
  <action>
    GATE: Read docs/table-3d/TP4_VERDICT.md. Extract the verdict string.

    IF verdict=acceptable: Write no changes. Produce a SUMMARY with "This plan was skipped — verdict:
    acceptable. Proceeding to 05-03 on the current validated contour." Stop here.

    IF verdict=lost or verdict=lost-in-specific-respect:

    (1) Wire the ?rail= flag in TableLab.tsx. Near the top of the Table component (alongside the
    existing ?chips= / ?card= / ?felt= flag reads via qp()), add:
      const railFlag = qp("rail");
      const isSlim = railFlag === "slim";
    Do not add isWelt / isNormals / isBrass yet — those are 05-03. Only isSlim here.

    (2) Apply the slim change behind the isSlim guard. Per the ACTION TRIGGERED field in
    TP4_VERDICT.md, change exactly ONE of:
      - woodCoamingProfile: inline a conditional that returns a slimmed profile when isSlim is true
        (yTop from 0.34 to 0.28, OR rOut from FELT_R*1.17 to FELT_R*1.155 — whichever the verdict
        names; verify the thin-disc arithmetic: rOut must be > bodyProfile apron outer + 0.13).
      - leatherProfile: inline a conditional that returns a narrower rOut (FELT_R*1.072 -> FELT_R*1.060)
        when isSlim is true. Never go below FELT_R*1.058 (the stitchSeam-visibility floor).

    The bodyProfile() function must not be touched. The slim is additive and reversible: default
    (no ?rail= flag) renders the unchanged current contour.

    (3) After wiring, run the Vite dev server if not running. Capture:
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&rail=slim" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp4/slim/hero-slim.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=rail&rail=slim" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp4/slim/rail-slim.png

    (4) Compare the slim captures against docs/table-3d/anchors/tp4-base/{hero,rail}.png (the current
    validated contour). Apply the stop-on-ambiguous protocol:
      - If slim reads as "better edge elegance without losing material/mass (leather grain + stitch
        still visible at MACRO; table does not look like a thin disc)": COMMIT the slim.
      - If ambiguous, thin-disc risk, or material story degraded: REVERT (restore the original profile
        constants). Record the revert reason verbatim.

    (5) Commit result: "feat(05-02): surgical slim ?rail=slim — [profile][value]" OR
    "revert(05-02): slim reverted — [reason]; current contour retained".

    Do NOT touch felt / cards / chips / cameras. Do NOT add welt / normalMaps / brass here (05-03).
    Do NOT change bodyProfile(). The ?rail=slim flag is never combined with ?rail=craft.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8'); if(!src.includes('bodyProfile')) { console.error('FAIL: bodyProfile missing from TableLab.tsx — must not be deleted'); process.exit(1); } const bodyFn=src.match(/function bodyProfile[\s\S]*?^}/m); if(bodyFn && src.indexOf('bodyProfile')<src.indexOf('leatherProfile')) {} console.log('OK — bodyProfile present and unmodified check passed'); process.exit(0);"</automated>
  </verify>
  <done>If verdict=acceptable: no code changes; SUMMARY records skip. If verdict=lost: TableLab.tsx has ?rail=slim wired via qp('rail'); the slim capture was reviewed; the slim was committed (better) or reverted (ambiguous/thin-disc/material-story-lost) with the reason recorded. bodyProfile() is unchanged. tsc + vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, no prod build surface. /table-lab.html is dev-server-only
and never in the production Vite build (SSOT §5.8).

| Boundary | Description |
|----------|-------------|
| verdict gate -> code changes | This plan must not execute code changes if verdict=acceptable; the verdict file is the gate |
| ?rail=slim -> ?rail=craft | The slim flag path and the craft levers flag path are strictly isolated; they must never be combined in the same capture |
| slim profile change -> thin-disc risk | The arithmetic invariant (rOut > apron + 0.13) must be verified before committing any slim |
| slim -> material story | If the leather stitch seam / pebble grain disappears at MACRO after the slim, that is a material-story deletion — REVERT immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02a | Tampering | auto-reverting to the old slim rail (discards validated mass) | mitigate | The slim changes ONE dimension by a small delta; the minimum rOut floors are enforced (FELT_R*1.155 for coaming, FELT_R*1.058 for leather); full revert-to-original is the only allowed rollback |
| T-05-02b | Tampering | thin-disc read after slim | mitigate | Arithmetic check before commit: rOut > bodyProfile fascia + 0.13; visual MACRO check for disc read |
| T-05-02c | Tampering | material story deleted by slim | mitigate | MACRO capture required after slim; leather seam and grain must be visible; stop-on-ambiguous = REVERT |
| T-05-02d | Tampering | ?rail=slim combined with ?rail=craft in a capture | mitigate | Only isSlim is wired in this plan; isCraft/isWelt/isNormals/isBrass are 05-03; Pitfall 7 enforced |
| T-05-02e | Tampering | bodyProfile modified (furniture mass added) | mitigate | bodyProfile() is read-only; a grep check in verify confirms it is still present and unchanged |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- docs/table-3d/TP4_VERDICT.md read; verdict respected: if acceptable — no code changes; if lost — slim applied to the named profile dimension only.
- If slim applied: thin-disc invariant verified (woodCoamingProfile rOut > FELT_R*1.14 + 0.13); leather rOut never below FELT_R*1.058.
- If slim applied: HERO + rail captures at ?rail=slim produced and compared against tp4-base corpus; committed (better) or reverted (ambiguous/thin-disc/material-story-lost) with the reason recorded.
- bodyProfile() unchanged.
- ?rail=slim never combined with ?rail=craft in any capture filename or URL.
- cd frontend && npx tsc --noEmit clean under frontend/src/lab/.
- cd frontend && npx vitest run src/lab/ green.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- The slim gate is respected: no code changes if verdict=acceptable.
- If slim shipped: one profile dimension changed behind ?rail=slim; thin-disc + material-story invariants hold; committed.
- If slim reverted: revert reason recorded; current contour retained (unchanged from TP3 HEAD).
- bodyProfile() inviolate; felt/cards/chips/cameras untouched; no furniture mass added.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-02-SUMMARY.md` when done,
recording the verdict read from 05-01, the plan disposition (skipped / slim-shipped / slim-reverted),
the profile dimension changed (if any), the before/after constants, the thin-disc arithmetic, and the
HERO + rail capture verdict.
</output>
