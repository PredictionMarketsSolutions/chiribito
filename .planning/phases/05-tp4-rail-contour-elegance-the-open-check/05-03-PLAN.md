---
phase: 05-tp4-rail-contour-elegance-the-open-check
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - frontend/src/lab/textures.ts
  - frontend/src/lab/TableLab.tsx
  - tools/table-3d/grep-check-tp4-05.cjs
autonomous: true
requirements:
  - "SSOT §TP4 — craft details: welt/cord at felt-to-rail seam (Lever A); wood + leather + brass normalMaps via shared helper heightToNormalMap + toNormalMapTexture NoColorSpace (Lever B, C); brass aged-brass roughness 0.34->0.42 + envMapIntensity 0.45 within M4 HSV target (Lever D); per-arc-length UV remap on lathe profiles (Lever E, best-effort); rail outer wall curved-volume read via normalMap directional bias (Lever F)"
  - "SSOT §TP4 — each craft lever ships independently iff it passes on its own (PASS->ship / FAIL->drop, non-blocking); behind ?rail= sub-flags for isolation; all accumulated in ?rail=craft"
  - "SSOT §TP4 — M4 brass PASS (H 35-48°, S<=0.55, V<=0.80, roughness 0.38-0.45) after Lever D; M10 no perf regression (<150 HERO) after Lever A (welt adds 1 draw)"
  - "SSOT §TP4 — normalMap colorspace: NoColorSpace via toNormalMapTexture (never srgb); the chipFaceNormalMap pattern is the canonical template"
  - "SSOT §TP4 — welt tube radius <=0.012 at FELT_R*0.960, y=0.022; color #2a1208 (near-black cognac); z-fighting guard with polygonOffset if needed"
must_haves:
  truths:
    - "The ?rail= flag system is fully wired: railFlag = qp('rail'); isWelt, isNormals, isBrass, isSlim, isCraft booleans derived from it; ?rail=craft activates all passing levers simultaneously"
    - "Lever D (brass roughness 0.34->0.42, envMapIntensity 0.45) has passed the M4 metric gate: node tools/table-3d/run-metrics.mjs returns H 35-48 / S<=0.55 / V<=0.80 on the post-change HERO capture"
    - "Lever C (leather normalMap) uses toNormalMapTexture (NoColorSpace), not srgb; bumpMap: leatherBump() is removed from the active ?rail=normals path and replaced by normalMap: leatherNapNormalMap()"
    - "Lever B (wood normalMap) uses toNormalMapTexture (NoColorSpace); normalScale <=0.20 (anti-noisy-normals-under-clearcoat with clearcoat 0.72)"
    - "Lever A (welt) tube radius is <=0.012 at FELT_R*0.960, positioned at y=0.022 above the brass reveal; roughness >=0.85; no z-fighting at the HERO shot"
    - "M10 draw count remains <150 at ?cam=hero after all passing levers (welt adds 1 draw)"
    - "grep-check-tp4-05.cjs exits 0 enforcing all structural invariants of the shipped levers"
    - "The grain-stretch fix (Lever E) is shipped if the UV buffer remap is clean, or dropped if UV seam artifacts appear — non-blocking; the decision is recorded"
  artifacts:
    - path: "frontend/src/lab/textures.ts"
      provides: "woodNapNormalMap(), leatherNapNormalMap() (both NoColorSpace via toNormalMapTexture); optionally brassNormalMap(); muted brass envMapIntensity wiring"
      contains: "woodNapNormalMap"
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "?rail= flag system (qp); welt mesh behind isWelt; wood/leather normalMaps behind isNormals; brass roughness tune behind isBrass; ?rail=craft accumulates all passing levers"
      contains: "railFlag"
    - path: "tools/table-3d/grep-check-tp4-05.cjs"
      provides: "Structural invariant checker: normalMap colorspace (NoColorSpace not srgb), bumpMap removed, brass roughness 0.38-0.45, welt geometry present (if Lever A shipped)"
      contains: "toNormalMapTexture"
  key_links:
    - from: "frontend/src/lab/textures.ts woodNapNormalMap"
      to: "frontend/src/lab/normalMapHelper.ts heightToNormalMap + toNormalMapTexture"
      via: "shared Sobel pipeline; NoColorSpace enforced by the helper — the felt/card/chip precedent"
      pattern: "heightToNormalMap"
    - from: "frontend/src/lab/TableLab.tsx brassMat roughness"
      to: "tools/table-3d/run-metrics.mjs M4 gate"
      via: "roughness 0.42 + envMapIntensity 0.45 must keep HSV in range; M4 run immediately after Lever D"
      pattern: "roughness.*0\\.4"
    - from: "tools/table-3d/grep-check-tp4-05.cjs"
      to: "frontend/src/lab/textures.ts + frontend/src/lab/TableLab.tsx"
      via: "exit-code gate on normalMap colorspace + bumpMap removal + brass roughness range"
      pattern: "grep-check-tp4"
---

<objective>
Implement and independently test the six craft levers (A through F), accumulate them behind ?rail=craft,
and author the structural invariant checker. Order: D (brass, simplest, M4 gate immediately) -> C
(leather normalMap, bumpMap upgrade) -> B (wood normalMap, anti-noisy check) -> F (volume outer wall
baked into B's height field) -> A (welt geometry, z-fight guard) -> E (UV remap, best-effort). Each
lever is captured independently before being folded into ?rail=craft; a failing lever is dropped without
blocking the others.

Purpose: SSOT §TP4 lists six independent craft details that ship iff they pass on their own. The pattern
is identical to the TP3 de-Vegas workstream — additive changes behind query-param flags, each with its
own non-blocking verdict. The normalMap pipeline (Levers B, C — and optionally D-sub) follows the
established shared helper pattern (heightToNormalMap -> toNormalMapTexture -> NoColorSpace) proven
through TP1 (felt nap), TP2 (card micro-relief), and TP3 (chip recessed-C).

Output: textures.ts with woodNapNormalMap() + leatherNapNormalMap(); TableLab.tsx with the ?rail= flag
system and all passing levers wired; tools/table-3d/grep-check-tp4-05.cjs enforcing the structural
invariants; an M4 PASS and M10 no-regression confirmation; and a per-lever pass/drop record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-CONTEXT.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-01-SUMMARY.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the ?rail= flag system and implement Lever D (brass aged-brass) + Lever C (leather normalMap) + Lever B (wood normalMap + volume outer wall F)</name>
  <files>frontend/src/lab/textures.ts, frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx ~L68-72 — the qp() flag helper (the established pattern for ?chips= / ?card= / ?felt=). The ?rail= flag reads here via `const railFlag = qp("rail")`. Derive isWelt, isNormals, isBrass, isSlim, isCraft booleans as documented in RESEARCH.md "?rail= Flag Design". Only isCraft and the lever booleans go in Task 1; isWelt goes in Task 2.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Lever D: Brass Aged-Brass Tune" — the exact delta: roughness 0.34 -> 0.42, add envMapIntensity: 0.45; color #b8915a unchanged. The M4 capture command using run-metrics.mjs + the --metrics M4 flag. The M4 gate values: H 35-48°, S <=0.55, V <=0.80. Current measured baseline: H 39.4° / S 0.38 / V 0.69 — PASS with margin.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Lever C: Leather NormalMap" — the leatherNapNormalMap() recipe: same height field as leatherBump() (2600 pebble blobs, speckle, stitch seam via drawStitchSeam); pipe through heightToNormalMap(c, 1.0) then toNormalMapTexture (NoColorSpace — MANDATORY); wrapT = ClampToEdgeWrapping. Replace bumpMap: leatherBump() + bumpScale in leatherMat with normalMap: leatherNapNormalMap() + normalScale: Vector2(0.22, 0.22).
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Lever B: Wood NormalMap" — the woodNapNormalMap() recipe: S=512, freq=12 horizontal grain ridges (anti-noisy-under-clearcoat), cross-profile gradient for volume read (Lever F, baked in). normalScale <=0.15 in woodMat; cap hard at 0.22. Anti-noisy-normals-under-clearcoat risk: clearcoat 0.72 amplifies; freq>12 or normalScale>0.22 produces chattering highlight.
    - frontend/src/lab/normalMapHelper.ts — heightToNormalMap + toNormalMapTexture (lines ~118-166). These are the functions to call; do not re-implement the Sobel logic.
    - frontend/src/lab/textures.ts — woodTexture (repeat x=13), leatherTexture + leatherBump (the height field to mirror for leatherNapNormalMap). The shared helper import is already present (used by feltNapNormalMap / cardMicroReliefNormalMap / chipFaceNormalMap). Mirror chipFaceNormalMap's final 2 lines verbatim for both new functions.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Common Pitfalls" Pitfall 1 (NoColorSpace) and Pitfall 2 (noisy wood normals under clearcoat 0.72).
    - frontend/src/lab/TableLab.tsx ~L563-567 — brassMat (current: color #b8915a, metalness 1, roughness 0.34). This is the target for Lever D.
    - frontend/src/lab/TableLab.tsx ~L547-560 — leatherMat (current: bumpMap leatherBump(), bumpScale 0.016). Lever C replaces bumpMap with normalMap behind the isNormals conditional.
    - frontend/src/lab/TableLab.tsx ~L (woodMat block) — the woodMat (current: roughness 0.38, clearcoat 0.72). Lever B adds normalMap + normalScale behind isNormals.
  </read_first>
  <action>
    STEP 1 — Wire the ?rail= flag system (before any lever code):
    After the existing qp() reads in the Table component, add:
      const railFlag = qp("rail");
      const isWelt    = railFlag === "welt"    || railFlag === "craft";
      const isNormals = railFlag === "normals"  || railFlag === "craft";
      const isBrass   = railFlag === "brass"   || railFlag === "craft";
      const isSlim    = railFlag === "slim"; // already added in 05-02 if verdict=lost; add here if not
    Ensure existing ?chips= / ?card= / ?cam= reads are undisturbed.

    STEP 2 — Lever D (brass, simplest, M4 gate first):
    In TableLab.tsx, change brassMat behind the isBrass conditional: when isBrass is true, use
    roughness: 0.42 (from 0.34) and add envMapIntensity: 0.45 to MeshStandardMaterial. Color
    #b8915a unchanged. Default (isBrass false) keeps roughness 0.34 for the baseline comparison.
    Capture: LAB_URL="http://localhost:5173/table-lab.html?cam=hero&rail=brass" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp4/craft/hero-brass.png
    Run the M4 gate: node tools/table-3d/run-metrics.mjs --frame .dev-stack/diag/table-3d/tp4/craft/hero-brass.png --metrics M4
    PASS criterion: H 35-48°, S <=0.55, V <=0.80. If PASS -> Lever D ships. If FAIL (unlikely per
    baseline) -> try roughness 0.45; if still FAIL -> drop Lever D, record the failure.

    STEP 3 — Lever C (leather normalMap, bumpMap upgrade):
    In textures.ts, add the leatherNapNormalMap() export. It draws the IDENTICAL height field to
    leatherBump() (canvas W=2048 H=512; 2600 pebble blobs via the same deterministic Math.sin()
    sequence; speckle; drawStitchSeam at H*0.3). Then pipes through heightToNormalMap(c, 1.0) ->
    toNormalMapTexture() (NoColorSpace — critical). wrapS=RepeatWrapping, wrapT=ClampToEdgeWrapping.
    No new repeat.set() call needed (leatherTexture wraps around; V is clamped).
    In TableLab.tsx, import leatherNapNormalMap from textures. In leatherMat construction:
    when isNormals is true, use normalMap: leatherNapNormalMap() + normalScale: new THREE.Vector2(0.22, 0.22);
    remove bumpMap: leatherBump() and bumpScale: 0.016 from this branch. When isNormals is false,
    keep the existing bumpMap + bumpScale (unchanged baseline).
    Capture: ?cam=macro&rail=normals — check leather grain reads as tooled-not-printed (not noisy).
    PASS -> Lever C ships. FAIL (plastic / flat read) -> drop, record.

    STEP 4 — Lever B + F (wood normalMap with cross-profile gradient for volume read):
    In textures.ts, add the woodNapNormalMap() export. Canvas S=512x512. Draw the height field:
    freq=12 horizontal grain ridges (Math.sin over py/S * PI * freq * 2, strength 0.88) plus very
    faint cross-grain (strength 0.12, freq*0.25). Bake in the cross-profile gradient for Lever F:
    add crossProfile = Math.sin((py/S)*PI) * 0.18 to the height value (peak at band center ->
    top-highlight / underside-shadow read). Total: h = (gy*0.82 + gx + crossProfile) * 255.
    Then heightToNormalMap(c, 1.0) -> toNormalMapTexture() (NoColorSpace). wrapS = wrapT =
    RepeatWrapping; repeat.set(13, 1) to match woodTexture.
    In TableLab.tsx, import woodNapNormalMap. In woodMat construction (when isNormals true): add
    normalMap: woodNapNormalMap() + normalScale: new THREE.Vector2(0.15, 0.15). Default branch
    (isNormals false) keeps no normalMap (unchanged).
    Capture: ?cam=macro&rail=normals — check wood grain under varnish reads as value variation (not
    plastic chattering). normalScale above 0.20 at clearcoat 0.72 = FAIL (noisy). PASS -> B+F ship.
    FAIL -> reduce normalScale to 0.08; retry. If still noisy -> drop Lever B, record.

    All three lever groups are behind the single isNormals + isBrass flag system. Commit after each
    passing lever (atomic commits): "feat(05-03): Lever D — brass roughness 0.42 + envMapIntensity 0.45"
    / "feat(05-03): Levers B+C+F — wood+leather normalMaps (NoColorSpace, shared helper)".
    Do NOT touch felt / cards / chips / cameras. Do NOT change bodyProfile(). Welt goes in Task 2.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('frontend/src/lab/textures.ts','utf8'); const hasBWood=/woodNapNormalMap/.test(t); const hasLeather=/leatherNapNormalMap/.test(t); const hasHelper=/heightToNormalMap/.test(t); const noSrgbOnNormals=!(/woodNapNormalMap[\s\S]{0,800}srgb\(/).test(t) && !(/leatherNapNormalMap[\s\S]{0,800}srgb\(/).test(t); if(!hasBWood||!hasLeather||!hasHelper){console.error('FAIL: woodNapNormalMap or leatherNapNormalMap or heightToNormalMap missing from textures.ts'); process.exit(1);} if(!noSrgbOnNormals){console.error('FAIL: srgb() used near a rail normalMap function — use toNormalMapTexture instead'); process.exit(1);} console.log('OK — texture normalMap functions present; no srgb on normalMap canvas');"</automated>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8'); const hasRailFlag=/railFlag\s*=\s*qp\s*\(\s*['\"]rail['\"]/.test(t); const hasBrass=/roughness\s*:\s*0\.4[2-5]/.test(t); const hasIsBrass=/isBrass/.test(t); const hasIsNormals=/isNormals/.test(t); if(!hasRailFlag||!hasBrass||!hasIsBrass||!hasIsNormals){console.error('FAIL: ?rail= flag system or isBrass or brass roughness 0.42-0.45 missing from TableLab.tsx'); process.exit(1);} console.log('OK — ?rail= flag system wired; isBrass + isNormals present; brass roughness in aged range');"</automated>
  </verify>
  <done>?rail= flag system wired (railFlag, isWelt, isNormals, isBrass, isSlim, isCraft); Lever D (brass roughness 0.42 + envMapIntensity 0.45) passed M4 gate and is committed; Lever C (leather normalMap, NoColorSpace, bumpMap replaced) captured and committed if pass; Lever B+F (wood normalMap freq=12 + cross-profile gradient, NoColorSpace, normalScale <=0.20) captured and committed if pass. tsc + vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Lever A (welt geometry + z-fight guard) + Lever E (UV arc-length remap, best-effort) + author grep-check-tp4-05.cjs + M10 gate</name>
  <files>frontend/src/lab/TableLab.tsx, tools/table-3d/grep-check-tp4-05.cjs</files>
  <read_first>
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Lever A: Welt/Cord" — the exact geometry: torusGeometry args [FELT_R*0.960, 0.012, 12, 180]; position y=0.022 (above the brass reveal at y=0.014); material color #2a1208, roughness 0.88, metalness 0, clearcoat 0.05. Anti-fussy-welt rule: tube radius <=0.012; if it reads as a color stripe at HERO (not shadow) -> reduce to 0.008 or drop. Z-fight guard: polygonOffset={true} polygonOffsetFactor={-1} polygonOffsetUnits={-1} if flickering at the brass reveal.
    - frontend/src/lab/TableLab.tsx ~L594-598 — the existing brass reveal torus (position y=0.014, radius FELT_R*0.957, tube 0.02, 16×180). The welt mesh goes AFTER this in JSX, at FELT_R*0.960 / y=0.022, active only when isWelt is true. Mirror the same JSX pattern (<mesh rotation={[-Math.PI/2, 0, 0]} position={...}><torusGeometry .../><primitive object={weltMat} attach="material" /></mesh>).
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Lever E: Per-Arc-Length UV" — the buffer attribute post-process approach (remapLatheUVsToArcLength utility). Scope: implement if the UV remap is clean (no seam artifacts); drop if artifacts appear. This is best-effort for TP4 — non-blocking.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "grep-check Helper Pattern" — the six checks to enforce:
        CHECK 1: wood normalMap wired (woodMat normalMap: assignment)
        CHECK 2: leather bumpMap REMOVED from active isNormals branch (no `bumpMap: leatherBump` in that branch)
        CHECK 3: leather normalMap wired (leatherMat normalMap:)
        CHECK 4: brass roughness in range (roughness 0.38-0.45, specifically 0.42)
        CHECK 5: NoColorSpace on rail normals (woodNapNormalMap and leatherNapNormalMap call toNormalMapTexture, not srgb)
        CHECK 6: welt present if Lever A shipped (torusGeometry with FELT_R * 0.960 in TableLab.tsx)
    - tools/table-3d/grep-check-tp3-03mat.cjs — the canonical pattern: plain Node stdlib only; read from repo root; exit 0 on all checks passing (one-line OK); exit 1 with descriptive message on any failure; flexible whitespace regex; strip comment lines before checks. Mirror this pattern exactly.
    - tools/table-3d/run-metrics.mjs + tools/table-3d/stats-read.mjs — the M4 gate runner and the M10 draw-count reader. M10: node tools/table-3d/stats-read.mjs --cam hero; must read <150 after all passing levers (welt adds 1 draw: current 105 HERO -> 106 if welt ships, still passes <150).
  </read_first>
  <action>
    STEP 1 — Lever A (welt geometry):
    In TableLab.tsx, add a weltMat (MeshPhysicalMaterial: color #2a1208, roughness 0.88, metalness 0,
    clearcoat 0.05, clearcoatRoughness 0.9). Wire as a useMemo inside the Table component (alongside
    other material useMemos). Add the welt mesh JSX after the brass reveal torus:
      {isWelt && (
        <mesh rotation={[-Math.PI/2, 0, 0]} position={[0, 0.022, 0]}>
          <torusGeometry args={[FELT_R * 0.960, 0.012, 12, 180]} />
          <primitive object={weltMat} attach="material" />
        </mesh>
      )}
    Capture: ?cam=hero&rail=welt — at HERO, the welt must NOT be identifiable as a color stripe;
    it must read as shadow/crease only. ?cam=macro&rail=welt — at MACRO, confirm the welt is a
    tight seam, not a decorative element. If it reads as a stripe: reduce tube to 0.008 and retry.
    If still prominent: drop Lever A (non-blocking). Check for z-fighting at orbit angles near
    the felt-brass junction: if flickering appears, add polygonOffset props to the mesh. Commit if
    PASS: "feat(05-03): Lever A — welt at felt-to-rail seam FELT_R*0.960 tube 0.012".

    STEP 2 — Lever E (UV arc-length remap, best-effort):
    Implement the remapLatheUVsToArcLength utility as described in RESEARCH.md Lever E — Approach B.
    Apply it in the Table component's useMemo after the LatheGeometry is constructed for the leather
    and wood profiles. The key parameters: OVAL_X = 1.22 (already a constant in the file); totalSeg
    must match the lathe segment count (check the existing torusGeometry / latheGeometry args for the
    segment count — use the same value). Apply the remap: compute arc lengths per segment on the
    ellipse (accounting for OVAL_X stretch), then remap uvAttr.x values proportionally, set
    uvAttr.needsUpdate = true.
    Capture: ?cam=macro&rail=normals (with normals active so grain is visible) — compare grain
    density at the oval long-ends vs short-ends. If grain reads uniform (no visible stretch):
    PASS -> commit "feat(05-03): Lever E — per-arc-length UV remap on lathe profiles".
    If UV seam artifacts appear (dark stripe at the seam, grain doubling): REVERT Lever E
    immediately (non-blocking). Record the decision: "Lever E dropped — UV seam artifacts at
    lathe weld; defer to TP7 geometry pass."

    STEP 3 — Wire ?rail=craft accumulator:
    In the railFlag conditional logic, ensure isCraft (railFlag === "craft") activates all the
    levers that actually shipped (those committed in Task 1 + Task 2). Do not include dropped
    levers in the isCraft path. Document the final flag map in the SUMMARY.

    STEP 4 — Author tools/table-3d/grep-check-tp4-05.cjs:
    Plain CommonJS, Node stdlib only. The script reads from repo root. Enforce only the invariants
    for levers that SHIPPED (skip the welt check if Lever A was dropped; skip UV check — Lever E
    has no structural code signature). Checks:
      CHECK 1: textures.ts contains `woodNapNormalMap` export that calls `heightToNormalMap` and
               `toNormalMapTexture` (not srgb) — grep with regex tolerating whitespace.
      CHECK 2: TableLab.tsx does NOT contain `bumpMap:\s*leatherBump` in the isNormals-active code
               path (the bumpMap upgrade is complete). Note: bumpMap may appear in the baseline
               (isNormals=false) branch — isolate the check to non-comment lines in the isNormals
               block. Use a heuristic: if leatherBump appears AND leatherNapNormalMap also appears,
               the bumpMap must be in the baseline branch only; check that the isNormals block
               references leatherNapNormalMap.
      CHECK 3: textures.ts contains `leatherNapNormalMap` export calling `toNormalMapTexture`
               (NoColorSpace) and NOT calling `srgb(` on the normalMap canvas.
      CHECK 4: TableLab.tsx brassMat contains `roughness:\s*0\.4[2-5]` (the aged-brass range).
               The grep is flexible to match 0.42 / 0.43 / 0.44 / 0.45.
      CHECK 5: textures.ts — woodNapNormalMap and leatherNapNormalMap each call `toNormalMapTexture`
               (not `srgb` or `gray`) on their normal canvas. The canonical pattern is the final
               two lines of each function.
      CHECK 6 (conditional — only if Lever A shipped): TableLab.tsx contains
               `FELT_R\s*\*\s*0\.96` in a torusGeometry args (the welt torus).
    Exit 0 with one-line OK listing all passing checks. Exit 1 with descriptive message on any failure.

    STEP 5 — M4 final confirmation + M10 gate:
    Run: node tools/table-3d/run-metrics.mjs --frame .dev-stack/diag/table-3d/tp4/craft/hero-brass.png --metrics M4
    Expected: H 35-48°, S <=0.55, V <=0.80. Record the exact measured values.
    Run: node tools/table-3d/stats-read.mjs --cam hero (at ?cam=hero, no extra flags — the default baseline draw count).
    Then run with the craft stack: start the lab at ?cam=hero&rail=craft and check draw count.
    Expected: <150 (current 105 + 1 welt = 106 if welt shipped; +0 for normalMaps).
    Record both counts in the SUMMARY.

    Commit: "chore(05-03): add grep-check-tp4-05.cjs; confirm M4 PASS + M10 no-regression".
    Do NOT touch felt / cards / chips / cameras. Do NOT change bodyProfile().
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp4-05.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8'); if(!/isCraft/.test(src)){console.error('FAIL: isCraft not wired in TableLab.tsx'); process.exit(1);} if(!/railFlag\s*=\s*qp/.test(src)){console.error('FAIL: railFlag = qp() not present'); process.exit(1);} console.log('OK — ?rail=craft accumulator wired');"</automated>
  </verify>
  <done>Lever A (welt) committed if pass or dropped with reason; Lever E (UV remap) committed if clean or dropped with reason; ?rail=craft accumulates all passing levers; grep-check-tp4-05.cjs exits 0; M4 PASS confirmed (H/S/V measured); M10 draw count <150 confirmed. tsc + vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, no prod build surface. /table-lab.html is dev-server-only
and never in the production Vite build (SSOT §5.8).

| Boundary | Description |
|----------|-------------|
| normalMap colorspace | NoColorSpace is the mandatory boundary for all rail normalMaps; srgb() on a normalMap canvas produces wrong normals (gamma decode on normal XYZ components) |
| welt tube radius | The welt must read as shadow crease (<=0.012 tube); larger reads as design stripe — fussy welt risk |
| brass M4 gate | roughness 0.42 + lower env must keep HSV in H 35-48 / S<=0.55 / V<=0.80; the metric is the arbiter |
| M10 draw count | Welt adds 1 draw; all normalMaps add 0; total must remain <150 HERO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03a | Tampering | normalMap colorspace (srgb instead of NoColorSpace) | mitigate | toNormalMapTexture() enforced by grep-check CHECK 5; the canonical chipFaceNormalMap pattern is the template — mirror its final 2 lines verbatim |
| T-05-03b | Tampering | noisy wood normals under clearcoat 0.72 | mitigate | freq<=12 and normalScale<=0.20 hard caps; MACRO capture reviewed before commit; if chattering -> normalScale 0.08 retry then drop |
| T-05-03c | Tampering | fussy welt (reads as stripe, not shadow) | mitigate | Tube 0.012, color #2a1208 near-black; HERO capture at anti-fussy-welt rule: if identifiable as "the welt" -> reduce to 0.008 or drop |
| T-05-03d | Tampering | welt z-fighting with brass reveal | mitigate | Welt at y=0.022 vs brass at y=0.014 (different Y plane); welt at FELT_R*0.960 vs brass at FELT_R*0.957 (different radius); polygonOffset available as fallback |
| T-05-03e | Tampering | brass M4 drift (gold/casino) | mitigate | Raising roughness 0.34->0.42 REDUCES specular V (safer direction); M4 gate run immediately after Lever D; if FAIL -> try 0.45; color #b8915a unchanged |
| T-05-03f | Tampering | M10 perf regression | mitigate | Welt adds 1 draw (105->106 HERO); normalMaps add 0; stats-read.mjs after all levers; must be <150 |
| T-05-03g | Tampering | UV seam artifacts from Lever E | mitigate | Non-blocking; if dark seam stripe appears at lathe weld -> immediate revert; defer to TP7 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (drei/three already present; grep-check uses Node stdlib; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp4-05.cjs exits 0 (all structural invariants hold for shipped levers).
- cd frontend && npx tsc --noEmit clean under frontend/src/lab/.
- cd frontend && npx vitest run src/lab/ green.
- M4 gate: node tools/table-3d/run-metrics.mjs --metrics M4 PASS (H 35-48° / S<=0.55 / V<=0.80) on the brass-tune HERO capture.
- M10 gate: node tools/table-3d/stats-read.mjs --cam hero returns <150 with all passing levers active.
- Each lever decision (PASS or drop + reason) recorded in the SUMMARY.
- ?rail=craft accumulates all passing levers; ?rail=slim is never combined with ?rail=craft.
- bodyProfile() unchanged; felt/cards/chips/cameras untouched.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Levers B, C, D, F: normalMaps on wood+leather (NoColorSpace), brass roughness 0.42 + envMapIntensity 0.45 — shipped (or dropped with reason).
- Lever A: welt at FELT_R*0.960, tube 0.012, y=0.022, color #2a1208 — reads as shadow crease at HERO (or dropped).
- Lever E: UV arc-length remap — clean if no seam artifacts (or dropped with reason).
- M4 PASS confirmed; M10 <150 HERO confirmed.
- grep-check-tp4-05.cjs exits 0; per-lever pass/drop record in SUMMARY.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-03-SUMMARY.md` when done,
recording the ?rail= flag map (all flag values and what they activate), the per-lever pass/drop record
(Levers A-F with the verdict and reason if dropped), the M4 measured values (H/S/V), the M10 draw count
before/after, and the final ?rail=craft composite description (which levers it activates).
</output>
