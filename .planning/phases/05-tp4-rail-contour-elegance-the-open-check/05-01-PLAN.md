---
phase: 05-tp4-rail-contour-elegance-the-open-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/table-3d/anchors/tp4-base/hero.png
  - docs/table-3d/anchors/tp4-base/rail.png
  - docs/table-3d/TP4_VERDICT.md
autonomous: true
requirements:
  - "SSOT §TP4 — VERDICT FIRST: structured side-by-side of current contour vs reference corpus; record 'edge elegance lost / acceptable / lost-in-specific-respect' before any geometry edit"
  - "SSOT §TP4 — edge-thickness ratio (rail band height / felt radius) measured before any change"
  - "SSOT §TP4 — the elev/ named slim/heavy SSOT anchors are absent from disk; the verdict basis must record this provenance explicitly (non-blocking)"
must_haves:
  truths:
    - "A pre-change capture at ?cam=hero and ?cam=rail exists at HEAD (zero code changes) committed to docs/table-3d/anchors/tp4-base/"
    - "The edge-thickness ratio (rail band height / felt radius = 0.565 / 6.5 ≈ 0.087) is computed from the verified profile constants and recorded in TP4_VERDICT.md"
    - "The verdict — 'lost', 'acceptable', or 'lost-in-specific-respect' — is recorded in TP4_VERDICT.md with rationale, the comparison basis (head/ + tp3-base/ corpus + operator on-device memory), and the explicit provenance note that the SSOT-named elev/ anchors are absent from disk"
    - "A git tag tp4-before-rail exists at the pre-change HEAD so the exact contour being adjudicated is pinned"
  artifacts:
    - path: "docs/table-3d/anchors/tp4-base/hero.png"
      provides: "Downscaled HERO capture at TP4 start (pre-change baseline for the gate comparison)"
      contains: "hero"
    - path: "docs/table-3d/anchors/tp4-base/rail.png"
      provides: "Downscaled rail/eye capture at TP4 start (the primary verdicting evidence)"
      contains: "rail"
    - path: "docs/table-3d/TP4_VERDICT.md"
      provides: "Written verdict record: edge-thickness ratio, comparison basis, provenance note, verdict string, rationale, and the action triggered (skip slim / apply slim to which profile)"
      contains: "Verdict"
  key_links:
    - from: "docs/table-3d/anchors/tp4-base/rail.png"
      to: "docs/table-3d/TP4_VERDICT.md"
      via: "The rail/eye capture is the primary comparison surface for the verdict reasoning"
      pattern: "tp4-base"
    - from: "docs/table-3d/TP4_VERDICT.md verdict string"
      to: "05-02-PLAN.md execution gate"
      via: "verdict=lost triggers 05-02 slim plan; verdict=acceptable skips it"
      pattern: "Verdict:"
---

<objective>
Establish the TP4 pre-change baseline and issue the elegance verdict — before a single line of code is
touched. Capture HERO + rail/eye at the current HEAD (zero code changes), pin the state with a git tag,
commit the downscaled captures to the permanent anchor corpus, compute the edge-thickness ratio, and record
a written verdict in TP4_VERDICT.md by comparing the current contour against every available reference.

Purpose: SSOT §TP4 mandates verdict before any geometry edit. The verdict determines the entire TP4 scope:
"acceptable/keep-as-is" means 05-02 (surgical slim) is skipped and craft levers (05-03) run on the current
contour. "lost" or "lost-in-specific-respect" means 05-02 runs first, targeting the named profile(s). The
elegance-check may legitimately conclude "keep as-is" — that is a valid outcome. This plan is
documentation-and-capture only; zero source code is changed.

Output: git tag tp4-before-rail + docs/table-3d/anchors/tp4-base/{hero,rail}.png committed +
docs/table-3d/TP4_VERDICT.md with the verdict string and rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-CONTEXT.md
@.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md
@docs/ROADMAP_TABLE_3D_PERFECTION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the pre-change state, capture HERO + rail/eye baseline, commit to tp4-base anchor corpus</name>
  <files>docs/table-3d/anchors/tp4-base/hero.png, docs/table-3d/anchors/tp4-base/rail.png</files>
  <read_first>
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Wave 0" section — the exact capture commands, the edge-thickness ratio pre-computation, and the required tag name (tp4-before-rail).
    - .dev-stack/lab-shot.mjs — confirm the harness exists and the ?cam=rail preset is available (SSOT-confirmed at TableLab.tsx ~L851: rail pos [0,2.4,9.6] target [0,0.15,4.9] fov 32).
    - docs/table-3d/anchors/ directory — the existing anchor corpus (head/, tp3-base/, tp2-base/, controls/, reference-tag/). The new tp4-base/ subdirectory goes here.
  </read_first>
  <action>
    First confirm the working tree is clean (git status must show no uncommitted changes in
    frontend/src/lab/ — this capture must be at the exact TP3-complete HEAD). Then:

    (1) Tag the pre-change state: `git tag tp4-before-rail` (lightweight tag; no push per Chiribito
    manual-deploy policy).

    (2) Start the Vite dev server in the background: `cd frontend && npm run dev`. Wait until the
    server is accepting connections on localhost:5173 before proceeding (check with a curl to
    http://localhost:5173/table-lab.html).

    (3) Capture at ?cam=hero (default, no ?rail= flag):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp4/verdict/hero-current.png

    (4) Capture at ?cam=rail (the rail/eye view, no ?rail= flag):
      LAB_URL="http://localhost:5173/table-lab.html?cam=rail" node .dev-stack/lab-shot.mjs .dev-stack/diag/table-3d/tp4/verdict/rail-current.png

    (5) Create docs/table-3d/anchors/tp4-base/ directory.

    (6) Downscale both captures to the anchor corpus format (consistent with the head/ corpus
    dimension — check docs/table-3d/anchors/head/hero.png dimensions and match). Copy/downscale
    to docs/table-3d/anchors/tp4-base/hero.png and docs/table-3d/anchors/tp4-base/rail.png.
    The anchor files are the committed permanent record; the .dev-stack/diag/ originals are
    gitignored scratch.

    (7) Commit: `git add docs/table-3d/anchors/tp4-base/ && git commit -m "docs(05-01): add tp4-base anchor captures (hero + rail) at pre-change HEAD"`.

    Do NOT start the Vite server if one is already running. Do NOT touch any frontend/src/lab/ files.
  </action>
  <verify>
    <automated>git tag | grep tp4-before-rail</automated>
    <automated>ls docs/table-3d/anchors/tp4-base/hero.png docs/table-3d/anchors/tp4-base/rail.png</automated>
    <automated>git log --oneline -1 | grep "tp4-base anchor"</automated>
  </verify>
  <done>git tag tp4-before-rail exists; docs/table-3d/anchors/tp4-base/hero.png and rail.png are committed; no changes to frontend/src/lab/.</done>
</task>

<task type="auto">
  <name>Task 2: Issue the elegance verdict — compare anchors, compute edge-thickness ratio, write TP4_VERDICT.md</name>
  <files>docs/table-3d/TP4_VERDICT.md</files>
  <read_first>
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Verdict-First Method" section — the structured side-by-side protocol, the edge-thickness ratio formula, the verdict table (lost/acceptable/lost-in-specific-respect), and the explicit provenance note about the absent elev/ anchors.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Reference Anchor Disk Audit" — the confirmed absence of the elev/ subdirectory and the six SSOT-named reference frames; the available corpus is head/ + tp3-base/ + tp2-base/ + the just-created tp4-base/.
    - docs/table-3d/anchors/tp4-base/rail.png (just captured) — the primary comparison surface.
    - docs/table-3d/anchors/head/hero.png — post-TP3 HEAD reference (same visual state as tp4-base/ if HEAD is clean).
    - docs/table-3d/anchors/tp3-base/hero.png — post-TP2 state (rail geometry was the same then; chips differ).
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-CONTEXT.md §code_context — leatherProfile rOut = FELT_R*1.072, woodCoamingProfile rOut = FELT_R*1.17, peak y ≈ 0.565; FELT_R = 6.5; edge-thickness ratio = 0.565 / 6.5 ≈ 0.087.
    - .planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-RESEARCH.md "Surgical Slim Recipe" — the tunable constants and minimum-viable slim strategy (only if verdict=lost).
  </read_first>
  <action>
    Produce docs/table-3d/TP4_VERDICT.md with the following required sections:

    (1) ANCHOR PROVENANCE NOTE (mandatory, verbatim): "The SSOT §TP4 names six slim/heavy reference
    frames (elev/00-base-wide, 04-wood-wide, final-wide, elev/05-leather-wide, REFERENCE-wide,
    12-backdrop-wide). As of 2026-06-12, the elev/ subdirectory does not exist on disk in
    docs/table-3d/anchors/. The verdict is therefore issued against the available committed corpus
    (head/, tp3-base/, tp2-base/, the newly captured tp4-base/) plus the operator's on-device
    memory of the slim-rail baseline. This is a non-blocking gap; the verdicting is valid against
    the available evidence."

    (2) EDGE-THICKNESS RATIO (before): Compute and record:
      FELT_R = 6.5
      leatherProfile peak y ≈ 0.565 (the rail band height above felt plane)
      woodCoamingProfile rOut = FELT_R * 1.17 = 7.605
      leatherProfile rIn = FELT_R * 0.962 = 6.253
      Rail total radial width = 7.605 - 6.253 = 1.352
      Edge-thickness ratio = 0.565 / 6.5 ≈ 0.087 (8.7% of FELT_R)
      Record this verbatim. This is the "before" value.

    (3) COMPARISON MATRIX: For each available anchor pair, note what the rail/contour reads as
    (proportions, whether the edge reads as a refined poker-table edge or a heavy band). Compare
    the tp4-base/rail.png (the rail/eye view) against tp3-base/hero.png (the post-TP2 contour
    — geometry unchanged since then, before the chip mass was added). Note any perceptual
    difference in how the rail presents.

    (4) VERDICT (choose exactly one of three):
      - "lost" — the edge clearly reads as thick/heavy; the slim rail is unambiguously preferable.
        Must name the specific profile dimension to target (woodCoamingProfile yTop vs leatherProfile
        rOut) and whether slimming is warranted.
      - "acceptable" — the current contour reads as a refined poker-table edge despite the mass.
        The craft levers (05-03) run on the current contour. 05-02 is SKIPPED.
      - "lost-in-specific-respect" — name the specific dimension (e.g. "wood coaming reads heavy
        but leather roll reads correct"). Only the named profile dimension is targeted in 05-02.

    (5) ACTION TRIGGERED: one of: "Skip 05-02 (verdict: acceptable)" / "Run 05-02 targeting
    woodCoamingProfile yTop (slim 0.34->0.28)" / "Run 05-02 targeting leatherProfile rOut
    (slim 1.072->1.060)" etc.

    The verdict must be evidence-driven — do not pre-decide. "acceptable" is a fully valid
    outcome and must not be avoided. Record the reasoning honestly. Commit:
    `git add docs/table-3d/TP4_VERDICT.md && git commit -m "docs(05-01): record TP4 elegance verdict"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const v=fs.readFileSync('docs/table-3d/TP4_VERDICT.md','utf8'); const hasVerdict=/Verdict\s*:\s*(lost|acceptable|lost-in-specific-respect)/.test(v); const hasRatio=/0\.087|edge-thickness ratio/.test(v); const hasProvenance=/elev\//.test(v); if(!hasVerdict||!hasRatio||!hasProvenance){console.error('FAIL: TP4_VERDICT.md missing required sections'); process.exit(1);} console.log('OK — TP4_VERDICT.md contains verdict string, ratio, and provenance note');"</automated>
    <automated>git log --oneline -2 | grep -E "tp4-base|verdict"</automated>
  </verify>
  <done>docs/table-3d/TP4_VERDICT.md is committed with: anchor provenance note (elev/ absent), edge-thickness ratio 0.087, comparison matrix against available corpus, a verdict string (one of lost/acceptable/lost-in-specific-respect) with rationale, and the action triggered for 05-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — capture-only plan with no untrusted input, no source code changes, no prod build surface.
/table-lab.html is dev-server-only and never in the production Vite build (SSOT §5.8).

| Boundary | Description |
|----------|-------------|
| current HEAD -> tp4-base anchors | The captures must reflect the exact TP3-complete HEAD with no uncommitted lab changes; git status clean is the gate |
| SSOT-named anchors -> available corpus | The elev/ frames named in the SSOT are absent; the verdict uses the available head/tp3-base/tp4-base corpus — this gap must be recorded explicitly, not hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01a | Tampering | Pre-change capture at wrong HEAD | mitigate | Confirm git status clean in frontend/src/lab/ before capture; tag tp4-before-rail before any capture |
| T-05-01b | Repudiation | Verdict issued without recording provenance gap | mitigate | TP4_VERDICT.md has a mandatory ANCHOR PROVENANCE NOTE section naming the absent elev/ anchors explicitly |
| T-05-01c | Tampering | verdict pre-decided (not evidence-driven) | mitigate | "acceptable" is a documented valid outcome; plan requires comparison against available corpus before committing to any verdict |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- git tag tp4-before-rail present.
- docs/table-3d/anchors/tp4-base/hero.png and rail.png committed.
- docs/table-3d/TP4_VERDICT.md committed with: anchor provenance note (elev/ absent from disk), edge-thickness ratio 0.087, comparison matrix, verdict string (lost / acceptable / lost-in-specific-respect), and the action triggered for 05-02.
- Zero changes to frontend/src/lab/ files — this plan is capture + documentation only.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Pre-change state pinned (tag + committed captures); the exact contour being adjudicated is unambiguous.
- Edge-thickness ratio recorded as a measurable before-value.
- Verdict issued with evidence, honestly, including the explicit provenance note about the absent SSOT anchors.
- 05-02 gate is set: either skipped (verdict=acceptable) or scoped to a specific profile (verdict=lost/*).
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/05-tp4-rail-contour-elegance-the-open-check/05-01-SUMMARY.md` when done,
recording the tag, the anchor corpus state (tp4-base/ added), the edge-thickness ratio, the verdict
string and rationale, the provenance note about the absent elev/ anchors, and the action triggered for
05-02.
</output>
