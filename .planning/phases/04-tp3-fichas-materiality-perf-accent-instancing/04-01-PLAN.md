---
phase: 04-tp3-fichas-materiality-perf-accent-instancing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/table-3d/anchors/tp3-base/hero.png
  - docs/table-3d/anchors/tp3-base/macro.png
  - docs/table-3d/anchors/tp3-base/pov.png
  - docs/table-3d/TP3_BASELINE.md
requirements:
  - "SSOT §TP3 — instancing = visual-parity: capture a tp3-base 'before' (the apples-to-apples basis for the strict-parity gate)"
  - "SSOT §TP3 — M10 must-ship-or-revert: record the current demoted-pot + ?chips=full draw-call baseline (the net-positive target basis)"
  - "SSOT §5.3 — per-phase rollback tag before any edit (tp3-before-chips at the pre-edit HEAD)"
  - "04-CONTEXT.md Sequencing & Eval-Rig — Instancing-FIRST + tp3-base; head/ + reference-tag/ + tp2-base/ stay IMMUTABLE"
autonomous: true
must_haves:
  truths:
    - "A rollback tag tp3-before-chips exists at the pre-edit HEAD on spike/table-3d-hero, LOCAL only, never pushed/deleted (SSOT §5.3)"
    - "A dated tp3-base of the current post-TP2 chips exists at docs/table-3d/anchors/tp3-base/{hero,macro,pov}.png — the strict-parity + de-Vegas 'before' basis"
    - "docs/table-3d/anchors/head/, reference-tag/, and tp2-base/ are byte-unchanged (the protected references are never degraded)"
    - "The current draw-call counts at HERO + ?chips=full are measured via stats-read.mjs and recorded as the M10 must-ship target basis (TP0 expectation: HERO ~233, ?chips=full ~637)"
  artifacts:
    - path: "docs/table-3d/anchors/tp3-base/hero.png"
      provides: "tp3-base at the frozen HERO money shot (M10 hierarchy + strict-parity delta basis for instancing)"
    - path: "docs/table-3d/anchors/tp3-base/macro.png"
      provides: "tp3-base at the frozen MACRO money shot (the chip-quality strict-parity basis — any look change after instancing = regression)"
    - path: "docs/table-3d/anchors/tp3-base/pov.png"
      provides: "tp3-base at the frozen POV money shot (full-corpus completeness)"
    - path: "docs/table-3d/TP3_BASELINE.md"
      provides: "the recorded M10 baseline draw counts (HERO + ?chips=full), the must-ship target (demoted-pot ~42 -> <=~10; ?chips=full < 220), and the capture provenance (commit, scene constants, frozen cams)"
      contains: "M10"
  key_links:
    - from: "git tag tp3-before-chips"
      to: "HEAD on spike/table-3d-hero (pre-edit)"
      via: "git tag (LOCAL, never pushed/deleted) — SSOT §5.3 per-phase rollback"
      pattern: "tp3-before-chips"
    - from: "docs/table-3d/anchors/tp3-base/macro.png"
      to: "the post-instancing MACRO capture (plan 04-02 strict-parity gate)"
      via: "instancing must match tp3-base pixel-for-pixel in chip look — any change = regression -> revert"
      pattern: "tp3-base"
    - from: "tools/table-3d/stats-read.mjs hero + &chips=full"
      to: "docs/table-3d/TP3_BASELINE.md M10 baseline"
      via: "draw-call read wraps drawElements/drawArrays/*Instanced — the must-ship-or-revert target basis"
      pattern: "stats-read"
---

<objective>
Wave-0 foundation for TP3 (chips materiality + perf), touching NO chip render output. Three things, in
strict order: (1) cut the per-phase rollback tag `tp3-before-chips` at the clean pre-edit HEAD on
spike/table-3d-hero BEFORE any chip edit (SSOT §5.3); (2) capture a NEW dated `tp3-base` of the current
post-TP2 chips at the 3 frozen money shots -> `docs/table-3d/anchors/tp3-base/` (the operator-decided
"capture a tp3-base as the apples-to-apples before" — the strict-visual-PARITY basis for the instancing
gate AND the before-basis for the de-Vegas gate); (3) record the current draw-call baseline at HERO +
`?chips=full` via `stats-read.mjs` as the M10 must-ship-or-revert target basis.

Purpose: TP3 has a SPLIT rollback disposition — instancing is must-ship-or-revert with STRICT visual
parity (any chip-look change after instancing = regression), and de-Vegas is non-blocking. Both need a
fixed, correct "before": `tp3-base` is the pixel basis the strict-parity MACRO check compares against,
and the recorded M10 baseline is the net-positive perf target the instancing must beat or be reverted.
The TP0/TP2 protected anchors (`head/`, `reference-tag/`, `tp2-base/`) stay IMMUTABLE.

Output: `tp3-before-chips` tag (LOCAL); `docs/table-3d/anchors/tp3-base/{hero,macro,pov}.png`;
`docs/table-3d/TP3_BASELINE.md` (M10 baseline draw counts + the must-ship targets + capture provenance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-PATTERNS.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the tp3-before-chips rollback tag + capture the tp3-base baseline at the 3 frozen shots</name>
  <files>docs/table-3d/anchors/tp3-base/hero.png, docs/table-3d/anchors/tp3-base/macro.png, docs/table-3d/anchors/tp3-base/pov.png</files>
  <read_first>
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §5 lines 187-207 (rollback disposition table — the TP3 row is "SPLIT: de-Vegas non-blocking; instancing must-ship-or-revert") and §TP3 lines 310-332. The per-phase rollback tag is cut BEFORE starting the TP. The doc WINS on any conflict with this plan.
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md "Sequencing & Eval-Rig" (lines 25-32) — operator-locked: capture a NEW dated tp3-base of the current post-TP2 chips; head/ + reference-tag/ + tp2-base/ stay IMMUTABLE; tp3-base dir naming is Claude's discretion.
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Recommended Project Structure" (lines 167-182) + "tp3-base capture (before instancing)" (lines 343-353) — the exact lab-shot.mjs capture commands and the anchor dir layout (hero/macro/pov.png).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-01-PLAN.md Task 1 (the structural template — same tag-then-capture flow; tp2-base used card/hero/macro naming, this plan uses hero/macro/pov per RESEARCH).
    - .dev-stack/lab-shot.mjs — the Playwright D3D11 headless capture harness: reads LAB_URL env, auto-appends spin=off, writes the PNG to the path arg. Confirm the actual dev-server port from the Vite console (resume protocol uses 5173; Vite may fall back to 5174/5180).
    - frontend/src/lab/TableLab.tsx lines 819-839 — the `?chips=` flag: default (no param) = the demoted accent pot (3 stacks C5/E3/B4 at group [3.0,0,1.5] scale 0.5); `?chips=full` = heavy pot; `?chips=off` = none. tp3-base is captured with NO `?chips=` override (the current default). These chips are FROZEN for this plan — capture them, do NOT edit them.
  </read_first>
  <action>
    First confirm the working tree is clean and HEAD is the intended pre-edit commit on spike/table-3d-hero
    (no chip edits yet) and that the existing tags include tp0-before-rig / tp1-before-felt / tp2-before-cards;
    then create `git tag tp3-before-chips` at HEAD (no -f, never push, never delete — SSOT §5.3). Do NOT make
    any commit before the tag; the tag MUST sit at the clean pre-edit HEAD. Then start the Vite dev server
    (`cd frontend && npm run dev`) and note the actual port. Capture the 3 frozen money shots of the current
    post-TP2 chips (NO `?chips=` flag — the default demoted accent pot IS the tp3-base "before") into the NEW
    tracked dir `docs/table-3d/anchors/tp3-base/` using `.dev-stack/lab-shot.mjs` with `LAB_URL` set to the lab
    URL at the correct port plus `?cam=hero`, `?cam=macro`, `?cam=card` (spin=off is auto-appended), writing to
    `hero.png`, `macro.png`, and `pov.png` respectively. Downscale each committed PNG to match the existing
    anchor corpus width (~1440px, the head/ + tp2-base/ convention) per the RESEARCH tracking decision; keep any
    full-res copies only under the gitignored `.dev-stack/diag/table-3d/tp3-base/`. Do NOT write or modify any
    file under `docs/table-3d/anchors/head/`, `reference-tag/`, or `tp2-base/` — they are immutable. Do NOT
    touch any chip material/geometry/flag here; this task only cuts the tag and captures pixels.
  </action>
  <verify>
    <automated>git tag --list tp3-before-chips</automated>
    <automated>git rev-list -n 1 tp3-before-chips</automated>
    <automated>node -e "const fs=require('fs');for(const f of ['hero','macro','pov']){if(!fs.existsSync('docs/table-3d/anchors/tp3-base/'+f+'.png')){console.error('MISSING '+f);process.exit(1)}}console.log('tp3-base 3/3 present')"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list tp3-before-chips` outputs `tp3-before-chips`; `git rev-list -n 1 tp3-before-chips` equals the pre-edit HEAD on spike/table-3d-hero; the tag is unpushed (LOCAL).
    - `docs/table-3d/anchors/tp3-base/hero.png`, `macro.png`, `pov.png` all exist and are non-empty PNGs captured at the frozen hero/macro/card cameras with the default (no-flag) demoted chip pot.
    - `git status` shows the 3 new tp3-base PNGs; a diff confirms `docs/table-3d/anchors/head/`, `reference-tag/`, and `tp2-base/` are byte-unchanged.
    - No edit to any frontend/src/lab/ chip material, geometry, texture, or camera preset; `git diff --name-only` shows no frontend/src/lab/ file changed.
  </acceptance_criteria>
  <done>Tag tp3-before-chips exists at the pre-edit HEAD (LOCAL); the 3 tp3-base money-shot PNGs are captured + tracked; the TP0/TP2 anchors are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record the M10 draw-call baseline (HERO + ?chips=full) as the must-ship-or-revert target basis</name>
  <files>docs/table-3d/TP3_BASELINE.md</files>
  <read_first>
    - tools/table-3d/stats-read.mjs (the whole file) — how M10 is read: it wraps drawElements / drawArrays / drawElementsInstanced / drawArraysInstanced on the raw WebGL context, samples 90 rAF frames, returns the MAX non-zero count. CLI: `node tools/table-3d/stats-read.mjs <baseUrl> <cam> "<extra>"`. NOT StatsProbe/gl.info (stale 1 under headless rAF throttle).
    - tools/table-3d/metrics.mjs lines 63-64 — THRESHOLDS.M10_DRAWCALL_MAX = 150 (HERO/POV/MACRO color pass), M10_DRAWCALL_CHIPS_FULL_MAX = 220 (?chips=full stress diagnostic, separate looser ceiling).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Question 2" (lines 312-367) — the exact CLI commands, the output JSON format ({cam, ok, calls, ...}), and the realistic draw-count delta math (demoted-pot ~42 -> ~10; ?chips=full ~637 -> well under 220).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md "Instancing & Perf" (lines 43-52) — the must-ship targets: demoted-pot chip draws ~42 -> <= ~10; ?chips=full back within < 220; M10 PASS. The rollback rule: if perf is NOT net-positive -> REVERT instancing.
    - docs/table-3d/anchors/tp3-base/hero.png (from Task 1) — the same dev-server + frozen cameras the stats read uses.
  </read_first>
  <action>
    With the Vite dev server running (from Task 1, at the confirmed port), run `stats-read.mjs` at the HERO
    camera with the default demoted pot (`node tools/table-3d/stats-read.mjs http://localhost:<port> hero`) and
    at HERO with the stress pot (`node tools/table-3d/stats-read.mjs http://localhost:<port> hero "&chips=full"`),
    capturing the `calls` value from each. Optionally also read MACRO (`stats-read.mjs ... macro`) for the
    full record. Write `docs/table-3d/TP3_BASELINE.md` recording: (a) the measured HERO draw count and the
    HERO `?chips=full` draw count (the TP0 expectation is ~233 and ~637 — record the ACTUAL measured numbers,
    not the expectation); (b) the M10 must-ship targets explicitly — HERO color pass must stay < 150
    (M10_DRAWCALL_MAX), `?chips=full` must come back within < 220 (M10_DRAWCALL_CHIPS_FULL_MAX), and the
    demoted-pot chip-draw component goes ~42 -> <= ~10; (c) the must-ship-or-revert rule stated verbatim:
    instancing ships ONLY if it is net-positive on draw count AND MACRO strict visual parity holds vs
    tp3-base/macro.png — otherwise plan 04-02 REVERTS the instancing; (d) the capture provenance (the
    tp3-before-chips commit, the frozen HERO fov 32 / MACRO fov 26 cameras, the scene constant H = 0.1 and the
    demoted pot 3 stacks C5/E3/B4). Do NOT change any THRESHOLDS or any tool; this task only reads + records.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('tools/table-3d/stats-read.mjs')){console.error('stats-read.mjs missing');process.exit(1)}console.log('stats-read.mjs present')"</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/table-3d/TP3_BASELINE.md','utf8');if(!/M10/.test(s)){console.error('TP3_BASELINE.md missing M10 record');process.exit(1)}if(!/chips=full/.test(s)){console.error('missing chips=full baseline');process.exit(1)}if(!/must-ship|net-positive|revert/i.test(s)){console.error('missing must-ship-or-revert rule');process.exit(1)}console.log('TP3_BASELINE records M10 + chips=full + must-ship rule')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/table-3d/TP3_BASELINE.md` records the ACTUAL measured HERO draw count and the HERO `?chips=full` draw count (from stats-read.mjs `calls`), not just the TP0 expectation.
    - The file states the M10 must-ship targets numerically: HERO < 150, `?chips=full` < 220, demoted-pot chip draws ~42 -> <= ~10.
    - The file states the must-ship-or-revert rule: instancing ships only if net-positive on draws AND MACRO strict parity holds vs tp3-base; otherwise revert.
    - The capture provenance is recorded (tp3-before-chips commit + frozen HERO/MACRO cams + demoted pot C5/E3/B4).
    - No tool or threshold edited: `git diff --name-only` shows no change under tools/table-3d/.
  </acceptance_criteria>
  <done>The current M10 draw-call baseline (HERO + ?chips=full) is measured and recorded as the must-ship-or-revert target basis, with the targets and the revert rule written into TP3_BASELINE.md; no tooling changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in the prod build. TP3 touches ONLY `frontend/src/lab/`
(and here `docs/` + a read of `tools/table-3d/` tooling); the lab route `/table-lab.html` is never in the
production Vite build (SSOT §5.8). No auth / network / data / user-input surface exists. The only boundaries
that matter are reference-integrity and scope.

| Boundary | Description |
|----------|-------------|
| pre-edit HEAD -> first chip edit | The rollback tag tp3-before-chips must be cut on the clean side (SSOT §5.3) |
| TP0/TP2 anchors (head/, reference-tag/, tp2-base/) -> new tp3-base/ | The protected references are immutable; only the new dated baseline dir may be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01a | Tampering | TP0/TP2 protected anchors (head/, reference-tag/, tp2-base/) | mitigate | This plan writes ONLY to the new tp3-base/ dir; acceptance asserts head/ + reference-tag/ + tp2-base/ are byte-unchanged |
| T-04-01b | Tampering | the M10 baseline trustworthiness | mitigate | Baseline read via stats-read.mjs (the GL-wrap reader, not stale gl.info) so the must-ship-or-revert gate has an honest target |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP3 (RESEARCH "No new packages required"; drei 9.114.0 already installed; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `git tag --list tp3-before-chips` prints the tag; `git rev-list -n 1 tp3-before-chips` resolves to the pre-edit HEAD.
- `docs/table-3d/anchors/tp3-base/{hero,macro,pov}.png` exist; `head/`, `reference-tag/`, `tp2-base/` byte-unchanged.
- `docs/table-3d/TP3_BASELINE.md` records the measured M10 baseline (HERO + ?chips=full), the must-ship targets, and the revert rule.
- `git status` shows only new docs/anchors files; no chip render code touched; lab still absent from the vite build input.
</verification>

<success_criteria>
- Rollback tag `tp3-before-chips` exists at the pre-edit HEAD (LOCAL, unpushed).
- The dated tp3-base baseline (3 money shots) exists + tracked; the TP0/TP2 anchors are IMMUTABLE and unchanged.
- The M10 draw-call baseline (HERO + ?chips=full) is measured + recorded with the must-ship-or-revert targets.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-01-SUMMARY.md` when done, and write the M10 baseline + must-ship targets to `docs/table-3d/TP3_BASELINE.md`.
</output>
