---
phase: 04-tp3-fichas-materiality-perf-accent-instancing
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - docs/table-3d/TP3_OPERATOR_AB.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
requirements:
  - "SSOT §TP3 perceptual gate — operator A/B at HERO + MACRO: worn artisanal clay that RECEDES, C tooled-not-printed, no Vegas gloss?"
  - "SSOT §TP3 — Stop-on-ambiguous: chips still pull the eye / read plastic -> STOP; any instancing look change = regression"
  - "SSOT §TP3 — Rollback SPLIT: de-Vegas non-blocking (keep current/instanced chips); instancing must-ship-or-revert (net-positive perf or don't ship)"
  - "SSOT §TP3 — HARD gates restated at the gate: M10 PASS (< 150 HERO, < 220 chips=full); M2 cards-vs-chips >= 2x"
autonomous: false
must_haves:
  truths:
    - "The operator has performed the A/B at HERO (?cam=hero, fov32) + MACRO (?cam=macro, fov26): the de-Vegas instanced chips vs tp3-base (the post-TP2 'before'), judging 'worn artisanal clay that RECEDES, C tooled-not-printed, no Vegas gloss?', with the ?chips= flags available to isolate instancing-only vs de-Vegas and the legacy vs instanced look"
    - "The SPLIT disposition is explicitly confirmed at the gate: instancing is must-ship-or-revert (it shipped only because it was net-positive on draws + strict MACRO parity), and de-Vegas is non-blocking (revertable to the instanced chips without blocking the program)"
    - "The HARD gates are confirmed: M10 PASS (HERO < 150, ?chips=full < 220) from the 04-02 measurement AND M2 cards-vs-chips >= 2x from the 04-03 measurement (re-confirmed live if the operator requests)"
    - "On stop-on-ambiguous (chips pull the eye / read plastic / any instancing look change), the offending workstream is identified via its ?chips= flag and handled per its disposition: de-Vegas reverts to the instanced chips (non-blocking); an instancing look-change/perf-non-positive triggers the instancing revert (must-ship-or-revert)"
  artifacts:
    - path: "docs/table-3d/TP3_OPERATOR_AB.md"
      provides: "the recorded operator verdict, the A/B basis (vs tp3-base), the per-workstream disposition (instancing ship/revert; de-Vegas ship/revert), the HARD-gate confirmation (M10 + M2), and the ?chips= flag map"
      contains: "Operator verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "the post-TP3 chips-row score (recorded only if shipped; must not regress)"
      contains: "chips"
  key_links:
    - from: "docs/table-3d/TP3_OPERATOR_AB.md"
      to: "docs/table-3d/anchors/tp3-base/{hero,macro}.png + the ?chips= flags"
      via: "the A/B compares the de-Vegas instanced chips against the post-TP2 baseline, isolating instancing-only vs de-Vegas"
      pattern: "tp3-base"
    - from: "the operator stop-on-ambiguous verdict"
      to: "the offending workstream's disposition (de-Vegas revert non-blocking / instancing revert must-ship)"
      via: "the SPLIT rollback — de-Vegas reverts to instanced chips; instancing reverts only on look-change/perf-non-positive"
      pattern: "revert"
---

<objective>
The TP3 operator perceptual gate — the GSD<->Chiribito human seam (SSOT §TP3 perceptual gate, §5.7
stop-on-ambiguous). The operator does an A/B at the two chip-telling shots (HERO fov32 for the hierarchy /
recede read, MACRO fov26 for the clay/tooled-C/gloss read) comparing the de-Vegas instanced chips (from
04-02 instancing + 04-03 de-Vegas) against the post-TP2 baseline (`docs/table-3d/anchors/tp3-base/`, the
apples-to-apples "before"). The judgement is the SSOT question: "worn artisanal clay that RECEDES, C
tooled-not-printed, no Vegas gloss?" The `?chips=` flags isolate instancing-only vs de-Vegas (and the
legacy per-chip look) so each workstream's contribution is separable. The HARD gates (M10 PASS, M2 >= 2x)
from 04-02/04-03 are restated and confirmed. The SPLIT rollback disposition is confirmed: instancing is
must-ship-or-revert; de-Vegas is non-blocking.

Purpose: Automated metrics (04-02/04-03) prove "net-positive draws / parity / M2 >= 2x / chips recede" but
cannot judge "reads as worn artisanal clay that recedes, not plastic, no Vegas gloss." Only the operator
can. This gate cannot be auto-approved (autonomous:false; auto_advance does not apply to a perceptual
gate). Stop-on-ambiguous applies the SPLIT disposition: de-Vegas reverts to the instanced chips
(non-blocking); an instancing look-change or non-positive perf triggers the instancing revert
(must-ship-or-revert). No heavy central pile; inter-chip AO is a TP6 bonus, never a TP3 gate.

Output: `docs/table-3d/TP3_OPERATOR_AB.md` recording the verdict, the per-workstream disposition, the
HARD-gate confirmation, and the ?chips= flag map; the chips-row scorecard delta if shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-VALIDATION.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-02-SUMMARY.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-03-SUMMARY.md
@docs/table-3d/TP3_BASELINE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — "worn artisanal clay that RECEDES, C tooled-not-printed, no Vegas gloss?" at HERO + MACRO</name>
  <files>docs/table-3d/TP3_OPERATOR_AB.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - docs/table-3d/TP3_BASELINE.md (from 04-01) — the M10 baseline + the must-ship targets + the must-ship-or-revert rule (the perf basis the operator is told instancing satisfied).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-02-SUMMARY.md (instancing) — the ?chips= flag map, the measured M10 before/after (HERO + ?chips=full), and the strict MACRO parity verdict (instancing shipped only if net-positive + parity).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-03-SUMMARY.md (de-Vegas) — the ?chips= flag map, the de-Vegas material/palette before/after, the M2 >= 2x ratio + the recede verdict + the MACRO-quality verdict.
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP3 lines 326-332 (the perceptual gate + stop-on-ambiguous + the SPLIT rollback: de-Vegas non-blocking / instancing must-ship-or-revert) + §5 line 197 (the TP3 rollback-disposition row) + lines 205-207 (the global escalation cap).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md "Instancing & Perf" + "De-Vegas Material" + "Locked by SSOT §TP3" (lines 43-63) — the operator-locked restraint defaults and the SPLIT disposition.
    - docs/table-3d/SCORECARD_TABLE_3D.md (the chips row + the chips rubric) — the score must not regress if shipped; AAA(5) may remain out of reach.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-06-PLAN.md (the structural template for this operator-gate plan — same checkpoint shape, restated HARD gates, per-item disposition, no-auto-approve).
  </read_first>
  <action>
    Present the A/B to the operator using the consolidated captures from 04-02/04-03 (and the live lab if
    requested), following the <how-to-verify> protocol. Do NOT auto-approve — this is a perceptual gate
    (autonomous:false; auto_advance does not apply). First restate the HARD gates from the 04-02/04-03
    summaries (M10 PASS: HERO < 150, ?chips=full < 220; M2 cards-vs-chips >= 2x) and the SPLIT disposition
    (instancing must-ship-or-revert — it shipped only because net-positive + strict MACRO parity; de-Vegas
    non-blocking) so the operator judges materiality on a proven-perf, proven-hierarchy base. Record the
    operator's verdict, the A/B basis (de-Vegas instanced chips vs tp3-base), the per-workstream disposition
    (de-Vegas ship/revert; instancing ship/revert), the HARD-gate confirmation, and the ?chips= flag map into
    docs/table-3d/TP3_OPERATOR_AB.md. On a stop-on-ambiguous: if the de-Vegas reads plastic / pulls the eye,
    the executor REVERTS the de-Vegas materiality ONLY (restore the instanced pre-de-Vegas chips — NON-BLOCKING;
    the instancing stays), re-captures, re-confirms the HARD gates, and re-presents; if instead the operator
    judges the INSTANCING changed the look (a parity break the automated check missed) or perf was not actually
    net-positive, that triggers the instancing revert (must-ship-or-revert — restore the per-chip ChipStack).
    If shipped, update the chips row in SCORECARD_TABLE_3D.md (must not regress; note if AAA(5) awaits a later
    phase). Do NOT add a heavy central pile, inter-chip/screen-space AO (TP6), or touch the felt / cards /
    cameras here.
  </action>
  <what-built>
    TP3 re-authored the chips as matte worn clay that RECEDES and instanced them, in two workstreams behind
    `?chips=` flags on the post-TP2 scene: (A) INSTANCING (04-02, must-ship-or-revert) — ChipStack's per-chip
    mesh loop (3 meshes/chip) replaced by drei <Instances> per denomination (one body InstancedMesh + one
    TOP-face per stack), the never-seen bottom face dropped, chip textures right-sized 2048 -> 512, the
    deterministic 10-group cream-insert phase-alignment broken (0.37 seed), the hand-stacked jitter seeds
    preserved (unit-tested byte-equivalence); it shipped only because the draw count was net-positive (HERO <
    150, ?chips=full < 220, demoted-pot chip draws ~42 -> <= ~10) AND MACRO chip look matched tp3-base
    pixel-for-pixel (strict parity). (B) DE-VEGAS (04-03, non-blocking) — body clearcoat 0.42 -> 0.32,
    clearcoatRoughness 0.46 -> 0.5, sheen 0.5 -> 0 (gloss killed); face bumpMap -> normalMap (the C/rim tooled
    as a RECESSED mark via the shared Sobel helper), clearcoatRoughness 0.36 -> 0.5; palette chroma muted ~20%
    + value lowered so the accent recedes; face logo desaturated + shrunk; restrained micro-grain. The HARD
    gates are green: M10 PASS, M2 cards-vs-chips >= 2x, the chips recede (no longer the 2nd-brightest/most-
    saturated object after the cards), MACRO chip quality >= the tp3-base baseline. Consolidated captures are
    in `.dev-stack/diag/table-3d/tp3/gate/` (hero.png fov32, macro.png fov26).
  </what-built>
  <how-to-verify>
    Judge the CHIP MATERIALITY + HIERARCHY. The SSOT question is: do the chips read as WORN ARTISANAL CLAY
    that RECEDES (a quiet accent, muted, matte, with the C/rim tooled-not-printed and NO Vegas gloss) while
    the cards stay the protagonist? Inter-chip crevice AO + lighting/depth are deferred (TP5/TP6) and must
    not trip this gate.

    1. Bring the dev server up if needed (`cd frontend && npm run dev`) and open the lab live, OR review the
       stills in `.dev-stack/diag/table-3d/tp3/gate/`.
    2. HERO read (the hierarchy / recede shot): open `http://localhost:5173/table-lab.html?cam=hero` and
       compare against `docs/table-3d/anchors/tp3-base/hero.png` (the post-TP2 "before"). Questions: (a) do
       the chips RECEDE — a quiet accent off to the side, NOT the 2nd-brightest/most-saturated object after
       the cards? (b) do the cards still clearly dominate (M2 >= 2x)?
    3. MACRO read (the clay / tooled-C / gloss shot): open `?cam=macro` and compare against
       `docs/table-3d/anchors/tp3-base/macro.png`. Questions: (a) does the chip read as WORN MATTE CLAY (not
       plastic, not glossy)? (b) is the C / rim a TOOLED RECESSED mark (an edge of light along a groove), NOT
       a flat print? (c) is the gloss killed — no Vegas sheen/shine? (d) is the chip quality at least as good
       as the baseline (the 512 textures + instancing did not degrade the read)?
    4. Isolate the workstreams via the `?chips=` flags (the map is in the 04-02/04-03 summaries): the
       instancing-only flag shows the instanced-but-pre-de-Vegas chips (should be a STRICT PARITY match to
       tp3-base — any change there is an instancing regression); the de-Vegas flag shows the full TP3 look;
       `?chips=full` is the stress pot (draw-call diagnostic); `?chips=off` clears them; the legacy flag shows
       the old per-chip path. This lets a bad workstream be named.
    5. HARD gates (already proven in the summaries — re-confirm live if you wish): M10 PASS (HERO < 150,
       ?chips=full < 220); M2 cards-vs-chips >= 2x. These are non-negotiable.
    6. SPLIT disposition note: instancing is must-ship-or-revert (it is in only because it was net-positive on
       draws + a strict MACRO parity match); de-Vegas is non-blocking (it can revert to the instanced chips
       without blocking the program). No heavy central pile (the pot stays demoted); inter-chip AO is a TP6
       bonus, NEVER a TP3 gate.

    DISPOSITION (record in docs/table-3d/TP3_OPERATOR_AB.md):
    - UNAMBIGUOUS YES (worn artisanal clay that recedes, C tooled-not-printed, no Vegas gloss, chips recede,
      HARD gates green) -> TP3 SHIPS (instancing + de-Vegas). Record the chips scorecard delta (must not
      regress; note whether AAA(5) awaits a later phase).
    - STOP-ON-AMBIGUOUS on the DE-VEGAS (chips read plastic OR pull the eye OR a Vegas gloss/edge-glow):
      STOP. The executor REVERTS the de-Vegas materiality (restore the instanced pre-de-Vegas chips —
      NON-BLOCKING; instancing stays), re-confirms the HARD gates, and re-presents. TP3 still ships the
      instancing.
    - STOP-ON-AMBIGUOUS on the INSTANCING (the operator sees a look change vs tp3-base that the automated
      parity check missed, OR perf was not actually net-positive): the instancing is REVERTED
      (must-ship-or-revert — restore the per-chip ChipStack). Record it; de-Vegas (if it depended on the
      instanced path) reverts with it.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" (ships instancing + de-Vegas) — OR "revert de-Vegas" + the issue (e.g. "reads
    plastic" / "pulls the eye" / "the C looks printed" / "Vegas gloss") to trigger the non-blocking de-Vegas
    revert + re-present (instancing kept) — OR "revert instancing" + the issue (look change vs tp3-base / perf
    not net-positive) to trigger the must-ship-or-revert instancing revert. Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has read HERO + MACRO against tp3-base, with the HARD gates (M10 PASS, M2 >= 2x) restated and
    each workstream isolatable via its ?chips= flag; the verdict + per-workstream disposition (instancing
    ship/revert; de-Vegas ship/revert) + the HARD-gate confirmation + the ?chips= flag map are recorded in
    docs/table-3d/TP3_OPERATOR_AB.md; the chips scorecard row is updated if shipped (must not regress). No
    push/deploy/merge; no heavy central pile; felt/cards/cameras unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in the prod build. `/table-lab.html` is never in the
production Vite build (SSOT §5.8). No auth / network / data / user-input surface. At this gate the
boundaries are the perceptual judgement seam, the HARD perf/hierarchy gates, and the SPLIT rollback
disposition.

| Boundary | Description |
|----------|-------------|
| automated pre-gate metrics -> human perceptual gate | M10 / M2 / parity / recede are necessary-not-sufficient; the operator is the sole judge of "worn clay vs plastic" and "recedes vs pulls the eye" |
| de-Vegas (non-blocking) -> instancing (must-ship-or-revert) | The SPLIT disposition — de-Vegas reverts to the instanced chips; instancing reverts only on a look-change/perf-non-positive |
| TP3 chips -> the felt / cards / cameras | This gate changes only the chip disposition; no heavy central pile, no felt/card/camera change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04a | Tampering | M10 perf / M2 hierarchy (HARD gates) | mitigate | Restated at the gate from the 04-02/04-03 summaries; the operator confirms M10 PASS + M2 >= 2x before shipping |
| T-04-04b | Tampering | plastic / Vegas-gloss / pull-the-eye drift (de-Vegas) | mitigate | Operator judges clay-vs-plastic + recede at HERO+MACRO; stop-on-ambiguous reverts the de-Vegas (non-blocking) |
| T-04-04c | Tampering | an instancing look-change / non-positive perf | mitigate | If the operator sees a parity break the auto-check missed, the instancing reverts (must-ship-or-revert) |
| T-04-04-GATE | Repudiation | the gate decision | mitigate | Verdict + per-workstream disposition recorded in docs/table-3d/TP3_OPERATOR_AB.md (non-auto-approvable; autonomous:false) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP3 |
</threat_model>

<verification>
- The operator has reviewed HERO (fov32) + MACRO (fov26) against tp3-base, with each workstream isolatable via its ?chips= flag.
- The HARD gates (M10 PASS: HERO < 150, ?chips=full < 220; M2 >= 2x) from the 04-02/04-03 summaries are restated and confirmed.
- `docs/table-3d/TP3_OPERATOR_AB.md` records the verdict, the per-workstream disposition (instancing ship/revert; de-Vegas ship/revert), the HARD-gate confirmation, and the ?chips= flag map.
- If shipped: the chips scorecard row is updated and does not regress; no heavy central pile; felt/cards/cameras unchanged.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at HERO + MACRO; verdict + per-workstream disposition recorded; HARD gates (M10 + M2) confirmed.
- Unambiguous yes -> TP3 ships (instancing + de-Vegas; chips scorecard not regressed); or de-Vegas non-blocking revert (instancing kept); or instancing must-ship-or-revert revert.
- The SPLIT rollback disposition is explicit in the record.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-04-SUMMARY.md` when done, and write the verdict to `docs/table-3d/TP3_OPERATOR_AB.md`. Update the chips row in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP3 score if shipped.
</output>
