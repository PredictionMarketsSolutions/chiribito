---
phase: 04-tp3-fichas-materiality-perf-accent-instancing
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/lab/TableLab.tsx
  - frontend/src/lab/textures.ts
  - frontend/src/lab/chipStack.ts
  - frontend/src/lab/chipStack.test.ts
requirements:
  - "SSOT §TP3 — PERF: stacks -> InstancedMesh/drei Instances per denomination (one body + one TOP-face per suit); drop the never-seen bottom face; right-size chip textures (2048 -> mip-friendly)"
  - "SSOT §TP3 — demoted-pot chip draws ~42 -> <= ~10; ?chips=full back within < 220; M10 PASS"
  - "SSOT §TP3 — MACRO chip quality >= baseline; instancing = visual-parity (any look change = regression)"
  - "SSOT §TP3 — break the deterministic 10-group cream-insert phase-alignment"
  - "04-CONTEXT.md Instancing & Perf — must-ship-or-revert: net-positive perf or don't ship; behind a ?chips= flag for A/B; keep hand-stacked jitter (same seeds)"
autonomous: true
must_haves:
  truths:
    - "ChipStack's per-chip mesh loop is replaced by drei <Instances> per denomination: one body InstancedMesh + one TOP-face InstancedMesh per stack, each carrying the SAME jitter seeds (Math.sin(i*2.3)*0.012, Math.cos(i*1.7)*0.012) so the hand-stacked look is byte-equivalent"
    - "The never-seen bottom face mesh is dropped entirely (the instanced stack renders body + TOP-face only)"
    - "Chip textures are right-sized 2048 -> 512 (chipFaceTexture S, chipFaceBump S) and the edge texture W 2048 -> 512"
    - "The instanced demoted pot draw-call count is net-positive vs the tp3-base M10 baseline AND MACRO chip look matches tp3-base/macro.png (strict parity); demoted-pot chip draws <= ~10, ?chips=full < 220, M10 PASS"
    - "The instanced path is behind a ?chips= flag for apples-to-apples A/B against the non-instanced default; the ?chips=full stress branch is also instanced per denomination"
    - "The deterministic 10-group cream-insert phase-alignment is broken (a non-near-rational rotation seed) without going random (determinism preserved)"
    - "A Vitest unit test proves the instanced jitter/seed math is byte-identical to the prior per-chip ChipStack seeds (visual-parity guard); lab Vitest stays green"
  artifacts:
    - path: "frontend/src/lab/chipStack.ts"
      provides: "the extracted deterministic chip-instance layout (jitter seeds + per-i body/face position + rotationY) so it is unit-testable apart from the R3F tree"
      min_lines: 20
    - path: "frontend/src/lab/chipStack.test.ts"
      provides: "Vitest proof that the instanced layout reproduces the exact pre-instancing seeds (parity) and that the cream phase-alignment seed is the documented non-near-rational value"
      contains: "describe"
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "InstancedChipStack (drei <Instances geometry/material> + <Instance> children) replacing ChipStack at both the demoted pot and ?chips=full; bottom face dropped"
      contains: "Instances"
    - path: "frontend/src/lab/textures.ts"
      provides: "right-sized chip textures (chipFaceTexture/chipFaceBump S = 512, chipEdgeTexture W = 512)"
      contains: "512"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx InstancedChipStack"
      to: "@react-three/drei <Instances>/<Instance> (v9.114.0)"
      via: "geometry={kit.body}/material={mat.body} as PROPS on <Instances>; one InstancedMesh draw per body + one per TOP-face per denomination"
      pattern: "Instances"
    - from: "frontend/src/lab/chipStack.ts seeds"
      to: "the pre-instancing ChipStack jitter (Math.sin(i*2.3)/Math.cos(i*1.7), rotationY)"
      via: "identical seed constants -> byte-equivalent hand-stacked positions (strict visual parity)"
      pattern: "2.3"
    - from: "tools/table-3d/stats-read.mjs hero + chips=full"
      to: "docs/table-3d/TP3_BASELINE.md M10 baseline"
      via: "post-instancing draw count must be net-positive (< 150 HERO, < 220 chips=full) or REVERT the instancing"
      pattern: "stats-read"
---

<objective>
The INSTANCING workstream — TP3's must-ship-or-revert perf de-risk (operator-locked: instancing FIRST,
before de-Vegas). Replace `ChipStack`'s per-chip mesh loop (3 meshes/chip = the program's #1 draw-call
liability) with drei `<Instances>` per denomination: one body `InstancedMesh` + one TOP-face
`InstancedMesh` per stack, dropping the never-seen bottom face, and right-size the chip textures 2048 ->
512. The instanced stack carries the SAME deterministic jitter seeds so the hand-stacked look is
byte-equivalent (STRICT visual parity). Break the deterministic 10-group cream-insert phase-alignment.
The path is behind a `?chips=` flag for apples-to-apples A/B.

Purpose: SSOT §TP3 perf — demoted-pot chip draws ~42 -> <= ~10, `?chips=full` back < 220, M10 PASS, with
MACRO chip quality >= baseline because instancing is a visual-parity transform (NOT a look change). This
is must-ship-or-revert: if the draw count is NOT net-positive OR the MACRO capture differs from
tp3-base/macro.png, the instancing is REVERTED (do not ship a perf-neutral/negative or look-changing
instancing). De-Vegas materiality is a SEPARATE plan (04-03) on top of the instanced clay body.

Output: an `InstancedChipStack` in TableLab.tsx (drei `<Instances>`) wired at both the demoted pot and
`?chips=full`, the bottom face dropped, the chip textures at 512, the 10-group cream phase-alignment
broken, an extracted unit-tested `chipStack.ts` layout proving seed parity, and a recorded M10 + strict
MACRO parity verdict (ship) OR a revert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-CONTEXT.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-PATTERNS.md
@.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-VALIDATION.md
@docs/table-3d/TP3_BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract + unit-test the deterministic chip-instance layout (seed-parity guard) and break the cream phase-alignment</name>
  <files>frontend/src/lab/chipStack.ts, frontend/src/lab/chipStack.test.ts</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 248-275 — the CURRENT ChipStack: the per-chip loop, the jitter seeds jx = Math.sin(i*2.3)*0.012, jz = Math.cos(i*1.7)*0.012, the body position [x+jx, y+i*H, z+jz], and rotationY = i*0.55. These seeds are the visual-parity contract — they must be reproduced exactly in the extracted layout.
    - frontend/src/lab/TableLab.tsx lines 216-246 — the Chip component: body mesh; the TOP face at position [0, H/2+0.002, 0] rotation [-Math.PI/2,0,0]; the BOTTOM face at [0,-H/2-0.002,0] rotation [Math.PI/2,0,0] (this one is dropped — the layout produces body + top only).
    - frontend/src/lab/TableLab.tsx lines 55-57 — the constants R = 1, H = 0.1 (chip thickness) that the layout uses for stacking height.
    - frontend/src/lab/textures.ts lines 394-408 — chipEdgeTexture's `const groups = 10; const cx = i*gw + gw*0.5` cream-insert loop (the deterministic 10-group alignment to break). RESEARCH (lines 411-415) explains why i*0.55 partially aligns (0.55/0.628 ~ 0.876, near-rational) and why a rotation seed of i*0.37 (0.37/0.628 ~ 0.589) breaks it without going random.
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Question 1" (lines 215-302) — the InstancedChipStack target structure + the useMemo instance-data shape (bodyPos, facePos, rotY); "Pitfall 8" (lines 639-642) re the phase-alignment.
    - frontend/src/lab/cards.test.ts (the whole file) — the structural analog for a pure-math lab Vitest: imports the layout fns from a sibling module, asserts deterministic numeric output with describe/it/expect/toBeCloseTo. Mirror this style.
    - frontend/src/lab/cards.ts lines 1-40 — the sibling-module convention (pure exported layout fns consumed by TableLab.tsx); chipStack.ts mirrors this for chips.
  </read_first>
  <behavior>
    - chipStackLayout(count, position) returns one entry per i in [0,count): bodyPos = [position[0] + Math.sin(i*2.3)*0.012, position[1] + i*H, position[2] + Math.cos(i*1.7)*0.012], facePos = [bodyPos[0], position[1] + i*H + H/2 + 0.002, bodyPos[2]], rotY = i * CHIP_ROT_SEED.
    - Seed parity: for the same count+position, bodyPos[i] equals the pre-instancing ChipStack position [x + Math.sin(i*2.3)*0.012, y + i*H, z + Math.cos(i*1.7)*0.012] exactly (toBeCloseTo at 1e-9) — the jitter seeds 2.3 / 1.7 / 0.012 are preserved.
    - facePos[i] sits exactly H/2 + 0.002 above bodyPos[i] in Y and shares X/Z (the TOP face only; no bottom entry exists in the layout).
    - The cream phase-alignment seed CHIP_ROT_SEED is the documented non-near-rational value (0.37, NOT 0.55); a test asserts CHIP_ROT_SEED !== 0.55 and that CHIP_ROT_SEED / (2*Math.PI/10) is not within 0.03 of a half-integer (the alignment-broken condition), documenting WHY.
    - count <= 0 returns an empty array.
  </behavior>
  <action>
    Create `frontend/src/lab/chipStack.ts` exporting a pure, deterministic `chipStackLayout(count, position)`
    function and a `CHIP_ROT_SEED` constant, mirroring the cards.ts sibling-module convention (pure exported
    layout fns, no R3F imports — only the numeric constant H = 0.1 re-declared or imported to match
    TableLab.tsx). The layout reproduces the EXACT pre-instancing jitter seeds (Math.sin(i*2.3)*0.012,
    Math.cos(i*1.7)*0.012, body Y = i*H) so positions are byte-equivalent to the current ChipStack — this is
    the strict-visual-parity contract. The TOP face entry sits at Y = i*H + H/2 + 0.002 sharing X/Z; there is
    NO bottom-face entry (the bottom face is dropped). Set CHIP_ROT_SEED = 0.37 to break the deterministic
    10-group cream-insert phase-alignment (0.37/(2*PI/10) ~ 0.589, not near a half-integer, so cream inserts
    spiral irregularly instead of column-aligning) — replacing the old i*0.55. Then create
    `frontend/src/lab/chipStack.test.ts` mirroring cards.test.ts: assert seed parity vs the literal
    pre-instancing positions, the top-face-only Y offset, the empty-for-count<=0 case, and that CHIP_ROT_SEED
    is the documented non-near-rational alignment-breaking value. Do NOT wire this into TableLab.tsx yet
    (Task 2 consumes it). Do NOT touch any material values (de-Vegas is plan 04-03).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/chipStack.test.ts</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lab/chipStack.test.ts` passes; the test asserts (a) bodyPos[i] equals the literal pre-instancing ChipStack position using seeds 2.3/1.7/0.012 (toBeCloseTo 1e-9), (b) facePos[i] is exactly H/2+0.002 above bodyPos[i] in Y sharing X/Z, (c) no bottom-face entry exists, (d) count<=0 -> empty, (e) CHIP_ROT_SEED === 0.37 (!= 0.55) and is the documented alignment-broken seed.
    - `frontend/src/lab/chipStack.ts` exports `chipStackLayout` and `CHIP_ROT_SEED` with NO R3F/three render imports (pure layout, parallel to cards.ts).
    - `cd frontend && npx vitest run src/lab/` is green (no pre-existing lab test regressed).
    - No material/clearcoat/sheen/chroma value changed (no diff to useChipKit body/face mats in this task).
  </acceptance_criteria>
  <done>The deterministic chip-instance layout is extracted to chipStack.ts with byte-equivalent jitter seeds, the bottom face is absent by construction, the cream phase-alignment seed is broken to 0.37, and a Vitest proves seed parity + the alignment-break; lab Vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert ChipStack -> drei InstancedChipStack (drop bottom face), right-size textures 2048->512, behind ?chips=; gate on M10 + strict MACRO parity (must-ship-or-revert)</name>
  <files>frontend/src/lab/TableLab.tsx, frontend/src/lab/textures.ts</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 17-23 — the drei import block (OrbitControls, PerspectiveCamera, Environment, Lightformer, ContactShadows) to extend with Instances, Instance.
    - frontend/src/lab/TableLab.tsx lines 248-275 (ChipStack), 216-246 (Chip, incl. the bottom face mesh at 238-243 to drop), 169-214 (ChipKit interface + useChipKit — body LatheGeometry, face PlaneGeometry, per-suit mats; do NOT change material values here).
    - frontend/src/lab/TableLab.tsx lines 819-839 — the ?chips= branch: ?chips=full (4 stacks C17/B12/E14/O9 + 2 loose Chips) and the default demoted pot (group [3.0,0,1.5] scale 0.5: C5/E3/B4). BOTH must use InstancedChipStack per denomination after this task.
    - frontend/src/lab/chipStack.ts + chipStack.test.ts (from Task 1) — the deterministic layout to consume; the <Instance> children map over chipStackLayout(count, position).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Question 1" (lines 215-308, the InstancedChipStack code + limit={count+4} + the face <Instance> rotation [-Math.PI/2, 0, rotY]), "Code Examples" InstancedChipStack (lines 701-749), and the Pitfalls: #1 geometry/material as PROPS not children (599-602), #2 limit too small -> silent truncation (604-608), #4 Instance rotation is an Euler tuple not a scalar (616-620), #6 top-face rotation must be -Math.PI/2 (629-632), #7 loose chips in ?chips=full (634-637).
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Question 5" (lines 489-516) — right-size chipFaceTexture/chipFaceBump S = 2048 -> 512 (lines 252, 311) and chipEdgeTexture W = 2048 -> 512 (line 369); mip-friendly, anisotropy 8 stays.
    - .planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-RESEARCH.md "Question 2" (lines 312-367) — the stats-read.mjs commands + the realistic delta; "Open Question 1" (lines 811-814) re castShadow/receiveShadow on <Instances>.
    - docs/table-3d/TP3_BASELINE.md (from 04-01) — the recorded M10 baseline (HERO + ?chips=full) and the must-ship targets + the must-ship-or-revert rule.
    - frontend/src/lab/textures.ts lines 250-413 — chipFaceTexture (S=2048 line 252, returns srgb(c)), chipFaceBump (S=2048 line 311, returns gray(c)), chipEdgeTexture (W=2048 line 369, H=256, returns srgb(c)) — the only S/W constants to change to 512.
  </read_first>
  <action>
    Add Instances, Instance to the drei import (lines 17-23). Define InstancedChipStack({kit, denom, count,
    position}) that consumes chipStackLayout(count, position) from chipStack.ts and renders, per denomination,
    two drei <Instances> nodes: a body Instances with geometry={kit.body} material={m.body} limit={count+4}
    castShadow receiveShadow, whose <Instance> children take position={layout[i].bodyPos} and
    rotation={[0, layout[i].rotY, 0]} (the jitter), and a TOP-face Instances with geometry={kit.face}
    material={m.face} limit={count+4} castShadow, whose <Instance> children take position={layout[i].facePos}
    and rotation={[-Math.PI/2, 0, layout[i].rotY]} (the plane laid flat on top). Pass geometry+material as
    PROPS (Pitfall #1), set limit={count+4} (Pitfall #2), use Euler-tuple rotations (Pitfall #4), and DROP the
    bottom face entirely. Replace every <ChipStack> usage at BOTH the demoted pot and ?chips=full (lines
    819-839) with <InstancedChipStack>; in ?chips=full either fold the 2 loose <Chip> into their denomination's
    instance list or leave them as 2 individual meshes (acceptable for the stress branch, Pitfall #7). Keep the
    original ChipStack/Chip available behind a ?chips= value for A/B (Claude's discretion on the exact flag
    names — e.g. default = instanced, ?chips=legacy = the old per-chip path; the existing ?chips=full /
    ?chips=off stay) so the instanced-vs-non-instanced look is apples-to-apples comparable, and document the
    chosen flag map in the SUMMARY. Then right-size the chip textures in textures.ts: S = 2048 -> 512 in
    chipFaceTexture (line 252) and chipFaceBump (line 311), and W = 2048 -> 512 in chipEdgeTexture (line 369);
    keep H = 256, the srgb()/gray() returns, anisotropy 8, and every drawing coordinate as fractions of S/W so
    the art is identical at the smaller size. Do NOT change any clearcoat/sheen/roughness/chroma — materiality
    is plan 04-03. Then run the M10 + strict-parity GATE: with the dev server up, run
    `node tools/table-3d/stats-read.mjs http://localhost:<port> hero` and the same with the extra arg
    "&chips=full"; capture MACRO (?cam=macro) of the instanced chips and compare against
    docs/table-3d/anchors/tp3-base/macro.png. SHIP only if BOTH hold: net-positive draw count (HERO < 150,
    ?chips=full < 220, demoted-pot chip-draw component <= ~10) AND strict MACRO visual parity (chip look matches
    tp3-base/macro.png — any change = regression). If the draw count is NOT net-positive OR parity breaks ->
    REVERT the instancing (restore ChipStack as default + revert the InstancedChipStack) per the
    must-ship-or-revert rule, and record the revert. Record the measured numbers + the parity verdict in the
    SUMMARY and append to docs/table-3d/TP3_BASELINE.md.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node tools/table-3d/grep-check-tp3-02.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lab/TableLab.tsx` imports Instances + Instance from @react-three/drei and renders InstancedChipStack with geometry/material passed as PROPS on <Instances>; Grep for `Instances` in TableLab.tsx returns >= 1 match outside the import line.
    - The bottom face is dropped: Grep (excluding comment lines) for the rotation literal `[Math.PI / 2, 0, 0]` (the bottom-face mesh) in TableLab.tsx returns 0 matches; the TOP-face rotation `[-Math.PI / 2, 0,` is still present.
    - Chip textures are right-sized: in textures.ts, chipFaceTexture and chipFaceBump declare `const S = 512` and chipEdgeTexture declares `const W = 512`; Grep for `const S = 2048` and `const W = 2048` inside the three chip-texture functions returns 0 (other non-chip textures may retain 2048).
    - `cd frontend && npx vitest run src/lab/` green (chipStack seed-parity test still passes — proves the jitter seeds were not altered) and `npx tsc --noEmit` reports no error in frontend/src/lab/.
    - The M10 + strict-parity verdict is recorded (measured HERO + ?chips=full draw counts vs the 04-01 baseline; MACRO parity vs tp3-base/macro.png): SHIP if net-positive AND parity holds (HERO < 150, ?chips=full < 220, demoted chip draws <= ~10), otherwise the instancing is REVERTED and the revert is recorded — never a perf-neutral/negative or look-changing ship.
  </acceptance_criteria>
  <done>ChipStack is converted to drei InstancedChipStack at both pots with the bottom face dropped and chip textures at 512, behind a ?chips= A/B flag; the M10 must-ship gate is measured (net-positive draws) AND strict MACRO parity confirmed vs tp3-base — shipped, or the instancing is reverted per the must-ship-or-revert rule. Lab Vitest + tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Author the grep-check helper used by Task 2's structural verify</name>
  <files>tools/table-3d/grep-check-tp3-02.cjs</files>
  <read_first>
    - tools/table-3d/stats-read.mjs lines 1-20 — the project's Node tooling convention (plain Node, no extra deps; reads files / runs assertions and exits non-zero on failure). The helper follows the same exit-code discipline.
    - This plan's Task 2 <acceptance_criteria> — the three structural assertions the helper must enforce (Instances wired; bottom-face rotation [Math.PI/2,0,0] absent outside comments; chip-texture S/W = 512 not 2048).
    - frontend/src/lab/TableLab.tsx + frontend/src/lab/textures.ts — the two files the helper reads (relative to repo root).
  </read_first>
  <action>
    Create `tools/table-3d/grep-check-tp3-02.cjs`, a plain CommonJS Node script (no new deps) that reads
    frontend/src/lab/TableLab.tsx and frontend/src/lab/textures.ts from the repo root and enforces the Task 2
    structural invariants, exiting non-zero with a clear message on any failure: (1) TableLab.tsx contains the
    token `Instances` (drei instancing wired); (2) after stripping lines whose trimmed start is `//`, TableLab.tsx
    does NOT contain the bottom-face rotation literal `[Math.PI / 2, 0, 0]` (tolerate flexible interior spacing
    via a regex that allows optional spaces around the `/`), while the TOP-face `[-Math.PI / 2, 0,` literal IS
    still present; (3) textures.ts, within the chipFaceTexture / chipFaceBump / chipEdgeTexture function bodies,
    declares the right-sized constants (`const S = 512` appears for the two face/bump functions and `const W = 512`
    for the edge function) and `const S = 2048` / `const W = 2048` no longer appear inside those three chip
    functions (a substring scan between each `export function chip...` header and the next `export function` is
    sufficient). On all checks passing, print a one-line OK. This script is the `node tools/table-3d/grep-check-tp3-02.cjs`
    command referenced by Task 2's <verify>; it must be created before or alongside Task 2 so that verify resolves.
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp3-02.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/table-3d/grep-check-tp3-02.cjs` exists and runs; it exits 0 only when all three Task 2 invariants hold (Instances wired; bottom-face rotation absent outside comments; chip S/W = 512), and exits non-zero with a descriptive message otherwise.
    - The script uses only the Node standard library (no new package); `git diff --name-only` shows the new file under tools/table-3d/ and no package.json change.
    - The script reads the two source files via repo-root-relative paths (run from the repo root, matching the other tools/table-3d scripts).
  </acceptance_criteria>
  <done>The structural grep-check helper exists, enforces the three Task 2 invariants via exit code with no new deps, and is the command Task 2's verify invokes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in the prod build. TP3 touches ONLY `frontend/src/lab/`
(+ a new tools/table-3d/ check helper here); `/table-lab.html` is never in the production Vite build (SSOT
§5.8). No auth / network / data / user-input surface. The boundaries that matter are visual-parity integrity
and the perf-ceiling guardrail.

| Boundary | Description |
|----------|-------------|
| tp3-base look -> instanced look | Instancing must be a visual-parity transform; the MACRO strict-parity check defends the chip look (any change = regression -> revert) |
| M10 baseline -> post-instancing draw count | The perf guardrail (must-ship-or-revert): instancing ships only if net-positive on draws |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02a | Tampering | the hand-stacked jitter seeds (visual parity) | mitigate | chipStack.ts reproduces the exact seeds (2.3/1.7/0.012); a Vitest asserts byte-equivalence vs the pre-instancing positions |
| T-04-02b | Tampering | the chip look at MACRO (strict parity) | mitigate | Task 2 captures MACRO of the instanced chips and compares to tp3-base/macro.png; any change = regression -> revert |
| T-04-02c | Denial of Service | the M10 perf ceiling | mitigate | Draw count measured via stats-read.mjs; ship only if net-positive (< 150 HERO, < 220 chips=full), else revert |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (drei 9.114.0 already present; the grep-check helper uses only the Node stdlib; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lab/` green — including chipStack.test.ts seed-parity (proves the jitter look is byte-equivalent).
- `cd frontend && npx tsc --noEmit` reports no error under frontend/src/lab/.
- `node tools/table-3d/grep-check-tp3-02.cjs` exits 0 (Instances wired; bottom-face rotation absent outside comments; chip S/W = 512).
- The M10 read (stats-read.mjs hero + &chips=full) is net-positive vs the 04-01 baseline AND MACRO parity vs tp3-base/macro.png holds — or the instancing is reverted (recorded).
- `git status`: only frontend/src/lab/ chip files + the new tools/table-3d/ helper + docs updates; lab still absent from the vite build input.
</verification>

<success_criteria>
- ChipStack -> drei InstancedChipStack per denomination at both the demoted pot and ?chips=full; bottom face dropped; chip textures 512; cream phase-alignment broken (0.37 seed, deterministic).
- Behind a ?chips= flag for A/B; the hand-stacked jitter seeds preserved (unit-tested byte-equivalence).
- M10 must-ship-or-revert SATISFIED: net-positive draws (< 150 HERO, < 220 chips=full, demoted <= ~10) AND strict MACRO parity vs tp3-base — shipped; otherwise reverted and recorded.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/04-tp3-fichas-materiality-perf-accent-instancing/04-02-SUMMARY.md` when done, recording the ?chips= flag map, the measured M10 numbers (HERO + ?chips=full, before/after), the strict MACRO parity verdict, and the ship-or-revert decision; append the M10 result to docs/table-3d/TP3_BASELINE.md.
</output>
