---
phase: 08-tp7-camaras-lock-money-shots
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP7 — optional non-canonical restrained cinematic flythrough behind ?fly flag; FREEZES to canonical hero still on ?spin=off (M9-safe); X-axis only; amplitude <= 0.25wu; frequency <= 0.3 rad/s; <= 40 new lines; skip if scope exceeded or M9 fails"
  - "SSOT §TP7 — flythrough does NOT touch the 3 frozen presets; does NOT add autoRotate={true}; does NOT add a second makeDefault PerspectiveCamera; default (no ?fly) remains the static preset"
  - "SSOT §TP7 — M9 PASS on ?fly&spin=off (byte-identical captures with flythrough code present); grep-check-tp7-08.cjs still exits 0 after flythrough added"

must_haves:
  truths:
    - "frontend/src/lab/TableLab.tsx has a ?fly flythrough branch (isFly + useFrame) of <= 40 new lines"
    - "When ?fly is absent (default): the scene is fully static — the hero preset is served unchanged"
    - "When ?fly is present and spin=off (capture path): useFrame callback is a no-op (isFrozen guard); M9 PASS"
    - "When ?fly is present and spin=off is absent (lab preview): camera X position oscillates with amplitude <= 0.25wu, frequency <= 0.3 rad/s, starting and ending at cam.pos[0]"
    - "autoRotate={false} is still hardcoded in OrbitControls — flythrough uses useFrame on camera position, NOT autoRotate"
    - "No second PerspectiveCamera with makeDefault introduced"
    - "grep-check-tp7-08.cjs exits 0 after flythrough code is added"
    - "If scope exceeds 40 lines OR M9 fails on ?fly&spin=off: flythrough is cut; TableLab.tsx is reverted; skip is recorded in SUMMARY; plan still PASSES (flythrough is non-blocking)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "TableLab with optional ?fly flythrough branch behind isFly guard; freeze-on-spin=off; X-axis arc only; autoRotate={false} unchanged"
      contains: "isFly"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx ?fly useFrame"
      to: "qp('spin') === 'off' freeze guard"
      via: "const isFrozen read at mount from URL; useFrame early-returns when isFrozen=true; M9-safe"
      pattern: "isFrozen"
---

<objective>
Add the optional non-canonical cinematic flythrough behind a ?fly URL flag to TableLab.tsx.
The flythrough is a restrained X-axis lateral arc that FREEZES to the canonical hero still
when the capture flag (spin=off) is present, making it completely invisible to the metric
pipeline. If the scope exceeds 40 new lines or M9 fails on the frozen path, cut it and
record the skip — the flythrough is non-blocking.

Purpose: The SSOT §TP7 explicitly mentions this as an optional "non-canonical restrained
cinematic flythrough that freezes to the canonical still under the capture flag." The CONTEXT
gives this to Claude's discretion. Including it adds a "living scene" quality without any
canonical capture impact. It is a thin, isolated, opt-in addition that does not touch the 3
frozen presets.

Output: frontend/src/lab/TableLab.tsx with <= 40 lines of new flythrough code behind ?fly;
OR a recorded skip if scope/M9 gates were not met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-CONTEXT.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-RESEARCH.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ?fly flythrough to TableLab.tsx (X-axis arc, <= 40 lines, freeze on spin=off) — or record skip</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx — read the full file structure around:
        (a) The qp() helper (URL param reader) — where it is defined and how it is used
            elsewhere (e.g., qp("spin"), qp("fx"), qp("cam")) so the flythrough reuses the
            same pattern.
        (b) The Scene component or equivalent — where useFrame is currently imported/used, if
            at all. The flythrough must be added at the same level as the existing R3F scene.
        (c) Where PerspectiveCamera is rendered — the flythrough animates this camera's
            position via a ref. Confirm the camera JSX form (<PerspectiveCamera makeDefault ...>
            or equivalent) so the ref can be attached.
        (d) The OrbitControls block (lines 1106-1119) — confirm autoRotate={false} is still
            unconditional. DO NOT change it.
        (e) Current import list — confirm useFrame, useRef are already imported from @react-three/fiber
            or need to be added. If useRef is not imported, add it alongside existing imports.
    - 08-01-SUMMARY.md — confirm M9 PASS was recorded in plan 08-01. If M9 FAILED in 08-01:
        SKIP this plan entirely. Record in SUMMARY: "08-02 SKIPPED — M9 FAILED in 08-01;
        flythrough would risk M9 further." Plan is still marked complete (non-blocking).
    - 08-RESEARCH.md "Optional Flythrough — Recommendation" and "Pattern 3: Optional Flythrough
      via useFrame + spin=off freeze" — the exact implementation constraints and the Flythrough
      component skeleton. These are the authoritative design specs.
  </read_first>
  <action>
    PRECONDITION CHECK: Read 08-01-SUMMARY.md. If M9 was FAILED in 08-01, record the skip
    and exit this plan immediately (no edits to TableLab.tsx). Record:
    "08-02 SKIPPED — M9 FAILED in 08-01; flythrough deferred; plan 08-02 complete (non-blocking)."

    If M9 passed, proceed with the flythrough implementation.

    SCOPE BUDGET CHECK: Before writing code, count the new lines required. If the implementation
    will exceed 40 new lines of TypeScript in TableLab.tsx (not counting blank lines or comments),
    record the skip and exit. Do NOT write partial implementations that exceed the budget.

    IMPLEMENTATION (if budget allows and M9 passed):

    STEP 1 — Add freeze + fly flags at Scene component level (2 lines):
      const isFly = qp("fly") !== null;
      const isFrozen = qp("spin") === "off";

    These read from URL at mount — static for the page lifetime. No reactive state. The harness
    always appends spin=off, so isFrozen will be true in all capture scenarios.

    STEP 2 — Add a camera ref (1 line, if useRef not already used for camera):
      const flyCamRef = useRef<THREE.PerspectiveCamera>(null);

    Attach to the existing PerspectiveCamera JSX: ref={flyCamRef}
    Do NOT add makeDefault to this ref attachment — the camera already has makeDefault. Do NOT
    add a second PerspectiveCamera. This ref attaches to the existing camera.

    STEP 3 — Add the useFrame loop (target: <= 10 lines):
      useFrame(({ clock }) => {
        if (!isFly || isFrozen || !flyCamRef.current) return;
        const t = clock.getElapsedTime();
        const swing = Math.sin(t * 0.22) * 0.20;   // X-axis, ~28s cycle, amplitude 0.20wu
        flyCamRef.current.position.x = cam.pos[0] + swing;
        // Y and Z stay at cam.pos — pure lateral arc
      });

    Constraint enforcement:
    - Amplitude: 0.20wu < 0.25wu HARD limit. Do NOT increase.
    - Frequency: 0.22 rad/s < 0.30 rad/s HARD limit. This gives ~28s full cycle — imperceptibly slow.
    - Axes: X only. Do NOT add Y or Z movement.
    - Freeze guard: the isFrozen check is the FIRST condition. If isFrozen=true, useFrame returns
      immediately with no camera mutation. This is what makes M9 PASS with ?fly&spin=off.
    - The arc centers on cam.pos[0] (the hero preset X position) so the camera always
      starts and ends at the canonical hero framing. The swing is symmetric around cam.pos[0].

    STEP 4 — SCOPE COUNT: Count the net new lines added to TableLab.tsx (const isFly, const
    isFrozen, const flyCamRef, ref= attachment change, useFrame block). Must be <= 40 total.
    If over 40: revert all changes; record skip.

    STEP 5 — Verify M9 PASS on ?fly&spin=off (freeze verification):
    With the dev server running, run M9 with the flythrough URL:
      node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp7-gate
    But pass the URL with ?fly&spin=off to the harness. Confirm the invocation method from the
    tool (some tools accept a URL override; if not, test by capturing two stills manually with
    ?cam=hero&fly&fx and checking their md5 sums match).
    Expected: PASS (byte-identical) — because isFrozen=true when spin=off, the useFrame returns
    immediately and the camera never moves.
    If M9 FAILS on ?fly&spin=off: revert all changes to TableLab.tsx; record:
    "08-02 SKIPPED — flythrough added but M9 FAILED on ?fly&spin=off; reverted; non-blocking."

    STEP 6 — Run grep-check-tp7-08.cjs after flythrough is added:
      node tools/table-3d/grep-check-tp7-08.cjs
    Must exit 0. CHECK 9 (autoRotate={false}) and CHECK 11 (no second makeDefault) are the
    critical ones — the flythrough must not break either.
    If grep-check FAILS: revert all changes; record the skip.

    STEP 7 — Commit (only if all gates passed):
    Commit message: "feat(08-02): add ?fly restrained flythrough (X-axis arc, freeze on spin=off, non-canonical)"

    SKIP RECORD FORMAT (if any gate fails):
    Write in SUMMARY: "08-02 SKIPPED — [reason: scope exceeded / M9 precondition failed /
    M9 FAIL on frozen path / grep-check FAIL]. TableLab.tsx unchanged. Plan 08-02 complete
    (non-blocking — flythrough is optional per SSOT §TP7). Operator gate (08-03) proceeds
    without flythrough."
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp7-08.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp6-07.cjs</automated>
    <automated>node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp7-gate</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
  </verify>
  <done>
    Either: (A) flythrough shipped — TableLab.tsx has <= 40 new lines of ?fly code; M9 PASS
    on ?fly&spin=off; grep-check-tp7-08.cjs exits 0; tsc clean; vitest green; committed.
    Or: (B) flythrough skipped — TableLab.tsx unchanged; skip reason recorded in SUMMARY;
    grep-check-tp7-08.cjs still exits 0; plan marked complete (non-blocking). LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only. No auth, no prod surface.

| Boundary | Description |
|----------|-------------|
| ?fly useFrame -> isFrozen freeze guard | The capture pipeline depends on isFrozen=true stopping all camera mutation; any leak causes M9 FAIL |
| flythrough X-axis arc -> OrbitControls azimuth clamp | A swing of 0.20wu at hero Z~8.2 ≈ arctan(0.20/8.2) ≈ 1.4 deg -- well inside the ±0.5 rad (~28.6 deg) clamp; no jitter expected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02a | Tampering | isFrozen check removed or conditioned — useFrame runs during capture | mitigate | isFrozen is the FIRST condition in useFrame; M9 gate re-run on ?fly&spin=off catches any leak; grep-check CHECK 9 ensures autoRotate unchanged |
| T-08-02b | Tampering | A second PerspectiveCamera makeDefault introduced for flythrough | mitigate | grep-check CHECK 11 asserts <= 1 makeDefault token; FAIL will block commit; read_first confirms existing camera JSX form |
| T-08-02c | Tampering | Flythrough amplitude grows beyond 0.25wu (scope creep into spectacle) | mitigate | Hard-coded 0.20wu constant; any operator visual test showing visible camera movement = halve the amplitude or skip |
| T-08-02d | Tampering | autoRotate={false} changed to support flythrough | mitigate | grep-check CHECK 9 asserts literal autoRotate={false}; flythrough uses useFrame not autoRotate; read_first explicitly reads OrbitControls block |
| T-08-02e | Repudiation | Flythrough skip not recorded (gate proceeds without knowing flythrough disposition) | mitigate | Skip record format is mandatory in SUMMARY; operator gate (08-03) reads SUMMARY before presenting shots |
| T-08-SC | Tampering | npm installs | accept | No new packages; useFrame and useRef already in @react-three/fiber which is installed |
</threat_model>

<verification>
- grep-check-tp7-08.cjs exits 0 (11/11 checks) — flythrough code must not break CHECK 9 or CHECK 11.
- grep-check-tp6-07.cjs exits 0 (backward compatibility).
- M9 PASS on ?fly&spin=off (if flythrough shipped) — freeze guard works.
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- Flythrough shipped OR skip reason recorded in SUMMARY (non-blocking either way).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Flythrough shipped (all gates green, <= 40 lines) OR skipped with recorded reason.
- grep-check-tp7-08.cjs exits 0 either way. tsc clean. vitest green.
- TableLab.tsx unchanged if skipped. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/08-tp7-camaras-lock-money-shots/08-02-SUMMARY.md` when done.
Record: flythrough disposition (SHIPPED or SKIPPED + reason), exact line count of new code
(if shipped), M9 result on ?fly&spin=off (if shipped), grep-check-tp7-08 11-check verdict,
tsc status, vitest status. The operator gate (08-03) reads this to know whether the flythrough
is available as an optional extra in the lab preview.
</output>
