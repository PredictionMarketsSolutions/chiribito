---
phase: 08-tp7-camaras-lock-money-shots
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - docs/table-3d/TP7_OPERATOR_GATE.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
autonomous: false
requirements:
  - "SSOT §TP7 — perceptual gate: operator confirms the canonical hero (+ the 3 shots) on the finished table — 'is THIS the money shot of the finished table?'"
  - "SSOT §TP7 — NOT auto-approvable; autonomous:false; stop-on-ambiguous = KEEP TP0 presets (no edit, no new framing)"
  - "SSOT §TP7 — gate record: docs/table-3d/TP7_OPERATOR_GATE.md records verdict + M1/M2/M9 confirmation + preset-locked status + flythrough disposition"
  - "SSOT §TP7 — SCORECARD_TABLE_3D.md cameras row updated with TP7 result"

must_haves:
  truths:
    - "The operator has reviewed the HERO, POV-card, and MACRO anchor stills from docs/table-3d/anchors/tp7-gate/"
    - "The HARD gates from 08-01-SUMMARY are restated: M1 >= 22px PASS, M2 >= 2.0x PASS, M9 byte-identical PASS, grep-check-tp7-08 11/11 PASS"
    - "The stop-on-ambiguous rule is presented and enforced: any ambiguous read -> KEEP TP0 (no edit, no new framing)"
    - "On approval: docs/table-3d/TP7_OPERATOR_GATE.md records verdict + M1/M2/M9 values + 'presets LOCKED' + flythrough disposition"
    - "On approval: SCORECARD_TABLE_3D.md cameras row updated (must not regress from TP6 column)"
  artifacts:
    - path: "docs/table-3d/TP7_OPERATOR_GATE.md"
      provides: "Recorded operator verdict, preset-LOCKED declaration, M1/M2/M9 values from 08-01-SUMMARY, flythrough disposition, stop-on-ambiguous outcome if triggered"
      contains: "Verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP7 score update for cameras/presets row"
      contains: "TP7"
  key_links:
    - from: "docs/table-3d/TP7_OPERATOR_GATE.md"
      to: "docs/table-3d/anchors/tp7-gate/{hero,card,macro}.png"
      via: "The operator reviews these three stills as the basis for the 'money shot of the finished table?' judgment"
      pattern: "tp7-gate"
    - from: "docs/table-3d/TP7_OPERATOR_GATE.md"
      to: "docs/ROADMAP_TABLE_3D_PERFECTION.md §TP7"
      via: "The gate record confirms the SSOT acceptance criterion: presets LOCKED, M1/M2 PASS, framing is final for TP9"
      pattern: "TP7_OPERATOR_GATE"
---

<objective>
The TP7 operator perceptual gate — the human seam for the camera confirmation phase. The operator
reviews the three frozen money shots (HERO fov32 / POV-card fov40 / MACRO fov26) captured on
the finished TP1-TP6 table and answers one question: "Is THIS the money shot of the finished table?"

Purpose: Automated metrics (M1/M2/M9) and grep-check-tp7-08.cjs are necessary-not-sufficient.
Only the operator can confirm the protagonist composition reads correctly on the finished table —
hole cards dominant lower-third, board mid-frame, rail framing, chips off-center accent. This
gate cannot be auto-approved (autonomous:false). Stop-on-ambiguous ALWAYS means KEEP TP0 — no
new framing is invented at this stage.

Output: docs/table-3d/TP7_OPERATOR_GATE.md (verdict + preset-LOCKED declaration + M1/M2/M9
confirmation + flythrough disposition); SCORECARD_TABLE_3D.md cameras row updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-CONTEXT.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-01-SUMMARY.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-02-SUMMARY.md
@docs/table-3d/TP6_OPERATOR_GATE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator gate — "Is THIS the money shot of the finished table?" at HERO + POV-card + MACRO</name>
  <files>docs/table-3d/TP7_OPERATOR_GATE.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - .planning/phases/08-tp7-camaras-lock-money-shots/08-01-SUMMARY.md — the complete pre-gate
      record: exact M1 px-height, M2 ratio (hero and card), M9 md5 hash, preset-value comparison
      (UNCHANGED or discrepancy), anchor paths, grep-check-tp7-08 11-check verdict. Restate all
      values at the gate presentation.
    - .planning/phases/08-tp7-camaras-lock-money-shots/08-02-SUMMARY.md — flythrough disposition:
      SHIPPED (line count, M9 on ?fly&spin=off) or SKIPPED (reason). This is a non-canonical opt-in
      detail to note in the gate record; it does not affect the gate verdict.
    - docs/table-3d/TP6_OPERATOR_GATE.md — gate record template shape (mirror this structure for
      TP7_OPERATOR_GATE.md); the TP7 gate is structurally similar but simpler (confirmation only,
      no A/B like TP6).
    - docs/table-3d/SCORECARD_TABLE_3D.md — current cameras/presets row to update if approved.
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP7 — the SSOT gate question and stop-on-ambiguous rule.
  </read_first>
  <action>
    Present the gate to the operator using the anchor captures from docs/table-3d/anchors/tp7-gate/
    (live lab preferred; static captures as fallback). Do NOT auto-approve — this is a perceptual
    gate (autonomous:false; auto_advance does not apply).

    First restate the HARD gates from 08-01-SUMMARY:
    - M1 hole-cards: [px-height]px at ?cam=card&fx — PASS (>= 22px)
    - M2 cards-vs-chips: [ratio]x at hero and card shots — PASS (>= 2.0x)
    - M9 determinism: [md5 hash] — PASS (byte-identical)
    - grep-check-tp7-08.cjs: exits 0 (11/11 invariants)
    - grep-check-tp6-07.cjs: exits 0 (8/8 backward compatibility)
    - TP0 preset values: UNCHANGED — card fov:40, hero fov:32, macro fov:26 (or flag discrepancy
      from 08-01 if any was found)

    Then restate the flythrough disposition from 08-02-SUMMARY:
    - Flythrough (?fly): SHIPPED ([line count] new lines, M9 PASS on ?fly&spin=off) — non-canonical,
      opt-in, does not affect any canonical preset
    - OR: Flythrough: SKIPPED ([reason]) — TableLab.tsx unchanged from 08-01

    Then present the three shots for operator review.

    On operator verdict, record into docs/table-3d/TP7_OPERATOR_GATE.md (mirror TP6_OPERATOR_GATE.md
    structure, adapted for TP7):
    - The verdict string.
    - The "presets LOCKED" declaration (or correction record if any minimal correction was applied).
    - M1/M2/M9 measured values from 08-01-SUMMARY.
    - The stop-on-ambiguous disposition (applied or not).
    - The flythrough disposition (SHIPPED or SKIPPED + reason).
    - The note: "This gate record is the framing basis for TP9 (final scorecard sign-off)."

    STOP-ON-AMBIGUOUS RULE:
    If the operator's answer is anything other than a clear YES on any preset:
    STOP. The rule is KEEP TP0. Record "stop-on-ambiguous: KEEP TP0 presets unchanged."
    Do NOT:
    - Invent a new fov or position.
    - Re-evaluate a "better" candidate angle.
    - Propose tweaks to make the operator more comfortable.
    The only valid action on ambiguous is to KEEP the TP0 preset and record the ambiguous
    read as a note (e.g., "hero reads slightly off-center — note for TP9, preset unchanged").

    MINIMAL CORRECTION GATE (applies only if a preset CLEARLY fails, not ambiguous):
    If the operator identifies a clear failure on one preset (e.g., "the macro is now behind
    the rail and the chips are cut off" — a structural framing break caused by a TP1-TP6
    geometry change), then and only then:
    - Apply a MINIMAL correction: fov ± 2 deg maximum, position ± 0.5wu maximum.
    - Re-run M1 (if POV/hero corrected) and M9 (always).
    - Re-run grep-check-tp7-08.cjs (the fov change will fail CHECK 10 — update the check with
      the corrected value and a recorded justification comment in the CJS file).
    - Re-present the corrected shot.
    - Record the correction in TP7_OPERATOR_GATE.md: which preset, original value, corrected
      value, reason, M1/M9 re-confirm.
    The bar for a clear failure is high: the preset must be compositionally broken (not "slightly
    less flattering"). If uncertain: KEEP TP0.

    On UNAMBIGUOUS APPROVAL (all three presets confirmed as money shots of the finished table):
    - Record: "HERO / POV-card / MACRO: ALL THREE PRESETS CONFIRMED LOCKED on the finished TP1-TP6 table."
    - Update SCORECARD_TABLE_3D.md cameras row with the post-TP7 result. Must not regress from
      the TP6 column. Note if the final AAA(5) sign-off awaits TP9.

    Do NOT change any geometry, lights, or postprocessing. LOCAL only — no push/deploy/merge.
  </action>
  <what-built>
    TP7 executed two automated plans before this gate:

    (A) CONFIRMATION CAPTURES + METRICS — 08-01:
    Three GPU-faithful anchor captures on the finished TP1-TP6 table with ?fx and spin=off:
      docs/table-3d/anchors/tp7-gate/card.png   — POV preset (?cam=card&fx)
      docs/table-3d/anchors/tp7-gate/hero.png   — HERO preset (?cam=hero&fx)
      docs/table-3d/anchors/tp7-gate/macro.png  — MACRO preset (?cam=macro&fx)
    M9 double-capture on hero: m9-hero-a.png + m9-hero-b.png (byte-identical).
    M1 re-confirmed: [px-height]px at card.png — PASS.
    M2 re-confirmed: [ratio]x at hero.png — PASS.
    TP0 preset values: CONFIRMED UNCHANGED — card fov:40, hero fov:32, macro fov:26.
    tools/table-3d/grep-check-tp7-08.cjs: authored (11 checks, exits 0).
    tools/table-3d/grep-check-tp6-07.cjs: still exits 0 (backward compatibility).

    (B) OPTIONAL FLYTHROUGH — 08-02:
    [SHIPPED: TableLab.tsx has isFly/isFrozen/useFrame block, <= 40 new lines, M9 PASS on
    ?fly&spin=off, grep-check exits 0.]
    OR [SKIPPED: reason from 08-02-SUMMARY; TableLab.tsx unchanged.]

    HARD GATES (confirmed in 08-01-SUMMARY):
    grep-check-tp7-08.cjs: exits 0 (11/11 checks).
    grep-check-tp6-07.cjs: exits 0 (8/8 checks).
    M1: [px-height]px — PASS. M2: [ratio]x — PASS. M9: byte-identical — PASS.
    TP0 presets: UNCHANGED.

    The 3 frozen TP0 money shots (exact values confirmed from TableLab.tsx source):
      HERO ¾    ?cam=hero  pos [1.2, 5.0, 8.2]  target [0, 0.5, 0]          fov 32  (~72mm-equiv)
      POV-card  ?cam=card  pos [0, 4.7, 10.6]   target [0, 0.25, 1.2]       fov 40  (~57mm-equiv)
      MACRO     ?cam=macro pos [-1.7, 1.7, 2.4]  target [-1.55, 0.05, 1.05] fov 26  (~88mm-equiv)
    All three are distortion-free longer-lens presets (~50-85mm-equiv product range).
    autoRotate={false} hardcoded in OrbitControls.
    OrbitControls clamp: polar [0.62, 1.12] rad, azimuth ±0.5 rad (preset-relative), distance [5, 13].
  </what-built>
  <how-to-verify>
    Judge the FRAMING + PROTAGONIST COMPOSITION at the three frozen money shots.

    1. Bring the dev server up if needed (cd frontend && npm run dev -- --port 5181) and open
       the lab live, OR review the still captures in docs/table-3d/anchors/tp7-gate/.

    2. HERO shot (fov32 — the definitive money shot; the canonical ¾ view):
       Open http://localhost:5181/table-lab.html?cam=hero&fx
       Compare against docs/table-3d/anchors/tp7-gate/hero.png.

       Gate questions for HERO:
       (a) "Does the protagonist composition hold on the FINISHED table — hole cards (Perla: Sota de
           Oros + 7 de Oros) dominant in the lower third, board mid-frame, rail framing the scene,
           chips off-center accent? Is this the poker-table money shot?"
       (b) "Is the HERO ¾ angle flattering — the table reads as a premium physical object, not a
           map or a layout diagram?"
       (c) "Does the framing read as longer-lens / natural — no visible wide-angle distortion? The
           felt, rail, and cards all appear in proportion?"
       (d) "On the finished TP1-TP6 table (real Fournier cards, instanced chips, slim rail, warm
           light, SoftShadows, AO, whisper DOF, vignette) — do the TP0 frozen presets still work
           as money shots, or has a geometry/material change in TP1-TP6 broken the framing?"

    3. POV-card shot (?cam=card — the close-up card legibility shot; M1 primary):
       Open http://localhost:5181/table-lab.html?cam=card&fx
       Compare against docs/table-3d/anchors/tp7-gate/card.png.

       Gate questions for POV-card:
       (a) "Are the hole cards (Sota de Oros + 7 de Oros) large in the foreground and fully legible
           — rank glyphs and pip detail readable at a glance?"
       (b) "Does the board read beyond the hole cards — a sense of depth and game state?"
       (c) "Is this the 'player's eye view' — immersive, close, grounded in the game action?"

    4. MACRO shot (?cam=macro — close chip/card detail; deep telephoto):
       Open http://localhost:5181/table-lab.html?cam=macro&fx
       Compare against docs/table-3d/anchors/tp7-gate/macro.png.

       Gate questions for MACRO:
       (a) "Does the MACRO read as a close material shot — chip stack texture, card edge, felt
           crevice AO, brass rail detail? Does the long lens (fov:26, ~88mm-equiv) flatten the
           scene correctly?"
       (b) "Does the framing include the right content — chips accent, card edge, felt? Not cut off
           awkwardly by the TP4 rail geometry change?"

    5. HARD gates (from 08-01-SUMMARY — these are non-negotiable):
       M1: [px-height]px at card.png — PASS (>= 22px). Re-run live if requested:
         node tools/table-3d/m1-m2-m12.mjs
       M2: [ratio]x at hero.png — PASS (>= 2.0x).
       M9: byte-identical — PASS. node tools/table-3d/grep-check-tp7-08.cjs → must exit 0.
       If any HARD gate FAILS live: record failure; do NOT approve; resolve before re-presenting.

    6. Optional flythrough preview (if 08-02 SHIPPED):
       Open http://localhost:5181/table-lab.html?cam=hero&fly
       Observe the X-axis lateral arc — it should be imperceptibly slow (~28s full cycle, amplitude
       <= 0.20wu). The camera barely moves; it feels "alive" not "animated."
       This is NON-CANONICAL and does not affect the gate verdict. It is an opt-in lab extra.
       If the motion reads as "too animated" or "spectacle": record in gate doc as
       "flythrough: shipped but operator prefers static — operator may use ?cam=hero without ?fly."
       The flythrough disposition does not block or change the preset-lock verdict.

    7. STOP-ON-AMBIGUOUS RULE (read carefully):
       Any answer that is not a clear "YES — this is the money shot":
       → STOP. Record "stop-on-ambiguous: KEEP TP0 presets." Do NOT invent new framing.
       The only action allowed on ambiguous is to KEEP the TP0 set unchanged.
       A "SLIGHT PREFERENCE" for a different angle is NOT a clear failure — the default is KEEP.
       A "CLEARLY BROKEN FRAMING" (chips cut off, cards behind rail, protagonist disappears)
       is a clear failure and allows a minimal correction per the correction gate above.

    DISPOSITION (record in docs/table-3d/TP7_OPERATOR_GATE.md):
    - UNAMBIGUOUS YES (all three presets confirmed as money shots of the finished table;
      HARD gates green): PRESETS LOCKED. Update SCORECARD_TABLE_3D.md cameras row.
    - STOP-ON-AMBIGUOUS on one or more presets: KEEP TP0 presets unchanged. Record the
      ambiguous read as a note for TP9. No preset edit.
    - CLEAR FAILURE on one preset (structural break): apply minimal correction (fov ±2°,
      pos ±0.5wu); re-run M1/M9; re-present that preset only. Record correction in gate doc.
    - CLEAR FAILURE on all three: extremely unlikely — but if so: flag to TP9 for a dedicated
      camera revisit. KEEP TP0 as interim. Do NOT re-baseline mid-program (red-team #6).
  </how-to-verify>
  <resume-signal>
    Operator types:
    - "approved" — all three presets confirmed LOCKED as money shots of the finished table.
      Execute: write TP7_OPERATOR_GATE.md (verdict + LOCKED + M1/M2/M9 values + flythrough
      disposition + note for TP9); update SCORECARD_TABLE_3D.md cameras row; commit both files.
      Commit message: "docs(08-03): TP7 operator gate APPROVED — presets LOCKED on finished table"
    - "approved — note [observation]" (e.g., "approved — note macro slightly tighter than ideal
      but acceptable; keep TP0") — ship with the note recorded in gate doc; same action as approved.
    - "stop — [preset] is ambiguous" — STOP. Record "stop-on-ambiguous: KEEP TP0 for [preset].
      Note: [observation]." Write TP7_OPERATOR_GATE.md with LOCKED status for the unambiguous
      presets; note the ambiguous one for TP9. Update SCORECARD_TABLE_3D.md.
    - "fix [preset] — [description of clear failure]" — executor applies minimal correction to
      the named preset only (fov ±2°, pos ±0.5wu max); re-runs M1/M9; re-presents that preset.
      Awaits re-approval on the corrected preset.
    Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has reviewed HERO, POV-card, and MACRO on the finished TP1-TP6 table. The gate
    question — "Is THIS the money shot of the finished table?" — has been answered with a verdict.
    HARD gates (M1/M2/M9/grep-check-tp7-08) restated from 08-01-SUMMARY. Stop-on-ambiguous rule
    presented. Verdict + preset-LOCKED declaration (or correction record) + flythrough disposition
    recorded in docs/table-3d/TP7_OPERATOR_GATE.md. SCORECARD_TABLE_3D.md cameras row updated.
    No geometry, lights, or postprocessing changed. LOCAL only — no push/deploy/merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, dev-server-only. The trust boundaries here are the
perceptual judgment seam and the HARD metric gates.

| Boundary | Description |
|----------|-------------|
| automated metrics (M1/M2/M9) -> human perceptual gate | Metrics are necessary-not-sufficient; operator judges protagonist composition on the finished table |
| stop-on-ambiguous rule -> default KEEP TP0 | Any ambiguous answer must default to KEEP; the only escape hatch is a clearly broken preset requiring a minimal correction |
| TP0-frozen presets -> TP9 evaluation | This gate record becomes the framing anchor for TP9 final scorecard sign-off; a correct LOCKED record is critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03a | Tampering | Auto-approve circumventing the operator gate | mitigate | autonomous:false; this checkpoint cannot be auto-approved; auto_advance does not apply |
| T-08-03b | Tampering | HARD gate values not restated (gate has no metric basis) | mitigate | read_first explicitly requires 08-01-SUMMARY with all M1/M2/M9 values; action block restates each before presenting shots |
| T-08-03c | Tampering | Stop-on-ambiguous rule bypassed (minimal correction applied to ambiguous cases) | mitigate | Action block distinguishes: CLEARLY BROKEN = minimal correction allowed; AMBIGUOUS = KEEP TP0 only. The fail bar is structural framing break, not preference |
| T-08-03d | Tampering | Correction exceeds minimal bound (fov > ±2°, pos > ±0.5wu) | mitigate | Correction gate hard limit stated in action; any correction requires grep-check re-run (CHECK 10 catches fov changes) |
| T-08-03e | Repudiation | Gate verdict not written to TP7_OPERATOR_GATE.md | mitigate | Resume-signal action block explicitly writes TP7_OPERATOR_GATE.md on every outcome path (approved / stop / fix); commit anchors the record |
| T-08-03f | Tampering | SCORECARD regresses from TP6 column | mitigate | read_first reads current SCORECARD_TABLE_3D.md; action block states "must not regress from TP6 column" |
| T-08-SC | Tampering | npm installs | accept | No package installs in the operator gate plan |
</threat_model>

<verification>
- Operator has reviewed HERO + POV-card + MACRO at docs/table-3d/anchors/tp7-gate/ (live or static).
- HARD gates from 08-01-SUMMARY restated: M1 PASS (>= 22px), M2 PASS (>= 2.0x), M9 PASS (byte-identical), grep-check-tp7-08 11/11.
- Stop-on-ambiguous rule applied: any ambiguous read -> KEEP TP0 (no new framing invented).
- docs/table-3d/TP7_OPERATOR_GATE.md written: verdict + preset-LOCKED declaration + M1/M2/M9 values + flythrough disposition + TP9 note.
- docs/table-3d/SCORECARD_TABLE_3D.md cameras row updated; must not regress from TP6 column.
- No geometry, lights, postprocessing, or preset values changed (or a minimal correction is recorded + justified).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at HERO + POV-card + MACRO; verdict recorded.
- Unambiguous yes: presets LOCKED; scorecard updated.
- OR: stop-on-ambiguous: KEEP TP0; note recorded for TP9; scorecard updated.
- OR: minimal correction applied to a clearly broken preset; re-confirmed; locked.
- TP7_OPERATOR_GATE.md written. SCORECARD_TABLE_3D.md updated. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/08-tp7-camaras-lock-money-shots/08-03-SUMMARY.md` when done,
and write the verdict to `docs/table-3d/TP7_OPERATOR_GATE.md`. Update the cameras/presets
row in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP7 score (must not regress from TP6
column; note if AAA(5) awaits TP9 final scorecard sign-off). The SUMMARY records the full gate
disposition: verdict, preset-LOCKED status (or correction record), flythrough disposition, HARD
gates as restated, stop-on-ambiguous outcome if triggered, and the TP9 framing note.
</output>
