---
phase: 08-tp7-camaras-lock-money-shots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/table-3d/grep-check-tp7-08.cjs
  - docs/table-3d/anchors/tp7-gate/card.png
  - docs/table-3d/anchors/tp7-gate/hero.png
  - docs/table-3d/anchors/tp7-gate/macro.png
  - docs/table-3d/anchors/tp7-gate/m9-hero-a.png
  - docs/table-3d/anchors/tp7-gate/m9-hero-b.png
autonomous: true
requirements:
  - "SSOT §TP7 — re-capture the 3 frozen presets (?cam=card/hero/macro with ?fx) on the finished TP1-TP6 table; commit anchors to docs/table-3d/anchors/tp7-gate/"
  - "SSOT §TP7 — re-confirm M1 (hole-card rank-glyph >= 22px at ?cam=card&fx) + M2 (cards-vs-chips >= 2.0x at hero/POV) PASS on the finished table"
  - "SSOT §TP7 — M9 autoRotate-off capture determinism PASS (two byte-identical hero captures)"
  - "SSOT §TP7 — author grep-check-tp7-08.cjs: forward-carry 8 checks from grep-check-tp6-07.cjs + CHECK 9 (autoRotate={false}) + CHECK 10 (fov 32/40/26 all present) + CHECK 11 (no second makeDefault PerspectiveCamera)"
  - "SSOT §TP7 — presets RECONFIRMED LOCKED; default = KEEP TP0; NO edit unless preset clearly fails; stop-on-ambiguous = KEEP"

must_haves:
  truths:
    - "docs/table-3d/anchors/tp7-gate/ contains card.png, hero.png, macro.png (captured with ?fx, spin=off, port 5181)"
    - "M1 PASS: rank-glyph bbox height >= 22px on card.png (1080p downscale via m1-m2-m12.mjs)"
    - "M2 PASS: card area >= 2.0x chip area on hero.png and card.png (manual-polygon measurement via m2CardsVsChips)"
    - "M9 PASS: m9-hero-a.png and m9-hero-b.png are byte-identical (md5 match via m9-determinism.mjs)"
    - "tools/table-3d/grep-check-tp7-08.cjs exits 0 (11 checks: 8 forward-carried + CHECK 9 autoRotate={false} + CHECK 10 fov 32/40/26 + CHECK 11 no-second-makeDefault)"
    - "tools/table-3d/grep-check-tp6-07.cjs still exits 0 (backward compatibility)"
    - "The 3 frozen preset values are UNCHANGED in TableLab.tsx: card fov:40, hero fov:32, macro fov:26; no edit was made (or a minimal correction recorded with justification)"
  artifacts:
    - path: "tools/table-3d/grep-check-tp7-08.cjs"
      provides: "TP7 invariant checker — 11 checks (8 forward-carried + autoRotate=false + fov 32/40/26 + no-second-makeDefault); exit 0/exit 1 discipline; Node stdlib only"
      contains: "grep-check-tp7-08"
    - path: "docs/table-3d/anchors/tp7-gate/hero.png"
      provides: "HERO preset capture on finished TP1-TP6 table (?cam=hero&fx, spin=off)"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp7-gate/card.png"
      provides: "POV/card preset capture on finished TP1-TP6 table (?cam=card&fx, spin=off)"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp7-gate/macro.png"
      provides: "MACRO preset capture on finished TP1-TP6 table (?cam=macro&fx, spin=off)"
      min_lines: 1
  key_links:
    - from: "tools/table-3d/grep-check-tp7-08.cjs"
      to: "frontend/src/lab/TableLab.tsx"
      via: "Reads TableLab.tsx non-comment code; asserts autoRotate={false} + fov 32/40/26 present + no second makeDefault camera"
      pattern: "grep-check-tp7-08"
    - from: "docs/table-3d/anchors/tp7-gate/m9-hero-a.png + m9-hero-b.png"
      to: "tools/table-3d/m9-determinism.mjs"
      via: "md5 compare proves autoRotate=false makes capture deterministic on the finished TP6 table"
      pattern: "m9-hero"
---

<objective>
Capture the three TP0-frozen money shots against the finished TP1-TP6 table, re-confirm M1/M2/M9
PASS, and author grep-check-tp7-08.cjs asserting the camera invariants.

Purpose: TP7 is a CONFIRMATION phase. The cameras were frozen at TP0 — this plan asks one
question: "do the TP0 presets still hold as money shots on the finished table?" The expected answer
is YES and the default action is KEEP. Automated gates (M1/M2/M9 + grep-check) are the
necessary-not-sufficient pre-condition before the operator perceptual gate in plan 08-03.

Output: three GPU-faithful anchor captures in docs/table-3d/anchors/tp7-gate/; M1/M2/M9 verdicts
recorded in SUMMARY; tools/table-3d/grep-check-tp7-08.cjs exits 0 (11 checks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-CONTEXT.md
@.planning/phases/08-tp7-camaras-lock-money-shots/08-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 3 frozen money shots + M9 double-capture against the finished TP1-TP6 table</name>
  <files>
    docs/table-3d/anchors/tp7-gate/card.png
    docs/table-3d/anchors/tp7-gate/hero.png
    docs/table-3d/anchors/tp7-gate/macro.png
    docs/table-3d/anchors/tp7-gate/m9-hero-a.png
    docs/table-3d/anchors/tp7-gate/m9-hero-b.png
  </files>
  <read_first>
    - .dev-stack/lab-shot.mjs — harness invocation pattern: LAB_URL env var, output path arg,
      appends spin=off automatically, uses D3D11 ANGLE, 2500ms settle. Confirm the exact CLI
      signature (LAB_URL="..." node .dev-stack/lab-shot.mjs <output-path>).
    - tools/table-3d/m9-determinism.mjs — invocation pattern: --shot hero --port 5181 --out
      <output-dir>. Confirm the exact arg names and what output files it writes (m9-hero-a.png /
      m9-hero-b.png or similar). If the script takes two pre-captured PNGs instead of capturing
      itself, adjust accordingly.
    - tools/table-3d/m1-m2-m12.mjs — confirm M1 export name and invocation. Reads from
      docs/table-3d/anchors/ by convention or takes a path arg.
    - frontend/src/lab/TableLab.tsx lines 1044-1066 (cam useMemo) — READ the exact 3 frozen
      preset values before capturing so SUMMARY records the verified-from-source values:
        card:  pos [0, 4.7, 10.6], target [0, 0.25, 1.2], fov 40
        hero:  pos [1.2, 5.0, 8.2], target [0, 0.5, 0], fov 32
        macro: pos [-1.7, 1.7, 2.4], target [-1.55, 0.05, 1.05], fov 26
      CONFIRM these values are byte-identical to the TP0 lock. If any value differs, STOP and
      record before continuing — do NOT silently correct.
  </read_first>
  <action>
    PREREQUISITE: The dev server must be running on port 5181 (cd frontend && npm run dev --
    --port 5181). If port 5181 is not confirmed reachable, stop and report — do not use a
    different port (the harness and metric tools default to 5181; changing port would require
    updating all tool invocations in this plan).

    STEP 1 — Create the anchor directory:
      Create docs/table-3d/anchors/tp7-gate/ if it does not exist. No output.

    STEP 2 — Capture the three canonical stills (with ?fx — the intended AAA final look):
      LAB_URL="http://localhost:5181/table-lab.html?cam=card&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp7-gate/card.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=hero&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp7-gate/hero.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=macro&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp7-gate/macro.png

    Note: captures use marks ON (default) — TP7 evaluates the finished table; marks are part
    of the final scene composition. Captures use ?fx — this is the representative final render.

    STEP 3 — M9 double-capture (autoRotate=false determinism on the hero preset):
      node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp7-gate
    Expected output: "M9: PASS (byte-identical)". The two captures land as m9-hero-a.png and
    m9-hero-b.png (or equivalent naming per the tool). Record the md5 hash in the SUMMARY.
    If M9 FAILS: the only permitted cause is the harness appending spin=off being insufficient
    (e.g., a useFrame loop running). Check if any useFrame is already present in TableLab.tsx.
    If a useFrame exists from a prior plan and is NOT guarded by spin=off → add the freeze guard
    and re-run. If nothing obvious: record FAIL + cause + stop — do NOT proceed to plan 08-02
    flythrough; treat as a hard blocker.

    STEP 4 — M1 re-read on card.png (primary legibility gate at POV/card preset):
      node tools/table-3d/m1-m2-m12.mjs
    (Confirm from read_first whether this tool takes the path as arg or reads from
    docs/table-3d/anchors/ by convention. Use the confirmed invocation.)
    Record the rank-glyph bbox height in pixels at 1080p downscale. Expected: >= 22px (TP6
    reading was 50px at hero; POV is closer so cards are larger — expect >= 22px comfortably).
    M1 legibility confirm is also a manual operator step at the gate (plan 08-03), not a hard
    automated gate here — record the px value and flag any reading < 22px as FAIL.

    STEP 5 — M2 re-read (manual polygon, hero.png and card.png):
    Using the m2CardsVsChips export from m1-m2-m12.mjs, trace the card region and chip region
    polygons on hero.png. Compute the area ratio.
    Expected: >= 2.0x (the TP3 demoted accent pot with scale={0.5} was designed to hold M2 >= 2.0x).
    M2 is NOT required on macro (by design — macro is a chip/detail shot where chips are large).
    Record both hero.png and card.png ratios in SUMMARY.

    STEP 6 — Verify preset values are TP0-identical:
    Read frontend/src/lab/TableLab.tsx lines 1044-1066. Compare each frozen preset value
    against the TP0 lock:
      card fov: must be 40  (operator locked at TP0; 37 was discarded)
      hero fov: must be 32
      macro fov: must be 26
    If all match: record "TP0 presets confirmed UNCHANGED" in SUMMARY.
    If any differ: STOP. Record the discrepancy. Do NOT automatically correct. The default is
    KEEP TP0. Any deviation requires an explicit operator gate decision — flag it to plan 08-03.

    STEP 7 — Commit anchors + pre-gate record:
    Stage and commit:
      docs/table-3d/anchors/tp7-gate/card.png
      docs/table-3d/anchors/tp7-gate/hero.png
      docs/table-3d/anchors/tp7-gate/macro.png
      docs/table-3d/anchors/tp7-gate/m9-hero-a.png (or equivalent)
      docs/table-3d/anchors/tp7-gate/m9-hero-b.png (or equivalent)
    Commit message: "chore(08-01): TP7 pre-gate captures — 3 money shots + M9 double-capture on finished TP6 table"
  </action>
  <verify>
    <automated>node -e "const f=require('fs'); ['docs/table-3d/anchors/tp7-gate/card.png','docs/table-3d/anchors/tp7-gate/hero.png','docs/table-3d/anchors/tp7-gate/macro.png'].forEach(p=>{if(!f.existsSync(p)){console.error('MISSING: '+p);process.exit(1);}});console.log('OK — 3 anchor captures present');"</automated>
    <automated>node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp7-gate</automated>
    <automated>node tools/table-3d/m1-m2-m12.mjs</automated>
  </verify>
  <done>
    card.png, hero.png, macro.png committed to docs/table-3d/anchors/tp7-gate/; M9 PASS (byte-
    identical double-capture on hero); M1 >= 22px on card.png (1080p downscale); M2 >= 2.0x on
    hero.png; TP0 preset values confirmed UNCHANGED (fov 32/40/26) or discrepancy flagged for
    gate. SUMMARY records the exact M1 px-height, M2 ratio, M9 md5 hash, and preset-value
    comparison result. LOCAL only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author tools/table-3d/grep-check-tp7-08.cjs (11 checks: 8 forward-carried + autoRotate + fovs + no-dup-cam)</name>
  <files>tools/table-3d/grep-check-tp7-08.cjs</files>
  <read_first>
    - tools/table-3d/grep-check-tp6-07.cjs — the EXACT canonical pattern to mirror: "use strict";
      const fs/path requires; readFile(filePath) helper; fail(message) -> console.error + exit(1);
      stripComments(src) -> filter lines starting with "//"; per-check blocks; one-line OK on
      exit 0 listing all check names. Mirror this structure exactly. Also extract the exact 8
      check bodies and regexes so CHECK 1-8 are bit-for-bit copies (forward-carry).
    - frontend/src/lab/TableLab.tsx — verify the exact string form of autoRotate and the fov
      assignments before writing the regexes:
        CHECK 9 target: literal string autoRotate={false} (not autoRotate alone; not autoRotate=false)
        CHECK 10 targets: "fov: 32", "fov: 40", "fov: 26" as they appear in the cam useMemo presets
        CHECK 11 target: absence of a second "makeDefault" on PerspectiveCamera
      Read the OrbitControls JSX (lines 1106-1119) and cam useMemo (lines 1044-1066) to confirm
      the exact token forms.
  </read_first>
  <action>
    Author tools/table-3d/grep-check-tp7-08.cjs as a CommonJS module. Mirror grep-check-tp6-07.cjs
    exactly in structure.

    The file MUST:
    - Use only fs, path from Node stdlib. Zero new packages.
    - Read from repo root: TABLE_LAB = path.join("frontend","src","lab","TableLab.tsx");
      LAB_DIR = path.join("frontend","src","lab").
    - Provide readFile(filePath), fail(message), and stripComments(src) helpers identical to tp6-07.
    - Run 11 checks in order; fail() on any; exit 0 with one-line OK listing all check names.

    CHECKS 1-8 — Forward-carry from grep-check-tp6-07.cjs EXACTLY:
    Copy each check block verbatim from grep-check-tp6-07.cjs. Do not paraphrase or simplify.
    CHECK 1: EffectComposer present (<EffectComposer).
    CHECK 2: N8AO present (<N8AO).
    CHECK 3: DepthOfField present or cut-documented (read the forward-carried logic from tp6-07;
             preserve the NOTE handling if DOF was cut in 07-03).
    CHECK 4: Vignette present (<Vignette).
    CHECK 5: No Bloom in LAB_DIR (scan all .tsx and .ts files).
    CHECK 6: brassMat roughness 0.42-0.45 (/roughness\s*:\s*0\.4[2-5]/).
    CHECK 7: SoftShadows present (<SoftShadows).
    CHECK 8: ContactShadows frames={1} present (/frames=\{1\}/).

    CHECK 9 — autoRotate={false} hardcoded (M9 invariant):
      nc = stripComments(readFile(TABLE_LAB))
      Pattern: /autoRotate=\{false\}/.test(nc)
      Fail msg: "CHECK 9 FAILED — autoRotate={false} not present in non-comment code in TableLab.tsx.\n" +
        "  M9 determinism HARD GATE: autoRotate must be hardcoded false. Do NOT change to\n" +
        "  autoRotate={qp('spin') !== 'off'} or any truthy expression — the harness param 'spin=off'\n" +
        "  is NOT the OrbitControls autoRotate prop. Keep autoRotate={false} unconditional."

    CHECK 10 — all three frozen fovs present in the cam useMemo (TP0-locked values):
      Pattern: /fov:\s*32/.test(nc) AND /fov:\s*40/.test(nc) AND /fov:\s*26/.test(nc)
      Fail msg: "CHECK 10 FAILED — one or more TP0-frozen preset fovs missing from TableLab.tsx.\n" +
        "  Required: fov: 32 (hero), fov: 40 (card/POV), fov: 26 (macro).\n" +
        "  If a fov value was changed during TP7 for a justified preset correction, update\n" +
        "  this check with the recorded correction value + gate reference."

    CHECK 11 — no second PerspectiveCamera with makeDefault (flythrough anti-pattern):
      Count occurrences of the string "makeDefault" in nc. A second makeDefault PerspectiveCamera
      would override the preset camera and break the framing.
      Pattern: (nc.match(/makeDefault/g) || []).length <= 1
      Fail msg: "CHECK 11 FAILED — more than one 'makeDefault' token found in non-comment code.\n" +
        "  A second PerspectiveCamera with makeDefault overrides the preset camera and breaks\n" +
        "  the frozen money shots. The optional ?fly flythrough must animate the existing camera\n" +
        "  reference via useFrame, NOT add a second makeDefault camera."

    ONE-LINE OK (exit 0):
      console.log(
        "OK — grep-check-tp7-08: all 11 TP7 camera invariants hold\n" +
        "  CHECK 1: EffectComposer present behind ?fx (TP6)\n" +
        "  CHECK 2: N8AO present — crevice darkening, M6 satisfier (TP6)\n" +
        "  CHECK 3: DepthOfField present (or cut — M1 HARD gate; disposition in 07-03-SUMMARY) (TP6)\n" +
        "  CHECK 4: Vignette present — frame darkening, M8 gate (TP6)\n" +
        "  CHECK 5: No Bloom in frontend/src/lab/ — M7 HARD GATE (permanently banned) (TP5)\n" +
        "  CHECK 6: brassMat roughness 0.42-0.45 — TP4-locked brass aging pass\n" +
        "  CHECK 7: SoftShadows unconditional in Scene — TP5 PCSS grounding\n" +
        "  CHECK 8: ContactShadows frames={1} — TP5 baked once at mount\n" +
        "  CHECK 9: autoRotate={false} hardcoded — M9 determinism HARD GATE (TP7)\n" +
        "  CHECK 10: fov: 32 / fov: 40 / fov: 26 all present — TP0-frozen money-shot presets (TP7)\n" +
        "  CHECK 11: no second makeDefault camera — flythrough uses useFrame only (TP7)"
      );
      process.exit(0);

    Commit: "chore(08-01): add grep-check-tp7-08.cjs — 11 invariant checks (8 forward-carry + 3 TP7 camera)"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp7-08.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp6-07.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
  </verify>
  <done>
    tools/table-3d/grep-check-tp7-08.cjs exits 0 with 11-check OK message; grep-check-tp6-07.cjs
    still exits 0 (backward compatibility); tsc clean under frontend/src/lab; vitest green.
    LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render and local tooling only. No auth, no API, not in prod build.

| Boundary | Description |
|----------|-------------|
| TP0-frozen presets -> TP7 confirmation | Default is KEEP; any preset value change is a trust boundary requiring explicit gate decision |
| automated metrics -> human perceptual gate | M1/M2/M9 + grep-check are necessary-not-sufficient; operator judges "money shot of the finished table" in 08-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01a | Tampering | Preset values silently changed before this plan runs | mitigate | Task 1 STEP 6 reads TableLab.tsx and records preset-value comparison; any discrepancy stops the plan and flags to 08-03 gate |
| T-08-01b | Tampering | CHECK 9 regex false positive (autoRotate= without {false}) | mitigate | Regex is /autoRotate=\{false\}/ — requires the literal brace-false form; confirmed against TableLab.tsx lines 1106-1119 in read_first |
| T-08-01c | Tampering | CHECK 10 matches a comment fov value (e.g., // fov: 32 in old comment) | mitigate | stripComments() removes lines starting with // before all checks; same convention as prior grep-checks |
| T-08-01d | Tampering | M9 FAIL caused by an existing useFrame not guarded by spin=off | mitigate | If M9 FAILS, inspect for unguarded useFrame; add freeze guard; re-run M9 before proceeding to 08-02 |
| T-08-01e | Repudiation | M1/M2/M9 verdicts not recorded (gate in 08-03 has no basis) | mitigate | SUMMARY explicitly requires exact px-height, M2 ratio, M9 md5, preset comparison result |
| T-08-SC | Tampering | npm installs | accept | grep-check-tp7-08.cjs uses Node stdlib only; no packages installed in this plan |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp7-08.cjs exits 0 (11 checks).
- node tools/table-3d/grep-check-tp6-07.cjs exits 0 (8 checks, backward compatibility).
- docs/table-3d/anchors/tp7-gate/ contains card.png, hero.png, macro.png (GPU-faithful, ?fx, spin=off).
- M9 PASS: m9-hero-a.png and m9-hero-b.png byte-identical.
- M1 >= 22px on card.png (1080p downscale).
- M2 >= 2.0x on hero.png (manual polygon).
- TP0 preset values UNCHANGED (fov 32/40/26) confirmed from source or discrepancy flagged.
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- grep-check-tp7-08.cjs exits 0 (11/11 checks); grep-check-tp6-07.cjs exits 0.
- 3 anchor captures committed; M1/M2/M9 all PASS; preset values UNCHANGED or discrepancy flagged.
- Complete pre-gate metric table in SUMMARY. tsc clean. vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/08-tp7-camaras-lock-money-shots/08-01-SUMMARY.md` when done.
This is the critical pre-gate record: exact M1 px-height (and tool used), exact M2 ratio (hero
and card shots), M9 md5 hash (PASS or FAIL + cause), preset-value comparison (UNCHANGED or
discrepancy), anchor capture paths, grep-check-tp7-08 11-check verdict, grep-check-tp6-07 8-check
verdict. The operator gate (08-03) reads this SUMMARY as its basis before presenting the money shots.
</output>
