---
phase: 02-tp1-felt-tapete-materiality-the-stage
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements: []   # TP1 gated by SSOT §TP1 acceptance + metric thresholds (no REQUIREMENTS.md IDs)
must_haves:
  truths:
    - "The felt's default (non-debug) material is a THREE.MeshPhysicalMaterial with a Charlie sheen lobe, a concentric nap normalMap, a subtle edge-darken, and roughness kept 0.90-0.94 (SSOT §TP1 + D-02)"
    - "Sheen is contained per D-02: sheen ~0.65-0.75, sheenColor a slightly-lighter Chiribito green, sheenRoughness 0.55-0.8, anisotropy 0.15-0.4 — visible as a grazing nap shift, not satin"
    - "The edge-darken is wired per the 02-01 A1 decision (aoMap with uv2 if required, else direct) and never reads as a heavy vignette"
    - "Fresh GPU-faithful HERO/MACRO/POV captures exist and the admitted metric gates PASS over HERO: M3 felt-hue ΔE<12, M5 highlight-clip on felt, +B fuzz-not-satin (sheen ≤8%)"
    - "The ?felt=magenta and ?felt=basic debug branches still work (born-in discipline + A/B hooks preserved)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "Felt material swapped MeshStandardMaterial -> MeshPhysicalMaterial (sheen/nap/aniso/aoMap); debug branches intact; aoMap uv2 wiring if A1 required it"
      contains: "MeshPhysicalMaterial"
    - path: ".dev-stack/diag/table-3d/tp1/hero.png"
      provides: "Fresh GPU-faithful HERO capture (fov32) for the M3/M5/+B gate"
    - path: ".dev-stack/diag/table-3d/tp1/macro.png"
      provides: "Fresh MACRO capture (fov26) for the operator inlay A/B in 02-04"
    - path: ".dev-stack/diag/table-3d/tp1/card.png"
      provides: "Fresh POV capture (fov40, ?cam=card) for the operator sheen/nap A/B in 02-04"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx (feltMat)"
      to: "frontend/src/lab/textures.ts (feltTexture S=2048, feltNapNormalMap, feltEdgeAoMap)"
      via: "import + useMemo material construction"
      pattern: "feltNapNormalMap|feltEdgeAoMap"
    - from: ".dev-stack/diag/table-3d/tp1/hero.png"
      to: "tools/table-3d/run-metrics.mjs (M3/M5/+B)"
      via: "run-metrics.mjs over the capture dir reads hero.png"
      pattern: "run-metrics"
---

<objective>
Swap the felt's default material from `MeshStandardMaterial` to `MeshPhysicalMaterial` in
`frontend/src/lab/TableLab.tsx` — adding the Charlie sheen lobe, the concentric nap normalMap, the
subtle edge-darken, and restrained anisotropy authored in 02-02 — while keeping roughness in the
anti-satin 0.90-0.94 band and preserving the `?felt=magenta|basic` debug branches and born-in
discipline. Then capture fresh GPU-faithful HERO/MACRO/POV frames via the FROZEN harness and run the
admitted metric gates (M3/M5/+B) over HERO. This is the perceptual variable for the operator gate in
02-04; the automated gates must be green before that gate runs.

Purpose: This is the one render-changing plan. It consumes the 02-02 factories, follows the proven
`leatherMat` MeshPhysicalMaterial compile path, and proves the change passes the frozen eval kit.

Output: modified TableLab.tsx (felt -> MeshPhysicalMaterial); fresh tp1-hero/tp1-macro/tp1/card.png
captures in the gitignored .dev-stack/diag scratch dir; a recorded M3/M5/+B PASS run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-CONTEXT.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-VALIDATION.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-01-SUMMARY.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-02-SUMMARY.md
@docs/table-3d/TP1_A1_AOMAP_UV.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap felt -> MeshPhysicalMaterial (sheen/nap/aniso/aoMap) + wire aoMap UV per A1</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 362-401 — the felt useMemo with its three-branch debug structure (magenta/basic/default) at 364-375, and the leatherMat MeshPhysicalMaterial+sheen proven compile path at 388-401 (the analog to copy).
    - frontend/src/lab/TableLab.tsx lines 428-433 — the felt mesh JSX: planeGeometry args [FELT_R*2, FELT_R*2, 1, 1] inside the <group scale={[OVAL_X,1,1]}> (OVAL_X=1.22). This is where a uv2 ref attaches IF A1 requires it.
    - docs/table-3d/TP1_A1_AOMAP_UV.md (from 02-01) — whether aoMap needs uv2 (add `geo.setAttribute('uv2', geo.getAttribute('uv'))` via the planeGeometry ref) or reads uv directly (no ref). If A1 chose the albedo-fallback, there is no aoMap to wire — omit aoMap/aoMapIntensity entirely.
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md "TableLab.tsx felt material swap" (lines 277-372) — the exact replacement block + import additions + the uv2 JSX-ref pattern + the DO-NOT computeTangents note.
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §1 + Pitfalls 1/2/3 (lines 143-203, 431-447) — sheen energy behavior, the satin floor (never roughness <0.90), the anisotropy ceiling (≤0.4), and the sheenColor M3 caution.
  </read_first>
  <action>
    Replace ONLY the default (third) branch of the felt material in the useMemo (currently
    `MeshStandardMaterial`, TableLab.tsx 369-375) with a `THREE.MeshPhysicalMaterial`, following the
    leatherMat analog. Keep the magenta and basic debug branches verbatim. Add `feltNapNormalMap` and
    `feltEdgeAoMap` to the existing `./textures` import. Material fields, all within the SSOT §TP1 bands +
    D-02: `map: feltTexture(logoImg, aceImgs)` (now S=2048), `normalMap: feltNapNormalMap()`,
    `normalScale: new THREE.Vector2(0.25, 0.25)` (SSOT 0.2-0.35; start 0.25 per Open-Q 2),
    `sheen: 0.70` (D-02 ~0.65-0.75), `sheenColor: new THREE.Color("#2aad7a")` (slightly-lighter
    Chiribito green — Pitfall 3: keep it green, not blue/desaturated), `sheenRoughness: 0.65`
    (SSOT 0.55-0.8, anti-satin), `anisotropy: 0.25` (SSOT 0.15-0.4, D-01), `anisotropyRotation: 0`
    (direction comes from the nap normalMap, not here), `roughness: 0.93` (UNCHANGED — anti-satin hard
    floor 0.90-0.94), `metalness: 0`, `envMapIntensity: 0.3` (unchanged), `alphaTest: 0.5` (born-in
    discipline). For the edge-darken, follow the 02-01 A1 record: if aoMap+uv2 -> add
    `aoMap: feltEdgeAoMap(), aoMapIntensity: 0.18` AND attach a planeGeometry ref that runs
    `geo.setAttribute("uv2", geo.getAttribute("uv"))` (the only JSX change to the mesh at 428-433); if
    aoMap+uv1 -> add aoMap/aoMapIntensity with no ref; if albedo-fallback -> omit aoMap entirely (the
    darken already lives in the albedo from 02-02). Do NOT call `.computeTangents()` (RESEARCH §2 — three
    derives the TBN from the UV gradient; the OVAL_X stretch is handled by the mesh scale). Do NOT change
    any camera preset, the OVAL_X group, FELT_R, or anything outside the felt material + (optional) the
    felt-mesh uv2 ref.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>grep -nE "MeshPhysicalMaterial|feltNapNormalMap|feltEdgeAoMap|sheen|anisotropy" src/lab/TableLab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` reports no errors.
    - The default felt branch is now MeshPhysicalMaterial: within the felt useMemo, exactly one felt `MeshPhysicalMaterial` is constructed (`grep -c "new THREE.MeshPhysicalMaterial" ` on the felt block region shows the felt one added alongside the existing leather/wood ones — felt branch no longer `MeshStandardMaterial`).
    - Sheen + nap + aniso present with in-band values: `sheen: 0.70`, `sheenColor` a green hex (#2aad7a), `sheenRoughness: 0.65`, `anisotropy: 0.25`, `roughness: 0.93` (grep-confirmable literals).
    - `?felt=magenta` and `?felt=basic` branches unchanged (`grep -c "feltKind === \"magenta\"" src/lab/TableLab.tsx` = 1 and `feltKind === \"basic\"` = 1).
    - No `computeTangents` in TableLab.tsx; no camera-preset edit (the `cam` presets block lines 628-643 unchanged).
    - Edge-darken wired per the A1 record (aoMap+uv2 ref / aoMap direct / albedo-fallback omits aoMap) — matches docs/table-3d/TP1_A1_AOMAP_UV.md.
  </acceptance_criteria>
  <done>Felt default material is a MeshPhysicalMaterial with contained sheen + concentric nap + restrained anisotropy + subtle edge-darken (per A1), roughness held at 0.93; debug branches + cameras intact; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Capture HERO/MACRO/POV (frozen harness) + run M3/M5/+B gates</name>
  <files>.dev-stack/diag/table-3d/tp1/hero.png, .dev-stack/diag/table-3d/tp1/macro.png, .dev-stack/diag/table-3d/tp1/card.png</files>
  <read_first>
    - .dev-stack/lab-shot.mjs lines 1-60 — the capture harness contract: `LAB_URL` env selects the page (append `?cam=hero|macro|card`), arg 1 is the out path, it appends `spin=off`, uses ANGLE D3D11 + deviceScaleFactor 2 (=> 2880×1800), prints GPU + ERRORS. FROZEN — do not modify.
    - tools/table-3d/run-metrics.mjs lines 1-19, 195-215 — CAPTURE-RUN mode takes a DIRECTORY and reads `hero.png` from it; prints a per-metric PASS/FAIL/informational table. FROZEN — do not modify.
    - tools/table-3d/metrics.mjs lines 28-105 — the baked thresholds (M3 ΔE<12 from #1f9163/#147a51/#0a4a33; M5 luma>250 <0.5% of felt; +B luma>210 ≤8% of felt) and the feltHero rect (760,500,200,120 on 2880×1800).
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §6 (lines 368-414) — the exact capture+gate sequence and the cam->fov mapping (hero=32, macro=26, card=POV 40).
  </read_first>
  <action>
    Ensure the Vite dev server is up on port 5173 (`cd frontend && npm run dev` in a background shell;
    confirm `/table-lab.html` responds). Capture three fresh GPU-faithful frames into the gitignored
    scratch dir `.dev-stack/diag/table-3d/`, naming HERO `hero.png` so the metric runner finds it — use a
    dedicated capture subdir to keep the run clean: write `hero.png` (LAB_URL `...?cam=hero`),
    `macro.png` (`?cam=macro`), and `card.png`/POV (`?cam=card`, fov40) all into
    `.dev-stack/diag/table-3d/tp1/`. Verify each capture printed a real GPU renderer (RTX 4060 / ANGLE
    D3D11, NOT SwiftShader) and `ERRORS []` — a SwiftShader or error capture is invalid and must be
    re-run. Then run `node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp1` and read the
    table: M3 (felt-hue), M5 (highlight-clip), and +B (felt-specular "fuzz not satin") MUST all show
    PASS. If +B FAILS (sheen sweep >8% — satin drift), per RESEARCH Open-Q 2 reduce normalScale first
    (0.25 -> 0.20), re-capture, re-run; if still failing reduce sheen toward 0.65 (stay ≥ the SSOT 0.6
    floor and roughness ≥ 0.90) — at most 2 tuning iterations, then record the state and flag for the
    operator gate (non-blocking per SSOT §5.3 TP1 row). If M3 FAILS, do NOT touch the albedo gradient
    (it is locked) — reduce sheen slightly per Pitfall 3. Do NOT modify any file under tools/table-3d/ or
    .dev-stack/lab-shot.mjs (frozen eval kit). Captures stay in the gitignored scratch dir (baseline/
    anchor promotion is NOT part of TP1).
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp1</automated>
  </verify>
  <acceptance_criteria>
    - `.dev-stack/diag/table-3d/tp1/hero.png`, `macro.png`, and `card.png` exist and are 2880×1800 GPU-faithful (capture log shows an RTX/ANGLE renderer and `ERRORS []`, not SwiftShader).
    - `node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp1` prints `PASS` for M3, M5, and +B in the verdict table (the three TP1 gates).
    - If any gate needed tuning, the final committed material values stay within the SSOT bands (sheen 0.6-0.85, sheenRoughness 0.55-0.8, normalScale 0.2-0.35, anisotropy 0.15-0.4, roughness 0.90-0.94) and the SUMMARY records the iteration count (≤2).
    - No file under `tools/table-3d/` or `.dev-stack/lab-shot.mjs` modified (`git status` shows them clean).
  </acceptance_criteria>
  <done>Three fresh GPU-faithful captures exist; M3/M5/+B all PASS over HERO; material values within SSOT bands; eval kit untouched; captures in gitignored scratch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

TP1 is an OFFLINE, static WebGL lab (no auth/network/secrets/user-input; not in prod build). The
boundaries are anti-casino identity, the metric gates, and the frozen eval kit.

| Boundary | Description |
|----------|-------------|
| material params -> casino/satin drift | sheen+roughness+anisotropy must stay in-band or the felt tips to satin/Vegas (anti-casino hard-NO) |
| capture path -> GPU faithfulness | a SwiftShader capture would falsely pass/fail the gates; only RTX/ANGLE captures are valid |
| eval kit (tools/table-3d, lab-shot.mjs) | FROZEN at TP0 — modifying it would break apples-to-apples (SSOT §5.6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | satin/casino drift | mitigate | roughness held 0.93, sheen 0.70, sheenRoughness 0.65, anisotropy 0.25 (all in-band); +B gate PASS enforces "fuzz not satin"; ≤2 tuning iterations then non-blocking flag |
| T-02-04 | Tampering | M3 hue / M5 clip on felt | mitigate | M3/M5 gates run on a fresh HERO capture and MUST PASS before the operator gate; albedo gradient never touched here |
| T-02-03 | Spoofing | GPU capture faithfulness | mitigate | Capture log asserts RTX/ANGLE renderer + ERRORS [] before metrics are trusted (no SwiftShader) |
| T-02-EVAL | Tampering | frozen eval kit | mitigate | No edits under tools/table-3d/ or lab-shot.mjs (git-status clean check) |
| T-02-SC | Tampering | npm installs | accept | No package installs in TP1 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- Felt default material is MeshPhysicalMaterial with in-band sheen/nap/aniso; roughness 0.93; debug branches + cameras intact; no computeTangents.
- Three fresh 2880×1800 GPU-faithful captures (hero/macro/card) with no console errors.
- `node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp1` -> M3, M5, +B all PASS.
- Eval kit files unmodified.
</verification>

<success_criteria>
- Felt relights as woven baize: MeshPhysicalMaterial with Charlie sheen + concentric nap + restrained anisotropy + subtle edge-darken (per A1), roughness 0.90-0.94.
- M3 felt-hue PASS, M5 highlight-clip PASS, +B fuzz-not-satin PASS over a fresh GPU-faithful HERO.
- MACRO + POV captures ready for the operator A/B in 02-04.
- No push/deploy/merge; eval kit frozen; captures in gitignored scratch.
</success_criteria>

<output>
Create `.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-03-SUMMARY.md` when done. Record the final material values, the metric verdicts (M3/M5/+B), any tuning iterations, and the capture dir path for 02-04.
</output>
