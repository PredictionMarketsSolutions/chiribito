---
phase: 02-tp1-felt-tapete-materiality-the-stage
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/lab/textures.ts
autonomous: true
requirements: []   # TP1 gated by SSOT §TP1 acceptance + metric thresholds (no REQUIREMENTS.md IDs)
must_haves:
  truths:
    - "The felt albedo no longer contains the baked radial vignette (D-03): the createRadialGradient(...0.5) block at textures.ts:492-497 is gone"
    - "The felt albedo canvas renders at S=2048 so the born-in mark + 4 suits are ~2x sharper at MACRO (D-04), with the M3 anchor gradient unchanged (same colors + same fractional stops)"
    - "A concentric/oval nap normalMap factory exists (D-01) built from the shared height->normal helper, returned LINEAR (NoColorSpace) with RepeatWrapping + repeat in the SSOT 6-10 band"
    - "A very-subtle light-responsive edge-darken map exists (D-03) — an aoMap radial grayscale (or the recorded A1-fallback) returned LINEAR"
  artifacts:
    - path: "frontend/src/lab/textures.ts"
      provides: "feltTexture() S=2048 + vignette removed; new feltNapNormalMap() (concentric, repeat 8); new feltEdgeAoMap() (subtle radial, LINEAR)"
      exports: ["feltTexture", "feltNapNormalMap", "feltEdgeAoMap"]
      contains: "feltNapNormalMap"
  key_links:
    - from: "frontend/src/lab/textures.ts (feltNapNormalMap)"
      to: "frontend/src/lab/normalMapHelper.ts (heightToNormalMap)"
      via: "import — concentric height field -> tangent-space normal"
      pattern: "heightToNormalMap|toNormalMapTexture"
    - from: "frontend/src/lab/textures.ts (feltTexture M3 gradient)"
      to: "tools/table-3d/metrics.mjs M3_FELT_ANCHORS (#1f9163/#147a51/#0a4a33)"
      via: "the base radial gradient stops MUST stay byte-identical so M3 still passes"
      pattern: "#1f9163|#147a51|#0a4a33"
---

<objective>
Author the felt's data maps and albedo upgrade entirely inside the single texture factory
`frontend/src/lab/textures.ts` (SSOT §5.9 — every new material lives in the one factory, colorspace
discipline preserved). Four surgical changes: (1) raise `feltTexture()` canvas S 1024 -> 2048 for a
sharper MACRO inlay (D-04) WITHOUT touching the M3 anchor gradient; (2) DELETE the baked albedo
vignette block (textures.ts:492-497) per D-03; (3) add `feltNapNormalMap()` — a concentric/oval nap
(D-01) built via the shared `heightToNormalMap` helper from 02-01, returned LINEAR with
`RepeatWrapping` + `repeat.set` in the SSOT 6-10 band; (4) add `feltEdgeAoMap()` — a VERY subtle
light-responsive edge-darken (D-03), following the A1 decision recorded in 02-01.

Purpose: Isolate ALL texture authoring here so the next plan (02-03) is a clean material swap + a
metric/capture loop. No render wiring happens in this plan — these factories are not yet consumed.

Output: modified `frontend/src/lab/textures.ts` (feltTexture upgraded; feltNapNormalMap + feltEdgeAoMap added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-CONTEXT.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-01-SUMMARY.md
@docs/table-3d/TP1_A1_AOMAP_UV.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: feltTexture() — S 1024->2048 (D-04) + delete the baked vignette (D-03)</name>
  <files>frontend/src/lab/textures.ts</files>
  <read_first>
    - frontend/src/lab/textures.ts lines 422-501 — the full feltTexture() body: S=1024 (426), M3 anchor gradient (435-438, LOCKED), logo/brass/suit draws (443-490, all fractions of r — confirmed no hardcoded pixels), the vignette block (492-497, to delete), the srgb() return (500).
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md "feltTexture() modification" (lines 174-212) — S resize is local; M3 gradient DO-NOT-TOUCH; vignette REMOVE; return stays srgb().
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §5 + Pitfalls 5/6 (lines 336-364, 455-466) — why S=2048 keeps M3 (fractional stops), VRAM delta ~16MB (acceptable, no M10 impact), and the audit that no pixel constant breaks on resize.
  </read_first>
  <action>
    In `feltTexture()` change ONLY `const S = 1024` to `const S = 2048` (line 426). Confirm by reading
    that every subsequent coordinate is `r * fraction` or `S * fraction` (logo lr=r*0.34, brass r*0.41/
    r*0.44 + lineWidth S*0.006/S*0.003, suitR=r*0.6, suit h=r*0.27, speckle by S) — they all scale
    automatically; do NOT rewrite them. DELETE the vignette block at lines 492-497 verbatim (the
    `const vig = ctx.createRadialGradient(r, r, r*0.42, r, r, r)` through its `fillRect`) per D-03 — the
    depth it provided moves to feltEdgeAoMap() (Task 3) + light, not baked albedo. Do NOT touch the M3
    base gradient (435-438: colors #1f9163/#147a51/#0a4a33 at stops 0/0.55/1) — those three colors ARE
    the M3 anchors and the stops are fractional so they survive the resize. Keep the `return srgb(c)`
    (albedo is an sRGB color texture; only data maps use gray()). Keep speckle, logo, brass ring, and the
    4 born-in suits exactly as-is (born-in discipline, D-04).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>grep -nE "const S = 2048" src/lab/textures.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` reports no errors.
    - In feltTexture(): `grep -c "createRadialGradient.*r \* 0.42\|rgba(0,0,0,0.5)" src/lab/textures.ts` returns 0 (vignette block gone).
    - The M3 anchors survive: `grep -c "#1f9163" src/lab/textures.ts` ≥ 1 AND `grep -c "#147a51" src/lab/textures.ts` ≥ 1 AND `grep -c "#0a4a33" src/lab/textures.ts` ≥ 1 (gradient untouched).
    - `S = 2048` is set in feltTexture(); no other felt draw line was rewritten (diff shows only the S line + the deleted vignette block).
  </acceptance_criteria>
  <done>feltTexture() renders at S=2048 with the baked vignette removed and the M3 anchor gradient byte-identical; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: feltNapNormalMap() — concentric/oval nap via the shared helper (D-01)</name>
  <files>frontend/src/lab/textures.ts</files>
  <read_first>
    - frontend/src/lab/normalMapHelper.ts (from 02-01) — heightToNormalMap signature + toNormalMapTexture (gray/NoColorSpace) wrapper to consume.
    - frontend/src/lab/textures.ts lines 309-358 (chipFaceBump — procedural canvas + createImageData/putImageData pixel-loop + gray() return idiom) and lines 132-143 (speckle pixel-loop indexing `(y*W+x)*4`).
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §2 + §3 + Pitfalls 7/8 + Open-Q 3 (lines 205-283, 467-477, 638-642) — concentric height field centered at UV(0.5,0.5); ringFreq≥4 so rings are sub-pixel at HERO and visible only at MACRO ("vinyl record" failure if too low); OVAL_X stretch is handled by the mesh scale (NO UV correction needed); normal maps MUST be LINEAR.
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md "feltNapNormalMap()" (lines 216-250) — chipFaceBump factory structure + the RepeatWrapping/repeat.set idiom (wrapS/wrapT BEFORE repeat).
  </read_first>
  <action>
    Add `export function feltNapNormalMap(): THREE.CanvasTexture` to textures.ts. Build a height-field
    canvas at S=512 via createImageData: for each pixel compute distance from center
    `dist = sqrt((px/S-0.5)^2 + (py/S-0.5)^2) * 2` (0 at center, ~1 at edge) and height
    `h = (sin(dist*PI*ringFreq*2)*0.5 + 0.5)*255` with `ringFreq = 5` — concentric rings centered at
    UV(0.5,0.5), which read as oval on the felt because the mesh sits inside the OVAL_X=1.22 scale group
    (D-01: concentric/oval, NOT linear, NOT radial-from-center). Pass that canvas to `heightToNormalMap`
    (strength 1.0) from normalMapHelper.ts, wrap via `toNormalMapTexture` (LINEAR/NoColorSpace — Pitfall 7),
    set `wrapS = wrapT = THREE.RepeatWrapping` THEN `repeat.set(8, 8)` (repeat 8 is inside the SSOT 6-10
    band; at ringFreq 5 + repeat 8 the rings are sub-pixel at HERO fov32, fine weave at MACRO fov26 —
    avoids the Pitfall-8 "vinyl record" read). The directional nap strength is tuned later by the
    material's `normalScale` in 02-03; keep `strength` neutral here. Reuse makeCanvas from textures.ts
    (already in scope). Do NOT call computeTangents anywhere (RESEARCH §2 — three uses the UV-derivative
    getTangentFrame; tangents would break the normalMap).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>grep -nE "feltNapNormalMap|heightToNormalMap|toNormalMapTexture" src/lab/textures.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` reports no errors.
    - `feltNapNormalMap` is exported and imports `heightToNormalMap` (or `toNormalMapTexture`) from `./normalMapHelper` — `grep -c "from \"./normalMapHelper\"" src/lab/textures.ts` ≥ 1.
    - The returned texture is LINEAR: the function path uses `toNormalMapTexture`/`gray` (NoColorSpace), NOT `srgb` — `grep -c "srgb" ` within the feltNapNormalMap body = 0.
    - `repeat.set(8, 8)` present and `RepeatWrapping` set before repeat (within SSOT 6-10 band).
    - No `computeTangents` anywhere in textures.ts (`grep -c computeTangents src/lab/textures.ts` = 0).
  </acceptance_criteria>
  <done>feltNapNormalMap() returns a LINEAR concentric nap normalMap (repeat 8) built from the shared Sobel helper; build clean; no tangent computation.</done>
</task>

<task type="auto">
  <name>Task 3: feltEdgeAoMap() — very subtle light-responsive edge-darken (D-03)</name>
  <files>frontend/src/lab/textures.ts</files>
  <read_first>
    - docs/table-3d/TP1_A1_AOMAP_UV.md (from 02-01) — the A1 decision: aoMap path (uv vs uv2) OR the albedo-fallback. This task builds the aoMap; if A1 chose the albedo-fallback, build instead a reusable subtle-edge-darken helper per that record (still LINEAR-data discipline if it returns a texture).
    - frontend/src/lab/textures.ts lines 673-697 (floorTexture — radial gradient factory) and 152-158 (gray()) — the radial-gradient + LINEAR-return idiom to copy.
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §4 (lines 287-332) — the aoMap radial recipe: white center -> ~#c0c0c0 at absolute edge (~25% AO), held white through ~0.7 radius; returned via gray() (LINEAR); intensity (wired in 02-03) starts 0.18, ≤0.25 max (D-03 ceiling).
  </read_first>
  <action>
    Add `export function feltEdgeAoMap(): THREE.CanvasTexture` to textures.ts (following the A1 decision
    recorded in 02-01). Build an S=512 canvas with a radial gradient centered at (S/2,S/2): stop 0 =
    `#ffffff` (no AO at center), stop 0.7 = `#ffffff` (keep the felt clean through most of the surface),
    stop 1 = `#c0c0c0` (a VERY subtle ~25% darken at the absolute edge — D-03 "barely reads"). Fill the
    canvas, return via `gray(c)` (NoColorSpace — this is LINEAR AO data, NOT a color texture; do NOT use
    srgb). Keep it deliberately gentle: this is physical-depth-only edge-darkening so the felt never
    regresses below the protected reference between TP1 and TP5/TP6 — it is NOT a premium vignette (that
    is TP6's deliverable). If the A1 record selected the albedo-fallback branch instead of aoMap, then
    instead bake the same gentle edge-darken directly into feltTexture() at low opacity
    (rgba(0,0,0,~0.12) at the absolute edge — explicitly NOT the old 0.5 vignette) and skip the separate
    map; record which branch was implemented in the SUMMARY. Do NOT exceed the subtle band in either case.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>grep -nE "feltEdgeAoMap|aoMap|rgba\(0,0,0,0.1" src/lab/textures.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` reports no errors.
    - Either `feltEdgeAoMap` is exported and returns via `gray()` (LINEAR; `grep -c "srgb" ` in its body = 0), OR (albedo-fallback branch) feltTexture() contains a single low-opacity edge-darken at ≤ rgba(0,0,0,0.15) — and the SUMMARY states which branch.
    - The edge-darken is gentle: edge stop is `#c0c0c0` (~25%) or opacity ≤ 0.15 — NOT a heavy vignette, NOT the removed 0.5 block.
    - No new `createRadialGradient(...0.5)` full-black vignette reintroduced (`grep -c "rgba(0,0,0,0.5)" src/lab/textures.ts` = 0).
  </acceptance_criteria>
  <done>A very-subtle light-responsive edge-darken exists (aoMap LINEAR map, or the recorded albedo-fallback), within the D-03 subtle band; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

TP1 is an OFFLINE, static WebGL lab (no auth/network/secrets/user-input; not in prod build). The
boundaries that matter are reference-integrity, palette-integrity, and scope.

| Boundary | Description |
|----------|-------------|
| felt albedo M3 anchors | The base gradient stops are the M3 source-of-truth — changing them silently breaks the hue gate |
| data-map colorspace | normal/AO maps MUST be LINEAR (NoColorSpace); albedo MUST be sRGB — crossing this wire produces wrong normals (Pitfall 7) |
| lab textures -> game tree | textures.ts is shared, but only feltTexture/felt maps are touched; the game never imports the lab felt material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | M3 felt-hue (palette) | mitigate | Task 1 leaves the #1f9163/#147a51/#0a4a33 gradient + fractional stops byte-identical (grep-verified); S=2048 preserves proportions |
| T-02-03 | Tampering | born-in mark / nap colorspace | mitigate | feltNapNormalMap + feltEdgeAoMap return LINEAR via gray()/toNormalMapTexture (Pitfall 7); born-in suits/logo untouched on resize |
| T-02-01 | Tampering | felt reference floor | mitigate | Vignette removal compensated by the subtle feltEdgeAoMap so felt does not regress below reference; rollback tag from 02-01 is the net |
| T-02-SC | Tampering | npm installs | accept | No package installs in TP1 (RESEARCH: zero new deps) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean after all three tasks.
- M3 anchors intact in feltTexture(); vignette block gone; S=2048.
- feltNapNormalMap + feltEdgeAoMap exported, both LINEAR (gray/toNormalMapTexture), nap repeat in 6-10 band.
- No computeTangents; no reintroduced 0.5 vignette.
- (Render verification deferred to 02-03 — these factories are not yet consumed.)
</verification>

<success_criteria>
- feltTexture(): S=2048, vignette removed, M3 gradient untouched, born-in mark/suits preserved.
- feltNapNormalMap(): concentric/oval (D-01), LINEAR, RepeatWrapping repeat 8 (SSOT band), built on the shared helper.
- feltEdgeAoMap() (or recorded albedo-fallback): very subtle light-responsive edge-darken (D-03), LINEAR.
- Build clean; no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-02-SUMMARY.md` when done. Record which feltEdgeAoMap branch (aoMap vs albedo-fallback) was implemented.
</output>
