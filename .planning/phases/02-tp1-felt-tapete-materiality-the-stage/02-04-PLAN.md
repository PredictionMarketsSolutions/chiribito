---
phase: 02-tp1-felt-tapete-materiality-the-stage
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - docs/table-3d/TP1_OPERATOR_AB.md
autonomous: false
requirements: []   # TP1 gated by SSOT §TP1 perceptual gate (no REQUIREMENTS.md IDs)
must_haves:
  truths:
    - "The operator has performed the materiality-only A/B at POV (fov40) + MACRO (fov26): TP1 felt vs anchors/head/ (must improve) and never below anchors/reference-tag/ (the floor)"
    - "The operator verdict is recorded against the D-05 north star (físico/castizo/táctil/premium, NO Vegas/casino/satin) for the cloth ONLY — grounding/depth are NOT judged (deferred to TP5/TP6)"
    - "On an unambiguous yes -> TP1 ships; on stop-on-ambiguous -> ≤2 iterations then NON-BLOCKING rollback (keep current felt, proceed); the disposition is recorded"
  artifacts:
    - path: "docs/table-3d/TP1_OPERATOR_AB.md"
      provides: "The recorded operator A/B verdict, the side-by-side basis, the disposition, and the scorecard felt delta"
      contains: "Operator verdict"
  key_links:
    - from: "docs/table-3d/TP1_OPERATOR_AB.md"
      to: ".dev-stack/diag/table-3d/tp1/ (macro.png, card.png) + docs/table-3d/anchors/{head,reference-tag}/"
      via: "the A/B compares the fresh TP1 captures against the head baseline and the reference floor"
      pattern: "anchors"
---

<objective>
The TP1 operator perceptual gate — the GSD<->Chiribito human seam (SSOT §TP1 perceptual gate, §5.7
stop-on-ambiguous). The operator does a materiality-ONLY A/B at the two felt-telling shots (POV fov40
for sheen/nap, MACRO fov26 for inlay sharpness + weave) comparing the fresh TP1 felt (from 02-03)
against the TP0 HEAD baseline (`anchors/head/`, the "before") and confirming it never falls below the
protected reference (`anchors/reference-tag/`, the floor). The judgement is the cloth alone, against
the D-05 north star (físico/castizo/táctil/premium, NO Vegas/casino/satin); grounding/depth are
explicitly NOT judged (deferred to TP5/TP6, so an ambiguous depth reaction does not trip this gate).

Purpose: Automated metrics (02-03) prove "not satin / hue in palette" but cannot judge "reads as real
woven baize." Only the operator can. This gate cannot be auto-approved (autonomous:false; auto_advance
does not apply to a perceptual gate).

Output: `docs/table-3d/TP1_OPERATOR_AB.md` recording the verdict, the disposition (ship / iterate /
non-blocking rollback), and the scorecard felt delta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-CONTEXT.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-VALIDATION.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-03-SUMMARY.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — materiality-only felt verdict at POV + MACRO</name>
  <files>docs/table-3d/TP1_OPERATOR_AB.md</files>
  <action>
    Present the materiality-only A/B to the operator using the captures from 02-03 (and the live lab if
    requested), following the <how-to-verify> protocol below. Do NOT auto-approve — this is a perceptual
    gate (autonomous:false; auto_advance does not apply). Record the operator's verdict, the side-by-side
    basis (vs anchors/head/ and vs anchors/reference-tag/), the disposition (ship / ≤2-iteration fix /
    non-blocking rollback per SSOT §5.3 TP1 row), and the felt scorecard delta into
    docs/table-3d/TP1_OPERATOR_AB.md. On a fix request, adjust ONLY within-band levers (sheen /
    sheenColor / normalScale / anisotropy / aoMapIntensity), re-capture + re-run M3/M5/+B (per 02-03
    Task 2), and re-present — at most twice, then invoke the non-blocking rollback (revert felt to the
    tp1-before-felt state and proceed).
  </action>
  <what-built>
    TP1 turned the felt's default material into a MeshPhysicalMaterial with a Charlie sheen lobe
    (sheen ~0.70, slightly-lighter-green sheenColor, sheenRoughness ~0.65, roughness held 0.90-0.94),
    a concentric/oval nap normalMap (D-01), restrained anisotropy (~0.25), a sharper MACRO inlay
    (albedo S=2048, D-04), the baked albedo vignette removed (D-03) and replaced with a very-subtle
    light-responsive edge-darken. The automated gates are green: M3 felt-hue ΔE<12 PASS, M5
    highlight-clip on felt PASS, +B "fuzz not satin" (sheen ≤8%) PASS over a fresh GPU-faithful HERO
    (see 02-03-SUMMARY for the exact final values + verdicts). Fresh captures are in
    `.dev-stack/diag/table-3d/tp1/` (hero.png fov32, macro.png fov26, card.png POV fov40).
  </what-built>
  <how-to-verify>
    Judge the CLOTH MATERIALITY ONLY (D-05). Do NOT judge grounding, shadows, depth, AO, or vignette
    weight — those are deferred to TP5/TP6, and an ambiguous depth reaction must NOT trip this gate
    (SSOT §TP1, red-team #8).

    1. Bring the dev server up if needed (`cd frontend && npm run dev`) and open the lab live for an
       interactive read, OR review the fresh stills in `.dev-stack/diag/table-3d/tp1/`.
    2. POV read (the sheen/nap shot): open `http://localhost:5173/table-lab.html?cam=card` (fov40) and
       compare against `docs/table-3d/anchors/head/card.png` (the TP0 "before"). Question: does the felt
       now read as real woven baize with a contained grazing-angle nap shift — "fuzz", NOT satin/modern-
       casino green? The concentric weave should feel like cloth fibre, not a brushed-metal streak.
    3. MACRO read (the inlay + weave shot): open `?cam=macro` (fov26) and compare against
       `docs/table-3d/anchors/head/macro.png`. Questions: (a) are the born-in mark + 4 suits noticeably
       SHARPER than the TP0 baseline (D-04)? (b) does the mark stay BORN-IN (inlaid in the cloth, no
       floating decal, no z-fight halo)? (c) is the micro-nap a fine weave, not visible concentric "vinyl
       record" rings?
    4. Reference-floor check (both shots): compare against `docs/table-3d/anchors/reference-tag/{card,macro}.png`
       — the felt must NEVER read worse than the protected reference. If removing the vignette made it
       feel flat/floating, that is the one regression to watch (the fix is a small aoMapIntensity raise
       up to 0.25, still D-03; full depth is TP6).
    5. North star (D-05): overall, does the cloth read "físico, castizo, táctil, premium — sin deriva a
       Vegas/casino"?

    DISPOSITION (record in docs/table-3d/TP1_OPERATOR_AB.md):
    - UNAMBIGUOUS YES (cloth clearly improved, no satin/casino drift, mark born-in + sharper, not below
      reference) -> TP1 SHIPS. Record the felt scorecard delta (TP1 success target = felt +≥2, toward ≥4).
    - STOP-ON-AMBIGUOUS (cloth not clearly improved, OR any satin/casino drift sensed, OR mark lost
      born-in / z-fights, OR reads below reference): STOP. Allow ≤2 fix iterations (the executor adjusts
      within-band levers — sheen / sheenColor / normalScale / anisotropy / aoMapIntensity — re-captures,
      re-runs M3/M5/+B, re-presents). After 2 iterations without an unambiguous yes -> NON-BLOCKING
      rollback per SSOT §5.3 TP1 row: revert the felt material to the tp1-before-felt state (keep current
      felt) and PROCEED — TP1 does not block the program. Record which lever was tried and the final
      disposition.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" (ships) — OR describes the specific issue (satin / casino-green / brushed-
    streak / lost born-in / flat-vs-reference / inlay-not-sharper) to trigger a ≤2-iteration fix — OR
    types "rollback" to invoke the non-blocking keep-current-felt disposition. Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has read POV + MACRO against anchors/head/ and anchors/reference-tag/; the verdict +
    disposition (ship / iterate / non-blocking rollback) and the felt scorecard delta are recorded in
    docs/table-3d/TP1_OPERATOR_AB.md. No push/deploy/merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

TP1 is an OFFLINE, static WebGL lab (no auth/network/secrets/user-input; not in prod build). At this
gate the boundaries are the perceptual judgement seam and the reference floor.

| Boundary | Description |
|----------|-------------|
| automated gate -> human perceptual gate | M3/M5/+B are necessary-not-sufficient; the operator is the sole judge of "reads as woven baize" |
| TP1 felt -> protected reference floor | the felt must never read below anchors/reference-tag/; the operator confirms this perceptually (M12 whole-frame is informational only) |
| materiality scope -> deferred depth | grounding/AO/vignette are TP5/TP6; an ambiguous DEPTH reaction must not trip this materiality-only gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | felt reference floor | mitigate | Operator A/B explicitly checks vs anchors/reference-tag/; if below, non-blocking rollback keeps current felt |
| T-02-02 | Tampering | satin/casino drift | mitigate | Operator judges "fuzz not satin / no casino-green" at POV; stop-on-ambiguous on any drift |
| T-02-03 | Tampering | born-in mark / z-fight | mitigate | Operator MACRO check for born-in + no floating decal / halo before ship |
| T-02-GATE | Repudiation | the gate decision | mitigate | Verdict + disposition recorded in docs/table-3d/TP1_OPERATOR_AB.md (non-auto-approvable; autonomous:false) |
| T-02-SC | Tampering | npm installs | accept | No package installs in TP1 |
</threat_model>

<verification>
- The operator has reviewed POV (fov40) + MACRO (fov26) against anchors/head/ and anchors/reference-tag/.
- `docs/table-3d/TP1_OPERATOR_AB.md` records the verdict (ship / iterate / non-blocking rollback) and the scorecard felt delta.
- If shipped: M3/M5/+B were green (02-03) AND the operator judged the cloth materiality an unambiguous improvement with no casino/satin drift and the mark born-in.
- No push/deploy/merge (manual-deploy invariant); all commits LOCAL.
</verification>

<success_criteria>
- Operator materiality-only A/B complete at POV + MACRO; verdict + disposition recorded.
- Unambiguous yes -> TP1 ships with felt scorecard +≥2; or non-blocking rollback (keep current felt, proceed) after ≤2 iterations.
- Grounding/depth NOT judged here (deferred to TP5/TP6).
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-04-SUMMARY.md` when done, and write the verdict to `docs/table-3d/TP1_OPERATOR_AB.md`. Update the felt row in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP1 score if shipped.
</output>
