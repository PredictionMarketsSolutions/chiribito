---
phase: 02-tp1-felt-tapete-materiality-the-stage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lab/normalMapHelper.ts
  - frontend/src/lab/normalMapHelper.test.ts
  - docs/table-3d/TP1_A1_AOMAP_UV.md
autonomous: true
requirements: []   # TP1 has no REQUIREMENTS.md IDs — gated by SSOT §TP1 acceptance + metric thresholds
must_haves:
  truths:
    - "A rollback tag tp1-before-felt exists at pre-edit HEAD (431c0e5), LOCAL only"
    - "A shared height->normal Sobel helper exists at frontend/src/lab/normalMapHelper.ts (FIRST USE per SSOT §5.9), reusable by TP3/TP4/TP5"
    - "The helper has a passing Vitest unit test proving the Sobel->RGB encoding (flat field -> #8080ff up-normal)"
    - "Assumption A1 (aoMap UV channel in three 0.169) is resolved from installed shader source and recorded, so 02-02/02-03 know whether aoMap needs uv2 or must fall back to subtle-albedo-darken"
  artifacts:
    - path: "frontend/src/lab/normalMapHelper.ts"
      provides: "heightToNormalMap (Sobel), makeHeightCanvas, makeCanvas, toNormalMapTexture (gray/NoColorSpace wrapper)"
      exports: ["heightToNormalMap", "makeHeightCanvas", "makeCanvas", "toNormalMapTexture"]
      min_lines: 50
    - path: "frontend/src/lab/normalMapHelper.test.ts"
      provides: "Pure-math unit coverage of the Sobel encoding + invariants"
      contains: "heightToNormalMap"
    - path: "docs/table-3d/TP1_A1_AOMAP_UV.md"
      provides: "Resolution of A1 (aoMap uv vs uv2 in three 0.169) — the decision 02-02/02-03 follow"
      contains: "aomap_pars_fragment"
  key_links:
    - from: "frontend/src/lab/normalMapHelper.ts"
      to: "frontend/src/lab/textures.ts (feltNapNormalMap, 02-02)"
      via: "heightToNormalMap import — concentric height field -> tangent-space normal"
      pattern: "heightToNormalMap"
    - from: "git tag tp1-before-felt"
      to: "HEAD 431c0e5"
      via: "git tag (LOCAL, never pushed/deleted) — SSOT §5.3 per-phase rollback"
      pattern: "tp1-before-felt"
---

<objective>
Wave-0 foundation for TP1 (felt materiality). Three things, in order: (1) cut the per-phase
rollback tag `tp1-before-felt` at the current pre-edit HEAD BEFORE any felt edit (SSOT §5.3);
(2) resolve research Assumption A1 — read the installed three.js `aomap_pars_fragment` shader chunk
to decide whether `aoMap` reads `uv` or requires `uv2` on the felt PlaneGeometry, and record the
decision so the downstream plans wire edge-darkening correctly; (3) author the shared
height->normal Sobel helper (`normalMapHelper.ts`) — its FIRST USE in the program (SSOT §5.9),
reusable by TP3 chips / TP4 wood / TP5 — with a Vitest unit test proving the encoding.

Purpose: This plan touches NO render output. It establishes the safety net (tag), removes the one
runtime unknown (A1), and builds the pure-logic primitive (Sobel) that the nap normalMap depends on.
Doing it first keeps the GPU/capture plans (02-02, 02-03) clean and fast.

Output: `tp1-before-felt` tag (LOCAL); `frontend/src/lab/normalMapHelper.ts` + `.test.ts`;
`docs/table-3d/TP1_A1_AOMAP_UV.md` (the A1 decision record).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-CONTEXT.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md
@.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the tp1-before-felt rollback tag + resolve A1 (aoMap UV channel)</name>
  <files>docs/table-3d/TP1_A1_AOMAP_UV.md</files>
  <read_first>
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §5.3 (lines 187-207) — per-phase rollback tag is created BEFORE starting each TP; tag name convention `tp1-before-felt`; LOCAL, retained.
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §4 + Pitfall 4 + Assumption A1 (lines 287-332, 449-465, 616) — the aoMap UV2 question + the two resolution branches.
    - frontend/node_modules/three/src/renderers/shaders/ShaderChunk/aomap_pars_fragment.glsl.js — the authoritative source for which UV varying aoMap samples (`vAoMapUv` vs `vUv` vs `vUv2`).
    - frontend/node_modules/three/src/renderers/shaders/ShaderChunk/uv_pars_vertex.glsl.js — how aoMap UV varyings are assigned (whether `AOMAP_UV` falls back to `uv` when no uv2 is present).
  </read_first>
  <action>
    First, verify HEAD is the intended pre-edit commit (expected 431c0e5; no felt edits yet) and that
    only `tp0-before-rig` exists, then create `git tag tp1-before-felt` at HEAD (no `-f`, never push,
    never delete — SSOT §5.3). Do NOT make any commit before the tag; the tag MUST sit at the clean
    pre-edit HEAD. Then resolve Assumption A1: read the two installed three.js shader chunks named in
    read_first and determine, from source, whether `aoMap` in 0.169 samples a dedicated `uv2`/`vAoMapUv`
    varying that requires a `uv2` attribute on the geometry, OR whether it falls back to `vUv` (channel 1)
    when no second UV set is declared. Record the finding in `docs/table-3d/TP1_A1_AOMAP_UV.md` with the
    exact source lines quoted, and state the DECISION the downstream plans must follow as one of two
    branches: (A1-uv2) "aoMap needs uv2 → 02-03 adds `geometry.setAttribute('uv2', geometry.getAttribute('uv'))`
    via the felt-mesh JSX ref"; or (A1-uv1) "aoMap reads uv → wire aoMap directly, no uv2 needed". If the
    source is ambiguous, default to the documented fallback per D-03 / RESEARCH Pitfall 4 (bake a VERY
    subtle edge-darken into the albedo at low opacity, NOT a vignette) and record that as the chosen path.
    This is a code-reading + decision-recording task only — write NO TypeScript here.
  </action>
  <verify>
    <automated>git tag --list tp1-before-felt   # prints exactly: tp1-before-felt</automated>
    <automated>git rev-list -n 1 tp1-before-felt   # resolves to the pre-edit HEAD (431c0e5)</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list tp1-before-felt` outputs `tp1-before-felt` and `git rev-list -n 1 tp1-before-felt` equals the pre-edit HEAD (431c0e5); the tag is unpushed (LOCAL).
    - `docs/table-3d/TP1_A1_AOMAP_UV.md` exists, quotes the relevant lines from `aomap_pars_fragment.glsl.js`, and states exactly one DECISION branch (A1-uv2 | A1-uv1 | A1-albedo-fallback) for 02-02/02-03 to follow.
    - No felt render code touched; `git status` shows only the new doc (the tag is a ref, not a working-tree change).
  </acceptance_criteria>
  <done>Tag tp1-before-felt exists at pre-edit HEAD (LOCAL); A1 resolved from source and recorded with a single actionable decision branch for the edge-darkening wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Author the shared height->normal Sobel helper + Vitest unit test</name>
  <files>frontend/src/lab/normalMapHelper.ts, frontend/src/lab/normalMapHelper.test.ts</files>
  <read_first>
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-PATTERNS.md "normalMapHelper.ts (NEW)" + "normalMapHelper.test.ts (NEW)" sections (lines 25-171) — the exact makeCanvas/gray analog, the Sobel pixel-write idiom, and the canonical Vitest invariant style to copy.
    - frontend/src/lab/textures.ts lines 120-158 — `makeCanvas` (120-125), `clamp` (127-129), `speckle` pixel-loop idiom (132-143), `gray()` NoColorSpace wrapper (152-158): the patterns to reuse/re-export.
    - frontend/src/lab/cards.test.ts lines 1-98 — canonical Vitest pure-function test structure for frontend/src/lab/ (describe/it/expect, no vi.mock needed; happy-dom provides document.createElement).
    - .planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-RESEARCH.md §3 (lines 217-283) — the Sobel->RGB tangent-space math (R=+X, G=+Y, B=+Z, encode (n+1)/2*255) verified against three.js shader expectation, and the reuse plan (TP3/TP4/TP5).
  </read_first>
  <behavior>
    - Flat uniform-gray height field (all 128) -> every output pixel is the up-normal: R≈128, G≈128, B≈255 (±1 rounding).
    - B (Z) channel is always ≥ 128 across all pixels (Z component always positive — normal points out of surface).
    - R and G channels stay within [0,255] for any input (encoded unit-vector components).
    - strength=0 -> pure up-normal everywhere (no gradient amplification) regardless of input height variation.
    - Output canvas dimensions equal input canvas dimensions.
    - A known monotonic horizontal ramp (height increasing left->right) -> R channel biased away from 128 in the expected direction (sign matches the Sobel convention in 02-PATTERNS).
  </behavior>
  <action>
    Create `frontend/src/lab/normalMapHelper.ts` following the 02-PATTERNS analog exactly. Export:
    (1) `makeCanvas(w, h)` — local copy of the textures.ts helper (it is NOT exported from textures.ts),
    re-exported here so tests can build canvases without importing textures.ts;
    (2) `heightToNormalMap(heightCanvas: HTMLCanvasElement, strength = 1.0): HTMLCanvasElement` — the
    3x3 Sobel core: read luminance via getImageData with wrapping `(x+W)%W,(y+H)%H` boundary, compute
    `gx`/`gy` per 02-PATTERNS lines 98-99, set `nx=-gx*strength, ny=-gy*strength, nz=1.0`, normalize,
    encode each component as `round((n+1)*0.5*255)`, alpha 255, putImageData; returns the canvas;
    (3) `makeHeightCanvas(size, drawFn): HTMLCanvasElement` — convenience that builds a size×size canvas
    and runs drawFn(ctx, size);
    (4) `toNormalMapTexture(canvas): THREE.CanvasTexture` — the `gray()` wrapper (NoColorSpace, anisotropy 8)
    so callers get a correctly-decoded normal texture (Pitfall 7: normal maps must NEVER be sRGB).
    Then write `frontend/src/lab/normalMapHelper.test.ts` covering every item in <behavior> using the
    cards.test.ts describe/it/expect style (no vi.mock). Keep the file pure — no React, no R3F imports
    beyond `import * as THREE from "three"` for the texture wrapper. Do NOT inline any felt-specific
    concentric-ring drawing here; that belongs to feltNapNormalMap() in 02-02.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/normalMapHelper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lab/normalMapHelper.test.ts` exits 0 with all behaviors green.
    - `frontend/src/lab/normalMapHelper.ts` exports `heightToNormalMap`, `makeHeightCanvas`, `makeCanvas`, `toNormalMapTexture`.
    - `toNormalMapTexture` sets `colorSpace = THREE.NoColorSpace` (grep confirms NoColorSpace present; no `SRGBColorSpace` on the normal path).
    - The helper imports nothing from `./textures` (it is self-contained — re-declares makeCanvas/gray locally), so TP3/TP4 can consume it without pulling felt code.
  </acceptance_criteria>
  <done>normalMapHelper.ts compiles, its Vitest suite passes (flat->#8080ff, B≥128, strength=0->up-normal, ramp-sign), and it is dependency-free for downstream reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

TP1 is an OFFLINE, static WebGL lab (no auth, no network, no secrets, no user input, NOT in the prod
build). There are no security trust boundaries in the conventional sense. The "boundaries" that matter
are reference-integrity and scope boundaries.

| Boundary | Description |
|----------|-------------|
| pre-edit HEAD -> first felt edit | The rollback tag must be cut on the clean side of this boundary (SSOT §5.3) |
| lab (`frontend/src/lab/`) -> prod build | The lab entry `/table-lab.html` must stay out of the production build input (SSOT §5.8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | felt reference floor (anchors/reference-tag/) | mitigate | This plan changes no render output; the tag `tp1-before-felt` is the rollback anchor created before any edit |
| T-02-SCOPE | Tampering | game tree / prod build | mitigate | This plan only adds files under frontend/src/lab/ + docs/; no game-tree or vite build-input edit |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP1 (RESEARCH §"No new packages"; Package Legitimacy Audit N/A); nothing to verify |
</threat_model>

<verification>
- `git tag --list tp1-before-felt` prints the tag; `git rev-list -n 1 tp1-before-felt` resolves to the pre-edit HEAD.
- `cd frontend && npx vitest run src/lab/normalMapHelper.test.ts` exits 0.
- `docs/table-3d/TP1_A1_AOMAP_UV.md` records the A1 decision branch.
- `git status` shows only new files under `frontend/src/lab/` and `docs/table-3d/` (no game-tree edits; lab still absent from vite build input).
</verification>

<success_criteria>
- Rollback tag `tp1-before-felt` exists at the pre-edit HEAD (LOCAL, unpushed).
- The shared Sobel height->normal helper exists and is unit-tested (FIRST USE, reusable).
- A1 (aoMap UV channel) is resolved from installed source with a single actionable decision branch.
- Zero render-output change; no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/02-tp1-felt-tapete-materiality-the-stage/02-01-SUMMARY.md` when done.
</output>
