---
phase: 10-tp9-unificacion-aaa-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/table-3d/anchors/tp9-gate/hero.png
  - docs/table-3d/anchors/tp9-gate/card.png
  - docs/table-3d/anchors/tp9-gate/macro.png
  - docs/table-3d/anchors/tp9-gate/card-1080p.png
  - docs/table-3d/anchors/tp9-gate/m9/m9-hero-a.png
  - docs/table-3d/anchors/tp9-gate/m9/m9-hero-b.png
autonomous: true
requirements:
  - "SSOT §TP9 — ALL admitted §4.5 metrics PASS simultaneously at the 3 locked shots in a SINGLE consolidated run"
  - "SSOT §TP9 — Full A/B matrix vs the correct basis: NEW vs TP0-baseline AND vs M1 captures for cards/composition AND vs protected tag for felt/rail/chips/lighting"
  - "SSOT §TP9 — M4/M5 on ?nofx path (documented structural split); M6/M8/+A on ?fx-on path"
  - "SSOT §TP9 — M10 draw count recorded on BOTH ?fx-off AND ?fx-on; ?fx-on 177dc is documented-architecture-not-blocker"
  - "SSOT §TP9 — M9 byte-identical PASS at hero shot; M7 code assert PASS; 18-check structural invariant PASS"
  - "SSOT §TP9 — vitest green + tsc clean under src/lab before proceeding to Wave 2"

must_haves:
  truths:
    - "All 3 canonical frozen captures (hero.png, card.png, macro.png) at ?cam=*/&fx/spin=off are committed to docs/table-3d/anchors/tp9-gate/"
    - "run-metrics.mjs PASS/FAIL table recorded: M3/M4/M5 on ?nofx path (PASS); M6/M8/+A on ?fx-on hero (PASS)"
    - "M7 bloom code assert exits 0"
    - "M9 determinism: m9-hero-a.png and m9-hero-b.png are byte-identical (md5 match)"
    - "M10 draw counts recorded for all paths: ?fx-off hero (~52-62dc PASS <150); ?fx-on hero (~177dc documented-architecture); ?chips=full hero (~133dc PASS <220)"
    - "M12 regional MSE: heroFelt + heroBrass identity regions vs TP0 anchors/head/ — MSE <= 1.0 (no unintended change in must-not-change regions)"
    - "M1 card-1080p.png produced; rank-glyph px-height measured or carried from TP7 (>=22px PASS, operator confirm noted)"
    - "M2 carry-forward documented: 3.66x/2.60x from TP7/TP8 (no geometry change in TP8/TP9 labwork)"
    - "grep-check-tp8-09.cjs exits 0 (18/18)"
    - "vitest green (all 45 tests); tsc --noEmit clean under src/lab"
    - "PASS/FAIL consolidated matrix recorded in SUMMARY (every metric, every shot, with the ?nofx/?fx split documented)"
  artifacts:
    - path: "docs/table-3d/anchors/tp9-gate/hero.png"
      provides: "TP9 canonical frozen hero capture at ?cam=hero&fx&spin=off (2880x1800)"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp9-gate/card.png"
      provides: "TP9 canonical frozen card capture at ?cam=card&fx&spin=off"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp9-gate/macro.png"
      provides: "TP9 canonical frozen macro capture at ?cam=macro&fx&spin=off"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp9-gate/card-1080p.png"
      provides: "M1 downscaled card capture for rank-glyph px-height measurement"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp9-gate/m9/m9-hero-a.png"
      provides: "M9 determinism capture A"
      min_lines: 1
    - path: "docs/table-3d/anchors/tp9-gate/m9/m9-hero-b.png"
      provides: "M9 determinism capture B (byte-identical to A)"
      min_lines: 1
  key_links:
    - from: "docs/table-3d/anchors/tp9-gate/hero.png"
      to: "tools/table-3d/run-metrics.mjs"
      via: "run-metrics.mjs reads hero.png from tp9-gate dir for M3/M4/M5/M6/M8/+A/+B"
      pattern: "run-metrics"
    - from: "docs/table-3d/anchors/tp9-gate/hero.png"
      to: "docs/table-3d/anchors/head/hero.png"
      via: "M12 regional MSE compares tp9-gate vs TP0 anchor corpus for must-not-change regions"
      pattern: "zero-change"
    - from: "tools/table-3d/grep-check-tp8-09.cjs"
      to: "frontend/src/lab/TableLab.tsx"
      via: "18-check structural invariant asserter — forward-carry chain from TP1-TP8"
      pattern: "grep-check-tp8-09"
---

<objective>
Run the full consolidated metric matrix at all 3 locked money shots in a single session.
Capture the canonical TP9 frozen anchors, run every admitted §4.5 metric with the documented
?nofx/?fx split, confirm the forward-carry structural invariant chain, and record the complete
PASS/FAIL matrix in the SUMMARY.

Purpose: TP9 is the final unification phase. Before the scorecard can be signed off and the
operator gate opened, every admitted metric must PASS simultaneously at the 3 shots. A metric
failure here blocks promotion — the gate order is immutable: metrics first, then scorecard, then
operator gate. This plan completes the metrics leg so plan 10-02 can sign off the scorecard.

Output: 6 committed anchors (3 money shots + card-1080p + 2 M9 pair); full PASS/FAIL metric
matrix documented in SUMMARY with measured values, threshold, and the ?nofx/?fx split recorded.
No production code changed. LOCAL only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-tp9-unificacion-aaa-lock/10-CONTEXT.md
@.planning/phases/10-tp9-unificacion-aaa-lock/10-RESEARCH.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@docs/table-3d/TP8_OPERATOR_GATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture TP9 canonical frozen anchors at all 3 locked shots</name>
  <files>
    docs/table-3d/anchors/tp9-gate/hero.png
    docs/table-3d/anchors/tp9-gate/card.png
    docs/table-3d/anchors/tp9-gate/macro.png
    docs/table-3d/anchors/tp9-gate/m9/m9-hero-a.png
    docs/table-3d/anchors/tp9-gate/m9/m9-hero-b.png
  </files>
  <read_first>
    - .dev-stack/lab-shot.mjs — confirm the CLI signature: LAB_URL env var + output path positional arg;
      confirm that the harness auto-appends &spin=off to the URL before launching Playwright. Confirm
      the DPR and viewport (expected: 2880x1800 @ DPR2, D3D11/ANGLE). If the harness does NOT
      auto-append spin=off, you must include &spin=off in the LAB_URL explicitly.
    - docs/table-3d/TP8_OPERATOR_GATE.md — confirm the TP8 motion verdict (MOTION APPROVED / HALVED /
      REMOVED) and the final MICRO_AMPLITUDE_Y value shipped. If motion was approved, the hero capture
      with spin=off will still freeze it (M9 design). If motion was removed, no difference.
    - tools/table-3d/m9-determinism.mjs — confirm the CLI signature:
        node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp9-gate/m9
      Confirm whether the tool produces m9-hero-a.png and m9-hero-b.png in the --out dir, or just
      a single capture with a suffix. Confirm whether the md5 comparison is performed by the tool
      itself (prints PASS/FAIL) or must be done externally.
  </read_first>
  <action>
    PREREQUISITE: Vite dev server must be running on port 5181 before any capture command.
    Start it if not already running:
      cd frontend && npm run dev -- --port 5181
    Wait until the dev server prints the "ready" message before proceeding. Do not proceed with
    captures if the server is not up.

    STEP 1 — Create the tp9-gate directory tree:
      mkdir -p docs/table-3d/anchors/tp9-gate/m9
    (mkdir -p is idempotent; safe to run even if the dir exists.)

    STEP 2 — Run grep-check-tp8-09.cjs FIRST (the structural invariant forward-carry chain).
    This catches any perf-creep regression (ContactShadows frames={1} removed, bloom re-enabled,
    amplitude bounds blown) BEFORE spending time on captures:
      node tools/table-3d/grep-check-tp8-09.cjs
    Expected: exits 0 with "OK — grep-check-tp8-09: all 18 TP8 micro-motion + TP7 camera invariants hold".
    If it exits 1: read the failure message, fix the named issue in TableLab.tsx or the tool source,
    re-run, and confirm exit 0 before proceeding to captures. Do NOT skip this step.

    STEP 3 — Capture the 3 canonical frozen money shots (?fx-on, spin=off):
    These are the definitive TP9 final-state captures. ?fx-on is the shipped postprocessing stack
    (N8AO + DOF + BrightnessContrast + Vignette + Noise). spin=off freezes HeroMotion and OrbitControls.

      LAB_URL="http://localhost:5181/table-lab.html?cam=hero&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp9-gate/hero.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=card&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp9-gate/card.png
      LAB_URL="http://localhost:5181/table-lab.html?cam=macro&fx" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp9-gate/macro.png

    Note on harness behavior: if lab-shot.mjs does NOT auto-append &spin=off, include it explicitly
    in each LAB_URL (confirmed from read_first). If it does auto-append, do not duplicate it.

    STEP 4 — Run M9 determinism (byte-identical double-capture at hero shot):
    M9 confirms the frozen capture pipeline is deterministic (same pixels every run). The Noise effect
    in the EffectComposer is UV-seeded and must produce byte-identical output under spin=off.
      node tools/table-3d/m9-determinism.mjs --shot hero --port 5181 --out docs/table-3d/anchors/tp9-gate/m9
    Expected: the tool captures hero twice, computes md5 of each PNG, prints PASS (md5 match) or
    FAIL (md5 mismatch). Record the md5 hash value from the PASS message in the SUMMARY.
    If M9 FAIL: investigate — likely the Noise effect seed changed (e.g., a @react-three/postprocessing
    package update). If Noise opacity can be confirmed as 0.03/premultiply=false (unchanged since TP6),
    check whether the dev server hot-reloaded mid-capture and retry. Do NOT promote with M9 failing.

    STEP 5 — Produce card-1080p.png for M1 rank-glyph px-height (M1 downscale):
    M1 measures rank-glyph bbox height on the card capture downscaled to 1080p viewport pov.
    Use the m1DownscalePov helper from m1-m2-m12.mjs:
      node -e "
        import('./tools/table-3d/m1-m2-m12.mjs').then(m =>
          m.m1DownscalePov('docs/table-3d/anchors/tp9-gate/card.png').then(r => {
            require('fs').writeFileSync('docs/table-3d/anchors/tp9-gate/card-1080p.png', r.buffer);
            console.log('card-1080p.png written');
          })
        );
      "
    Then measure the rank-glyph bbox height visually on card-1080p.png. The threshold is >=22px.
    Last confirmed values: 80px at TP7 (?fx-on), 50px at TP6. Both well above the floor.
    If a pixel-ruler tool is unavailable, carry the TP7 value (80px) forward with an explicit
    audit note: "M1 carried from TP7 gate (80px >= 22px PASS; no card geometry or camera change
    in TP8 or TP9 labwork; operator manual confirm required per SSOT §4.5 M1 downgrade)."
    Record the measured or carried value in the SUMMARY.

    STEP 6 — Commit the captures:
    Stage and commit:
      docs/table-3d/anchors/tp9-gate/hero.png
      docs/table-3d/anchors/tp9-gate/card.png
      docs/table-3d/anchors/tp9-gate/macro.png
      docs/table-3d/anchors/tp9-gate/card-1080p.png
      docs/table-3d/anchors/tp9-gate/m9/m9-hero-a.png
      docs/table-3d/anchors/tp9-gate/m9/m9-hero-b.png
    Commit message: "chore(10-01): TP9 gate anchors -- 3 frozen money shots + M9 pair + card-1080p (spin=off, fx)"

    Do NOT change any source code (TableLab.tsx, *.ts, *.tsx, tool scripts). If grep-check-tp8-09
    fails at STEP 2 due to a code issue, fix the code, re-run the check, then proceed to captures.
    The fix must be the minimum change needed to pass the structural invariant (e.g., restore
    frames={1} on ContactShadows). Record any such fix in the SUMMARY.
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp8-09.cjs</automated>
    <automated>node -e "const f=require('fs');['docs/table-3d/anchors/tp9-gate/hero.png','docs/table-3d/anchors/tp9-gate/card.png','docs/table-3d/anchors/tp9-gate/macro.png','docs/table-3d/anchors/tp9-gate/card-1080p.png','docs/table-3d/anchors/tp9-gate/m9/m9-hero-a.png','docs/table-3d/anchors/tp9-gate/m9/m9-hero-b.png'].forEach(p=>{if(!f.existsSync(p)){console.error('MISSING: '+p);process.exit(1);}});console.log('OK -- all 6 tp9-gate captures present');"</automated>
  </verify>
  <done>
    grep-check-tp8-09.cjs exits 0 (18/18). Six anchors committed to docs/table-3d/anchors/tp9-gate/.
    M9 md5 hash recorded (PASS). M1 rank-glyph px-height recorded or carried from TP7 (>=22px PASS).
    No source code changed (or minimum fix applied + documented). LOCAL only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full consolidated metric matrix (?nofx/?fx split) and record PASS/FAIL table</name>
  <files>
    docs/table-3d/anchors/tp9-gate/hero.png
    docs/table-3d/anchors/tp9-gate/card.png
    docs/table-3d/anchors/tp9-gate/macro.png
  </files>
  <read_first>
    - tools/table-3d/run-metrics.mjs — confirm that: (a) the tool runs against the dir (reads
      hero.png from the provided dir path); (b) which metrics it covers (expected: M3/M4/M5/M6/M8/+A/+B);
      (c) the output format (PASS/FAIL table with measured values and thresholds). The tool must be
      invoked separately for ?nofx and ?fx-on captures because M4/M5 are structurally designed for
      the ?nofx path (per TP6 gate doc and RESEARCH.md §Critical Finding).
    - tools/table-3d/stats-read.mjs — confirm CLI signature:
        node tools/table-3d/stats-read.mjs http://localhost:<PORT> <shot> "<query-suffix>"
      e.g.: node tools/table-3d/stats-read.mjs http://localhost:5181 hero "&fx"
      Confirm what the third argument controls and that "&fx" adds ?fx to the shot URL.
    - docs/table-3d/TP6_OPERATOR_GATE.md — read the M10 draw-count values recorded at TP6:
      ?fx-off ~62dc, ?fx-on ~177dc, ?chips=full ~133dc. These are the expected values; if TP9
      values differ substantially (e.g., M10 > 200 on ?fx-on), investigate before proceeding.
    - docs/table-3d/anchors/head/ — confirm the directory exists and contains hero.png, macro.png
      (the TP0 anchor corpus for M12 regional MSE comparison).
  </read_first>
  <action>
    PREREQUISITE: dev server running on port 5181; tp9-gate captures from Task 1 present.

    IMPORTANT — THE ?nofx/?fx METRIC SPLIT (document this clearly in the SUMMARY):
    M4 (brass-not-gold) and M5 (highlight-clip) are designed for and pass on the ?nofx path.
    On ?fx-on, the BrightnessContrast effect shifts brass hue ~-2.4° (below M4 floor) and DOF
    creates highlight halos that push M5 frameClipPct to ~28.6% (above M5 ceiling). This is
    DOCUMENTED in the TP6 gate record — these are expected structural effects of the postprocessing
    compositor, NOT regressions. M6, M8, +A are postprocessing-dependent metrics that require ?fx-on.
    The consolidated matrix records BOTH paths; the split is not a gap but the intended test design.

    STEP 1 — Capture the ?nofx hero shot for M4/M5 measurement:
    This is a SEPARATE capture from the ?fx-on anchors committed in Task 1. Capture to a temp path:
      LAB_URL="http://localhost:5181/table-lab.html?cam=hero" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp9-gate/hero-nofx.png
    Note: do NOT commit hero-nofx.png as a permanent anchor. It is a measurement-only capture for
    M4/M5. If lab-shot.mjs does not auto-append &spin=off, add it to the LAB_URL.

    STEP 2 — Run run-metrics.mjs on the ?nofx hero capture (M4/M5 path):
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp9-gate/hero-nofx.png
    If run-metrics.mjs expects a directory (reads hero.png from the dir), either:
      (a) Copy hero-nofx.png to a temp dir as hero.png and point the tool there, or
      (b) Invoke run-metrics.mjs with the exact flag/path the tool supports for single-file mode.
    Determine the correct invocation from the read_first pass on run-metrics.mjs.
    Record: M4 measured value (H°/S/V) vs threshold H 35-48°/S<=0.55/V<=0.80; M5 measured
    frameClipPct vs threshold <0.5% felt / <1.5% frame.
    Expected: M4 PASS (H~35.4°, S~0.52, V~0.715); M5 PASS (0% / 0% from TP5).

    STEP 3 — Run run-metrics.mjs on the ?fx-on captures (M3/M6/M8/+A/+B path):
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp9-gate
    This reads hero.png (?fx-on) from the tp9-gate dir. Covers: M3 (felt hue ΔE), M6 (contact
    shadow delta), M8 (vignette band), +A (warm corner), +B (felt specular). Also reports M4/M5
    on the ?fx-on path — these will FAIL as expected; document them as "expected structural
    behavior of the postprocessing compositor; M4/M5 pass on ?nofx path (Step 2)."
    Expected results on ?fx-on hero.png:
      M3: ΔE < 12 (last: ~8.5 at TP1) — PASS
      M6: >= 12% crevice delta (last: 27.74% at TP6) — PASS
      M8: 8-20% vignette band (last: 13.97% at TP6, recalibrated felt lateral rects) — PASS
      +A: cornerLuma >= 18 / hue 15-75° (last: 31.9 / 29.1° at TP6) — PASS
      +B: < 8% felt sheen (last: < 8% at TP1) — PASS

    STEP 4 — M7 bloom code assert:
      node tools/table-3d/m7-bloom-assert.mjs
    Expected: exits 0 with 0 Bloom tokens found. PASS.

    STEP 5 — M10 draw count at all paths (BOTH ?fx-off and ?fx-on):
    Record the clean-scene path (the SSOT <150 gate) and the postprocessing overhead (documented architecture):
      node tools/table-3d/stats-read.mjs http://localhost:5181 hero ""
      (This covers ?nofx/default — expected ~52-62dc; SSOT gate PASS <150)
      node tools/table-3d/stats-read.mjs http://localhost:5181 hero "&fx"
      (This covers ?fx-on — expected ~177dc; above <150 ceiling but documented architecture)
      node tools/table-3d/stats-read.mjs http://localhost:5181 card "&fx"
      node tools/table-3d/stats-read.mjs http://localhost:5181 macro "&fx"
      node tools/table-3d/stats-read.mjs http://localhost:5181 hero "&fx&chips=full"
      (chips=full path — expected ~133dc; SSOT gate PASS <220)
    Record every draw count. If any ?fx-off value exceeds 150 or ?chips=full exceeds 220: STOP and
    investigate before proceeding (perf creep). The ?fx-on ~177dc ceiling exceedance is
    pre-documented and not a blocker; record it with the explicit note:
      "M10 ?fx-on 177dc exceeds the <150 scene ceiling. This is the documented DOF multi-pass
      postprocessing overhead (+115dc over the 62dc base scene), recorded as architecture not
      uncontrolled creep. The SSOT §8 perf guardrail (<150) is satisfied by the clean scene path."

    STEP 6 — M12 regional MSE (zero-change proof vs TP0 anchor corpus):
    M12 compares TP9 captures vs the TP0 anchors/head/ corpus for MUST-NOT-CHANGE identity regions
    (heroFelt hue patch, heroBrass reveal). This is a zero-change guard for those regions, NOT a
    perceptual improvement proof (the ?fx-on frames differ from the bare TP0 frames by design).
      node tools/table-3d/m1-m2-m12.mjs --zero-change docs/table-3d/anchors/tp9-gate docs/table-3d/anchors/head
    Expected: MSE <= 1.0 for the identity regions. If MSE is non-zero over felt/brass identity regions
    (not the global frame — just those specific rect patches), investigate before promoting.
    Record the MSE value in the SUMMARY.

    STEP 7 — M2 carry-forward audit:
    TP8 added only HeroMotion (returns null, no scene geometry). TP9 adds no scene geometry.
    Therefore M2 is unchanged from the TP7/TP8 recorded value: 3.66x (hero) / 2.60x (card).
    Confirm by reading the TP8_OPERATOR_GATE.md M2 entry. Record in SUMMARY:
      "M2: 3.66x hero / 2.60x card (TP7-TP8 value, unchanged — TP9 adds no scene geometry).
      M2 floor >= 2x: PASS."
    If for any reason the TP8 gate doc records a different value, use that value and confirm >= 2x.

    STEP 8 — vitest + tsc gate:
      cd frontend && npx vitest run
      cd frontend && npx tsc --noEmit 2>&1 | grep "src/lab"
    Both must pass (0 failures). Record results in SUMMARY.

    STEP 9 — Build the consolidated PASS/FAIL matrix for the SUMMARY.
    Structure it as a table covering every admitted metric, the path it was measured on, the
    measured value, the threshold, and the verdict:

    | Metric | Path | Measured Value | Threshold | Verdict |
    |--------|------|----------------|-----------|---------|
    | M1 legibility | card.png ?fx-on (1080p downscale) | [px] | >=22px | PASS (or CARRY from TP7) |
    | M2 cards-vs-chips | hero+card (TP7/TP8 carry) | 3.66x/2.60x | >=2.0x | PASS |
    | M3 felt hue ΔE | hero.png ?fx-on | [ΔE] | <12 | PASS |
    | M4 brass-not-gold | hero-nofx.png ?nofx | H[°]/S/V | H35-48°/S<=0.55/V<=0.80 | PASS (?nofx) |
    | M5 highlight-clip | hero-nofx.png ?nofx | [felt%/frame%] | <0.5%/<1.5% | PASS (?nofx) |
    | M6 contact shadow | hero.png ?fx-on | [%delta] | >=12% | PASS |
    | M7 bloom code assert | lab source | 0 tokens | 0 | PASS |
    | M8 vignette band | hero.png ?fx-on | [%] | 8-20% | PASS |
    | M9 determinism | hero (2x capture) | [md5 hash] | byte-identical | PASS |
    | M10 draws ?fx-off | hero | [dc] | <150 | PASS |
    | M10 draws ?fx-on | hero | ~177dc | [architecture note] | DOCUMENTED (not blocker) |
    | M10 draws ?chips=full | hero | ~133dc | <220 | PASS |
    | M12 regional MSE | hero+macro vs anchors/head | [MSE] | <=1.0 | PASS |
    | +A warm corner | hero.png ?fx-on | luma/hue | luma>=18/hue15-75° | PASS |
    | +B felt specular | hero.png ?fx-on | [%sheen] | <8% | PASS |
    | grep-check-tp8-09 | TableLab.tsx | 18/18 | 18/18 | PASS |
    | vitest | frontend/src/lab/ | 45/45 | all green | PASS |
    | tsc --noEmit | src/lab | 0 errors | 0 | PASS |

    Also record the A/B basis clearly:
      NEW vs TP0-baseline: tp9-gate/ vs anchors/head/ — M12 MSE confirms no unintended change
        in identity regions; perceptual A/B (overall read) is the operator's judgment at the
        FINAL gate (plan 10-03).
      NEW vs M1-capture corpus (cards/composition): tp9-gate/card.png vs TP7/TP2 gate card captures
        (M1 px-height stable: ~80px / ~50px across gates; no card geometry change post-TP2;
        encuadre frozen at TP2 with operator-approval).
      NEW vs protected tag (felt/rail/chips/lighting): M12 covers identity regions;
        perceptual comparison (felt greener/warmer, chips more matte, lighting more shaped) is
        the operator's comparative read at the FINAL gate (plan 10-03).

    NOTE ON M11 (frame-time): M11 cannot be reliably measured under headless Playwright rAF
    (documented TP0 limitation — headless rAF throttles, giving unreliable timing). M11 is an
    operator-confirm gate. Record in SUMMARY: "M11 frame-time: operator-confirm at TP9 FINAL gate
    (plan 10-03 live view). RTX 4060 confirmed comfortable at 177dc at TP6 gate — no new draw-call
    creep detected (M10 stable). Headless measurement unreliable per documented TP0 limitation."

    If ANY metric other than the documented M10 ?fx-on and M11 fails: STOP. Do NOT proceed to
    plan 10-02 scorecard sign-off. Record the failing metric and the blocking condition in the
    SUMMARY and surface it to the orchestrator.
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp9-gate</automated>
    <automated>node tools/table-3d/m7-bloom-assert.mjs</automated>
    <automated>node tools/table-3d/grep-check-tp8-09.cjs</automated>
    <automated>cd frontend && npx vitest run</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep "src/lab" | head -10</automated>
  </verify>
  <done>
    Consolidated PASS/FAIL matrix recorded in SUMMARY covering all admitted metrics with measured
    values and thresholds. The ?nofx/?fx split is documented. M10 ?fx-on overhead noted as
    architecture not blocker. M9 byte-identical PASS with md5 hash. M12 MSE <= 1.0 for identity
    regions. vitest 45/45 green. tsc clean under src/lab. No metric failure (excluding documented
    ?fx-on M10 overhead and M11 deferred to operator gate). LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev server -> Playwright harness | Captures are trusted only if spin=off is active (HeroMotion frozen); confirm harness appends it |
| ?nofx capture -> M4/M5 measurement | M4/M5 must be run on the clean scene path; using the ?fx-on hero.png for M4/M5 produces expected structural failures (not regressions) |
| tp9-gate/ captures -> M12 comparison | M12 identity-region comparison is valid only if the anchors/head/ TP0 corpus is intact; verify before running |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01a | Tampering | Capture taken WITHOUT spin=off (HeroMotion active) — M9 will fail | mitigate | Task 1 read_first confirms harness auto-appends spin=off; if not, spin=off is explicit in LAB_URL; M9 double-capture is the verification gate |
| T-10-01b | Information Disclosure | M4/M5 ?fx-on failures interpreted as regressions | mitigate | RESEARCH.md §Critical Finding documents the split; SUMMARY must explicitly label ?fx-on M4/M5 failures as "expected structural behavior, not regression" |
| T-10-01c | Tampering | M10 ?fx-on 177dc treated as a blocking failure | mitigate | Action STEP 5 and RESEARCH.md §Critical Finding document this as architecture overhead; SUMMARY records it as "documented not blocker" |
| T-10-01d | Tampering | grep-check-tp8-09 skipped before captures (perf creep undetected) | mitigate | Task 1 STEP 2 runs grep-check FIRST; action block states "Do NOT skip this step" |
| T-10-01e | Tampering | M12 anchors/head/ corpus not intact (captures accidentally overwritten) | mitigate | Task 2 read_first confirms anchors/head/ exists and contains hero.png, macro.png; if missing, regenerate from the tag before running M12 |
| T-10-01f | Tampering | Late grade/tone-map change between Task 1 captures and Task 2 metric run | mitigate | No source code is changed in this plan; if any fix is needed (grep-check failure), the minimum change is applied + documented before captures; no tone-map or grade changes are permitted |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all tooling established from TP0 |
</threat_model>

<verification>
- grep-check-tp8-09.cjs exits 0 (18/18) — run before any capture.
- 6 anchors committed: hero.png, card.png, macro.png (3 canonical ?fx-on frozen shots), card-1080p.png, m9/m9-hero-a.png, m9/m9-hero-b.png.
- M9: md5 of m9-hero-a.png == md5 of m9-hero-b.png (PASS); hash recorded in SUMMARY.
- run-metrics.mjs on tp9-gate dir (?fx-on path): M3/M6/M8/+A/+B all PASS.
- run-metrics.mjs on ?nofx hero capture: M4/M5 PASS (on clean scene path).
- M7 bloom code assert exits 0.
- M10 recorded: ?fx-off ~52-62dc PASS <150; ?fx-on ~177dc documented architecture; ?chips=full ~133dc PASS <220.
- M12 regional MSE <= 1.0 for heroFelt + heroBrass identity regions.
- M1 px-height recorded or carried from TP7 (>=22px PASS).
- M2 carry-forward documented (3.66x/2.60x >= 2.0x PASS).
- vitest 45/45 green; tsc clean under src/lab.
- PASS/FAIL matrix in SUMMARY covers every admitted metric with measured values, thresholds, and ?nofx/?fx split documented.
- A/B basis documented (NEW vs TP0 via M12; NEW vs M1 corpus; NEW vs protected tag — perceptual A/B deferred to 10-03 operator gate).
- No source code changed (or minimum fix applied + documented). LOCAL only.
</verification>

<success_criteria>
- Every admitted metric recorded as PASS (with M10 ?fx-on and M11 explicitly documented as architecture/deferred, not failures).
- The ?nofx/?fx split is clearly documented in the SUMMARY.
- All 6 captures committed and verified present.
- grep-check-tp8-09.cjs exits 0 — the TP1-TP8 forward-carry chain is intact.
- vitest + tsc green.
- Plan 10-02 has a complete, unambiguous metric record to read from the SUMMARY.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/10-tp9-unificacion-aaa-lock/10-01-SUMMARY.md` when done.
Record: the full PASS/FAIL consolidated metric matrix (every metric, measured value, threshold,
verdict); the ?nofx/?fx split with the M4/M5 measured values on ?nofx; M9 md5 hash; M10 draw
counts on all 4 paths; M12 MSE value; M1 measured or carried px-height value; M2 carry-forward
value; grep-check-tp8-09 18-check verdict; vitest/tsc results; A/B basis documentation; and any
unexpected findings. If any metric fails (other than documented exceptions), record it prominently
as BLOCKING with the failing value and name the issue before plan 10-02 proceeds.
</output>
