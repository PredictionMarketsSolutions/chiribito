---
phase: 10-tp9-unificacion-aaa-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/table-3d/SCORECARD_TABLE_3D.md
autonomous: true
requirements:
  - "SSOT §TP9 — Scorecard ALL-GREEN: every element >=4/5; record post-TP9 column + total vs baseline"
  - "SSOT §TP9 — Elements 12 (cameras), 14 (social-read), 15 (premium-overall) get explicit TP9 sign-off scores"
  - "SSOT §TP9 — TP6-flagged lighting (8) and tactility (13) honestly reconciled (scores stand if rubric criteria documented)"
  - "SSOT §TP9 — Grade/tone-map: ACES Filmic exposure=1.05 confirmed and locked; AgX only if washing observed (none documented)"
  - "SSOT §TP9 — §8 mesa terminada checklist verified item-by-item against Wave 1 metric results"
  - "SSOT §TP9 — If any element is <4: FLAG + name the failing phase + DO NOT ADVANCE to 10-03"

must_haves:
  truths:
    - "SCORECARD_TABLE_3D.md post-TP9 column filled in for all 15 elements (every element >=4)"
    - "Elements 12 (cameras), 14 (social-read), 15 (premium-overall) have explicit scores recorded with rubric justification"
    - "Lighting (8) and tactility (13) scores at 5 confirmed with documentable rubric criterion evidence from 10-01-SUMMARY"
    - "Scorecard total vs baseline recorded (baseline 51/75; post-TP8 pre-sign-off 64/75; post-TP9 anticipated 67-70/75)"
    - "ACES Filmic exposure=1.05 decision recorded as LOCKED (no change made; AgX not triggered)"
    - "All 14 §8 mesa terminada checklist items verified item-by-item with explicit PASS/NOTE for each"
    - "§8 item 11 (perf guardrail) cross-referenced to 10-01-SUMMARY M10 values"
    - "§8 item 13 (metric integrity) notes the positive-control validation performed in Wave 1"
    - "§8 item 14 (verdict) is NOT filled in here — that is the operator's domain at plan 10-03"
  artifacts:
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP9 score column for all 15 elements; TP9 log entries; scorecard total vs baseline"
      contains: "TP9"
  key_links:
    - from: "docs/table-3d/SCORECARD_TABLE_3D.md"
      to: ".planning/phases/10-tp9-unificacion-aaa-lock/10-01-SUMMARY.md"
      via: "Scorecard TP9 log entries reference the 10-01 metric matrix results for justification"
      pattern: "10-01-SUMMARY"
    - from: "docs/table-3d/SCORECARD_TABLE_3D.md"
      to: "docs/ROADMAP_TABLE_3D_PERFECTION.md §8"
      via: "§8 checklist is verified item-by-item against Wave 1 results; 14/14 PASS required for TP9 scorecard sign-off"
      pattern: "mesa terminada"
---

<objective>
Sign off the scorecard all-green, record the grade/tone-map finalization decision, and verify
the §8 "mesa terminada" checklist item-by-item using the Wave 1 metric results.

Purpose: The gate order is immutable — metrics first (plan 10-01), then scorecard sign-off (this
plan), then operator FINAL gate (plan 10-03). This plan consumes the PASS/FAIL matrix from
10-01-SUMMARY, fills in the TP9 column for all 15 scorecard elements, and produces the
all-green verdict that the operator gate reads as its pre-condition. Elements 12 (cameras),
14 (social-read), and 15 (premium-overall) receive their first explicit TP9 scores. The TP6-flagged
lighting and tactility scores are honestly reconciled. The §8 checklist is crossed against Wave 1
data. If any element is below 4, this plan halts and flags it rather than advancing.

Output: SCORECARD_TABLE_3D.md with post-TP9 column filled in (all 15 elements >=4); TP9 log
entries; scorecard total vs baseline; grade/tone-map decision recorded; §8 checklist verified.
No code changes. LOCAL only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-tp9-unificacion-aaa-lock/10-CONTEXT.md
@.planning/phases/10-tp9-unificacion-aaa-lock/10-RESEARCH.md
@.planning/phases/10-tp9-unificacion-aaa-lock/10-01-SUMMARY.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@docs/table-3d/TP8_OPERATOR_GATE.md
@docs/table-3d/TP7_OPERATOR_GATE.md
@docs/table-3d/TP6_OPERATOR_GATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill in TP9 scorecard column + grade/tone-map decision + §8 checklist</name>
  <files>docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - .planning/phases/10-tp9-unificacion-aaa-lock/10-01-SUMMARY.md — READ THE FULL FILE.
      This is the primary input to every score justification. Extract:
        (a) Every metric PASS/FAIL verdict and measured value from the consolidated matrix.
        (b) M9 md5 hash and PASS status.
        (c) M10 draw-count values (?fx-off ~52-62dc, ?fx-on ~177dc, ?chips=full ~133dc).
        (d) M12 MSE value for the identity regions.
        (e) M1 px-height value (measured or TP7 carry).
        (f) Any unexpected findings or blocking flags. If ANY metric is flagged BLOCKING in
            10-01-SUMMARY, HALT immediately — do not update SCORECARD — and surface the
            blocking condition to the orchestrator.
    - docs/table-3d/SCORECARD_TABLE_3D.md — READ THE FULL CURRENT FILE before editing.
        (a) Understand the exact table structure (column headers, row order, score format).
        (b) Identify the post-TP8 column (the most recent scores) to confirm no regression is
            introduced by the TP9 column.
        (c) Identify the existing TP9 log entry section if any (there may be placeholder rows).
    - docs/ROADMAP_TABLE_3D_PERFECTION.md — lines 511-553 (§8 "mesa terminada" checklist, 14 items
      verbatim). Do NOT rely on RESEARCH.md's §8 verbatim restatement alone — confirm directly from
      the SSOT ROADMAP for the checklist text. Note: if lines 511-553 are not the exact §8 range,
      adjust the offset to find "mesa terminada" or "§8".
    - docs/table-3d/TP6_OPERATOR_GATE.md — the specific values that justify lighting (element 8)
      at AAA(5): key angle, ratio, hemisphere bounce, rim, M8 13.97%, +A 31.9/29.1°. These are the
      rubric-criterion reference for the TP9 honest reconciliation.
    - docs/table-3d/TP8_OPERATOR_GATE.md — the TP8 motion verdict and the specific values that
      justify tactility (element 13) at AAA(5) static level; also the social-read completeness record
      for element 14 and the non-blocking-rollback status.
    - docs/table-3d/TP7_OPERATOR_GATE.md — the cameras (element 12) evaluation record: the three
      money shots (fov:32/40/26) confirmed operator-blessed at TP7; the "AAA(5) deferred to TP9
      final scorecard where all 15 elements are evaluated against the completed table" note.
  </read_first>
  <action>
    PRE-CONDITION CHECK: Before any SCORECARD edit, confirm from 10-01-SUMMARY that no metric
    is flagged BLOCKING. If 10-01-SUMMARY contains a BLOCKING flag: do NOT edit SCORECARD_TABLE_3D.md.
    Write a SUMMARY note: "10-02 HALTED — metric BLOCKING condition from 10-01: [failing metric].
    Plan 10-03 MUST NOT be started. Orchestrator must surface this to the operator and return to
    the named failing phase." Then stop.

    If no BLOCKING condition: proceed with the following steps.

    STEP 1 — Scorecard all-green evaluation: score all 15 elements for the TP9 column.

    For each element, apply the criterion honestly. The rubric target is every element >=4. If any
    element scores 3 or below, FLAG it and name the phase responsible — do NOT promote.

    Use the following evaluations (derived from RESEARCH.md and gate records):

    Element 1 (felt): Post-TP8 = 4. TP9 re-evaluation: M3 PASS from 10-01-SUMMARY (ΔE confirmed
    <12); M8 PASS (vignette band 8-20%); born-in mark, crisp inlay, directional nap sheen. No new
    felt work in TP9. Score: 4 (unchanged; no TP9 work on felt beyond confirming metrics).

    Element 2 (cards): Post-TP8 = 4. TP9 re-evaluation: M1 PASS (>=22px; TP7-carried or measured);
    M2 PASS (3.66x/2.60x >= 2x); Fournier faces, real card stock impression, protagonist read.
    No new card geometry in TP8/TP9. Encuadre frozen at TP2. Score: 4 (unchanged; AAA(5) requires
    CARD_W encuadre pass per prior convention — not delivered in this program).

    Element 3 (chips): Post-TP8 = 4. TP9 re-evaluation: M10 ?chips=full ~133dc PASS <220; matte
    clay, de-Vegas, demoted recessive, instanced. M2 confirms chips are not dominating. Score: 4
    (unchanged; operator-gated at 3 chips/stack per TP3; AAA(5) deferred per TP3 gate).

    Element 4 (leather rail): Post-TP8 = 4. TP9 re-evaluation: welt/cord at seam, pebbled normal,
    leatherNapNormalMap, per-arc UV on oval ends. AO (N8AO) crevice darkening at seams from TP6.
    Score: 4 (unchanged).

    Element 5 (wood coaming): Post-TP8 = 4. TP9 re-evaluation: normalMap + per-arc-length UV. Per
    RESEARCH.md open question Q3: AAA(5) grain-follows-oval was not delivered (TP7 did not address
    per-arc UV). Score: 4 (unchanged; AAA(5) deferred — carried to future program if needed; 4 meets
    the all-green criterion).

    Element 6 (brass): Post-TP8 = 4. TP9 re-evaluation: M4 PASS on ?nofx (H~35.4°, aged-brass
    within HSV target, not gold); M12 heroBrass MSE <= 1.0 (identity region unchanged). Score: 4
    (unchanged).

    Element 7 (body/contour): Post-TP8 = 4. TP9 re-evaluation: contour elegance held, no furniture
    mass added (the body pre-existed). Per §8 item 4 note: "no furniture mass was ADDED." grep-check
    confirms rail/slim default (yTop=0.28). Score: 4 (unchanged).

    Element 8 (lighting): Post-TP8 = 5 (AAA). TP9 HONEST RECONCILIATION (SSOT required):
    Apply the specific AAA(5) rubric criteria against the TP6 gate evidence:
      (1) Shaped warm gradient (key+fills+rim): SATISFIED — TP5 key angle=0.72/ratio=2.75x,
          fill=0.8, hemisphere green-bounce, directional back rim, floor wash pointLight.
      (2) Warm floor bounce washing apron: SATISFIED — pointLight at y=-0.25 color=#ffcd95.
      (3) No casino uplighting: SATISFIED — anti-casino sentinel ratio 2.75x < 3.5x ceiling; M7=0 Bloom.
      (4) M8 PASS (corners 8-20% below center): SATISFIED — 13.97% at TP6 (recalibrated felt lateral rects).
      (5) +A cornerLuma >= 18, hue warm: SATISFIED — 31.9/29.1° at TP6.
    All 5 rubric criteria are documentably satisfied by gate evidence. Score: 5 (CONFIRMED — not inflated).

    Element 9 (shadows): Post-TP8 = 5 (AAA). TP9 re-evaluation: PCSS soft shadows; baked
    ContactShadows frames={1} (grep-check CHECK 15 confirms); N8AO crevice darkening M6 27.74% (PASS).
    grep-check-tp8-09 confirms frames={1} (CHECK 15). Score: 5 (confirmed).

    Element 10 (depth): Post-TP8 = 5 (AAA). TP9 re-evaluation: N8AO + DOF (EffectComposer confirmed
    by grep-check CHECK 8-10); vignette M8 PASS; faint filmic grade/grain; M7=0 Bloom. Score: 5 (confirmed).

    Element 11 (composition): Post-TP8 = 5 (AAA). TP9 re-evaluation: §8 item 8 — no dead zones;
    CENTER table-state (deck stub/dealer button/center discard); cards>board>rail layering; no per-seat
    objects (M2 >= 2x; no per-seat geometry per grep-check + social-read audit). Score: 5 (confirmed).

    Element 12 (cameras): Post-TP8 = 4 (AAA(5) deferred to TP9 sign-off per TP7 gate). TP9 SIGN-OFF:
    Apply the rubric evaluation framework stated at TP7:
    "At TP9, if the three presets still read as definitive money shots on the completed TP1-TP8 table,
    the score can legitimately move to 5."
    Evidence: Three locked money shots (fov:32 hero / fov:40 card / fov:26 macro) confirmed by
    grep-check CHECK 17; autoRotate={false} confirmed by CHECK 16; no second makeDefault camera by
    CHECK 18; M9 PASS (deterministic under spin=off). The three presets each tell a different story
    (hero = overview/social-read; card = protagonist close-up; macro = materiality) and are
    operator-blessed as the finished-table views since TP7. This is the TP9 evaluation — score moves.
    Score: 5 (TP9 sign-off — three locked money shots, each distinctive, confirmed on the completed table).

    Element 13 (tactility): Post-TP8 = 5 (AAA, static; live-feel FLAGGED for TP9 confirm). TP9 SIGN-OFF:
    HONEST RECONCILIATION against the specific AAA(5) rubric criteria:
      (1) "You could pick it up" test (felt nap, leather softness, wood grain, brass weight, clay chip
          texture — all present and distinct): SATISFIED at STATIC level — TP4 craft levers + TP5
          per-material specular + TP6 N8AO crevice darkening all confirmed by metric matrix.
      (2) Macro shot reads like a product photograph: SATISFIED at STATIC level — M6 27.74% PASS (N8AO
          inter-material crevice darkening confirmed at TP6); macro.png is a genuine product-photo read.
    The static tactility AAA(5) criteria are documentably satisfied. The LIVE tactile feel via
    HeroMotion (TP8) is a reinforce-or-confirm dimension — if the operator judges it sub-threshold
    at the 10-03 live gate, the static score is unaffected (the TP8 APPROVED/REMOVED disposition
    from TP8_OPERATOR_GATE.md is what governs the live-feel note).
    Score: 5 (CONFIRMED at static level; live-feel note from TP8 gate carried to 10-03 for the
    operator's live-view confirmation).

    Element 14 (social-read): Post-TP8 = 4 (potential AAA(5) pending live-feel confirm per TP8 gate).
    TP9 SIGN-OFF: The static social-read is COMPLETE — CenterGameState + staged Perla + community board
    + demoted pot all unconditionally mounted (confirmed in 10-01-SUMMARY social-read audit from TP8
    carry, no new center objects in TP9). The live HeroMotion breathing (if approved at TP8 gate)
    reinforces the "someone was just playing here" read. The AAA(5) upgrade for social-read requires
    the operator's live-view judgment at the 10-03 FINAL gate — the static score is confirmed at 4;
    the operator may choose to note 5 at the gate if the complete live view supports it.
    Score: 4 (static; LIVE AAA(5) deferred to 10-03 operator gate — per SSOT: "scoring discipline,
    final live-view judgment is the operator's domain"). Do NOT pre-score 5 here.

    Element 15 (premium-overall): Post-TP8 = 3 (baseline; no phase updated this — it is the holistic
    TP9 verdict). TP9 SIGN-OFF: This element scores the holistic completeness of the table. It cannot
    be scored below 4 if all other elements are at 4 or 5 — but it must not be inflated beyond honest
    judgment. Evidence: Elements 1-14 are all at 4 or 5. Every admitted metric passes. The §8 checklist
    items 1-13 are verified. The tone-map is ACES Filmic (no washing reported). The completed table
    is the full TP1-TP8 stack. The holistic read from the frozen captures (hero/card/macro from
    10-01) is of an AAA, premium, castizo, hand-fabricated look for a Spanish card table.
    The orchestrator's CEO visual read of the 3 frozen captures, viewed alongside the metric record:
    "Is this unambiguously premium-artisanal — not embarrassing next to a real Spanish card table?"
    Score this element honestly at 4 or 5:
      4 = Genuinely premium, all criteria met, clearly better than the TP0 reference.
      5 = AAA — unmistakably a hand-crafted, castizo, premium Spanish card table with photographic depth.
    Record the score and the justification sentence. The operator's 10-03 gate may revise this up
    if the live view adds to the frozen evidence. Record as "provisional 10-02 / final at 10-03."

    SCORING INVARIANT: If ANY element scores below 4 — stop. Do NOT write the new column to SCORECARD.
    Write a SUMMARY flag: "ELEMENT [N] BELOW-GREEN: scored [X]. Phase [Y] is responsible.
    PROMOTION BLOCKED. Plan 10-03 must NOT be started." Surface to orchestrator.

    STEP 2 — Calculate the post-TP9 scorecard total and record vs baseline.
    Baseline total: 51/75 (confirmed in RESEARCH.md).
    Post-TP8 pre-sign-off total: 64/75 (elements 12/14/15 held pending TP9).
    Post-TP9 anticipated range (depending on element 15 score):
      If element 15 = 4, element 14 = 4, element 12 = 5: 64 - 4 (element 12 carried at 4) - 3 (element 15 held) + 5 + 4 + 4 = 67/75
      If element 15 = 5, element 14 = 4, element 12 = 5: 68/75
      If element 15 = 4, element 14 = 5, element 12 = 5: 68/75 etc.
    Calculate the exact total from the scores recorded in STEP 1 and record it.

    STEP 3 — Tone-map decision record.
    Read the Canvas gl config from frontend/src/lab/TableLab.tsx lines ~1454-1462 to confirm:
      toneMapping: THREE.ACESFilmicToneMapping
      toneMappingExposure: 1.05
    (Do not read more of the file — the single Canvas gl block is sufficient.)
    Confirm no AgX washing was observed (no gate doc in TP1-TP9 records ACES washing low-contrast
    textures). Record in SCORECARD_TABLE_3D.md TP9 log section:
      "TONE-MAP DECISION (TP9 LOCK): ACES Filmic (THREE.ACESFilmicToneMapping), exposure=1.05.
      No AgX triggered — no low-contrast texture washing observed across TP1-TP9. Grade stack:
      BrightnessContrast brightness=0.03/contrast=0.05, Vignette offset=0.70/darkness=0.12,
      Noise opacity=0.03/premultiply=false (all confirmed at TP6; unchanged). LOCKED."
    Note: If during the 10-01-SUMMARY metric matrix run, any washing was noted (unusual given the
    gate history), document it and record the AgX path as a flag for the operator at 10-03.
    Default (and expected): ACES confirmed, no change, locked.

    STEP 4 — §8 "mesa terminada" checklist verification, item by item.
    Read §8 from docs/ROADMAP_TABLE_3D_PERFECTION.md (lines ~511-553, or locate the §8 section).
    For each of the 14 items, cross-reference the 10-01-SUMMARY metric data, gate docs, and
    grep-check results to produce a PASS or NOTE verdict:

      Item 1 (FELT): M3 ΔE < 12 from 10-01-SUMMARY; M8 band 8-20% from 10-01; born-in mark; nap sheen.
        Expected verdict: PASS. Note if M3 value is close to 12 (threshold risk).

      Item 2 (CARDS): M1 >=22px from 10-01-SUMMARY; M2 >=2x from 10-01; Fournier faces; card-stock
        impression; tight near-edge contact (TP2). Expected verdict: PASS (with M1 operator-confirm note
        if px-height was carried from TP7 rather than freshly measured).

      Item 3 (CHIPS): M10 ?chips=full ~133dc < 220; matte clay; de-Vegas; instanced (TP3); demoted
        recessive; tooled-not-printed C. Expected verdict: PASS. Note chip-stack count (3/stack per TP3
        gate; operator-gated).

      Item 4 (RAIL/CONTOUR): M4 PASS on ?nofx (brass = aged-brass, not gold); welt/cord at seam;
        per-arc UV normalMap; slim default (yTop=0.28 per grep-check); rail outer wall curved volume;
        no furniture mass added. Expected verdict: PASS.

      Item 5 (LIGHTING): Shaped warm gradient confirmed (TP5 key+fills+rim+floor bounce); M7=0 Bloom;
        +A warm corner PASS; M8 PASS; no casino cone (ratio 2.75x < 3.5x anti-casino ceiling).
        Expected verdict: PASS.

      Item 6 (SHADOWS/GROUNDING): PCSS soft shadows; baked ContactShadows frames={1} (grep-check
        CHECK 15); N8AO crevice AO (grep-check CHECK 9); M6 27.74% PASS. Expected verdict: PASS.

      Item 7 (DEPTH): N8AO + DOF (grep-check CHECK 8-10); M8 PASS; BrightnessContrast + Noise grade
        (TP6 grade stack confirmed); M7=0 Bloom (CHECK 12). Expected verdict: PASS.

      Item 8 (COMPOSITION): CenterGameState + staged Perla + community board + demoted pot
        (from 10-01-SUMMARY social-read audit); no per-seat objects (M2 confirms, social-read audit
        from TP8 carry); cards>board>rail layering. Expected verdict: PASS.
        NEGATIVE CHECK: confirm "no per-seat / opponent / occupant object anywhere" — this is red-team #1.
        If social-read audit in 10-01-SUMMARY notes any per-seat object, this is a FAIL. Do not pass it.

      Item 9 (CAMERAS): grep-check CHECK 16 (autoRotate={false}), CHECK 17 (fov:32/40/26 all present),
        CHECK 18 (no second makeDefault); M9 PASS (deterministic). Three operator-blessed presets.
        Expected verdict: PASS.

      Item 10 (FEEL): HeroMotion sub-threshold breathing (TP8 APPROVED disposition per TP8_OPERATOR_GATE.md
        — or REMOVED if that was the verdict). If REMOVED: record "static-but-complete; breathing removed
        per stop-on-ambiguous." If APPROVED: confirmed frozen under spin=off (M9) and reduced-motion
        (grep-check CHECK 7). Expected verdict: PASS (either APPROVED or REMOVED is valid).

      Item 11 (PERF GUARDRAIL): M10 ?fx-off ~52-62dc < 150 PASS; M10 ?chips=full ~133dc < 220 PASS;
        M11 deferred to operator live-view gate (plan 10-03). Record: "PASS for automated M10 gates;
        M11 frame-time deferred to 10-03 live-view (RTX 4060 comfortable at 177dc per TP6 record;
        no new draw-call creep at TP9)."

      Item 12 (REFERENCE INTEGRITY): The protected tag table-3d-premium-reference-2026-06-04 was
        never degraded — all shared elements (felt/rail/chips/lighting) have been improved (M12 MSE
        <= 1.0 for identity regions means must-not-change areas did not regress; perceptual improvement
        is the operator's visual read at 10-03). M2 PASS for cards/composition. New protected reference
        tag will be created at 10-03. Expected verdict: PASS (pending operator visual read at 10-03).

      Item 13 (METRIC INTEGRITY): All §4.5 metrics validated against the consolidated matrix in plan
        10-01. The ?nofx positive-control confirmed M4/M5 pass on the clean scene path. M12 identity
        regions confirmed with the TP0 anchor corpus. Meta-gate (positive+negative control framing)
        documented in 10-01-SUMMARY. Expected verdict: PASS.

      Item 14 (VERDICT): "AAA scorecard all-green (every element >=4/5), all objective metrics pass
        simultaneously at the locked shots, and the operator gives an unambiguous on-device final yes."
        This item is NOT checked here — it is the operator's exclusive domain at plan 10-03.
        Record: "PENDING — operator FINAL gate at plan 10-03."

    After verifying items 1-13 against the evidence, record the §8 checklist results table in
    SCORECARD_TABLE_3D.md TP9 log section (or in the SUMMARY — whichever matches the existing
    scorecard structure).

    STEP 5 — Write the TP9 log entries to SCORECARD_TABLE_3D.md.
    Follow the exact existing log structure (headers, score format, column placement). Add:
      - The TP9 column scores for all 15 elements.
      - The honest reconciliation notes for elements 8 (lighting), 12 (cameras), 13 (tactility),
        14 (social-read), 15 (premium-overall).
      - The scorecard total vs baseline row.
      - The grade/tone-map decision record (from STEP 3).
      - The §8 checklist summary (from STEP 4).
      - A TP9 sign-off header noting that plan 10-03 is the FINAL gate.

    Do NOT change any prior TP column scores. Do NOT update .planning/STATE.md or ROADMAP.md
    (that happens after the FINAL operator gate at plan 10-03).

    COMMIT after completing SCORECARD_TABLE_3D.md:
    Commit message: "docs(10-02): TP9 scorecard all-green + tone-map lock + mesa terminada checklist"
  </action>
  <verify>
    <automated>node -e "const f=require('fs'),s=f.readFileSync('docs/table-3d/SCORECARD_TABLE_3D.md','utf8');if(!s.includes('TP9')){console.error('FAIL: TP9 column not found in SCORECARD_TABLE_3D.md');process.exit(1);}console.log('OK -- TP9 column present in SCORECARD');"</automated>
    <automated>node -e "const f=require('fs'),s=f.readFileSync('docs/table-3d/SCORECARD_TABLE_3D.md','utf8');if(!s.includes('ACES Filmic')||!s.includes('1.05')){console.error('FAIL: tone-map lock record not found');process.exit(1);}console.log('OK -- tone-map lock recorded');"</automated>
    <automated>node -e "const f=require('fs'),s=f.readFileSync('docs/table-3d/SCORECARD_TABLE_3D.md','utf8');if(!s.includes('mesa terminada')){console.error('FAIL: mesa terminada checklist not found in SCORECARD');process.exit(1);}console.log('OK -- mesa terminada checklist section present');"</automated>
    <automated>node tools/table-3d/grep-check-tp8-09.cjs</automated>
  </verify>
  <done>
    SCORECARD_TABLE_3D.md TP9 column filled in for all 15 elements (every element >=4). Elements 12,
    14, 15 have explicit sign-off scores with rubric justification. Lighting (8) and tactility (13)
    confirmed at 5 with documented evidence. Scorecard total vs baseline recorded. ACES Filmic
    exposure=1.05 decision locked. §8 checklist items 1-13 verified PASS; item 14 marked PENDING
    operator 10-03 gate. No code changes. grep-check-tp8-09 exits 0. LOCAL only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 10-01-SUMMARY -> scorecard scores | All score justifications must reference measured values from 10-01-SUMMARY; inference without data is not acceptable |
| Honest reconciliation -> operator gate | The TP9 scorecard is what the operator reads at 10-03; inflated scores that do not survive the rubric criteria honest review undermine the gate |
| §8 item 8 negative check -> promotion gate | The "no per-seat object" check is a red-team invariant; a per-seat object in the scene blocks §8 PASS |
| element 15 provisional score -> 10-03 | Premium-overall is marked provisional/10-02 to make it explicit that the operator's live gate (10-03) is the final arbiter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02a | Tampering | Scorecard updated despite BLOCKING metric flag in 10-01-SUMMARY | mitigate | PRE-CONDITION CHECK reads 10-01-SUMMARY first; explicit HALT and do-not-write instruction if BLOCKING found |
| T-10-02b | Tampering | Prior TP column scores regressed in the SCORECARD update | mitigate | read_first reads full current SCORECARD before editing; action block states "Do NOT change any prior TP column scores" |
| T-10-02c | Tampering | Element 14 pre-scored to 5 without operator live-view (scoring discipline violation) | mitigate | Element 14 score fixed at 4 (static); action block explicitly states "Do NOT pre-score 5 here"; AAA(5) deferred to 10-03 |
| T-10-02d | Tampering | §8 item 8 negative check (no per-seat object) skipped or passed without evidence | mitigate | Action STEP 4 item 8 includes explicit NEGATIVE CHECK with "if social-read audit notes any per-seat object, this is a FAIL" |
| T-10-02e | Tampering | Element 8 (lighting) or 13 (tactility) moderated down to 4 without evidence of rubric criterion failure | mitigate | Action STEP 1 elements 8/13 list the specific rubric criteria each must satisfy; moderation requires naming a criterion that is not met |
| T-10-02f | Tampering | Late tone-map change between 10-01 captures and 10-02 record | mitigate | No code changes in this plan; tone-map record reads the CURRENT TableLab.tsx config (expected unchanged); if changed, full matrix re-run is mandatory per SSOT invariant |
| T-10-02g | Repudiation | Scorecard update not committed, leaving the gate record incomplete | mitigate | STEP 5 includes explicit commit instruction with commit message |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- 10-01-SUMMARY confirmed: no BLOCKING metric flag present before SCORECARD edit.
- SCORECARD_TABLE_3D.md TP9 column present and filled for all 15 elements.
- Every element is >=4; if any element is <4, plan halted and orchestrator notified.
- Elements 12 (cameras=5), 13 (tactility=5 static), 14 (social-read=4 static), 15 (premium-overall=4 or 5) have explicit rubric justifications.
- Lighting (8) and tactility (13) confirmed at 5 with documentable criterion evidence — NOT moderated without evidence.
- Scorecard total vs baseline (51/75) recorded.
- ACES Filmic exposure=1.05 tone-map decision recorded as LOCKED.
- §8 checklist items 1-13 verified PASS; item 14 marked PENDING 10-03.
- No code changes in this plan.
- grep-check-tp8-09.cjs exits 0.
- SCORECARD committed. LOCAL only.
</verification>

<success_criteria>
- SCORECARD_TABLE_3D.md all-green: every element >=4 in the TP9 column.
- Sign-off elements (12, 14, 15) have explicit TP9 scores with rubric evidence.
- Honest reconciliation of elements 8 and 13 documented (scores stand with evidence).
- Grade/tone-map decision LOCKED (ACES Filmic, exposure=1.05).
- §8 "mesa terminada" 14-item checklist: items 1-13 PASS; item 14 PENDING 10-03.
- Plan 10-03 has an unambiguous all-green scorecard to read as its pre-condition.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/10-tp9-unificacion-aaa-lock/10-02-SUMMARY.md` when done.
Record: all 15 element scores with the rubric justification for each; the all-green verdict
(total vs baseline); the tone-map lock record; the §8 checklist table (items 1-14 with PASS/NOTE/PENDING);
any blocking condition found in 10-01-SUMMARY and the HALT disposition applied; the commit hash.
Plan 10-03 reads this SUMMARY as the pre-gate record alongside the 10-01-SUMMARY metric matrix.
</output>
