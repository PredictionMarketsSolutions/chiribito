---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - frontend/src/lab/TableLab.tsx
  - docs/table-3d/TP2_PREGATE.md
requirements:
  - "SSOT §TP2 — near-edge contact-shadow tightened/darkened vs the lifted far edge (card visibly bites the cloth — M6)"
  - "SSOT §TP2 — M1 legibility MUST NOT regress; M2 cards-vs-chips >= 2x maintained"
  - "SSOT §TP2 — restrained dealt variance frozen deterministically (M9 byte-identity at the consolidated capture)"
  - "SSOT §5 — only ONE shadow-casting light; ALL crevice/screen-space AO is TP6 (do not build the TP5/TP6 shadow stack here)"
autonomous: true
must_haves:
  truths:
    - "Lever 7 (contact-shadow): the near-edge card-to-felt contact shadow is tightened/darkened so the card visibly bites the cloth (M6 PASS on the recalibrated rect from 03-01), via the MINIMAL change to the existing single shadow source — without building the TP5 PCSS / TP6 AO stack and without adding a second shadow-casting light, behind a ?card= flag"
    - "The consolidated pre-gate capture at the 3 frozen money shots with the full TP2 stack ON shows: M1 >= the tp2-base floor (HARD), M2 cards-vs-chips >= 2x (HARD), M5 highlight-clip PASS, M6 PASS, M9 byte-identical, M12 zero-change on the non-card regions vs tp2-base"
    - "Every TP2 lever (1-7) is independently toggleable via its ?card= flag so the operator can isolate any single lever at the 03-06 gate"
    - "The lab Vitest stays green and the lab is still absent from the prod Vite build (isolation preserved)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "the lever-7 contact-shadow tightening (minimal edit to the existing shadow source) + the consolidated ?card= flag surface"
      contains: "ContactShadows"
    - path: "docs/table-3d/TP2_PREGATE.md"
      provides: "the consolidated metric ledger before the operator gate — M1/M2/M5/M6/M9/M12 verdicts at the 3 frozen shots with the full TP2 stack, plus the per-lever ?card= flag map"
      contains: "M2"
  key_links:
    - from: "the single shadow-casting spotLight (Lights, ~lines 603-613) and/or ContactShadows (~lines 799-809)"
      to: "the near-edge card-to-felt contact region (REGIONS.underCardHero, recalibrated in 03-01)"
      via: "the minimal shadow tighten/darken that makes M6 PASS without a new shadow light or the TP5/TP6 stack"
      pattern: "ContactShadows|shadow-radius"
    - from: "the per-lever ?card= flags (levers 1-7)"
      to: "the operator A/B at 03-06"
      via: "each lever independently toggleable so a bad lever can be isolated"
      pattern: "qp\\(\"card\"\\)"
---

<objective>
TP2 lever 7 + the pre-gate consolidation. Lever 7: tighten/darken the near-edge card-to-felt contact
shadow so the card visibly BITES the cloth (M6), using the MINIMAL change to the EXISTING single shadow
source — the scene has exactly ONE shadow-casting light (the spotLight in Lights) plus a ground-level
ContactShadows; SSOT §5 forbids a second shadow-casting light and reserves PCSS (TP5) and all
crevice/screen-space AO (TP6) for later phases, so this lever must NOT build that stack. Then the
consolidation: with the full TP2 stack ON (levers 1-7), run the complete metric ledger at the 3 frozen
money shots — M1 (HARD, >= floor), M2 (HARD, cards-vs-chips >= 2x), M5, M6 (on the recalibrated rect),
M9 (byte-identity), M12 (zero-change on non-card regions vs tp2-base) — and record it in
docs/table-3d/TP2_PREGATE.md so the operator gate (03-06) has the apples-to-apples numbers and a clean
per-lever ?card= flag map.

Purpose: M6 is the last lever; after it, the protagonist must be proven legible (M1), dominant (M2),
deterministic (M9), and non-regressive on the rest of the frame (M12) BEFORE the operator judges
"physical stock while razor-legible". This plan produces that ledger and guarantees every lever is
isolatable for the gate.

Output: the lever-7 contact-shadow edit (minimal); docs/table-3d/TP2_PREGATE.md (the consolidated metric
ledger + flag map); fresh consolidated captures at the 3 frozen shots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-04-SUMMARY.md
@docs/table-3d/TP2_BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lever 7 — tighten/darken the near-edge card-to-felt contact shadow (M6), minimal edit to the single shadow source, behind ?card=</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 594-621 (Lights — the ONLY shadow-casting light is the spotLight at 603-613: castShadow gated on `qp("sh") !== "off"`, shadow-mapSize [2048,2048], shadow-bias -0.0003, shadow-radius 8; the other lights do NOT cast), 333-334 (Card meshes: body castShadow+receiveShadow, face castShadow), 799-809 (ContactShadows — note position [0,-1.48,0] is GROUND level / under the TABLE, scale FELT_R*3, blur 2.8, far 4, opacity 0.55 — this is the table's ground shadow, likely NOT the near-edge card-to-felt shadow).
    - docs/table-3d/TP2_BASELINE.md (from 03-01) — the recalibrated M6 rect coords + the tp2-base M6 baseline luma values; M6 PASS = under-card luma >= 12% darker than adjacent felt.
    - tools/table-3d/metrics.mjs lines 98-105 (the M6 rects, possibly recalibrated in 03-01) + the m6ContactShadow gate.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md "LEVER 7" diagram (lines 232-238: ContactShadows blur 2.8->2.0, far 4->3, opacity 0.55->0.65 as the suggested tighten) — BUT first confirm WHICH shadow actually falls under the hole cards (the ground ContactShadows may not reach them; the spotLight shadow-map may be the real source).
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §5 line 179 ("only ONE shadow-casting light") + §TP2 line 302 (near-edge contact-shadow tightened — M6) + the TP5/TP6 scope (PCSS = TP5, ALL crevice/screen-space AO = TP6) — the lever must stay inside TP2 scope.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md (lines 47-48) — lever 7 = near-edge contact-shadow tighten/darken (M6); deferred (line 144): TP5 lighting/PCSS/ContactShadows, TP6 crevice/screen-space AO — do NOT pull them forward.
  </read_first>
  <action>
    First DIAGNOSE the real source of the under-card shadow: capture HERO `?cam=hero` and inspect the
    recalibrated M6 under-card rect on tp2-base/hero.png vs a fresh capture — determine whether the near-edge
    contact darkening under the hole cards comes from the spotLight shadow-map (lines 603-613) or the
    ground-level ContactShadows (lines 799-809, which sits at y=-1.48 under the table and may not reach the
    card-on-felt contact). Apply the MINIMAL tightening to whichever is the real source so the card visibly
    bites the cloth and M6 PASSES on the recalibrated rect: for the spotLight, a small shadow-radius decrease
    (tighter penumbra) and/or a shadow-bias tweak; for ContactShadows, the RESEARCH-suggested
    blur 2.8->~2.0 / far 4->~3 / opacity 0.55->~0.65 (or add a SECOND ContactShadows scoped to the card area
    at felt height IF and only if the ground one does not reach — but do NOT add a second shadow-CASTING
    light, SSOT §5). Do NOT install PCSS/SoftShadows (TP5) and do NOT add any screen-space/crevice AO (TP6) —
    those are explicitly later phases. Gate the change behind the same `?card=` flag family (`?card=base` =
    pre-TP2 contact shadow). Re-capture HERO and run the admitted metrics confirming M6 PASS on the
    recalibrated rect and M5 still PASS; re-measure M1 on POV (>= floor). STOP criterion: if tightening the
    shadow darkens the felt globally (M3/M5 regression) or reads harsh, back off or revert (non-blocking,
    flag). Record the source diagnosis + the minimal change + the M6/M5/M1 verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8');const casters=(s.match(/castShadow/g)||[]).length;const lightCasters=(s.match(/(spotLight|directionalLight|pointLight)[\s\S]{0,200}?castShadow/g)||[]).length;if(lightCasters>1){console.error('more than ONE shadow-casting light (SSOT §5): '+lightCasters);process.exit(1)}if(/SoftShadows|PCSS|N8AO|<EffectComposer/.test(s)){console.error('TP5/TP6 shadow/AO stack must not be added in TP2');process.exit(1)}console.log('single shadow-casting light preserved; no TP5/TP6 stack')"</automated>
  </verify>
  <acceptance_criteria>
    - The near-edge card-to-felt contact shadow is tightened/darkened via the MINIMAL edit to the existing shadow source; the grep gate confirms NO second shadow-casting light was added and NO SoftShadows/PCSS/N8AO/EffectComposer (TP5/TP6) was introduced.
    - M6 PASSES on the recalibrated rect (under-card luma >= 12% darker than adjacent felt) on a fresh HERO; M5 still PASS; M1 re-measured >= the tp2-base floor.
    - The change is behind a `?card=` flag (`?card=base` = pre-TP2 contact shadow).
    - `cd frontend && npx vitest run src/lab/` exits 0 and `npx tsc --noEmit` is clean.
    - The SUMMARY records the shadow-source diagnosis, the minimal change applied, and the M6/M5/M1 verdicts.
  </acceptance_criteria>
  <done>The near-edge contact shadow is tightened so the card bites the cloth (M6 PASS) with the single-shadow-light invariant + TP2 scope intact; M5 PASS + M1 >= floor (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidated pre-gate metric ledger — M1/M2/M5/M6/M9/M12 at the 3 frozen shots with the full TP2 stack</name>
  <files>docs/table-3d/TP2_PREGATE.md</files>
  <read_first>
    - tools/table-3d/run-metrics.mjs lines 195-220 (captureRun over a dir's hero.png — M3/M4/M5/M6/M8/+A/+B) — run it over a fresh full-TP2-stack capture dir.
    - tools/table-3d/m1-m2-m12.mjs lines 140-175 (m1Legibility / m1DownscalePov — M1 px-height), 200-215 (m2CardsVsChips — manual polygon areas, cards/chips screen area, >= 2x gate), 285-309 (CLI: --zero-change <curDir> <baseDir> for M12 on non-card regions) — the M1/M2/M12 procedure.
    - tools/table-3d/m9-determinism.mjs (CLI: --shot <cam> --port <devport> — md5 byte-identity of two consecutive captures) — the M9 gate with the full stack (incl. dealt variance) ON.
    - tools/table-3d/stats-read.mjs (CLI: <baseUrl> <cam> -> JSON {cam, calls}; default port 5180) — the M10 draw-call WATCH (informational for TP2; M10 fix is TP3 — record but do not gate).
    - docs/table-3d/TP2_BASELINE.md (from 03-01) — the M1 floor + the tp2-base M5/M6 baseline numbers (the apples-to-apples "before").
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-VALIDATION.md "Sampling Rate" (lines 30-37: before the operator gate -> M1 not regressed vs tp2-base, M2 >= 2x, all admitted metrics green at the locked shots) + "Manual-Only Verifications" (lines 68-70).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Locked by SSOT" (lines 68-77: HARD gates M1 must not regress + M2 >= 2x).
  </read_first>
  <action>
    Bring the dev server up and capture the 3 frozen money shots with the FULL TP2 stack ON (default scene =
    all levers active once shipped; no `?card=base`) into `.dev-stack/diag/table-3d/tp2/gate/` as
    card.png/hero.png/macro.png. Then run the complete pre-gate ledger and record every verdict in
    docs/table-3d/TP2_PREGATE.md against the tp2-base "before": (a) M1 — downscale gate/card.png to the M1
    reference height, measure the rank-glyph px-height, assert >= the tp2-base floor (HARD); (b) M2 — manual
    polygon trace of the cards' screen area vs the chips' screen area on gate/hero.png, run m2CardsVsChips,
    assert >= 2x (HARD); (c) admitted metrics via `node tools/table-3d/run-metrics.mjs
    .dev-stack/diag/table-3d/tp2/gate` (M5 highlight-clip PASS, M6 PASS on the recalibrated rect); (d) M9 —
    `node tools/table-3d/m9-determinism.mjs --shot card --port <devport>` md5-identical with the full stack
    (incl. dealt variance) ON; (e) M12 — `node tools/table-3d/m1-m2-m12.mjs --zero-change
    .dev-stack/diag/table-3d/tp2/gate docs/table-3d/anchors/tp2-base` to confirm the non-card regions
    (felt/brass) are unchanged vs tp2-base; (f) M10 draw-call WATCH via stats-read.mjs at card + hero —
    record the count but DO NOT gate on it (the M10 ceiling fix is TP3 scope). Also record the per-lever
    `?card=` flag map (lever -> flag name -> what it isolates) so the operator can A/B any single lever at
    03-06. If any HARD gate fails (M1 < floor OR M2 < 2x OR M9 non-identical), identify the offending lever
    via its flag and STOP — record which lever to revert (non-blocking, flag) and surface it for the gate.
    This task writes only the ledger doc + scratch captures; it changes no render code.
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp2/gate</automated>
    <automated>node tools/table-3d/m1-m2-m12.mjs --zero-change .dev-stack/diag/table-3d/tp2/gate docs/table-3d/anchors/tp2-base</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/table-3d/TP2_PREGATE.md','utf8');for(const k of ['M1','M2','M5','M6','M9','M12']){if(!new RegExp(k).test(s)){console.error('TP2_PREGATE.md missing '+k);process.exit(1)}}console.log('pre-gate ledger records M1/M2/M5/M6/M9/M12')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/table-3d/TP2_PREGATE.md records, at the 3 frozen shots with the full TP2 stack: M1 px-height >= the tp2-base floor (HARD), M2 cards-vs-chips >= 2x (HARD), M5 PASS, M6 PASS (recalibrated rect), M9 md5-identical, M12 zero-change on non-card regions vs tp2-base, and the M10 draw-call WATCH count (informational, not gated).
    - `node tools/table-3d/run-metrics.mjs .dev-stack/diag/table-3d/tp2/gate` prints the admitted metric table (M5/M6 PASS) and `--zero-change` confirms non-card regions unchanged vs tp2-base.
    - The doc includes the per-lever `?card=` flag map (each lever -> flag -> what it isolates) for the operator gate.
    - If any HARD gate failed, the offending lever + its flag + the revert recommendation are recorded; no render code was changed by this task.
  </acceptance_criteria>
  <done>The consolidated pre-gate ledger (M1/M2/M5/M6/M9/M12 + M10 watch) at the 3 frozen shots is recorded vs tp2-base, with the per-lever flag map; HARD gates green (or the offending lever flagged for revert) — ready for the operator gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. Only `frontend/src/lab/`
is touched (+ a docs ledger); `/table-lab.html` is never in the production Vite build (SSOT §5.8). No
auth/network/data/user-input surface. The boundaries that matter are the HARD legibility + hierarchy
gates, M9 determinism, the single-shadow-light invariant, and TP2 scope (no TP5/TP6 stack).

| Boundary | Description |
|----------|-------------|
| TP2 contact-shadow -> TP5/TP6 stack | Lever 7 may only tighten the existing single shadow source; PCSS (TP5) + crevice/screen-space AO (TP6) are out of scope |
| full TP2 stack -> the operator gate | Every HARD gate (M1 >= floor, M2 >= 2x, M9 byte-identity) is proven before the operator judges materiality |
| changed (card) regions -> unchanged (felt/brass) regions | M12 zero-change confirms the levers only touched the cards, not the rest of the frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05a | Tampering | single shadow-casting light (SSOT §5) | mitigate | grep gate asserts <= 1 shadow-casting light + no SoftShadows/PCSS/N8AO/EffectComposer added |
| T-03-05b | Tampering | M1 legibility floor + M2 hierarchy (HARD gates) | mitigate | Consolidated ledger asserts M1 >= floor AND M2 >= 2x with the full stack before the gate; a failure flags the offending lever |
| T-03-05c | Tampering | M9 byte-determinism with full stack | mitigate | M9 md5 re-run with all levers (incl. dealt variance) ON; non-identity flags the offending lever |
| T-03-05d | Tampering | non-card regions (M12) | mitigate | --zero-change vs tp2-base confirms felt/brass unchanged; a regression flags an over-broad lever |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 (RESEARCH "No new packages needed") |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lab/` exits 0 and `npx tsc --noEmit` clean after lever 7.
- Single shadow-casting light preserved; no TP5/TP6 shadow/AO stack (grep gate).
- docs/table-3d/TP2_PREGATE.md records M1 (>= floor, HARD), M2 (>= 2x, HARD), M5/M6 PASS, M9 md5-identical, M12 zero-change, M10 watch + the per-lever flag map.
- M6 PASS on the recalibrated rect; HARD gates green (or the offending lever flagged for revert).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Lever 7 (near-edge contact-shadow tighten, M6 PASS) shipped behind ?card= with the single-shadow-light + TP2-scope invariants intact.
- The consolidated pre-gate ledger proves M1 >= floor (HARD), M2 >= 2x (HARD), M5/M6 PASS, M9 byte-identity, M12 zero-change — ready for the operator gate; every lever isolatable via its ?card= flag.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-05-SUMMARY.md` when done, and write the consolidated ledger + flag map to `docs/table-3d/TP2_PREGATE.md`.
</output>
