---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - docs/table-3d/TP2_OPERATOR_AB.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
requirements:
  - "SSOT §TP2 perceptual gate — operator A/B at POV + MACRO: physical printed STOCK while razor-legible?"
  - "SSOT §TP2 — Stop-on-ambiguous: any legibility loss OR more plastic/laminated -> STOP and revert that lever (non-blocking, flag)"
  - "SSOT §TP2 — HARD gates restated at the gate: M1 not regressed vs tp2-base; M2 cards-vs-chips >= 2x"
autonomous: false
must_haves:
  truths:
    - "The operator has performed the A/B at POV (?cam=card, fov40) + MACRO (?cam=macro, fov26): the full TP2 card stack vs tp2-base (the post-encuadre 'before'), judging 'physical printed STOCK while razor-legible?' with each lever independently toggleable via its ?card= flag to isolate a bad one"
    - "The HARD gates are confirmed at the gate: M1 NOT regressed vs the tp2-base floor AND M2 cards-vs-chips >= 2x (from the 03-05 pre-gate ledger, re-confirmed live if the operator requests)"
    - "On stop-on-ambiguous (any legibility loss OR more plastic/laminated), the offending lever is identified via its ?card= flag and REVERTED (non-blocking, flag) — TP2 does not block the program (protagonist already shipped at M1)"
    - "The honesty escalation is recorded: smaller cards (CARD_W 2.05) lower the index px-height (crispness sharpens but does NOT enlarge); if crispness cannot hold 'razor-legible', the verdict escalates back to the CARD_W/encuadre decision as a SEPARATE operator call — CARD_W is NOT silently changed in TP2"
  artifacts:
    - path: "docs/table-3d/TP2_OPERATOR_AB.md"
      provides: "the recorded operator verdict, the A/B basis (vs tp2-base), the per-lever disposition (ship / revert which lever), the HARD-gate confirmation, and any CARD_W/encuadre escalation"
      contains: "Operator verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "the post-TP2 cards-row score (recorded only if shipped; baseline 4, target >= 4 — must not regress)"
      contains: "cards"
  key_links:
    - from: "docs/table-3d/TP2_OPERATOR_AB.md"
      to: "docs/table-3d/anchors/tp2-base/{card,macro}.png + the per-lever ?card= flags"
      via: "the A/B compares the full TP2 stack against the post-encuadre baseline, isolating any single lever"
      pattern: "tp2-base"
    - from: "the operator stop-on-ambiguous verdict"
      to: "the offending lever's ?card= flag + revert (or the CARD_W/encuadre escalation)"
      via: "non-blocking rollback of the single bad lever, or escalation of legibility to a separate CARD_W call"
      pattern: "revert"
---

<objective>
The TP2 operator perceptual gate — the GSD<->Chiribito human seam (SSOT §TP2 perceptual gate, §5.7
stop-on-ambiguous). The operator does an A/B at the two card-telling shots (POV `card` fov40 for the
hero hole-card read, MACRO fov26 for the stock/grain/seam/edge) comparing the full TP2 card stack
(levers 1-7 from 03-02 … 03-05) against the post-encuadre baseline (`docs/table-3d/anchors/tp2-base/`,
the apples-to-apples "before"). The judgement is the SSOT question: "physical printed STOCK while
razor-legible?" Each lever is independently toggleable via its `?card=` flag so a single bad lever can
be isolated. The HARD gates (M1 not regressed vs tp2-base, M2 cards-vs-chips >= 2x) from the 03-05
pre-gate ledger are restated and confirmed at the gate.

Purpose: Automated metrics (03-05) prove "legible enough (>= floor) / dominant (>= 2x) / deterministic /
non-regressive" but cannot judge "reads as physical printed stock, not plastic/laminated." Only the
operator can. This gate cannot be auto-approved (autonomous:false; auto_advance does not apply to a
perceptual gate). Stop-on-ambiguous reverts the single offending lever (non-blocking — the protagonist
already shipped at M1). The honesty escalation: if crispness cannot hold razor-legibility on the smaller
cards, that is a CARD_W/encuadre decision (a SEPARATE operator call), NOT a silent CARD_W change in TP2.

Output: `docs/table-3d/TP2_OPERATOR_AB.md` recording the verdict, the per-lever disposition, the
HARD-gate confirmation, and any CARD_W escalation; the cards-row scorecard delta if shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-VALIDATION.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-05-SUMMARY.md
@docs/table-3d/TP2_BASELINE.md
@docs/table-3d/TP2_PREGATE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — "physical printed STOCK while razor-legible?" at POV + MACRO</name>
  <files>docs/table-3d/TP2_OPERATOR_AB.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - docs/table-3d/TP2_PREGATE.md (from 03-05) — the consolidated HARD-gate ledger (M1 >= floor, M2 >= 2x, M5/M6 PASS, M9 byte-identity, M12 zero-change) + the per-lever ?card= flag map (which flag isolates which lever).
    - docs/table-3d/TP2_BASELINE.md (from 03-01) — the post-encuadre M1 floor + the tp2-base "before" numbers.
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP2 lines 304-308 (the perceptual gate + stop-on-ambiguous + the non-blocking-flag rollback) + §5 lines 196 (TP2 rollback disposition: "non-blocking, flag — protagonist already shipped at M1") + 205-207 (the global escalation cap).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Honesty note" (lines 53-57) — smaller cards lower px-height; if crispness can't hold razor-legible, escalate to the CARD_W/encuadre decision (separate operator call; encuadre is adopted, not reopened unilaterally).
    - docs/table-3d/SCORECARD_TABLE_3D.md line 29 (cards row: baseline 4, target >= 4) + lines 56-59 (the cards rubric: AAA(5) = rank glyph >= 22px on 1080p POV downscale) — the score must not regress below 4; AAA(5) may remain out of reach until CARD_W is revisited.
  </read_first>
  <action>
    Present the A/B to the operator using the consolidated captures from 03-05 (and the live lab if
    requested), following the <how-to-verify> protocol. Do NOT auto-approve — this is a perceptual gate
    (autonomous:false; auto_advance does not apply). First restate the HARD-gate ledger from TP2_PREGATE.md
    (M1 >= floor, M2 >= 2x, M9 byte-identity) so the operator judges materiality on a proven-legible base.
    Record the operator's verdict, the A/B basis (full TP2 stack vs tp2-base), the per-lever disposition
    (ship all / revert specific lever[s] by ?card= flag), the HARD-gate confirmation, and — if legibility is
    the sticking point — the CARD_W/encuadre escalation, into docs/table-3d/TP2_OPERATOR_AB.md. On a
    stop-on-ambiguous for a SPECIFIC lever, the executor reverts ONLY that lever (toggle it off / restore its
    pre-TP2 value), re-captures, re-confirms the HARD gates, and re-presents; the disposition is non-blocking
    (TP2 ships the surviving levers, or keeps the pre-TP2 card if all levers are reverted). If shipped,
    update the cards row in SCORECARD_TABLE_3D.md (must stay >= the baseline 4; note if AAA(5) is gated on a
    future CARD_W revisit). Do NOT change CARD_W or any camera here — a legibility escalation is recorded for
    a separate operator call, not actioned in TP2.
  </action>
  <what-built>
    TP2 pushed the card from "good decal" to "real card STOCK" via seven levers, each behind a `?card=`
    flag, applied in a legibility-first order on the adopted post-encuadre scene (CARD_W 2.05, 5-card board):
    (1) face anisotropy raised to the GPU max (cap 16) via useThree + mipmaps confirmed; (2) face-to-bevel
    seam fix (no cream rim / z-fight at MACRO, CARD_CORNER 0.17 + curveSegments 14 unchanged); (3) a faint
    shared card-stock micro-relief normal (normalScale ~0.12, via the shared Sobel helper, NoColorSpace);
    (4) a clearcoat whisper (0.12 / clearcoatRoughness 0.55 — not glossy); (5) a warm sheen-rim paper-edge
    (sheen-only, no texture, no transmission); (6) restrained deterministic dealt variance (<= 1.5 deg,
    seeded, M9 byte-identical); (7) a tightened near-edge card-to-felt contact shadow (M6). The pre-gate
    ledger (03-05, TP2_PREGATE.md) is green on the HARD gates: M1 >= the tp2-base floor, M2 cards-vs-chips
    >= 2x, M5 highlight-clip PASS, M6 PASS, M9 byte-identical, M12 zero-change on non-card regions. Fresh
    consolidated captures are in `.dev-stack/diag/table-3d/tp2/gate/` (card.png POV fov40, macro.png fov26,
    hero.png fov32).
  </what-built>
  <how-to-verify>
    Judge the CARD STOCK MATERIALITY + LEGIBILITY. The SSOT question is: does the card read as PHYSICAL
    PRINTED STOCK (weighty legible Fournier paper with faint grain, a coated-not-plastic sheen, a paper
    edge, tight contact, subtle dealt life) WHILE staying razor-legible? Grounding/depth beyond the
    near-edge contact bite is deferred (TP5/TP6) and must not trip this gate.

    1. Bring the dev server up if needed (`cd frontend && npm run dev`) and open the lab live, OR review the
       stills in `.dev-stack/diag/table-3d/tp2/gate/`.
    2. POV read (the hero hole-card shot): open `http://localhost:5173/table-lab.html?cam=card` and compare
       against `docs/table-3d/anchors/tp2-base/card.png` (the post-encuadre "before"). Questions: (a) are the
       rank + suit STILL razor-legible (not softer than the baseline)? (b) does the card read as printed
       paper STOCK, not a flat decal and not plastic/laminated?
    3. MACRO read (the stock/grain/seam/edge shot): open `?cam=macro` and compare against
       `docs/table-3d/anchors/tp2-base/macro.png`. Questions: (a) faint paper grain present without reading
       embossed/fake? (b) the face-to-bevel seam clean (no cream rim / z-fight halo)? (c) the coat a whisper,
       NOT a plastic-wrap glare? (d) the paper edge a warm rim, NOT a glowing casino border? (e) the card
       visibly bites the cloth (near-edge contact)?
    4. Isolate levers if anything reads off: toggle each lever via its `?card=` flag (the map is in
       TP2_PREGATE.md) — e.g. `?card=base` shows the pre-TP2 card; the per-lever flags isolate aniso / seam /
       normal / coat / edge / variance / contact-shadow one at a time, so a single bad lever can be named.
    5. HARD gates (already proven in TP2_PREGATE.md — re-confirm live if you wish): M1 not regressed vs
       tp2-base; M2 cards-vs-chips still >= 2x. These are non-negotiable.
    6. Honesty note: the cards are smaller than pre-encuadre (CARD_W 2.05), so the index px-height is lower
       (was ~17px; the AAA scorecard target is >= 22px). Crispness AFILA (sharpens) but does NOT enlarge. If
       the card cannot read "razor-legible" no matter the levers, that is a CARD_W / encuadre decision — a
       SEPARATE operator call — NOT a TP2 lever and NOT a silent CARD_W change here.

    DISPOSITION (record in docs/table-3d/TP2_OPERATOR_AB.md):
    - UNAMBIGUOUS YES (reads as physical printed stock, razor-legible, no plastic/laminated, no casino
      edge-glow, HARD gates green) -> TP2 SHIPS. Record the cards scorecard delta (baseline 4, target >= 4;
      note whether AAA(5) awaits a CARD_W revisit).
    - STOP-ON-AMBIGUOUS (any legibility loss OR any lever reads more plastic/laminated OR a casino
      edge-glow): STOP. Name the offending lever via its `?card=` flag; the executor REVERTS that single
      lever (non-blocking, flag), re-confirms the HARD gates, and re-presents. TP2 ships the surviving
      levers (or keeps the pre-TP2 card if all are reverted) — TP2 does not block the program (§5: protagonist
      already shipped at M1). If the sticking point is legibility on the smaller cards and no lever fixes it,
      record the CARD_W/encuadre ESCALATION as a separate operator call (do not action it here).
  </how-to-verify>
  <resume-signal>
    Operator types "approved" (ships all levers) — OR names the specific lever + issue (e.g. "the coat
    reads plastic" / "the edge glows" / "the grain looks embossed" / "the rank softened") to trigger a
    single-lever revert + re-present — OR types "escalate CARD_W" to record the legibility escalation as a
    separate operator call. Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has read POV + MACRO against tp2-base, with the HARD gates restated and each lever
    isolatable via its ?card= flag; the verdict + per-lever disposition (ship / revert which lever) + the
    HARD-gate confirmation + any CARD_W escalation are recorded in docs/table-3d/TP2_OPERATOR_AB.md; the
    cards scorecard row is updated if shipped (>= baseline 4). No push/deploy/merge; CARD_W + cameras unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. `/table-lab.html` is never
in the production Vite build (SSOT §5.8). No auth/network/data/user-input surface. At this gate the
boundaries are the perceptual judgement seam, the HARD legibility/hierarchy gates, and the CARD_W/encuadre
escalation boundary.

| Boundary | Description |
|----------|-------------|
| automated pre-gate ledger -> human perceptual gate | M1/M2/M5/M6/M9/M12 are necessary-not-sufficient; the operator is the sole judge of "physical stock vs plastic/laminated" |
| full TP2 stack -> the pre-TP2 baseline | Each lever is isolatable via ?card= so a single bad lever is reverted (non-blocking), not the whole phase |
| TP2 levers -> the CARD_W/encuadre decision | A legibility shortfall on the smaller cards escalates to a SEPARATE operator call; CARD_W is never silently changed in TP2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06a | Tampering | M1 legibility / M2 hierarchy (HARD gates) | mitigate | Restated at the gate from TP2_PREGATE.md; the operator confirms M1 not regressed + M2 >= 2x before shipping |
| T-03-06b | Tampering | plastic/laminated or casino edge-glow drift | mitigate | Operator judges stock-vs-plastic + edge-glow at MACRO; stop-on-ambiguous reverts the named lever |
| T-03-06c | Tampering | the CARD_W/encuadre decision | mitigate | A legibility shortfall is recorded as a SEPARATE operator escalation; CARD_W + cameras are NOT changed in this plan |
| T-03-06-GATE | Repudiation | the gate decision | mitigate | Verdict + per-lever disposition recorded in docs/table-3d/TP2_OPERATOR_AB.md (non-auto-approvable; autonomous:false) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 |
</threat_model>

<verification>
- The operator has reviewed POV (fov40) + MACRO (fov26) against tp2-base, with each lever isolatable via its ?card= flag.
- The HARD gates (M1 >= floor, M2 >= 2x) from TP2_PREGATE.md are restated and confirmed.
- `docs/table-3d/TP2_OPERATOR_AB.md` records the verdict, the per-lever disposition (ship / revert which lever), and any CARD_W/encuadre escalation.
- If shipped: the cards scorecard row is updated and stays >= baseline 4; CARD_W + cameras unchanged.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at POV + MACRO; verdict + per-lever disposition recorded; HARD gates confirmed.
- Unambiguous yes -> TP2 ships (cards scorecard >= 4); or single-lever non-blocking revert; or CARD_W/encuadre escalation recorded as a separate operator call (CARD_W not changed here).
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-06-SUMMARY.md` when done, and write the verdict to `docs/table-3d/TP2_OPERATOR_AB.md`. Update the cards row in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP2 score if shipped.
</output>
