---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/lab/TableLab.tsx
  - frontend/src/lab/cards.ts
requirements:
  - "SSOT §TP2 — face anisotropy 8 -> renderer.capabilities.getMaxAnisotropy() (cap 16); mipmaps + LinearMipmapLinear confirmed"
  - "SSOT §TP2 — fix the face-to-bevel seam (no thin cream rim / z-fight at MACRO; CARD_CORNER 0.17 unchanged, curveSegments >= 14)"
  - "SSOT §TP2 — M1 legibility MUST NOT regress (re-checked after EVERY lever)"
autonomous: true
must_haves:
  truths:
    - "Lever 1 (anisotropy): the card face texture anisotropy is set to Math.min(gl.capabilities.getMaxAnisotropy(), 16) read via useThree — NOT a hardcoded 8 or 16 — and mipmaps stay on (minFilter LinearMipmapLinearFilter, generateMipmaps true), behind a ?card= flag so the pre-TP2 state is A/B-recoverable"
    - "Lever 1: M1 rank-glyph px-height re-measured on the ?card=aniso capture is >= the tp2-base floor (sharpened, never reduced); if it regresses, the lever STOPS + reverts (non-blocking, flag)"
    - "Lever 2 (seam fix): the face-to-bevel seam reads clean at MACRO (no thin cream rim / no z-fight halo around the face decal), CARD_CORNER stays 0.17 and curveSegments stays >= 14"
    - "The 27/27 lab Vitest geometry/determinism invariants stay green after the seam-fix edit (HOLE_PITCH < CARD_W, HOLE_Z > 2, the hole-pair height-stagger)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "useThree-sourced maxAniso passed into useCardFaces; the ?card= A/B flag for the aniso + seam levers"
      contains: "getMaxAnisotropy"
    - path: "frontend/src/lab/cards.ts"
      provides: "cardBodyGeometry seam parameters (bevelSegments raised only if the MACRO seam still shows; CARD_CORNER 0.17 + curveSegments 14 unchanged)"
      contains: "cardBodyGeometry"
  key_links:
    - from: "useThree((s) => s.gl) in Scene (TableLab.tsx)"
      to: "useCardFaces() anisotropy assignment"
      via: "Math.min(gl.capabilities.getMaxAnisotropy(), 16) passed down as maxAniso"
      pattern: "getMaxAnisotropy"
    - from: "?card= flag (qp helper)"
      to: "useCardFaces anisotropy + cardBodyGeometry bevel"
      via: "one-variable-per-gate A/B toggle (default scene = full TP2 once shipped; ?card=base = pre-TP2)"
      pattern: "qp\\(\"card\"\\)"
---

<objective>
TP2 levers 1 + 2 — the LEGIBILITY-FIRST sharpening levers that lead the operator-locked sequence
(03-CONTEXT.md "Legibility-first"). Lever 1: raise the card-face texture anisotropy from the hardcoded
8 to `Math.min(gl.capabilities.getMaxAnisotropy(), 16)` (read via R3F `useThree`), confirming mipmaps
stay on — this sharpens the rank/suit at the grazing POV angle and is the primary legibility defense for
the smaller CARD_W 2.05 cards. Lever 2: fix the face-to-bevel seam so no thin cream rim / z-fight halo
shows at MACRO, keeping `CARD_CORNER 0.17` and `curveSegments` >= 14 unchanged. Both behind a `?card=`
A/B flag; M1 re-checked after EACH lever (always-on legibility gate).

Purpose: These two levers can only HELP legibility (sharper texture lookup; cleaner edge) and SSOT
orders them first so the legibility floor is defended before any softening lever (micro-relief,
clearcoat) is added. One variable per gate, captured against tp2-base.

Output: an edited `TableLab.tsx` (useThree maxAniso + ?card flag) and `cards.ts` (seam params if the
MACRO seam still shows); fresh `?card=aniso` and `?card=seam` captures with M1 re-measured >= floor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-01-SUMMARY.md
@docs/table-3d/TP2_BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lever 1 — max-anisotropy on the card face (via useThree) + confirm mipmaps, behind ?card=</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 15 (current fiber import: `Canvas, useLoader` — useThree is NOT yet imported), 67-71 (qp helper), 303-316 (useCardFaces — anisotropy currently hardcoded `t.anisotropy = 8`, colorSpace SRGB, needsUpdate), 656-658 (where useCardKit() + useCardFaces() are called inside Scene — the mount point where gl is accessible).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md Pattern 1 (lines 270-295: useThree((state)=>state.gl) + Math.min(gl.capabilities.getMaxAnisotropy(),16)), Pattern 2 (lines 297-323: minFilter LinearMipmapLinearFilter + generateMipmaps already DEFAULT in 0.169; NO mipmapBias exists), Pitfall 1 (lines 540-550: no lodBias; if still soft, lower faceMat roughness — NOT a bias), Pitfall 2 (lines 552-560: gl.capabilities only valid inside a mounted R3F component — read it in Scene), Anti-Patterns lines 498-500 (never hardcode 16; some GPUs cap lower).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "Face texture anisotropy upgrade" (lines 81-90) + "Renderer capabilities access pattern" (lines 273-279) + the ?card flag template (lines 260-271).
    - docs/table-3d/TP2_BASELINE.md (from 03-01) — the recorded M1 floor px-height this lever must not regress below.
  </read_first>
  <action>
    Add `useThree` to the existing `@react-three/fiber` import in TableLab.tsx. In the Scene component
    (where useCardKit/useCardFaces are called, ~line 657), read the renderer once with
    `const gl = useThree((s) => s.gl)` and compute `const maxAniso = Math.min(gl.capabilities.getMaxAnisotropy(), 16)`
    in a useMemo keyed on gl. Pass `maxAniso` into `useCardFaces` (add a parameter) and, inside its useMemo,
    set `t.anisotropy = maxAniso` instead of the hardcoded 8, leaving `t.colorSpace = THREE.SRGBColorSpace`
    and `t.needsUpdate = true` as-is; explicitly set `t.minFilter = THREE.LinearMipmapLinearFilter` and
    `t.generateMipmaps = true` to make the mipmap chain assertion-visible (they are already the 0.169
    defaults — this just makes the intent explicit, per RESEARCH Pattern 2). Gate the whole upgrade behind a
    `?card=` flag using the existing qp() pattern: `qp("card") === "base"` keeps the pre-TP2 anisotropy 8
    (the A/B "before"); any other value (including no param) uses maxAniso. Do NOT add a mip bias (none
    exists in three 0.169 — RESEARCH Pitfall 1). Do NOT touch the frozen cameras, the clearcoat/sheen values,
    or any geometry in this task. Then bring the dev server up, capture POV at `?cam=card&card=aniso` and
    `?cam=card&card=base` into the gitignored `.dev-stack/diag/table-3d/tp2/lever1-aniso/`, downscale the
    aniso capture to the M1 reference height, and manually re-measure the rank-glyph px-height. Record the
    aniso-vs-base px-height and the verdict (sharpened / unchanged / regressed) in the SUMMARY. STOP criterion:
    if M1 regresses below the tp2-base floor, revert lever 1 (restore anisotropy 8) and flag it (non-blocking).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8');if(!/getMaxAnisotropy/.test(s)){console.error('maxAniso not wired');process.exit(1)}if(/anisotropy\s*=\s*16\b/.test(s)){console.error('hardcoded 16 forbidden — use Math.min(getMaxAnisotropy(),16)');process.exit(1)}console.log('lever1 aniso wired via getMaxAnisotropy')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lab/TableLab.tsx` imports `useThree` and sets the card-face `anisotropy` to `Math.min(gl.capabilities.getMaxAnisotropy(), 16)` (read in Scene); the grep gate confirms `getMaxAnisotropy` present and no bare hardcoded `anisotropy = 16`.
    - `minFilter = THREE.LinearMipmapLinearFilter` and `generateMipmaps = true` are explicit on the face texture; no `mipmapBias`/`lodBias` property is added (it does not exist in three 0.169).
    - The upgrade is behind a `?card=` flag (`?card=base` = pre-TP2 anisotropy 8; otherwise maxAniso) using the qp() pattern.
    - `cd frontend && npx vitest run src/lab/` exits 0 (27/27 green) and `npx tsc --noEmit` is clean.
    - The SUMMARY records the M1 rank-glyph px-height on `?card=aniso` vs the tp2-base floor and the verdict; if it regressed, the lever was reverted + flagged (non-blocking).
  </acceptance_criteria>
  <done>Card-face anisotropy reads the GPU max (cap 16) via useThree, mipmaps confirmed, behind ?card=; M1 re-measured >= the tp2-base floor (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Lever 2 — face-to-bevel seam fix at MACRO (no cream rim / z-fight), CARD_CORNER + curveSegments unchanged</name>
  <files>frontend/src/lab/cards.ts, frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/cards.ts lines 33 (CARD_CORNER 0.17 — LOCKED), 141-151 (cardBodyGeometry: bevelThickness 0.012, bevelSize 0.012, bevelSegments 2, curveSegments 14), 158-174 (cardFaceGeometry inset 0.018 + CARD_FACE_Z = CARD_T + 0.012 + 0.004 — the face-decal float offset that prevents z-fight with the body front cap).
    - frontend/src/lab/TableLab.tsx lines 318-337 (Card component: faceMat with side DoubleSide; the face mesh at position [0,0,CARD_FACE_Z]) — the seam is the join between the body bevel rim and the face decal.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md "LEVER 2" diagram block (lines 179-188: bevelSegments 2 -> 4-6 to smooth the seam; faceMat alphaTest 0.01 OR shift CARD_FACE_Z +0.002; curveSegments already 14; CARD_CORNER stays 0.17), Open Question 2 (lines 744-751: raising bevelSegments does not change the UV bbox or card dims — 27/27 should stay green).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "Card geometry seam fix" (lines 117-134) — seam fix achieved by bevelSize <= bevelThickness and bevelSegments >= 2; raise bevelSegments to 3-4 only if the MACRO seam still shows; CARD_CORNER 0.17 untouched.
    - docs/table-3d/anchors/tp2-base/macro.png (from 03-01) — the MACRO "before" to compare the seam against.
  </read_first>
  <action>
    Diagnose first: capture MACRO at `?cam=macro&card=seam` (lever 1 active) into
    `.dev-stack/diag/table-3d/tp2/lever2-seam/` and inspect the join between the body bevel rim and the
    face decal at the card corners. The seam fault is a thin cream rim (body stock showing past the face
    decal) and/or a z-fight halo (face decal and body front cap competing for depth). Apply the MINIMAL
    correction: if a z-fight halo shows, add `alphaTest: 0.01` to the faceMat in the Card component (or nudge
    `CARD_FACE_Z` by +0.002 — pick ONE, the smaller visual change); if a cream rim shows at the rounded
    corners, raise `bevelSegments` in cardBodyGeometry from 2 to 3-4 (do NOT exceed 6) to smooth the
    face-to-bevel transition. Keep `CARD_CORNER` at 0.17, `curveSegments` at 14, and `bevelSize <=
    bevelThickness` (both 0.012) — all LOCKED by SSOT §TP2. Wire the seam fix behind the same `?card=` flag
    family so it is independently A/B-toggleable (e.g. the seam params only differ when the card flag is not
    `base`). Re-capture MACRO after the fix and confirm the seam reads clean vs tp2-base/macro.png. Re-measure
    M1 on POV `?cam=card` after the seam edit (the always-on gate) and confirm it is still >= the floor. If
    the seam was already clean on tp2-base/macro.png, record "seam already clean — no geometry change" and
    make no edit beyond confirming the flag wiring.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('frontend/src/lab/cards.ts','utf8');const m=s.match(/CARD_CORNER\s*=\s*([0-9.]+)/);if(!m||m[1]!=='0.17'){console.error('CARD_CORNER must stay 0.17, got '+(m&&m[1]));process.exit(1)}if(!/curveSegments:\s*14/.test(s)){console.error('curveSegments must stay >=14 (14)');process.exit(1)}console.log('CARD_CORNER 0.17 + curveSegments 14 preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lab/` exits 0 (27/27 green — the geometry invariants HOLE_PITCH < CARD_W, HOLE_Z > 2, hole-pair height-stagger unaffected) and `npx tsc --noEmit` is clean.
    - The grep gate confirms `CARD_CORNER = 0.17` and `curveSegments: 14` are unchanged; if bevelSegments was raised it is within 3-4 (<= 6) and `bevelSize <= bevelThickness`.
    - The SUMMARY records the MACRO seam verdict (clean vs tp2-base/macro.png) and which minimal correction was applied (alphaTest 0.01 | CARD_FACE_Z +0.002 | bevelSegments raise | none-needed).
    - M1 re-measured on POV after the seam edit is still >= the tp2-base floor.
  </acceptance_criteria>
  <done>The MACRO face-to-bevel seam reads clean (cream rim / z-fight halo gone or already absent) with CARD_CORNER 0.17 + curveSegments 14 intact; M1 still >= floor; lab Vitest + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. Only `frontend/src/lab/`
is touched; `/table-lab.html` is never in the production Vite build (SSOT §5.8). No
auth/network/data/user-input surface. The boundaries that matter are the legibility floor and the
one-variable-per-gate A/B discipline.

| Boundary | Description |
|----------|-------------|
| pre-TP2 card state -> lever 1/2 state | Each lever is behind a ?card= flag so the pre-TP2 baseline is A/B-recoverable in one query param |
| sharpening levers -> the M1 floor | M1 is re-measured after EACH lever; a regression STOPS + reverts that lever (non-blocking) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02a | Tampering | M1 legibility floor | mitigate | Anisotropy + seam are legibility-positive; M1 re-measured after each lever vs the tp2-base floor; regression -> revert + flag |
| T-03-02b | Tampering | locked geometry (CARD_CORNER, curveSegments) | mitigate | grep gate asserts CARD_CORNER 0.17 + curveSegments 14 unchanged; bevelSegments raise bounded to <= 6 |
| T-03-02c | Tampering | frozen cameras / lab isolation | mitigate | No camera-preset or non-lab edit; tsc + vitest confirm the lab tree compiles and invariants hold |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 (RESEARCH "No new packages needed") |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lab/` exits 0 (27/27) and `npx tsc --noEmit` is clean after both levers.
- Anisotropy reads `Math.min(gl.capabilities.getMaxAnisotropy(), 16)` via useThree; no hardcoded 16; mipmaps explicit.
- CARD_CORNER 0.17 + curveSegments 14 preserved; bevelSegments (if raised) within 3-4.
- M1 rank-glyph px-height re-measured >= the tp2-base floor after EACH lever (recorded in SUMMARY); any regression -> lever reverted + flagged.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Lever 1 (max-anisotropy + mipmaps confirmed) and Lever 2 (seam fix) shipped behind a ?card= flag, one variable per gate.
- M1 not regressed vs the tp2-base floor after either lever; locked geometry intact; lab Vitest 27/27 + tsc green.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-02-SUMMARY.md` when done — record the per-lever M1 px-height vs floor, the seam verdict, and the ?card= flag names used.
</output>
