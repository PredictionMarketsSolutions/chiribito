---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/src/lab/textures.ts
  - frontend/src/lab/TableLab.tsx
requirements:
  - "SSOT §TP2 — faint card-stock micro-relief normal (linen/emboss hint), built via the shared height->normal helper"
  - "SSOT §TP2 — a whisper of coat (clearcoat 0.12-0.18, clearcoatRoughness 0.5-0.6) — NOT glossy/laminated"
  - "SSOT §TP2 — M1 legibility MUST NOT regress (re-checked after EVERY lever); M5 highlight-clip PASS (no over-clearcoat flare)"
autonomous: true
must_haves:
  truths:
    - "Lever 3 (micro-relief): a new cardMicroReliefNormalMap() exists in textures.ts, built via normalMapHelper.heightToNormalMap + toNormalMapTexture (NoColorSpace, mirroring feltNapNormalMap), created ONCE in useCardKit and attached by reference to the stock body material with normalScale ~0.12 — behind a ?card= flag"
    - "Lever 3: the normal map is NOT sRGB (toNormalMapTexture sets NoColorSpace) and is NOT re-allocated per card (one CanvasTexture instance shared across all cards — Pitfall 5)"
    - "Lever 4 (clearcoat): the stock clearcoat is lowered to 0.12 with clearcoatRoughness 0.55 (restraint-first, SSOT range 0.12-0.18 / 0.5-0.6) — NOT glossy; behind a ?card= flag"
    - "After lever 3 AND after lever 4: M1 re-measured >= the tp2-base floor; M5 highlight-clip PASS on the fresh HERO (no over-clearcoat plastic flare); any plastic/laminated read OR M1 regression -> STOP + revert that lever (non-blocking, flag)"
  artifacts:
    - path: "frontend/src/lab/textures.ts"
      provides: "cardMicroReliefNormalMap() — a faint linen/emboss height field -> Sobel normal (NoColorSpace), mirroring feltNapNormalMap"
      exports: ["cardMicroReliefNormalMap"]
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "useCardKit wiring the shared normal map (normalScale 0.12) onto the stock body + clearcoat 0.12 / clearcoatRoughness 0.55; ?card= flags for both levers"
      contains: "cardMicroReliefNormalMap"
  key_links:
    - from: "frontend/src/lab/normalMapHelper.ts (heightToNormalMap, toNormalMapTexture)"
      to: "cardMicroReliefNormalMap() in textures.ts"
      via: "the same Sobel + NoColorSpace chain feltNapNormalMap uses (SSOT §5.9 shared helper)"
      pattern: "heightToNormalMap"
    - from: "cardMicroReliefNormalMap() (created once in useCardKit)"
      to: "kit.stock.normalMap + normalScale 0.12"
      via: "single shared CanvasTexture attached by reference to the body material (Pitfall 5 — not per-card)"
      pattern: "normalMap"
---

<objective>
TP2 levers 3 + 4 — the first SOFTENING levers, added only after the sharpening levers (1+2) defended
the floor. Lever 3: a faint card-stock micro-relief normal map — a linen/emboss height field converted
via the shared `normalMapHelper` Sobel chain (exactly as TP1's `feltNapNormalMap`), created ONCE in
`useCardKit` and attached by reference to the stock BODY material at `normalScale ~0.12` (CONTEXT.md
"faint"). Lever 4: a whisper of clearcoat — lower the stock clearcoat from the current 0.16 to the
restraint-first 0.12 with `clearcoatRoughness 0.55` (SSOT range 0.12-0.18 / 0.5-0.6) so the card reads
coated-not-laminated. Both behind `?card=` flags; M1 re-checked after EACH lever, plus M5 highlight-clip
(the over-clearcoat plastic-flare guard).

Purpose: micro-relief adds stock weight/grain; the clearcoat whisper adds a coated-card read. Both are
restraint-first and the most likely to read "plastic" if overdone — so they come after the legibility
levers and each is gated by M1 (legibility) + M5 (highlight-clip) and the operator's plastic/laminated
STOP criterion (judged at the gate, 03-06).

Output: a new `cardMicroReliefNormalMap()` in `textures.ts`; `useCardKit` wiring the shared normal +
the clearcoat whisper behind `?card=` flags; fresh captures with M1 >= floor and M5 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-02-SUMMARY.md
@docs/table-3d/TP2_BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lever 3 — cardMicroReliefNormalMap() in textures.ts + wire onto the stock body (normalScale 0.12), behind ?card=</name>
  <files>frontend/src/lab/textures.ts, frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/textures.ts lines 14 (import { heightToNormalMap, toNormalMapTexture } from "./normalMapHelper"), 121-125 (makeCanvas), 146-159 (srgb / gray colorspace helpers — gray() = NoColorSpace), 510-541 (feltNapNormalMap — the EXACT pattern to mirror: imgData height-field loop, heightToNormalMap(c, strength), toNormalMapTexture, RepeatWrapping, repeat.set).
    - frontend/src/lab/normalMapHelper.ts lines 59-111 (heightDataToNormalData Sobel core — already unit-tested), 118-132 (heightToNormalMap canvas wrapper), 161-166 (toNormalMapTexture — sets colorSpace NoColorSpace + anisotropy 8).
    - frontend/src/lab/TableLab.tsx lines 281-300 (useCardKit — the stock MeshPhysicalMaterial; add the normalMap here, ONCE), 318-337 (Card — note faceMat is per-card; the normal goes on kit.stock NOT faceMat so the printed face stays crisp).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md Pattern 4 (lines 361-405: cardStockNormalMap factory — linen sine ridges, S=256, repeat ~ a few tiles), Pitfall 3 (lines 562-572: normal map MUST be NoColorSpace — use toNormalMapTexture, never srgb()), Pitfall 5 (lines 586-596: create the map ONCE in useCardKit, share by reference — not per-card), Code Examples lines 638-660 (target useCardKit shape: normalMap + normalScale Vector2(0.12,0.12)).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "cardMicroReliefNormalMap()" (lines 151-219) + "Sobel height->normal reuse" (lines 366-375) + "Texture colorspace discipline" (lines 345-354).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Card-Stock Aesthetics" (lines 59-67) — micro-relief very faint, normalScale ~0.12, via normalMapHelper.
  </read_first>
  <action>
    Add `cardMicroReliefNormalMap(): THREE.CanvasTexture` to textures.ts, mirroring feltNapNormalMap
    EXACTLY in structure but with a faint linen/emboss height field instead of concentric rings: a small
    canvas (S=256 is sufficient for a face-scale normal per RESEARCH), a per-pixel height field of fine
    horizontal/cross sine ridges (the laid-paper grain — see RESEARCH Pattern 4 / PATTERNS for the exact
    loop), then `heightToNormalMap(c, ...)` and `toNormalMapTexture(...)` (which sets NoColorSpace — NEVER
    use srgb() for a normal map, Pitfall 3), with RepeatWrapping and a face-scale `repeat.set(...)` (a few
    tiles across the card — RESEARCH suggests repeat 2-4; tune within "faint"). In TableLab.tsx useCardKit,
    create the texture ONCE (Pitfall 5 — not inside Card, not per-card) and attach it to the stock body
    material as `normalMap` with `normalScale: new THREE.Vector2(0.12, 0.12)` (CONTEXT "faint"). Keep the
    normal OFF the per-card faceMat so the printed Fournier face stays maximally crisp (legibility-first).
    Gate the normal behind a `?card=` flag using the qp() pattern (`qp("card") === "base"` = no normal map;
    otherwise on). Then capture POV `?cam=card&card=normal` and MACRO `?cam=macro&card=normal` into
    `.dev-stack/diag/table-3d/tp2/lever3-normal/`, re-measure M1 (rank-glyph px-height >= the tp2-base
    floor), and run the admitted metrics over a fresh HERO to confirm M5 highlight-clip still PASS. STOP
    criterion: if M1 regresses OR the stock reads plastic/laminated at MACRO, lower normalScale or revert
    lever 3 (non-blocking, flag). Record the chosen normalScale + repeat + the M1/M5 verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('frontend/src/lab/textures.ts','utf8');if(!/export function cardMicroReliefNormalMap/.test(t)){console.error('cardMicroReliefNormalMap not exported');process.exit(1)}if(/srgb\([^)]*\)[\s\S]{0,80}cardMicroRelief|cardMicroRelief[\s\S]{0,200}srgb\(/.test(t)){console.error('normal map must not use srgb()');process.exit(1)}const k=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8');if(!/cardMicroReliefNormalMap/.test(k)){console.error('normal map not wired in TableLab');process.exit(1)}console.log('lever3 normal map present + NoColorSpace path + wired')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lab/textures.ts` exports `cardMicroReliefNormalMap()` built via `heightToNormalMap` + `toNormalMapTexture` (NoColorSpace); it does NOT use `srgb()` on the normal path (Pitfall 3).
    - `useCardKit` in TableLab.tsx creates the normal map ONCE and attaches it to the stock body with `normalScale: Vector2(0.12, 0.12)` (not on the per-card faceMat); the grep gate confirms wiring.
    - The normal is behind a `?card=` flag (`?card=base` = off).
    - `cd frontend && npx vitest run src/lab/` exits 0 (27/27) and `npx tsc --noEmit` is clean.
    - The SUMMARY records the M1 px-height vs floor on `?card=normal`, the M5 highlight-clip verdict (PASS), and the chosen normalScale/repeat; any regression -> lever reverted + flagged.
  </acceptance_criteria>
  <done>A faint shared card-stock normal map (NoColorSpace, one instance) is on the stock body at normalScale 0.12 behind ?card=; M1 >= floor + M5 PASS (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Lever 4 — clearcoat whisper on the stock (0.12 / clearcoatRoughness 0.55), behind ?card=; re-check M1 + M5</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 287-300 (useCardKit stock: clearcoat currently 0.16, clearcoatRoughness 0.5, sheen 0.22, sheenColor #fff6e0), 318-330 (Card faceMat: clearcoat 0.1, clearcoatRoughness 0.55 — already near the target).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Card-Stock Aesthetics" (lines 59-67) — clearcoat 0.12 (low end) / clearcoatRoughness 0.55; raise toward 0.18 ONLY if the coat reads absent; restraint-first.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md Pitfall 7 (lines 607-617: over-clearcoat -> plastic at MACRO; stay 0.12; clearcoatRoughness 0.55 reduces the risk), Code Examples lines 645-657 (target stock clearcoat 0.12 / clearcoatRoughness 0.55).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "TP2 card stock target fields" (lines 66-78) + the faceMat clearcoat note (lines 105-114: faceMat clearcoat 0.1 -> 0.12 consistent with stock).
    - docs/table-3d/TP2_BASELINE.md — the M1 floor + the tp2-base M5 baseline value (so the M5 delta is apples-to-apples).
  </read_first>
  <action>
    In useCardKit, lower the stock material `clearcoat` from 0.16 to 0.12 and set `clearcoatRoughness` to
    0.55 (restraint-first per CONTEXT — NOT glossy). Optionally align the per-card faceMat clearcoat to 0.12
    / clearcoatRoughness 0.55 for consistency (it is already 0.1 / 0.55 — a whisper bump only). Gate the
    clearcoat change behind the same `?card=` flag family (`?card=base` keeps the pre-TP2 clearcoat 0.16).
    Do NOT exceed clearcoat 0.18 and do NOT touch sheen/sheenColor here (that is lever 5, plan 03-04).
    Capture POV `?cam=card&card=coat` and MACRO `?cam=macro&card=coat` into
    `.dev-stack/diag/table-3d/tp2/lever4-coat/`, re-measure M1 (>= floor), and run the admitted metrics over
    a fresh HERO confirming M5 highlight-clip still PASS (the over-clearcoat flare guard). STOP criterion: if
    the coat reads plastic/laminated at MACRO OR M5 fails OR M1 regresses, revert to clearcoat 0.16 (or hold
    at the highest value that still reads coated-not-plastic) and flag (non-blocking). Record the final
    clearcoat value + the M1/M5 verdicts in the SUMMARY. Note for the gate: clearcoat may be raised toward
    0.18 ONLY if the operator reads the coat as absent at 03-06 — do not pre-raise it here.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8');const m=[...s.matchAll(/clearcoat:\s*([0-9.]+)/g)].map(x=>parseFloat(x[1]));if(m.some(v=>v>0.18)){console.error('clearcoat exceeds 0.18 (plastic risk): '+m.join(','));process.exit(1)}console.log('clearcoat values within <=0.18: '+m.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - The stock `clearcoat` is 0.12 with `clearcoatRoughness` 0.55 (and the faceMat clearcoat is <= 0.12); the grep gate confirms NO clearcoat value exceeds 0.18.
    - The clearcoat change is behind a `?card=` flag (`?card=base` = pre-TP2 0.16); sheen/sheenColor unchanged in this task.
    - `cd frontend && npx vitest run src/lab/` exits 0 (27/27) and `npx tsc --noEmit` is clean.
    - The SUMMARY records the final clearcoat value, the M1 px-height vs floor on `?card=coat`, and the M5 highlight-clip verdict (PASS); any plastic read / M5 fail / M1 regression -> reverted + flagged.
  </acceptance_criteria>
  <done>Clearcoat whisper at 0.12 / clearcoatRoughness 0.55 (<= 0.18, coated-not-plastic) behind ?card=; M1 >= floor + M5 PASS (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. Only `frontend/src/lab/`
is touched; `/table-lab.html` is never in the production Vite build (SSOT §5.8). No
auth/network/data/user-input surface. The boundaries that matter are the legibility floor, the
highlight-clip ceiling, and the plastic/laminated STOP criterion.

| Boundary | Description |
|----------|-------------|
| sharpening levers (1/2) -> softening levers (3/4) | Micro-relief + clearcoat are the first levers that can read "plastic"; each is behind a ?card= flag and gated by M1 + M5 |
| color textures -> data textures | The new normal map MUST be NoColorSpace (toNormalMapTexture), never sRGB (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03a | Tampering | M1 legibility floor | mitigate | M1 re-measured after lever 3 AND lever 4 vs the tp2-base floor; regression -> revert + flag |
| T-03-03b | Tampering | M5 highlight-clip (over-clearcoat plastic flare) | mitigate | M5 run on fresh HERO after lever 4; clearcoat capped <= 0.18; grep gate enforces the cap |
| T-03-03c | Spoofing | normal-map colorspace | mitigate | toNormalMapTexture sets NoColorSpace; grep gate forbids srgb() on the normal path (Pitfall 3) |
| T-03-03d | Tampering | GPU texture-object count | mitigate | Normal map created ONCE in useCardKit + shared by reference (Pitfall 5), not per-card |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 (RESEARCH "No new packages needed") |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lab/` exits 0 (27/27) and `npx tsc --noEmit` clean after both levers.
- `cardMicroReliefNormalMap()` exported, NoColorSpace, created once in useCardKit, on the stock body at normalScale 0.12.
- No clearcoat value exceeds 0.18 (grep gate); stock clearcoat 0.12 / clearcoatRoughness 0.55.
- M1 re-measured >= the tp2-base floor after lever 3 AND lever 4; M5 highlight-clip PASS on fresh HERO; regressions -> reverted + flagged.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Lever 3 (faint shared micro-relief normal, NoColorSpace, normalScale 0.12) and Lever 4 (clearcoat whisper 0.12/0.55, <= 0.18) shipped behind ?card= flags, one variable per gate.
- M1 not regressed vs floor after either lever; M5 highlight-clip PASS; lab Vitest 27/27 + tsc green.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-03-SUMMARY.md` when done — record the chosen normalScale/repeat, the final clearcoat value, and per-lever M1 vs floor + M5 verdicts.
</output>
