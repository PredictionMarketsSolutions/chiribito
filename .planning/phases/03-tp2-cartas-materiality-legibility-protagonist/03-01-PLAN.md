---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/table-3d/metrics.mjs
  - docs/table-3d/anchors/tp2-base/card.png
  - docs/table-3d/anchors/tp2-base/hero.png
  - docs/table-3d/anchors/tp2-base/macro.png
  - docs/table-3d/TP2_BASELINE.md
requirements:
  - "SSOT §TP2 — re-baseline post-encuadre (apples-to-apples basis)"
  - "SSOT §TP2 — M1 legibility MUST NOT regress (establish the post-encuadre floor)"
  - "SSOT §TP2 — near-edge contact-shadow tightened (M6) — recalibrate the M6 region rects first"
  - "SSOT §5.3 — per-phase rollback tag before any edit"
autonomous: true
must_haves:
  truths:
    - "A rollback tag tp2-before-cards exists at the pre-edit HEAD (045f748), LOCAL only, never pushed"
    - "A dated TP2 baseline of the ADOPTED post-encuadre scene exists at docs/table-3d/anchors/tp2-base/{card,hero,macro}.png — the apples-to-apples delta basis for every TP2 lever"
    - "docs/table-3d/anchors/head/ and docs/table-3d/anchors/reference-tag/ are byte-unchanged (the protected references are never degraded)"
    - "The M1 hole-card rank-glyph px-height on the adopted scene is measured and recorded as the post-encuadre legibility FLOOR (no lever may regress below it)"
    - "The M6 region rects (REGIONS.underCardHero / feltAdjacentHero) are verified-or-recalibrated against tp2-base/hero.png so M6 lands under a real hole card on the CARD_W 2.05 / HOLE_Z 2.3 scene"
  artifacts:
    - path: "docs/table-3d/anchors/tp2-base/hero.png"
      provides: "TP2 apples-to-apples baseline at the frozen HERO money shot (the M-metric delta basis)"
    - path: "docs/table-3d/anchors/tp2-base/card.png"
      provides: "TP2 baseline at the frozen POV money shot (M1 legibility floor capture)"
    - path: "docs/table-3d/anchors/tp2-base/macro.png"
      provides: "TP2 baseline at the frozen MACRO money shot (seam / micro-relief delta basis)"
    - path: "docs/table-3d/TP2_BASELINE.md"
      provides: "The recorded M1 floor (measured rank-glyph px-height), the M6 rect verdict (kept or recalibrated with before/after coords + re-admission), and the capture provenance (commit 045f748, scene constants)"
      contains: "M1"
  key_links:
    - from: "git tag tp2-before-cards"
      to: "HEAD 045f748 on spike/table-3d-hero"
      via: "git tag (LOCAL, never pushed/deleted) — SSOT §5.3 per-phase rollback"
      pattern: "tp2-before-cards"
    - from: "docs/table-3d/anchors/tp2-base/hero.png"
      to: "tools/table-3d/run-metrics.mjs + m1-m2-m12.mjs --zero-change"
      via: "every TP2 lever measures its M5/M6/M12 delta against tp2-base, not against head/"
      pattern: "tp2-base"
    - from: "tools/table-3d/metrics.mjs REGIONS.underCardHero"
      to: "the adopted-scene hole-card pixel position in tp2-base/hero.png"
      via: "M6 rect recalibration so under-card luma samples a real card shadow, not open felt"
      pattern: "underCardHero"
---

<objective>
Wave-0 foundation for TP2 (card materiality). Three things, in strict order, touching NO card render
output: (1) cut the per-phase rollback tag `tp2-before-cards` at the clean pre-edit HEAD (045f748)
BEFORE any card edit (SSOT §5.3); (2) capture a NEW dated TP2 baseline of the ADOPTED post-encuadre
scene (FELT_R 6.5, CARD_W 2.05, 5-card board, HOLE_Z 2.3) at the 3 frozen money shots →
`docs/table-3d/anchors/tp2-base/` (the operator-decided "Re-baseline post-encuadre" — the
apples-to-apples basis for every TP2 metric delta) and re-measure M1 on it to establish the
post-encuadre legibility FLOOR; (3) verify-or-recalibrate the M6 region rects, which RESEARCH flagged
as calibrated on the OLD TP0 scene (CARD_W 2.4) and possibly now sampling open felt on the smaller-card
scene — so M6 can be trusted as a gate for the lever plans.

Purpose: TP0 anchors capture the OLD scene; TP2 needs an apples-to-apples "before" of the CURRENT
scene. This plan establishes the safety net (tag), the delta basis (tp2-base), the always-on legibility
floor (M1), and a trustworthy M6 — so the lever plans (03-02 … 03-05) can each do a one-variable A/B
against a fixed, correct baseline. The TP0 protected anchors (`head/`, `reference-tag/`) stay IMMUTABLE.

Output: `tp2-before-cards` tag (LOCAL); `docs/table-3d/anchors/tp2-base/{card,hero,macro}.png`;
`docs/table-3d/TP2_BASELINE.md` (M1 floor + M6 rect verdict + capture provenance); and, only if
recalibration was required, an edit to `REGIONS.underCardHero`/`feltAdjacentHero` in `metrics.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the tp2-before-cards rollback tag + capture the tp2-base baseline at the 3 frozen shots</name>
  <files>docs/table-3d/anchors/tp2-base/card.png, docs/table-3d/anchors/tp2-base/hero.png, docs/table-3d/anchors/tp2-base/macro.png</files>
  <read_first>
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §5 lines 187-207 (rollback disposition table) and §TP2 lines 291-308 — the per-phase rollback tag is cut BEFORE starting the TP; the TP2 row is "non-blocking, flag". The doc WINS on any conflict with this plan.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Eval-Rig Reconciliation — post-encuadre" (lines 25-34) — operator-locked: capture a NEW dated tp2-base of the adopted scene; head/ and reference-tag/ stay IMMUTABLE.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md "Pattern 7" (lines 456-490) + "Code Examples" (lines 663-697) — the exact lab-shot.mjs capture commands, the anchor dir layout, and the tracking/downscale decision (commit the PNGs; head/ PNGs are ~1440px wide).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "docs/table-3d/anchors/tp2-base/" section (lines 291-327) — tracking = COMMITTED (parallel to head/), capture command with spin=off, file naming card/hero/macro.png.
    - .dev-stack/lab-shot.mjs — the Playwright D3D11 headless capture harness: reads LAB_URL env, auto-appends spin=off, writes the PNG to the path arg. Confirm the dev-server port it expects (the resume protocol uses 5173; Vite may fall back to 5174/5180 — read the actual port from the dev-server console).
    - frontend/src/lab/TableLab.tsx lines 673-696 — the frozen camera presets card (fov 40, line 678), hero (fov 32, line 680), macro (fov 26, line 683). These pos/fov are FROZEN — capture them, do NOT edit them.
  </read_first>
  <action>
    First confirm the working tree is clean and HEAD is the intended pre-edit commit (expected 045f748 on
    spike/table-3d-hero, no card edits yet) and that only `tp0-before-rig` and `tp1-before-felt` tags exist;
    then create `git tag tp2-before-cards` at HEAD (no -f, never push, never delete — SSOT §5.3). Do NOT make
    any commit before the tag; the tag MUST sit at the clean pre-edit HEAD. Then start the Vite dev server
    (`cd frontend && npm run dev`) and note the actual port. Capture the 3 frozen money shots of the ADOPTED
    scene (no card lever flags — the default scene = the post-encuadre baseline) into the NEW tracked dir
    `docs/table-3d/anchors/tp2-base/` using `.dev-stack/lab-shot.mjs` with `LAB_URL` set to the lab URL at the
    correct port plus `?cam=card`, `?cam=hero`, `?cam=macro` (spin=off is auto-appended). Downscale each
    committed PNG to match the head/ corpus width (~1440px) per the RESEARCH/PATTERNS tracking decision; keep
    any full-res copies only under the gitignored `.dev-stack/diag/table-3d/tp2-base/`. Do NOT write or modify
    any file under `docs/table-3d/anchors/head/` or `docs/table-3d/anchors/reference-tag/` — they are immutable.
    Do NOT touch any card material/geometry/flag here; this task only cuts the tag and captures pixels.
  </action>
  <verify>
    <automated>git tag --list tp2-before-cards</automated>
    <automated>git rev-list -n 1 tp2-before-cards</automated>
    <automated>node -e "const fs=require('fs');for(const f of ['card','hero','macro']){if(!fs.existsSync('docs/table-3d/anchors/tp2-base/'+f+'.png')){console.error('MISSING '+f);process.exit(1)}}console.log('tp2-base 3/3 present')"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list tp2-before-cards` outputs `tp2-before-cards`; `git rev-list -n 1 tp2-before-cards` equals the pre-edit HEAD (045f748); the tag is unpushed (LOCAL).
    - `docs/table-3d/anchors/tp2-base/card.png`, `hero.png`, `macro.png` all exist and are non-empty PNGs captured at the frozen card/hero/macro cameras of the default (post-encuadre) scene.
    - `git status` shows the 3 new tp2-base PNGs (and nothing under head/ or reference-tag/); a diff confirms `docs/table-3d/anchors/head/` and `docs/table-3d/anchors/reference-tag/` are byte-unchanged.
    - No edit to any frontend/src/lab/ card material, geometry, or camera preset.
  </acceptance_criteria>
  <done>Tag tp2-before-cards exists at pre-edit HEAD (LOCAL); the 3 tp2-base money-shot PNGs are captured + tracked; the TP0 anchors are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the M1 legibility floor + verify/recalibrate the M6 region rects on tp2-base</name>
  <files>docs/table-3d/TP2_BASELINE.md, tools/table-3d/metrics.mjs</files>
  <read_first>
    - tools/table-3d/m1-m2-m12.mjs lines 38-45 (THRESHOLDS, M1_PX_HEIGHT_MIN = 22), lines 140-175 (m1Legibility / m1DownscalePov — px-height + operator-confirm, NO OCR) — the exact M1 measurement convention.
    - tools/table-3d/metrics.mjs lines 98-105 — REGIONS.underCardHero `{left:360, top:1230, width:220, height:90}` and feltAdjacentHero `{left:360, top:1090, width:220, height:90}`, calibrated on the TP0 scene (comment line 99: under-card luma ≈172 vs felt ≈200 → ≈14% darker, ≥12% gate).
    - tools/table-3d/run-metrics.mjs lines 60-67 (M6 control entry → m6ContactShadow(png, REGIONS.underCardHero, REGIONS.feltAdjacentHero)) and lines 195-220 (captureRun runs M6 on hero.png) — how M6 reads the rects; lines 229-237 (CLI: bare dir = capture run, --meta-gate <controlsDir> = re-admission).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md Pitfall 8 (lines 618-633) + Open Question 1 (lines 735-742) + Assumption A2 (line 726) — WHY the M6 rect may now sample open felt and the recalibration procedure (inspect tp2-base/hero.png; if open felt, move the rect under a real hole card and re-run the meta-gate).
    - frontend/src/lab/cards.ts lines 30, 72-73 — CARD_W 2.05, HOLE_Z 2.3, HOLE_PITCH CARD_W*0.98 — the new scene constants that moved the hole cards vs the TP0 calibration.
    - docs/table-3d/anchors/tp2-base/hero.png (from Task 1) — the actual frame to inspect for the M6 rect position.
  </read_first>
  <action>
    M1 floor: downscale tp2-base/card.png to the M1 reference height (the M1_DOWNSCALE_H used by
    m1DownscalePov — read it from m1-m2-m12.mjs), open the downscale, and manually measure the hole-card
    rank-glyph bounding-box height in pixels (the SSOT/M1 convention is px-height + operator confirm, NOT
    OCR). Record the measured px-height in docs/table-3d/TP2_BASELINE.md as the post-encuadre M1 FLOOR, noting
    that at CARD_W 2.05 it is expected to be below the 22px M1_PX_HEIGHT_MIN target (the pre-encuadre reading
    was ~17px) — TP2 levers must SHARPEN but MUST NOT reduce this number; if a lever drops below the floor,
    that lever STOPS (SSOT §TP2). M6 rect check: inspect tp2-base/hero.png at the current underCardHero rect
    (left:360, top:1230, 220×90) and feltAdjacentHero rect (left:360, top:1090, 220×90). If underCardHero still
    lands under a hole card and feltAdjacentHero on adjacent open felt → record "M6 rects KEPT (still valid on
    adopted scene)" with the observed luma values. If underCardHero now samples open felt (the failure RESEARCH
    predicts) → update BOTH rects in metrics.mjs to the new hole-card pixel position, then re-run the meta-gate
    (`node tools/table-3d/run-metrics.mjs --meta-gate docs/table-3d/anchors/controls`) and confirm M6 is still
    ADMITTED on the recalibrated rects; record the before/after coords + the re-admission verdict in
    TP2_BASELINE.md. Also run the admitted metric set over tp2-base
    (`node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp2-base`) and record the baseline M5/M6 values
    so the lever plans have the "before" numbers. Do NOT change any THRESHOLDS; only the REGIONS rect coords
    may change, and only if recalibration is required.
  </action>
  <verify>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp2-base</automated>
    <automated>node tools/table-3d/run-metrics.mjs --meta-gate docs/table-3d/anchors/controls</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/table-3d/TP2_BASELINE.md','utf8');if(!/M1/.test(s)||!/M6/.test(s)){console.error('TP2_BASELINE.md missing M1 or M6 record');process.exit(1)}console.log('baseline doc records M1 + M6')"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp2-base` runs and prints the admitted-metric table over tp2-base/hero.png (the M5/M6 baseline values are recorded in TP2_BASELINE.md).
    - `node tools/table-3d/run-metrics.mjs --meta-gate docs/table-3d/anchors/controls` exits 0 with M6 still listed ADMITTED (whether the rects were kept or recalibrated).
    - docs/table-3d/TP2_BASELINE.md records: the measured M1 rank-glyph px-height (the post-encuadre FLOOR), the M6 rect verdict (KEPT or RECALIBRATED with before→after coords), and the capture provenance (commit 045f748 + scene constants CARD_W 2.05 / HOLE_Z 2.3).
    - If metrics.mjs was edited, ONLY REGIONS.underCardHero / feltAdjacentHero coords changed — `git diff tools/table-3d/metrics.mjs` shows no THRESHOLDS or metric-fn changes.
  </acceptance_criteria>
  <done>The post-encuadre M1 floor is measured + recorded; M6 is verified-or-recalibrated and still ADMITTED via the meta-gate; the tp2-base M5/M6 baseline numbers are recorded for the lever plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. TP2 touches ONLY
`frontend/src/lab/` (and here `tools/table-3d/` metric tooling + `docs/`); the lab route
`/table-lab.html` is never in the production Vite build (SSOT §5.8). No auth/network/data/user-input
surface exists. The only "boundaries" that matter are reference-integrity and scope.

| Boundary | Description |
|----------|-------------|
| pre-edit HEAD -> first card edit | The rollback tag `tp2-before-cards` must be cut on the clean side of this boundary (SSOT §5.3) |
| TP0 anchors (head/, reference-tag/) -> new tp2-base/ | The protected references are immutable; only the new dated baseline dir may be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | TP0 protected anchors (head/, reference-tag/) | mitigate | This plan writes ONLY to the new tp2-base/ dir; acceptance asserts head/ + reference-tag/ are byte-unchanged |
| T-03-02 | Tampering | M6 metric trustworthiness on the adopted scene | mitigate | M6 rects verified-or-recalibrated against the real tp2-base/hero.png + re-admitted via the meta-gate before any lever can gate on M6 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 (RESEARCH "No new packages needed"; Package Legitimacy Audit N/A) — nothing to verify |
</threat_model>

<verification>
- `git tag --list tp2-before-cards` prints the tag; `git rev-list -n 1 tp2-before-cards` resolves to the pre-edit HEAD (045f748).
- `docs/table-3d/anchors/tp2-base/{card,hero,macro}.png` exist; `head/` and `reference-tag/` byte-unchanged.
- `node tools/table-3d/run-metrics.mjs --meta-gate docs/table-3d/anchors/controls` exits 0 (M6 ADMITTED on the kept/recalibrated rects).
- `docs/table-3d/TP2_BASELINE.md` records the M1 floor px-height + the M6 rect verdict + capture provenance.
- `git status` shows only new docs/anchors files + (optionally) a REGIONS-only edit to metrics.mjs; no card render code touched; lab still absent from the vite build input.
</verification>

<success_criteria>
- Rollback tag `tp2-before-cards` exists at the pre-edit HEAD (LOCAL, unpushed).
- The dated tp2-base baseline (3 money shots) exists + tracked; the TP0 anchors are IMMUTABLE and unchanged.
- The post-encuadre M1 legibility FLOOR is measured + recorded (no lever may regress below it).
- M6 is verified-or-recalibrated for the adopted scene and still ADMITTED.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-01-SUMMARY.md` when done, and write the M1 floor + M6 verdict to `docs/table-3d/TP2_BASELINE.md`.
</output>
