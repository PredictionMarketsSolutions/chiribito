---
phase: 03-tp2-cartas-materiality-legibility-protagonist
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - frontend/src/lab/TableLab.tsx
  - frontend/src/lab/cards.ts
  - frontend/src/lab/cards.test.ts
requirements:
  - "SSOT §TP2 — paper-edge via a CHEAP fake (warm sheen rim on the stock body) — explicitly NO MeshTransmissionMaterial/transmission"
  - "SSOT §TP2 — restrained dealt variance: per-card resting micro-tilt/yaw <= ~1.5-2 deg, frozen deterministically (M9)"
  - "SSOT §TP2 — M1 legibility MUST NOT regress (re-checked after EVERY lever); M9 byte-determinism HARD for the dealt variance"
autonomous: true
must_haves:
  truths:
    - "Lever 5 (paper-edge): a warm sheen-rim read is added by tuning the stock material sheen/sheenColor only (no new texture, NO MeshTransmissionMaterial / no transmission property), behind a ?card= flag; watched for a casino edge-glow (STOP if it glows)"
    - "Lever 6 (dealt variance): per-card resting micro-tilt/yaw <= 1.5 deg (<= 0.026 rad) added to communityLayout + holeLayout via a DETERMINISTIC seed (Math.sin(i * integer-prime) * amplitude), frozen at construction (NOT in useFrame), behind a ?card= flag"
    - "Lever 6 preserves the existing holeLayout invariants: the hole pair still fans in opposite z-directions (sign of rotation[2] stays opposite between the two cards) and the height-stagger (dy > 0.05) is unaffected — the 27/27 lab Vitest stays green and a NEW unit test asserts the <= 0.026 rad bound"
    - "M9 byte-determinism HOLDS with dealt variance on: two consecutive captures at ?cam=card&spin=off are md5-identical; M1 re-measured >= the tp2-base floor after each lever"
  artifacts:
    - path: "frontend/src/lab/cards.ts"
      provides: "deterministic per-card micro-tilt/yaw (<= 0.026 rad, seeded by card index) in communityLayout + holeLayout, frozen at construction"
      contains: "Math.sin"
    - path: "frontend/src/lab/cards.test.ts"
      provides: "a unit test asserting the dealt-variance jitter stays within <= 0.026 rad AND the hole-pair opposite-sign fan is preserved"
      contains: "0.026"
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "the stock sheen/sheenColor paper-edge tuning + the ?card= flags for levers 5 and 6"
      contains: "sheenColor"
  key_links:
    - from: "communityLayout / holeLayout (cards.ts)"
      to: "the rendered card poses (CardGroup in TableLab.tsx)"
      via: "deterministic Math.sin(i * prime) * (<= 0.026 rad) micro-tilt/yaw applied at construction (M9-safe, not useFrame)"
      pattern: "Math\\.sin\\(i"
    - from: "stock material sheen / sheenColor (useCardKit)"
      to: "the beveled card rim under the key light"
      via: "warm sheen-rim paper-edge read (no new texture, no transmission)"
      pattern: "sheenColor"
---

<objective>
TP2 levers 5 + 6 — the paper-edge read and the dealt life. Lever 5: a CHEAP paper-edge — a subtle warm
sheen-rim achieved by tuning the stock material `sheen` / `sheenColor` only (reusing the existing sheen
lobe; NO new texture, and explicitly NO `MeshTransmissionMaterial` / no transmission — SSOT-locked) so
the beveled card rim catches a warm glow under the key. Lever 6: restrained dealt variance — per-card
resting micro-tilt/yaw <= 1.5 deg (<= 0.026 rad) added to `communityLayout` + `holeLayout`, generated by
a DETERMINISTIC seed (`Math.sin(i * integer-prime) * amplitude`) frozen at construction (NOT in useFrame)
so M9 byte-determinism holds. Both behind `?card=` flags; M1 re-checked after each; M9 verified with
variance on.

Purpose: the paper-edge sells "printed stock" at the card rim; the dealt variance sells "hand-dealt, not
machined" without exceeding the messy/non-deterministic bounds. The variance is the one lever with a HARD
automated gate (M9 byte-identity) and a unit-testable bound — so it gets a dedicated test. The paper-edge
is the lever most at risk of a casino edge-glow, watched at the gate.

Output: `cards.ts` deterministic micro-variance in both layouts; a new `cards.test.ts` bound assertion;
`TableLab.tsx` sheen-rim tuning + `?card=` flags; fresh captures with M1 >= floor and M9 md5-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md
@.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-03-SUMMARY.md
@docs/table-3d/TP2_BASELINE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lever 5 — paper-edge warm sheen-rim via sheen/sheenColor only (no texture, no transmission), behind ?card=</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx lines 287-300 (useCardKit stock: sheen currently 0.22, sheenColor #fff6e0; clearcoat already 0.12 from 03-03) — the only fields lever 5 may touch.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md "Card-Stock Aesthetics" (lines 59-67) — paper-edge = subtle WARM sheen-rim, reuse the material sheen, NO new texture, explicitly NO MeshTransmissionMaterial / transmission.
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md "LEVER 5" diagram (lines 211-219: sheen 0.22 -> ~0.35, sheenColor -> warm wheat #f5deb5, optional sheenRoughness ~0.6; effect = warm rim glow on the bevel; watch for casino edge-glow), Anti-Patterns lines 494-497 (NO MeshTransmissionMaterial — transmission pass cost + reads as glass/resin).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "TP2 card stock target fields" (lines 66-78: sheen / sheenColor warm) + the felt TP1 sheen analog (lines 41-62 — the proven sheen API at this three version).
  </read_first>
  <action>
    In useCardKit, tune the stock material toward a warm sheen-rim: raise `sheen` modestly (RESEARCH
    suggests ~0.22 -> ~0.35; stay restrained) and warm the `sheenColor` toward a warm-wheat (RESEARCH
    suggests #f5deb5); optionally add `sheenRoughness ~0.6` (matching the leather/warm convention). Do NOT
    add any new texture, do NOT add `transmission` / do NOT switch to MeshTransmissionMaterial (SSOT-locked
    — both forbidden). Gate the change behind the same `?card=` flag family (`?card=base` keeps the pre-TP2
    sheen 0.22 / #fff6e0). Capture POV `?cam=card&card=edge` and MACRO `?cam=macro&card=edge` into
    `.dev-stack/diag/table-3d/tp2/lever5-edge/`, re-measure M1 (>= floor), and run the admitted metrics over
    a fresh HERO confirming M5 highlight-clip still PASS. STOP criterion (the casino edge-glow guard): if the
    card rim reads as a glowing border / casino edge-glow, OR M1 regresses, lower the sheen / cool the
    sheenColor or revert lever 5 (non-blocking, flag). Record the final sheen / sheenColor / sheenRoughness +
    the M1/M5 verdicts and the edge-glow judgement in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('frontend/src/lab/TableLab.tsx','utf8');if(/transmission/i.test(s)||/MeshTransmissionMaterial/.test(s)){console.error('transmission / MeshTransmissionMaterial is FORBIDDEN for the paper-edge (SSOT)');process.exit(1)}if(!/sheenColor/.test(s)){console.error('sheenColor not present');process.exit(1)}console.log('paper-edge via sheen only; no transmission')"</automated>
  </verify>
  <acceptance_criteria>
    - The paper-edge is achieved by tuning `sheen` / `sheenColor` (+ optional `sheenRoughness`) on the stock body ONLY; the grep gate confirms NO `transmission` property and NO `MeshTransmissionMaterial` anywhere in TableLab.tsx.
    - No new texture was added for the edge; the change is behind a `?card=` flag (`?card=base` = pre-TP2 sheen).
    - `cd frontend && npx vitest run src/lab/` exits 0 (27/27) and `npx tsc --noEmit` is clean.
    - The SUMMARY records the final sheen/sheenColor/sheenRoughness, the M1 px-height vs floor on `?card=edge`, the M5 verdict (PASS), and the casino-edge-glow judgement; a glow / regression -> reverted + flagged.
  </acceptance_criteria>
  <done>A subtle warm sheen-rim paper-edge (sheen-only, no texture, no transmission) behind ?card=; no casino edge-glow; M1 >= floor + M5 PASS (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lever 6 — deterministic dealt variance (<= 0.026 rad) in cards.ts + unit test + M9 byte-determinism, behind ?card=</name>
  <files>frontend/src/lab/cards.ts, frontend/src/lab/cards.test.ts, frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - frontend/src/lab/cards.ts lines 60-68 (communityLayout — existing deterministic yaw `Math.sin(i * 1.7) * 0.025`), 93-115 (holeLayout — rotation `[-Math.PI/2 + lift, 0, dir * fan]` where dir = ∓0.5 for the pair and fan = HOLE_FAN 0.14; position height-stagger via HOLE_STACK 0.1 along the lifted normal).
    - frontend/src/lab/cards.test.ts lines 75-98 (holeLayout tests) — the invariants the variance MUST NOT break: `Math.sign(poses[0].rotation[2]) === -Math.sign(poses[1].rotation[2])` (opposite-direction fan), `rotation[0] > -Math.PI/2` (lifted), `dy > 0.05` (height-stagger); lines 51-73 (communityLayout invariants: same height/z row, rotation[0] ≈ -PI/2).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-RESEARCH.md Pattern 5 (lines 407-429: MAX_TILT_RAD = 1.5*PI/180 = 0.0262; microTilt = Math.sin(i*7.3)*MAX*0.5 on x; microYaw = Math.sin(i*3.1)*MAX on z; integer-constant seeds -> same every render -> M9), Pitfall 4 (lines 574-584: <= 1.5 deg keeps the hole pair clear of coplanar z-fight — HOLE_STACK provides ample clearance), Anti-Patterns lines 502-504 (NEVER Math.random — violates M9).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-PATTERNS.md "Dealt variance pattern" (lines 136-147: <= 0.022 rad ≈ 1.26 deg, applied at construction not useFrame) + "Test invariant pattern" (lines 377-396: the exact Vitest bound-assertion style to copy).
    - .planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-CONTEXT.md (line 66) — dealt variance <= 1.5 deg, frozen deterministically (M9).
  </read_first>
  <behavior>
    - For all card indices i in 0..6, the added micro-tilt and micro-yaw each satisfy |jitter| <= 0.026 rad (<= 1.5 deg).
    - The seeds are integer-constant `Math.sin(i * prime) * amplitude` (NO Math.random) -> the same pose every call for the same i (deterministic; unit-testable by calling the layout twice and asserting equality).
    - holeLayout STILL fans the pair in opposite z-directions: sign(poses[0].rotation[2]) === -sign(poses[1].rotation[2]) (the added z-yaw amplitude <= 0.026 is strictly smaller than |dir*fan| = 0.07, so the base sign cannot flip).
    - holeLayout STILL height-staggers the pair: poses[1].position[1] - poses[0].position[1] > 0.05 (the micro-tilt does not touch the HOLE_STACK height term).
    - communityLayout STILL lays each card flat-ish (rotation[0] within the micro-tilt of -PI/2) and keeps the row centred in x.
  </behavior>
  <action>
    Add deterministic per-card micro-variance to communityLayout and holeLayout in cards.ts. Define a bound
    constant `MAX_TILT_RAD = (1.5 * Math.PI) / 180` (= 0.0262 rad) and derive per-card offsets with
    integer-constant seeds: a micro-tilt on the x-rotation `Math.sin(i * <primeA>) * MAX_TILT_RAD * <=1` and
    a micro-yaw on the z-rotation `Math.sin(i * <primeB>) * MAX_TILT_RAD` (PATTERNS uses ~0.022 amplitude ≈
    1.26 deg — stay <= 0.026). Apply the yaw ADDITIVELY to the existing terms: community keeps its
    `Math.sin(i*1.7)*0.025` base and adds the new term within bound; hole keeps `dir * fan` and adds the new
    z-yaw — CRITICAL: keep the added z amplitude (<= 0.026) strictly below |dir*fan| (= 0.07) so the
    opposite-sign fan invariant holds (Pitfall 4 / the cards.test.ts assertion). NEVER use Math.random
    (M9). Apply ALL variance at construction time inside the layout functions (NOT in useFrame) so it is
    frozen and pixel-deterministic under spin=off. Gate the variance behind the same `?card=` flag family in
    TableLab.tsx (`?card=base` = the pre-TP2 minimal community yaw, NO hole variance) — thread a boolean into
    communityLayout/holeLayout via their existing options object or a parameter so the flag turns the new
    term on/off. Then EXTEND cards.test.ts with a new describe block covering every item in <behavior> (the
    <= 0.026 bound for all i, determinism via double-call equality, the preserved opposite-sign fan, the
    preserved height-stagger), following the existing Vitest style. Capture POV `?cam=card&card=var` into
    `.dev-stack/diag/table-3d/tp2/lever6-var/`, re-measure M1 (>= floor), and run M9 byte-determinism
    (`node tools/table-3d/m9-determinism.mjs --shot card --port <devport>`) confirming the two captures are
    md5-identical with variance ON. STOP criterion: if M9 fails (non-deterministic) OR M1 regresses OR the
    layout reads messy, halve the amplitude or revert lever 6 (non-blocking, flag). Record the chosen
    primes/amplitude + the M1/M9 verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('frontend/src/lab/cards.ts','utf8');if(/Math\.random/.test(c)){console.error('Math.random forbidden — M9 determinism');process.exit(1)}const t=fs.readFileSync('frontend/src/lab/cards.test.ts','utf8');if(!/0\.026/.test(t)){console.error('cards.test.ts missing the <=0.026 rad bound assertion');process.exit(1)}console.log('deterministic variance + bound test present')"</automated>
  </verify>
  <acceptance_criteria>
    - communityLayout + holeLayout add per-card micro-tilt/yaw via integer-constant `Math.sin(i * prime) * amplitude` with every offset <= 0.026 rad; the grep gate confirms NO `Math.random` in cards.ts.
    - The hole-pair opposite-sign fan and the dy > 0.05 height-stagger invariants are preserved (the added z amplitude < |dir*fan|); `cards.test.ts` has a NEW assertion for the <= 0.026 bound + determinism, and `cd frontend && npx vitest run src/lab/` exits 0 (now > 27 assertions, all green).
    - `npx tsc --noEmit` is clean; the variance is behind a `?card=` flag (`?card=base` = no hole variance).
    - M9 byte-determinism: `node tools/table-3d/m9-determinism.mjs --shot card` reports the two captures md5-identical with variance ON (recorded in the SUMMARY); M1 re-measured >= the tp2-base floor.
  </acceptance_criteria>
  <done>Deterministic dealt variance (<= 0.026 rad, seeded, frozen at construction) in both layouts behind ?card=, with a passing bound+determinism unit test; M9 md5-identical + M1 >= floor (or reverted + flagged); lab Vitest + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not shipped in the prod build. Only `frontend/src/lab/`
is touched; `/table-lab.html` is never in the production Vite build (SSOT §5.8). No
auth/network/data/user-input surface. The boundaries that matter are M9 byte-determinism, the legibility
floor, the geometry invariants, and the casino edge-glow STOP criterion.

| Boundary | Description |
|----------|-------------|
| static pose -> dealt variance | The variance MUST be deterministic (seeded, frozen at construction) so M9 byte-identity holds; Math.random is forbidden |
| restrained sheen -> casino edge-glow | The paper-edge sheen must not tip into a glowing border; operator STOP criterion + M5 guard |
| new variance -> existing geometry invariants | The added yaw amplitude < |dir*fan| so the opposite-sign fan + height-stagger invariants (cards.test.ts) survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04a | Tampering | M9 byte-determinism | mitigate | Variance uses integer-seed Math.sin (no Math.random), frozen at construction; M9 md5 verified with variance ON; grep gate forbids Math.random |
| T-03-04b | Tampering | hole-pair geometry invariants | mitigate | Added z amplitude <= 0.026 < |dir*fan| 0.07; cards.test.ts asserts opposite-sign fan + dy>0.05 still hold |
| T-03-04c | Tampering | M1 legibility floor | mitigate | M1 re-measured after lever 5 AND lever 6 vs the tp2-base floor; regression -> revert + flag |
| T-03-04d | Tampering | casino edge-glow (anti-casino identity) | mitigate | Paper-edge sheen-only; operator STOP on a glowing border; M5 highlight-clip guard |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in TP2 (RESEARCH "No new packages needed") |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lab/` exits 0 (> 27 assertions incl. the new variance bound + determinism) and `npx tsc --noEmit` clean.
- Paper-edge via sheen/sheenColor only; grep gate confirms NO transmission / NO MeshTransmissionMaterial.
- Variance <= 0.026 rad, integer-seeded (no Math.random); opposite-sign fan + height-stagger invariants preserved.
- M9 byte-determinism md5-identical with variance ON; M1 re-measured >= floor after each lever.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Lever 5 (warm sheen-rim paper-edge, no texture/no transmission) and Lever 6 (deterministic dealt variance <= 0.026 rad, M9-safe, unit-tested) shipped behind ?card= flags, one variable per gate.
- M1 not regressed vs floor; M9 byte-identical; geometry invariants intact; lab Vitest + tsc green.
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/03-tp2-cartas-materiality-legibility-protagonist/03-04-SUMMARY.md` when done — record the final sheen values, the variance primes/amplitude, and per-lever M1 vs floor + M9 + M5 verdicts.
</output>
