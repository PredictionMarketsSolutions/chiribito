---
phase: 07-tp6-profundidad-composicion-depth
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP6 — DepthOfField: focusDistance tied to hole-card world position, bokehScale 1.5-3; hole cards RAZOR-SHARP (M1 HARD gate); board/rail/accent gently soft"
  - "SSOT §TP6 — DOF NEVER softens the hero — M1 HARD gate; re-measure M1 with ?fx ON after DOF is added"
  - "SSOT §TP6 — worldFocusDistance in world units computed per camera preset (static useMemo); worldFocusRange 1.5 half-band"
  - "SSOT §TP6 — if worldFocusRange must be set so wide DOF becomes imperceptible, CUT DOF (non-blocking; second cut-weakest candidate after Noise)"

must_haves:
  truths:
    - "With ?fx, the hero shot hole cards (the Perla: Sota + 7 de Oros) are visually razor-sharp — rank glyphs crisp, pip detail not blurred"
    - "M1 PASSES with ?fx ON: hole card rank-glyph bbox height >= 22px at 1080p downscale, or operator confirms legible — re-measured post-DOF addition"
    - "Board/community cards (COMMUNITY_Z ≈ -0.55) and far rail receive perceptible gentle bokeh with ?fx (the DOF effect is present but not aggressive)"
    - "worldFocusDistance is computed from camera preset position (static useMemo — no useFrame hook); hero cam distance ≈ 7.8 world units, card cam ≈ 9.5 world units"
    - "If M1 fails (hero soft): worldFocusRange widened or bokehScale reduced to restore sharpness; if DOF imperceptible at safe params, DOF is cut and disposition recorded"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "DepthOfField added inside EffectComposer after N8AO; worldFocusDistance computed per cam preset; bokehScale/worldFocusRange at SSOT-range starting values"
      contains: "DepthOfField"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx DepthOfField"
      to: "cam preset (hero cam pos [1.2, 5.0, 8.2])"
      via: "worldFocusDistance = CAM_WORLD.distanceTo(HOLE_WORLD) in useMemo; HOLE_WORLD = [0, 0.07, 2.3] from cards.ts HOLE_Z"
      pattern: "worldFocusDistance"
---

<objective>
Add DepthOfField as the second effect inside the EffectComposer, after N8AO. The DOF must keep
the hole cards razor-sharp (M1 HARD gate) while gently softening the board/rail. WorldFocusDistance
is computed statically from the active camera preset position and the known hole-card world position.

Purpose: A whisper of DOF separates the hero foreground (hole cards) from the board and rail,
creating depth hierarchy without the gimmick read. The M1 HARD gate is absolute — if DOF softens
the hero at any bokehScale within the SSOT range, it is cut (non-blocking; the table ships without
DOF; the other effects remain).

Output: TableLab.tsx with DepthOfField at tuned params; M1 re-confirmed PASS with ?fx on;
M11 DOF delta recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DepthOfField with worldFocusDistance computed from cam preset; re-confirm M1 with ?fx ON</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md — the prop name
      findings for DepthOfField specifically: worldFocusDistance confirmed present? worldFocusRange
      confirmed? If worldFocusDistance was NOT found in the installed dist, use the fallback:
      compute the normalized focusDistance using camera near/far (near=0.1, far=1000 R3F default;
      normalized value = distance / (far - near) ≈ 7.8 / 999.9 ≈ 0.0078 for hero cam).
    - 07-RESEARCH.md "Pattern 3: DepthOfField — the M1 HARD constraint" — the full worldFocusDistance
      computation (HERO cam ≈ 7.8wu, CARD cam ≈ 9.5wu, MACRO ≈ 4.5wu), the useMemo pattern, the
      HOLE_WORLD position [0, 0.07, 2.3] from cards.ts HOLE_Z=2.3 / FELT_REST_Y.
    - 07-RESEARCH.md "Pitfall 2: DOF softens the hero (M1 HARD gate failure)" — the exact
      diagnosis and correction path (widen worldFocusRange, lower bokehScale, or cut DOF).
    - frontend/src/lab/cards.ts — confirm HOLE_Z value and FELT_REST_Y computation. Also confirm
      the cam preset array: hero pos [1.2, 5.0, 8.2], card/POV pos [0, 4.7, 10.6], macro pos.
      Read the first 80 lines to locate these constants.
    - frontend/src/lab/TableLab.tsx — locate: (a) the cam preset useMemo (cam.pos is [x,y,z]);
      (b) how the active cam preset is selected (qp("cam") param pattern); (c) where scene
      useThree is called (camera ref needed for worldFocusDistance); (d) the EffectComposer block
      from 07-01/07-02. DepthOfField goes as second child inside EffectComposer (after N8AO).
  </read_first>
  <action>
    STEP 1 — Read the prop name verification from 07-01-SUMMARY. Decide:
      A) If worldFocusDistance confirmed: use worldFocusDistance + worldFocusRange.
      B) If not confirmed (absent): use focusDistance (normalized) — compute as
         holeCardDistance / (camera.far - camera.near) from useThree camera ref.

    STEP 2 — Update import line:
      FROM: import { EffectComposer, N8AO } from "@react-three/postprocessing";
      TO:   import { EffectComposer, N8AO, DepthOfField } from "@react-three/postprocessing";

    STEP 3 — Add the hole-card world position and per-preset focus distance computation.
    Inside the Scene component (or whichever R3F component owns the cam preset logic), add:

      // Hole card world position — from cards.ts HOLE_Z=2.3, FELT_REST_Y+0.02≈0.07
      const HOLE_WORLD = useMemo(
        () => new THREE.Vector3(0, 0.07, 2.3),
        []
      );

      // Camera world position from the active preset (cam.pos is [x, y, z])
      const CAM_WORLD = useMemo(
        () => new THREE.Vector3(...cam.pos),
        [cam]
      );

      // Distance: camera to hole cards (world units)
      const holeCardDistance = useMemo(
        () => CAM_WORLD.distanceTo(HOLE_WORLD),
        [CAM_WORLD, HOLE_WORLD]
      );

    This gives: hero cam ≈7.8wu, card/POV cam ≈9.5wu, macro cam ≈4.5wu (per RESEARCH Pattern 3).
    The THREE import is already present (three is an existing dep); no new import needed.

    STEP 4 — Add DepthOfField as second child inside EffectComposer (after N8AO):

      {qp("fx") !== null && (
        <EffectComposer multisampling={4} enableNormalPass={false}>
          <N8AO aoRadius={...} intensity={...} distanceFalloff={...} halfRes={...} screenSpaceRadius={false} />
          <DepthOfField
            worldFocusDistance={holeCardDistance}
            worldFocusRange={1.5}
            bokehScale={2.0}
            focalLength={0.025}
          />
          {/* Vignette / Grade / Noise — added in 07-04 */}
        </EffectComposer>
      )}

    Starting params (per RESEARCH Pattern 3 + SSOT §TP6):
    - worldFocusDistance={holeCardDistance} — computed per cam preset at mount
    - worldFocusRange={1.5} — half-band in world units; hole card pair spans ~2wu center-to-edge
    - bokehScale={2.0} — mid-range of SSOT 1.5-3; start restrained
    - focalLength={0.025} — controls falloff speed; keep this value unless M1 indicates the DOF
      is too aggressive on board cards (lower focalLength = slower falloff = gentler DOF)

    STEP 5 — Capture hero shot with ?fx and re-run M1:
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/dof-hero.png
      node tools/table-3d/m1-m2-m12.mjs .dev-stack/diag/tp6/dof-hero.png
    M1 gate: rank-glyph bbox height >= 22px at 1080p downscale. If FAILS (hero soft):
      - Option 1: Increase worldFocusRange to 2.0 (wider sharp band; both hole cards more safely inside).
      - Option 2: Lower bokehScale to 1.5 (less aggressive blur falloff).
      - Option 3: If still failing at bokehScale=1.5 and worldFocusRange=2.5 → CUT DOF. Remove
        DepthOfField from the compositor entirely. Record disposition "DOF cut — M1 HARD gate;
        bokehScale range insufficient to keep hero sharp at legal params." The table ships without
        DOF; N8AO + Vignette + Grade + Noise remain.
    If M1 PASSES (hero sharp): proceed. Capture all 3 shots for the SUMMARY record:
      LAB_URL="http://localhost:5173/table-lab.html?cam=card&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/dof-card.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/dof-macro.png

    STEP 6 — M11 DOF isolation delta:
      node tools/table-3d/stats-read.mjs http://localhost:5173 hero
    Record DOF-only delta (vs. N8AO-only baseline from 07-02-SUMMARY). If the combined
    N8AO+DOF total breaches M11 (> 8ms), first try halfRes on N8AO (if not already); if still
    breaching, cut DOF (the second cut candidate). Record M11 values.

    STEP 7 — Verify M7 still PASS:
      node tools/table-3d/grep-check-tp5-06.cjs (should exit 0 — Bloom absent, EffectComposer present is OK)

    Commit: "feat(07-03): add DepthOfField worldFocusDistance=holeCardDistance (M1 PASS, whisper DOF)"
    OR if DOF cut: "feat(07-03): DOF evaluated and cut — M1 HARD gate; N8AO continues in compositor"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(/Bloom/.test(s)){console.error('FAIL: Bloom detected — M7 HARD gate');process.exit(1);} console.log('OK — no Bloom in TableLab.tsx');"</automated>
  </verify>
  <done>DepthOfField present in TableLab.tsx with worldFocusDistance computed per cam preset from static useMemo (or documented as cut with explicit reason). M1 re-confirmed PASS with ?fx on at hero shot (rank-glyph >= 22px or operator-confirm legible). M11 DOF delta recorded. grep-check exits 0. tsc clean. vitest green. Captures at .dev-stack/diag/tp6/dof-{hero,card,macro}.png. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only.

| Boundary | Description |
|----------|-------------|
| M1 HARD gate | Hole cards MUST remain razor-sharp under ?fx; if DOF softens them, DOF must be cut — this gate cannot be waived |
| worldFocusDistance prop presence (A1 assumption) | Verified in 07-01-SUMMARY; if absent, use normalized focusDistance fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03a | Tampering | DOF softens hero hole cards (M1 HARD gate failure) | mitigate | Widen worldFocusRange to 2.0 first; lower bokehScale to 1.5 second; CUT DOF if still failing at legal params; record disposition explicitly |
| T-07-03b | Tampering | worldFocusDistance wrong (wrong cam preset or wrong HOLE_WORLD) | mitigate | useMemo depends on cam.pos (active preset); HOLE_Z=2.3 confirmed from cards.ts in read_first; HERO distance ≈7.8wu matches RESEARCH computation |
| T-07-03c | Tampering | M11 breach from combined N8AO + DOF | mitigate | Measure DOF isolation delta; if combined total > 8ms, try halfRes N8AO first; cut DOF if still breaching (second cut candidate per SSOT §TP6 non-blocking rollback) |
| T-07-03d | Tampering | Bloom accidentally imported alongside DepthOfField | mitigate | Only DepthOfField added to import line; grep-check-tp5-06 CHECK 5 + explicit verify step |
| T-07-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp5-06.cjs exits 0.
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- M1 PASS at hero shot with ?fx (rank-glyph >= 22px or explicit cut disposition recorded).
- M7 PASS (no Bloom).
- M11 DOF delta recorded in SUMMARY.
- DepthOfField present in TableLab.tsx non-comment code (or cut disposition documented).
- Captures at .dev-stack/diag/tp6/dof-{hero,card,macro}.png.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- M1 PASS with ?fx on (hole cards razor-sharp — the HARD gate). DOF present OR cut with explicit recorded reason.
- worldFocusDistance computed from cam preset via static useMemo.
- M11 delta recorded. tsc clean. vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md` when done.
Include: final DOF params (or "CUT — M1 HARD gate" with disposition), M1 measurement with ?fx
(px-height value or operator-confirm status), M11 DOF delta, captures committed. This is the
input contract for 07-06 (full metric suite) and the operator gate (07-07).
</output>
