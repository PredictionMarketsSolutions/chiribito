---
phase: 07-tp6-profundidad-composicion-depth
plan: 05
type: execute
wave: 4
depends_on:
  - 07-04
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP6 — composition: kill empty-felt zones with CENTER-OF-TABLE table-STATE only — a face-down deck stub + a dealer button"
  - "SSOT §TP6 — NO opponent hand / per-seat object / props / room; NO new chip piles; deck stub + dealer button ONLY"
  - "SSOT §TP6 — deck stub reuses existing cardBodyGeometry + kit.stock (zero new deps); dealer button = cylinderGeometry + MeshPhysicalMaterial matte cream"
  - "SSOT §TP6 — CenterGameState: center-only (world radius <= 2wu from [0,0,0]); cards>board>rail hierarchy reinforced"

must_haves:
  truths:
    - "The large empty felt area to the left of center is no longer empty — the deck stub and dealer button read as 'mid-hand game in progress'"
    - "NO objects at seat-adjacent positions (radius > 2wu from [0,0,0]) were added; the center scope audit passes"
    - "cards>board>rail visual hierarchy is reinforced (hole cards most prominent, board visible, rail receding)"
    - "The CenterGameState adds ONLY two object types: a face-down deck stub (4-card stack) and a dealer button (cream disc)"
    - "?fx-off path still has the center game-state (CenterGameState is NOT gated by ?fx — it is always present)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "CenterGameState component: 4-card face-down deck stub at [0.3, FELT_REST_Y, -1.3] + cream dealer button at [-0.7, 0.022, -1.6]"
      contains: "CenterGameState"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx CenterGameState"
      to: "cards.ts cardBodyGeometry + kit.stock material"
      via: "Deck stub uses mesh geometry=kit.body material=kit.stock (shared geometry, no new assets)"
      pattern: "kit.stock"
    - from: "frontend/src/lab/TableLab.tsx CenterGameState"
      to: "Scene JSX tree (unconditional mount)"
      via: "CenterGameState is a sibling of Table/Cards/Chips — NOT inside the ?fx EffectComposer guard"
      pattern: "CenterGameState"
---

<objective>
Add the CenterGameState — a face-down deck stub (4-card stack) + a dealer button (cream disc) —
to the Scene JSX, placed at table center. This kills the empty-felt zones that made the
composition read as a props still-life rather than a mid-hand game.

Purpose: The center game-state is the composition fix for TP6. Without it, the felt left of the
demoted chip pot reads as dead space. With it, the table communicates "game in progress" at a
glance. The deck stub and button are the only two objects added — no per-seat objects, no
opponent hands, no chip piles, no environmental props.

Output: CenterGameState component inline in TableLab.tsx; deck stub at [0.3, FELT_REST_Y, -1.3];
dealer button at [-0.7, 0.022, -1.6]; unconditional mount (not gated by ?fx).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CenterGameState (deck stub + dealer button) to TableLab.tsx</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - 07-RESEARCH.md "Pattern 6: Center Game-State (deck stub + dealer button)" — the full
      component code pattern: DECK_POS [0.3, FELT_REST_Y, -1.3], BUTTON_POS [-0.7, 0.022, -1.6],
      4 cards in deckCards array (slight x/z scatter + ry rotation per card), MeshPhysicalMaterial
      for dealer button (color "#f0e8d0", roughness 0.80, metalness 0), cylinderGeometry
      args [0.28, 0.28, 0.04, 24]. Anti-scope-creep invariant: any position at radius > 2wu from
      [0,0,0] = seat-adjacent, flag immediately.
    - frontend/src/lab/cards.ts — FULL FILE. Locate: (a) FELT_REST_Y (the y-position of cards
      resting on felt — needed for DECK_POS[1]); (b) CARD_T (card thickness constant — the y
      offset per stacked card in the deck stub); (c) cardBodyGeometry() export name and signature;
      (d) CardKit type definition (to type the { kit } prop). The CenterGameState component
      receives kit from its parent and reuses kit.body (body geometry) + kit.stock (stock material).
    - frontend/src/lab/TableLab.tsx — locate: (a) how CardKit / kit is currently passed to other
      components (the pattern for reusing kit.body and kit.stock); (b) where in the JSX tree to
      add CenterGameState (after Table, before the EffectComposer — as a scene-content sibling);
      (c) THREE import (THREE.MeshPhysicalMaterial and THREE.Color are needed for the button mat
      useMemo; THREE is already imported if three is an existing dep).
  </read_first>
  <action>
    STEP 1 — Read cards.ts to extract FELT_REST_Y, CARD_T, cardBodyGeometry export, CardKit type.
    Use the exact constant names from the file. If FELT_REST_Y is computed as a sum rather than
    a named constant, calculate its numeric value from the component terms.

    STEP 2 — Inline CenterGameState as a function component in TableLab.tsx (before the default
    export, or as a local function inside the Scene component — match the existing code style for
    sub-components in TableLab.tsx):

      function CenterGameState({ kit }: { kit: CardKit }) {
        const DECK_POS: [number, number, number] = [0.3, FELT_REST_Y, -1.3];
        const BUTTON_POS: [number, number, number] = [-0.7, 0.022, -1.6];

        // Deck stub: 4 cards stacked face-down at center, slight scatter
        const deckCards = useMemo(() => (
          [0, 1, 2, 3].map(i => ({
            x: DECK_POS[0] + Math.sin(i * 1.7) * 0.008,
            y: DECK_POS[1] + i * CARD_T,
            z: DECK_POS[2] + Math.cos(i * 2.3) * 0.006,
            ry: i * 0.04,
          }))
        ), []);

        const buttonMat = useMemo(() => new THREE.MeshPhysicalMaterial({
          color: new THREE.Color("#f0e8d0"),
          roughness: 0.80,
          metalness: 0,
        }), []);

        return (
          <group>
            {/* Face-down deck stub — reuses kit.body geometry + kit.stock material */}
            {deckCards.map((c, i) => (
              <mesh
                key={i}
                geometry={kit.body}
                material={kit.stock}
                position={[c.x, c.y, c.z]}
                rotation={[-Math.PI / 2, 0, c.ry]}
                castShadow
                receiveShadow
              />
            ))}
            {/* Dealer button — matte cream disc */}
            <mesh
              position={BUTTON_POS}
              rotation={[-Math.PI / 2, 0, 0]}
              castShadow
            >
              <cylinderGeometry args={[0.28, 0.28, 0.04, 24]} />
              <primitive object={buttonMat} attach="material" />
            </mesh>
          </group>
        );
      }

    Use the exact FELT_REST_Y and CARD_T values from cards.ts (read in STEP 1). Substitute any
    named constant references for their actual values if they are not imported at the module scope.

    STEP 3 — Add CenterGameState to the Scene JSX, AFTER the existing Table/Cards/Chips groups
    and BEFORE the EffectComposer conditional:

      {/* Center game-state — unconditional mount; not gated by ?fx */}
      <CenterGameState kit={kit} />

      {qp("fx") !== null && (
        <EffectComposer ...>
          ...
        </EffectComposer>
      )}

    CenterGameState must NOT be inside the ?fx guard. It is a scene-graph object, not a
    postprocessing effect. The ?fx flag controls the postprocessing stack, not the scene content.

    STEP 4 — Scope audit (anti-scope-creep):
    After writing the component, verify all positions are within 2wu of [0,0,0]:
    - DECK_POS [0.3, *, -1.3]: distance from center = sqrt(0.09 + 1.69) = sqrt(1.78) ≈ 1.33wu — OK.
    - BUTTON_POS [-0.7, *, -1.6]: distance = sqrt(0.49 + 2.56) = sqrt(3.05) ≈ 1.75wu — OK.
    Both are within the 2wu center-only radius. No other positions exist. Report in SUMMARY.

    STEP 5 — Capture all 3 shots (without and with ?fx) and inspect:
    Without ?fx (composition read):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/cgs-hero.png
    With ?fx (full TP6 stack):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/cgs-hero-fx.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=card&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/cgs-card-fx.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/cgs-macro-fx.png
    Inspect: deck stub reads as a face-down card stack at center; dealer button reads as a small
    cream disc; no seat-adjacent objects; empty felt zone to the left of center is filled.

    Commit: "feat(07-05): add CenterGameState (deck stub + dealer button) at table center"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8'); if(!/CenterGameState/.test(s)){console.error('FAIL: CenterGameState not in TableLab.tsx');process.exit(1);} if(/Bloom/.test(s.split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'))){console.error('FAIL: Bloom detected — M7 HARD gate');process.exit(1);} console.log('OK — CenterGameState present, no Bloom');"</automated>
  </verify>
  <done>CenterGameState present in TableLab.tsx; deck stub (4 cards, kit.body + kit.stock) and dealer button (cream disc) placed at DECK_POS + BUTTON_POS within 2wu of center; unconditionally mounted (not gated by ?fx); scope audit confirms no seat-adjacent objects; tsc clean; vitest green; captures at .dev-stack/diag/tp6/cgs-{hero,hero-fx,card-fx,macro-fx}.png. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only.

| Boundary | Description |
|----------|-------------|
| CenterGameState scope (Pitfall 5) | Only deck stub + dealer button — any object at radius > 2wu from origin is seat-adjacent scope creep |
| CenterGameState inside ?fx guard | Must be in the scene graph (BEFORE EffectComposer), never inside the postprocessing conditional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05a | Tampering | CenterGameState scope creep (extra objects beyond deck stub + button) | mitigate | Component is strictly two object types; scope audit (position radius check) in STEP 4; any position > 2wu from origin is seat-adjacent and must not exist |
| T-07-05b | Tampering | CenterGameState inside ?fx guard (makes it invisible in default path) | mitigate | CenterGameState placed BEFORE the EffectComposer qp("fx") conditional; unconditional mount; both paths show the center objects |
| T-07-05c | Tampering | New geometry / new material not reusing existing kit | mitigate | Deck stub uses kit.body (geometry) + kit.stock (material) from cards.ts — zero new assets; dealer button is a cylinderGeometry with a MeshPhysicalMaterial (standard Three.js, no new dep) |
| T-07-05d | Tampering | FELT_REST_Y / CARD_T constants wrong (deck cards float or clip) | mitigate | Read cards.ts fully in read_first to extract exact constant values; deck cards at DECK_POS[1] + i * CARD_T must sit on the felt, not below or floating |
| T-07-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp5-06.cjs exits 0 (M7 PASS).
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- CenterGameState in TableLab.tsx; deck stub + dealer button present; no Bloom.
- Scope audit: both positions within 2wu of [0,0,0].
- ?fx-off capture shows deck stub + button (center objects unconditionally present).
- ?fx-on capture shows full stack + center objects.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Deck stub (4-card face-down stack at [0.3, FELT_REST_Y, -1.3]) + dealer button (cream disc at [-0.7, 0.022, -1.6]) present in scene.
- Unconditionally mounted (both ?fx-on and ?fx-off paths show the center objects).
- Scope audit PASS (no seat-adjacent objects). tsc clean. vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-05-SUMMARY.md` when done.
Include: exact FELT_REST_Y and CARD_T values used (from cards.ts), position scope audit (radii
from origin for both objects), capture paths, visual description of the center-state read at hero.
This is the input contract for the operator gate (07-07) center-game-state section.
</output>
