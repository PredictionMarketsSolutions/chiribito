---
phase: 07-tp6-profundidad-composicion-depth
plan: 06
type: execute
wave: 5
depends_on:
  - 07-05
files_modified:
  - tools/table-3d/grep-check-tp5-06.cjs
  - tools/table-3d/grep-check-tp6-07.cjs
autonomous: true
requirements:
  - "SSOT §TP6 — author grep-check-tp6-07.cjs (8 checks: EffectComposer + N8AO + DOF + Vignette present; no Bloom anywhere; brassMat roughness 0.42-0.45; SoftShadows; ContactShadows frames={1})"
  - "SSOT §TP6 — full metric suite final run: M1/M6/M7/M8/M9/M10/M11/+A all confirmed with ?fx at all 3 frozen shots"
  - "SSOT §TP6 — grep-check-tp6-07.cjs uses plain Node stdlib only; exit 0 / exit 1 discipline; mirrors grep-check-tp5-06 pattern"
  - "SSOT §TP6 — per-effect M11 isolation measurements recorded (N8AO delta, DOF delta if not cut); cut the weakest effect if combined M11 breaches"

must_haves:
  truths:
    - "tools/table-3d/grep-check-tp6-07.cjs exists and exits 0 (all 8 TP6 invariants hold)"
    - "CHECK 1-4: EffectComposer + N8AO + DepthOfField (or cut-noted) + Vignette present in TableLab.tsx non-comment code"
    - "CHECK 5: No Bloom in any lab .tsx/.ts file (M7 HARD gate — both grep-check-tp5-06 and tp6-07 enforce it)"
    - "CHECK 6-8: brassMat roughness 0.42-0.45 / SoftShadows / ContactShadows frames={1} still hold (forward-carried TP4+TP5 invariants)"
    - "Full metric suite PASS: M1 hole-cards razor-sharp at ?fx hero (re-confirmed from 07-03); M6 crevice >= 12%; M8 vignette 8-20%; M9 determinism byte-identical (or Noise cut noted); M10/M11 within floor"
  artifacts:
    - path: "tools/table-3d/grep-check-tp6-07.cjs"
      provides: "TP6 postprocessing invariant checker — 8 checks; exit 0 / exit 1 discipline; Node stdlib only"
      contains: "grep-check-tp6-07"
  key_links:
    - from: "tools/table-3d/grep-check-tp6-07.cjs"
      to: "frontend/src/lab/TableLab.tsx"
      via: "Reads TableLab.tsx non-comment code; asserts EffectComposer/N8AO/DepthOfField/Vignette present and Bloom absent"
      pattern: "grep-check-tp6"
    - from: "tools/table-3d/grep-check-tp5-06.cjs CHECK 5"
      to: "tools/table-3d/grep-check-tp6-07.cjs CHECK 5"
      via: "Both enforce no-Bloom; tp5-06 was relaxed in 07-01 to drop EffectComposer; tp6-07 is the positive assertion of effects presence"
      pattern: "Bloom"
---

<objective>
Author tools/table-3d/grep-check-tp6-07.cjs with 8 structural invariant checks, then run the
full metric suite (M1/M6/M7/M8/M9/M10/M11/+A) with ?fx at all 3 frozen money shots as the final
automated pre-gate confirmation.

Purpose: The operator gate (07-07) is the human seam. The automated suite (grep-check + metrics)
is the necessary-not-sufficient pre-condition — the machine-readable proof that all TP6 structural
commitments are present and no hard gate is failing. Only after this plan exits 0 on all checks
does the operator review begin.

Output: tools/table-3d/grep-check-tp6-07.cjs (exits 0); final metric suite PASS for all 8
metrics at all 3 frozen shots with ?fx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-02-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-04-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tools/table-3d/grep-check-tp6-07.cjs (8 checks, Node stdlib, exit-code discipline)</name>
  <files>tools/table-3d/grep-check-tp6-07.cjs</files>
  <read_first>
    - tools/table-3d/grep-check-tp5-06.cjs — the EXACT canonical pattern to mirror: "use strict";
      const fs/path requires; readFile(filePath) helper; fail(message) -> console.error + exit(1);
      stripComments(src) -> filter lines starting with "//"; per-check blocks with explicit fail
      messages; one-line OK on exit 0 listing all check names. Mirror this structure exactly.
    - 07-RESEARCH.md "Grep-Check Transition: grep-check-tp6-07.cjs skeleton" — the exact 8 checks,
      regexes, and the full skeleton. Read this section carefully; the skeleton IS the authoritative
      spec for this file.
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md — whether DepthOfField
      was CUT in 07-03. If cut: CHECK 3 (DepthOfField present) must be updated to a softer check
      or skipped. Record the adaptation in the SUMMARY.
    - frontend/src/lab/TableLab.tsx — current state of the file (non-comment source) — confirm the
      exact tokens that CHECK 1-4 should match before writing the regexes.
  </read_first>
  <action>
    Author tools/table-3d/grep-check-tp6-07.cjs as a CommonJS module. Mirror grep-check-tp5-06.cjs
    exactly in structure (header comment, "use strict", helpers, per-check blocks, one-line OK).

    The file MUST:
    - Read from repo root using path.join (TABLE_LAB = path.join("frontend","src","lab","TableLab.tsx"),
      LAB_DIR = path.join("frontend","src","lab")).
    - Use only fs, path from Node stdlib. Zero new packages.
    - Provide readFile(filePath), fail(message), and stripComments(src) helpers identical to tp5-06.
    - Run 8 checks in order; fail() on any; exit 0 with one-line OK listing all check names.

    CHECK 1 — EffectComposer present (TP6 ships it):
      Pattern: /<EffectComposer/.test(nc)
      Fail msg: "CHECK 1 FAILED — EffectComposer not in non-comment code in TableLab.tsx.
        TP6 ships the EffectComposer scaffold (07-01). If the ?fx guard was accidentally removed,
        restore it: {qp('fx') !== null && <EffectComposer multisampling={4} enableNormalPass={false}>}"

    CHECK 2 — N8AO present (crevice AO required, M6 satisfier):
      Pattern: /<N8AO/.test(nc)
      Fail msg: "CHECK 2 FAILED — N8AO not in non-comment code in TableLab.tsx.
        TP6 requires N8AO for M6 crevice darkening. N8AO is bundled in @react-three/postprocessing."

    CHECK 3 — DepthOfField present OR cut-documented:
      Read from 07-03-SUMMARY to determine if DOF was cut. If NOT cut:
        Pattern: /<DepthOfField/.test(nc)
        Fail msg: "CHECK 3 FAILED — DepthOfField not in non-comment code in TableLab.tsx."
      If DOF WAS cut in 07-03 (M1 HARD gate failure at legal params):
        Change CHECK 3 to a softer check that passes when DepthOfField is absent:
          Pattern: true (always passes — DOF legitimately cut)
          Log a note: console.log("  CHECK 3 NOTE: DepthOfField was cut in 07-03 (M1 HARD gate — disposition recorded in 07-03-SUMMARY)");
        Update the OK message to note "CHECK 3: DepthOfField CUT (M1 — see 07-03-SUMMARY)".

    CHECK 4 — Vignette present (frame darkening, M8):
      Pattern: /<Vignette/.test(nc)
      Fail msg: "CHECK 4 FAILED — Vignette not in non-comment code in TableLab.tsx.
        TP6 requires Vignette for M8 vignette band 8-20%."

    CHECK 5 — No Bloom anywhere in lab/ (M7 HARD gate, permanent ban):
      Read all .tsx and .ts files in LAB_DIR (non-comment lines).
      For each file: if /Bloom/.test(stripComments(readFile(f))) → fail with file path.
      Fail msg: "CHECK 5 FAILED — Bloom detected in [filepath] non-comment code.
        M7 HARD GATE: Bloom is permanently banned (casino trap + perf sink). Remove all Bloom
        imports and JSX from frontend/src/lab/."

    CHECK 6 — brassMat roughness 0.42-0.45 (forward TP4 invariant):
      Pattern: /roughness\s*:\s*0\.4[2-5]/.test(nc)
      Fail msg: "CHECK 6 FAILED — brassMat roughness 0.42-0.45 not found in TableLab.tsx
        non-comment code. TP4-locked invariant: brass aging pass (roughness 0.42) must not
        drift. M4 guards against brass-gold drift."

    CHECK 7 — SoftShadows present unconditionally (forward TP5 invariant):
      Pattern: /<SoftShadows/.test(nc)
      Fail msg: "CHECK 7 FAILED — SoftShadows not in non-comment code in TableLab.tsx.
        TP5 ships SoftShadows unconditionally in Scene (PCSS injected once; no per-flag toggle).
        Confirm <SoftShadows size={30} samples={16} focus={0} /> is present above Lights."

    CHECK 8 — ContactShadows frames={1} present (forward TP5 invariant):
      Pattern: /frames=\{1\}/.test(nc)
      Fail msg: "CHECK 8 FAILED — ContactShadows frames={1} not in non-comment code in TableLab.tsx.
        TP5 bakes ContactShadows once at mount (frames={1}) for M11 improvement."

    ONE-LINE OK (exit 0):
      console.log(
        "OK — grep-check-tp6-07: all 8 TP6 postprocessing invariants hold\n" +
        "  CHECK 1: EffectComposer present behind ?fx (TP6 adds compositor)\n" +
        "  CHECK 2: N8AO present — honest crevice darkening, M6 satisfier\n" +
        "  CHECK 3: DepthOfField present (or cut — M1 HARD gate; disposition in 07-03-SUMMARY)\n" +
        "  CHECK 4: Vignette present — frame darkening, M8 8-20% gate\n" +
        "  CHECK 5: No Bloom in frontend/src/lab/ — M7 HARD GATE (permanently banned)\n" +
        "  CHECK 6: brassMat roughness 0.42-0.45 — TP4-locked brass aging pass\n" +
        "  CHECK 7: SoftShadows unconditional in Scene — TP5 PCSS grounding\n" +
        "  CHECK 8: ContactShadows frames={1} — TP5 baked once at mount (M11)"
      );
      process.exit(0);

    Commit: "chore(07-06): add grep-check-tp6-07.cjs — 8 TP6 postprocessing invariant checks"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp6-07.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
  </verify>
  <done>tools/table-3d/grep-check-tp6-07.cjs exits 0 with 8-check OK; grep-check-tp5-06.cjs still exits 0 (backward compatibility); tsc clean; vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Full metric suite — M1/M6/M7/M8/M9/M10/M11/+A all PASS with ?fx at all 3 shots</name>
  <files>docs/table-3d/anchors/tp6-gate/hero-final.png</files>
  <read_first>
    - 07-RESEARCH.md "Metric-Gate Assertions" table — all 8 metrics, their tools, and PASS thresholds.
    - 07-RESEARCH.md "Per-effect isolation measurement protocol" — the N8AO / DOF isolation
      measurement sequence. Results should already be in 07-02/07-03-SUMMARY; re-run to confirm
      the combined total M11 is within floor.
    - tools/table-3d/ — check which metric tools are available: m1-m2-m12.mjs (M1), run-metrics.mjs
      (M6/M8/+A/multi-metric), m7-bloom-assert.mjs (M7), m9-determinism.mjs (M9), stats-read.mjs
      (M10/M11 draw count + frame-time).
    - 07-02-SUMMARY.md, 07-03-SUMMARY.md, 07-04-SUMMARY.md — the per-metric values already
      measured in prior plans; re-confirm each rather than re-deriving.
  </read_first>
  <action>
    STEP 1 — Ensure dev server is running with all TP6 changes active (all 5 prior plans shipped).

    STEP 2 — Take clean final hero capture with ?fx (the pre-gate anchor):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/hero-final.png
    Take the complete anchor set (3 shots with ?fx + 3 shots without ?fx for A/B):
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/hero-nofx.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=card&fx&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/card-final.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=card&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/card-nofx.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro&fx&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/macro-final.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/macro-nofx.png

    STEP 3 — Run full metric suite on hero-final.png (primary gate shot):
      node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp6-gate/hero-final.png
    Record each gate value:
      M1: rank-glyph bbox height >= 22px at hero shot with ?fx (also run m1-m2-m12.mjs directly)
      M6: crevice luma delta >= 12% under hole cards + chip stacks vs adjacent open felt — PASS
      M8: corner vs center luma delta 8-20% — PASS
      +A: corner luma > 0 + warm hue H in [15,75] — PASS

    STEP 4 — Run M7 code gate:
      node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab
    Expected: exits 0 (M7 PASS — no Bloom anywhere).

    STEP 5 — Run M9 determinism gate:
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/m9-a.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs docs/table-3d/anchors/tp6-gate/m9-b.png
      node tools/table-3d/m9-determinism.mjs docs/table-3d/anchors/tp6-gate/m9-a.png docs/table-3d/anchors/tp6-gate/m9-b.png
      OR: node -e "const c=require('crypto'),f=require('fs'); const ha=c.createHash('md5').update(f.readFileSync('docs/table-3d/anchors/tp6-gate/m9-a.png')).digest('hex'); const hb=c.createHash('md5').update(f.readFileSync('docs/table-3d/anchors/tp6-gate/m9-b.png')).digest('hex'); if(ha!==hb){console.error('FAIL: M9 md5 mismatch ('+ha+' vs '+hb+')');process.exit(1);} console.log('OK — M9 PASS byte-identical');"
    If M9 fails here but Noise was NOT cut in 07-04: go back and cut Noise now; re-capture; re-run M9.

    STEP 6 — Run M10/M11 gate (draw count + frame-time):
      node tools/table-3d/stats-read.mjs http://localhost:5173 hero
    Record: draw count (must be < 150; compare to TP5 baseline <=106 and note delta);
    frame-time (must be median < 8ms). If M11 frame-time breaches 8ms:
      Per-effect isolation: temporarily disable the most GPU-expensive effect (N8AO fullRes is
      the primary candidate), measure again. Apply the cheapest fix that brings M11 within floor:
      (1) halfRes N8AO if not already — lowest-cost cut; (2) cut DOF if already halfRes and still
      breaching; (3) document the disposition in the SUMMARY.

    STEP 7 — Run both grep-checks one final time:
      node tools/table-3d/grep-check-tp6-07.cjs
      node tools/table-3d/grep-check-tp5-06.cjs

    STEP 8 — Record the complete pre-gate metric table in the SUMMARY (this is what the operator
    gate (07-07) reads before presenting the A/B):
      M1: [px-height value] — PASS/FAIL + operator confirm status
      M6: [luma delta %] at hero-final.png — PASS
      M7: 0 Bloom tokens — PASS
      M8: [corner-center delta %] — PASS
      M9: md5 match — PASS (or "Noise cut — M9 not applicable")
      M10: draw count [value] vs TP5 baseline [value]
      M11: median frame-time [value]ms — PASS (< 8ms)
      +A: corner luma [value], hue [value] — PASS
      grep-check-tp6-07: exits 0 (8/8 checks)
      grep-check-tp5-06: exits 0 (6/6 checks, CHECK 5 relaxed)

    Commit: "chore(07-06): TP6 pre-gate — all metrics PASS, grep-check-tp6-07 exits 0"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp6-07.cjs</automated>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab</automated>
    <automated>node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp6-gate</automated>
    <automated>node -e "const f=require('fs'); const p='docs/table-3d/anchors/tp6-gate/hero-final.png'; if(!f.existsSync(p)){console.error('MISSING: '+p);process.exit(1);} console.log('OK — hero-final capture present');"</automated>
  </verify>
  <done>grep-check-tp6-07.cjs exits 0 (8 checks); grep-check-tp5-06.cjs exits 0 (6 checks); m7-bloom-assert exits 0 (M7 PASS); run-metrics exits 0 with M1/M6/M8/+A all PASS; M9 byte-identical (or Noise cut with reason); M10 draw count and M11 frame-time within floor; all 6 anchor captures (3x ?fx + 3x ?nofx) committed to docs/table-3d/anchors/tp6-gate/; complete pre-gate metric table in SUMMARY. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only.

| Boundary | Description |
|----------|-------------|
| DOF cut adaptation in CHECK 3 | If DOF was cut in 07-03, CHECK 3 must pass (soft check or always-true); must not cause exit 1 |
| M11 combined perf (N8AO + DOF + others) | Per-effect isolation measurement done in prior plans; final combined measurement here is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06a | Tampering | grep-check regex false negative (too strict) | mitigate | Test each regex against actual TableLab.tsx content in read_first; CHECK 5 scans all lab files |
| T-07-06b | Tampering | CHECK 3 DOF adaptation not synced with 07-03-SUMMARY verdict | mitigate | read_first explicitly reads 07-03-SUMMARY to determine DOF cut status before writing CHECK 3 |
| T-07-06c | Tampering | M11 breach not caught (combined effects exceed 8ms) | mitigate | stats-read.mjs run; if breach: apply halfRes N8AO or cut DOF; record disposition |
| T-07-06d | Tampering | M9 determinism failure not caught before gate | mitigate | Two captures + md5 compare in STEP 5; if mismatch and Noise not yet cut: cut now and re-capture |
| T-07-06e | Repudiation | Pre-gate metric table not recorded (gate has no basis) | mitigate | SUMMARY explicitly requires the complete M1/M6/M7/M8/M9/M10/M11/+A table; operator gate (07-07) reads it |
| T-07-SC | Tampering | npm installs | accept | grep-check-tp6-07.cjs uses Node stdlib only; no packages installed |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp6-07.cjs exits 0 (8 TP6 invariants).
- node tools/table-3d/grep-check-tp5-06.cjs exits 0 (6 TP5 invariants, CHECK 5 relaxed).
- node tools/table-3d/m7-bloom-assert.mjs --src frontend/src/lab exits 0 (M7 PASS).
- node tools/table-3d/run-metrics.mjs docs/table-3d/anchors/tp6-gate exits 0 (M1/M6/M8/+A PASS).
- M9 PASS (md5 match on two consecutive hero ?fx captures) or Noise cut disposition.
- M11 < 8ms median frame-time confirmed via stats-read.mjs.
- 6 anchor captures in docs/table-3d/anchors/tp6-gate/ (hero/card/macro × ?fx + ?nofx).
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- grep-check-tp6-07.cjs exits 0 (8 checks); grep-check-tp5-06.cjs exits 0.
- Full metric suite (M1/M6/M7/M8/M9/M10/M11/+A) all PASS with ?fx at hero shot.
- Anchor captures committed. Complete pre-gate metric table in SUMMARY. tsc clean. vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-06-SUMMARY.md` when done.
This is the critical pre-gate record: complete metric table (all 8 metrics with exact measured
values), grep-check-tp6-07 8-check verdict, grep-check-tp5-06 6-check verdict, anchor capture
paths, per-effect M11 isolation table (N8AO delta, DOF delta if not cut, combined total),
and the cut-weakest-effect disposition if any effect was removed to meet M11.
The operator gate (07-07) reads this SUMMARY as its pre-gate basis before presenting the A/B.
</output>
