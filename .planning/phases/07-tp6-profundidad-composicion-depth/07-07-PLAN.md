---
phase: 07-tp6-profundidad-composicion-depth
plan: 07
type: execute
wave: 6
depends_on:
  - 07-06
files_modified:
  - docs/table-3d/TP6_OPERATOR_GATE.md
  - docs/table-3d/SCORECARD_TABLE_3D.md
autonomous: false
requirements:
  - "SSOT §TP6 — perceptual gate: operator A/B ?fx off vs on at all 3 shots — 'cinematic-premium honest depth, hero tack-sharp, no dead zones, no glow/gimmick?'"
  - "SSOT §TP6 — NOT auto-approvable; autonomous:false; this is the operator human seam"
  - "SSOT §TP6 — gate record: docs/table-3d/TP6_OPERATOR_GATE.md records verdict + per-effect disposition + HARD gate confirmation + ?fx flag map + non-blocking rollback disposition"
  - "SSOT §TP6 — SCORECARD update: depth/AO/composition rows for all effects if shipped; must not regress from TP5 column"

must_haves:
  truths:
    - "The operator has reviewed all 3 frozen money shots at ?fx-off (pre-TP6) and ?fx-on (postprocessing active), with A/B captures from the tp6-gate anchor dir available"
    - "The HARD gates are confirmed: M1 PASS (hero razor-sharp with ?fx); M6 PASS; M7=0 (no Bloom); M8 PASS (8-20%); M11 within floor — from 07-06-SUMMARY"
    - "The non-blocking rollback disposition is presented: the table ships without ?fx (no EffectComposer mounted = exact TP5 look); the postprocessing stack is the separable workstream behind ?fx"
    - "On approval: docs/table-3d/TP6_OPERATOR_GATE.md records verdict + A/B basis + per-effect disposition + HARD-gate confirmation + ?fx flag map"
    - "On approval: SCORECARD_TABLE_3D.md depth/AO/composition rows updated (must not regress from TP5 column)"
    - "On stop-on-ambiguous: the affected effect is identified, reduced, and re-presented; OR the weakest effect is cut and the gate re-presented; the table ships the remaining effects"
  artifacts:
    - path: "docs/table-3d/TP6_OPERATOR_GATE.md"
      provides: "Recorded operator verdict, A/B basis (vs tp6-gate/hero-nofx.png), per-effect disposition, HARD gate confirmation (M1/M6/M7/M8/M9/M11), ?fx flag map, non-blocking rollback disposition"
      contains: "Verdict"
    - path: "docs/table-3d/SCORECARD_TABLE_3D.md"
      provides: "Post-TP6 score update for depth/AO/composition rows — must not regress from TP5 column"
      contains: "TP6"
  key_links:
    - from: "docs/table-3d/TP6_OPERATOR_GATE.md"
      to: "docs/table-3d/anchors/tp6-gate/{hero-final,card-final,macro-final,hero-nofx,card-nofx,macro-nofx}.png"
      via: "The A/B compares ?fx-on (tp6 full stack) against ?fx-off (pre-TP6 look = TP5 shipped default); both paths committed as anchors"
      pattern: "tp6-gate"
    - from: "?fx flag map"
      to: "non-blocking rollback disposition"
      via: "?fx absent = exact TP5 look (no compositor mounted); ?fx present = full TP6 postprocessing stack; the off-path is the non-blocking rollback"
      pattern: "fx"
---

<objective>
The TP6 operator perceptual gate — the GSD / Chiribito human seam. The operator reviews all
three frozen money shots comparing ?fx-off (default, pre-TP6 exact look) against ?fx-on (full
postprocessing stack: N8AO + DOF-or-cut + BrightnessContrast + Vignette + Noise-or-cut). The gate
question: "cinematic-premium honest depth, hero tack-sharp, no dead zones, no glow/gimmick?"

Purpose: Automated metrics (M1/M6/M7/M8/M9/M10/M11) and grep-check-tp6-07.cjs are necessary-
not-sufficient. Only the operator can judge whether the postprocessing reads as cinematic depth
(not an Instagram filter, not a casino effect-reel, not a tech demo) with the hero unambiguously
sharp. This gate cannot be auto-approved (autonomous:false; auto_advance does not apply).

Output: docs/table-3d/TP6_OPERATOR_GATE.md recording verdict + per-effect disposition + HARD-gate
confirmation + ?fx flag map; SCORECARD_TABLE_3D.md updated if shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-02-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-04-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-05-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-06-SUMMARY.md
@docs/table-3d/TP5_OPERATOR_GATE.md
@docs/table-3d/SCORECARD_TABLE_3D.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator A/B — "cinematic-premium honest depth, hero tack-sharp, no dead zones, no glow/gimmick?" at HERO + CARD + MACRO with ?fx-off vs ?fx-on</name>
  <files>docs/table-3d/TP6_OPERATOR_GATE.md, docs/table-3d/SCORECARD_TABLE_3D.md</files>
  <read_first>
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-06-SUMMARY.md — the complete
      pre-gate metric table (M1 px-height, M6 luma delta, M7 status, M8 delta, M9 determinism,
      M10 draw count, M11 frame-time, +A corner luma/hue). Restate all 8 metric values at gate.
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-02-SUMMARY.md — N8AO final params
      (aoRadius/intensity/distanceFalloff/halfRes decision).
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md — DOF disposition
      (final params or "CUT — M1 HARD gate").
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-04-SUMMARY.md — Vignette params,
      BrightnessContrast params, Noise disposition, fog near value.
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-05-SUMMARY.md — CenterGameState
      positions and visual read of the center game-state.
    - docs/table-3d/TP5_OPERATOR_GATE.md — the gate record template shape (mirror this structure
      for TP6_OPERATOR_GATE.md).
    - docs/table-3d/SCORECARD_TABLE_3D.md — current depth/AO/composition rows to update if approved.
    - docs/ROADMAP_TABLE_3D_PERFECTION.md §TP6 — the SSOT gate question and stop-on-ambiguous rule.
  </read_first>
  <action>
    Present the A/B to the operator using the anchor captures from docs/table-3d/anchors/tp6-gate/
    (live lab preferred; static captures as fallback), following the how-to-verify protocol.
    Do NOT auto-approve — this is a perceptual gate (autonomous:false; auto_advance does not apply).

    First restate the HARD gates from the 07-06-SUMMARY:
    - M1 hole-cards: [px-height value]px at hero shot with ?fx — razor-sharp PASS (>= 22px)
    - M6 crevice AO: [luma delta %] under objects vs adjacent felt — PASS (>= 12%)
    - M7 Bloom: 0 forbidden tokens — PASS (grep-check-tp6-07 CHECK 5 exits 0)
    - M8 vignette: [corner-center delta %] — PASS (8-20%)
    - M9 determinism: [byte-identical / Noise cut] — PASS
    - M10/M11: draw count [value] / frame-time [value]ms — within floor
    - +A warm corner: luma [value] / hue [value] — PASS
    - grep-check-tp6-07.cjs: exits 0 (8/8 structural invariants)
    - grep-check-tp5-06.cjs: exits 0 (6/6 TP5 invariants, CHECK 5 relaxed)

    Then present the per-effect disposition from the TP6 summaries:
    - N8AO: aoRadius [value] / intensity [value] / distanceFalloff [value] / halfRes [true/false]
    - DepthOfField: [params] OR "CUT — M1 HARD gate at legal bokehScale range"
    - BrightnessContrast: brightness [value] / contrast [value]
    - Vignette: offset [value] / darkness [value]
    - Noise: opacity [value] OR "CUT — M9 determinism failure"
    - Fog near: [tuned value or "unchanged at 20"]
    - CenterGameState: deck stub at [0.3, FELT_REST_Y, -1.3] + button at [-0.7, 0.022, -1.6]

    On operator verdict, record into docs/table-3d/TP6_OPERATOR_GATE.md (mirror TP5_OPERATOR_GATE.md
    structure):
    - The verdict string.
    - The A/B basis (?fx-off = exact TP5 look; ?fx-on = full TP6 stack; anchor captures in tp6-gate/).
    - The per-effect disposition (N8AO: shipped/cut; DOF: shipped/cut; Vignette: shipped;
      BrightnessContrast: shipped; Noise: shipped/cut; fog: tuned/unchanged; CenterGameState: shipped).
    - The HARD-gate confirmation (M1/M6/M7/M8/M9/M11 with measured values from 07-06-SUMMARY).
    - The ?fx flag map (see below).
    - The non-blocking rollback disposition.

    ?fx FLAG MAP to document:
      (default, no ?fx param)  = pre-TP6 look (EffectComposer NOT mounted; exact TP5 render;
                                   CenterGameState still visible — it is not gated by ?fx)
      ?fx (any value)          = full TP6 postprocessing stack: N8AO + [DOF-or-cut] +
                                   BrightnessContrast + Vignette + [Noise-or-cut]
      Note: fog tuning is ALWAYS ON in both paths (fog near change, if made, is unconditional).
            CenterGameState is ALWAYS ON in both paths (deck stub + dealer button unconditional).

    NON-BLOCKING ROLLBACK DISPOSITION:
    The entire postprocessing stack is behind ?fx. If the operator rejects the ?fx path entirely,
    the table ships at the pre-TP6 look (?fx absent = TP5-identical render). The CenterGameState
    and fog tune ship regardless (they are unconditional). This is the "cut the weakest effect;
    keep the table without ?fx" rollback from SSOT §TP6.

    On UNAMBIGUOUS APPROVAL: update SCORECARD_TABLE_3D.md depth/AO/composition + center-state rows
    with the post-TP6 score. Score must not regress from the TP5 column. Note if AAA(5) awaits
    TP7 (cameras) or TP9 (final scorecard sign-off).

    On STOP-ON-AMBIGUOUS (one effect reads gimmicky / hero soft / effect-y):
    STOP. Identify the offending effect by name. Execute the appropriate correction ONLY:
      - "DOF reads gimmicky" or "hero looks soft": reduce bokehScale by 0.5 (if DOF not already
        cut); recapture; re-run M1; re-present hero shot.
      - "AO halos visible": lower N8AO aoRadius to 0.5 and distanceFalloff to 0.5; recapture;
        re-run M6; re-present.
      - "vignette too dark / casino-vignette": lower Vignette darkness to 0.5; recapture; re-run
        M8; re-present. If M8 < 8% → note in gate record as "vignette reduced at operator gate".
      - "grain reads as noise artifact" (Noise not cut): cut Noise now; recapture; re-run M9;
        re-present.
      - "center game-state reads as clutter": this is a scope question (CenterGameState is the
        SSOT-locked composition fix). If clutter read is strong: reduce deck stub from 4 cards to
        2 (less scatter); re-present. Do NOT remove the center game-state entirely — it is the
        TP6 composition deliverable.
      - "overall effect reads 'too processed'": DEFAULT STOP. Cut the weakest remaining mounted
        effect one at a time (Noise → DOF → N8AO) and re-present each iteration until the read
        passes or only Vignette + BrightnessContrast remain (the most restrained sub-stack).

    DEFAULT STOP (overall ambiguous or "effect-y"): cut the weakest mounted effect; re-present.
    The table ships whatever sub-stack passes the gate. Record each cut in TP6_OPERATOR_GATE.md.
    CenterGameState and fog tune always ship (unconditional). At minimum the ?fx-off default path
    ships unchanged (TP5-identical), which is the non-blocking rollback disposition.

    Do NOT add new lights. Do NOT change geometry or cameras. LOCAL only — no push/deploy/merge.
  </action>
  <what-built>
    TP6 executed five implementation workstreams before this gate, all locally only:

    (A) INSTALL + SCAFFOLD — 07-01:
    @react-three/postprocessing@2.19.1 + postprocessing@^6.39.1 installed.
    EffectComposer scaffold behind qp("fx") !== null. Default: ?fx absent = TP5 exact look.
    grep-check-tp5-06 CHECK 5 relaxed (EffectComposer permitted; Bloom still banned).

    (B) N8AO CREVICE AO — 07-02:
    aoRadius=[value] / intensity=[value] / distanceFalloff=[value] / halfRes=[value].
    Honest crevice darkening under hole cards, chip stacks, felt-to-rail seam.
    M6 PASS: [luma delta %] under objects. M11 N8AO delta: [value]ms.

    (C) DEPTHOFFIELD — 07-03:
    [params: worldFocusDistance/worldFocusRange/bokehScale/focalLength] OR [CUT — M1 HARD gate].
    M1 re-confirmed PASS with ?fx on: [px-height]px at hero shot.
    Board/rail receive gentle bokeh; hole cards razor-sharp.

    (D) VIGNETTE + GRADE + NOISE — 07-04:
    Vignette offset=[value] / darkness=[value] (M8 PASS: [delta %]).
    BrightnessContrast brightness=[value] / contrast=[value] (+A PASS: luma [value] / hue [value]).
    Noise opacity=[value] OR [CUT — M9 failure]. M9: [PASS / "Noise cut"].
    Fog near: [tuned to value / unchanged at 20].

    (E) CENTER GAME-STATE — 07-05:
    Deck stub (4 cards face-down, kit.body+kit.stock) at [0.3, FELT_REST_Y, -1.3].
    Dealer button (cream disc, cylinderGeometry, MeshPhysicalMaterial roughness=0.80) at [-0.7, 0.022, -1.6].
    Unconditionally mounted (both ?fx paths show the center objects).

    HARD GATES (confirmed in 07-06):
    grep-check-tp6-07.cjs: exits 0 (8/8 structural invariants).
    grep-check-tp5-06.cjs: exits 0 (6/6, CHECK 5 relaxed).
    M1: [px-height]px — PASS. M6: [%] — PASS. M7: 0 Bloom tokens — PASS.
    M8: [%] delta — PASS. M9: [PASS / Noise cut]. M11: [ms] — PASS.
    +A: luma [value] / hue [value] — PASS.
  </what-built>
  <how-to-verify>
    Judge the DEPTH + COMPOSITION read at the three frozen money shots.

    1. Bring the dev server up if needed (cd frontend && npm run dev) and open the lab live,
       OR review the still captures in docs/table-3d/anchors/tp6-gate/.

    2. HERO shot (fov32 — the definitive money shot; the hole-card sharpness gate):
       Open http://localhost:5173/table-lab.html?cam=hero (default, no ?fx — pre-TP6 exact look).
       Then open http://localhost:5173/table-lab.html?cam=hero&fx (full TP6 postprocessing stack).
       Compare against the anchor captures (hero-nofx.png vs hero-final.png in tp6-gate/).

       Gate questions for HERO:
       (a) "Are the hole cards (Perla: Sota de Oros + 7 de Oros) TACK-SHARP under ?fx — rank
           glyphs crisp, pip detail fully legible, NOT softened or blurred?"
       (b) "Is the AO honest — crevices under cards and chips read darker without halos around
           large objects (rail, table body)? Does it feel GROUNDED, not gimmicky?"
       (c) "Is the vignette restrained — corners are darker but not a black frame; the table
           center is the focal zone, not a casino-vignette tunnel?"
       (d) "Does the overall ?fx-on read feel CINEMATIC-PREMIUM and HONEST — photographed, not
           over-processed? No glow, no Instagram filter, no arcade sheen?"
       (e) "Does the center deck stub + dealer button read as 'mid-hand game in progress'? Is the
           empty felt zone now filled without reading as a prop still-life?"

    3. CARD shot (?cam=card — close-up card/felt/chip read):
       Open http://localhost:5173/table-lab.html?cam=card (no ?fx).
       Then open http://localhost:5173/table-lab.html?cam=card&fx.
       Compare: card-nofx.png vs card-final.png.

       Gate questions for CARD:
       (a) "Do hole cards catch more AO grounding under ?fx — the felt-to-card crevice darker?"
       (b) "Is the vignette subtle at this closer framing — not over-darkening the card edges?"
       (c) "Does the DOF (if not cut) render board cards softly without softening the hole cards?"

    4. MACRO shot (?cam=macro — close-up chip/wood/brass material read):
       Open http://localhost:5173/table-lab.html?cam=macro (no ?fx).
       Then open http://localhost:5173/table-lab.html?cam=macro&fx.
       Compare: macro-nofx.png vs macro-final.png.

       Gate questions for MACRO:
       (a) "Does AO read under chip stacks — the inter-chip crevice darker, chip-to-felt contact
           present? No halos around the chip body?"
       (b) "Is the filmic grade warm but not Instagram-warm — shadows lifted slightly, not crushed
           nor tinted orange? Brass still reads AGED (warm subdued), not golden/saturated?"
       (c) "Is the overall macro MACRO read materially improved — the scene reads more like a
           physical photograph than a real-time render?"

    5. The ?fx flag map (for A/B isolation):
       ?fx absent (default)  = pre-TP6 look (EffectComposer not mounted; exact TP5 render)
       ?fx (any value)       = full TP6 postprocessing stack: N8AO + [DOF] + BrightnessContrast
                                + Vignette + [Noise]
       Note: CenterGameState (deck stub + button) and fog tune are ALWAYS ON in both paths.

    6. HARD gates (confirmed in 07-06; re-confirm live if requested):
       M1 PASS (?fx hero: [value]px >= 22px or operator "legible"); M6 PASS; M7 PASS; M8 PASS;
       M11 PASS. node tools/table-3d/grep-check-tp6-07.cjs → must exit 0.
       These are non-negotiable; the operator cannot approve if any metric FAILS.

    7. Stop-on-ambiguous protocol:
       - "DOF gimmicky" or hero softens: STOP. Lower bokehScale by 0.5; recapture; re-run M1; re-present.
       - "AO halos": STOP. Lower aoRadius to 0.5; recapture; re-run M6; re-present.
       - "Vignette too dark / casino look": STOP. Lower darkness to 0.5; recapture; re-run M8; re-present.
       - "Grain reads as artifact": STOP. Cut Noise; recapture; re-present.
       - "Too processed overall": DEFAULT STOP. Cut weakest effect (Noise → DOF → N8AO);
         re-present each iteration. CenterGameState always ships. Minimum viable: ?fx-off default.

    DISPOSITION (record in docs/table-3d/TP6_OPERATOR_GATE.md):
    - UNAMBIGUOUS YES (cinematic-premium honest depth, hero tack-sharp, no dead zones,
      no glow/gimmick; all HARD gates green): TP6 SHIPS (full stack with shipped effects).
      Update SCORECARD_TABLE_3D.md (must not regress; note AAA(5) row for TP9 final sign-off).
    - STOP-ON-AMBIGUOUS on one effect: fix + re-present; if re-present passes → ship that fix.
    - DEFAULT STOP (overall "too processed"): cut weakest effect(s); re-present; ship whatever
      sub-stack passes. CenterGameState + fog tune always ship.
    - FULL REJECTION of ?fx path: ship the ?fx-off default (TP5 exact look) + CenterGameState +
      fog tune; record "TP6 postprocessing rejected — center game-state and fog tune ship;
      postprocessing deferred."
  </how-to-verify>
  <resume-signal>
    Operator types:
    - "approved" — TP6 ships (full postprocessing stack as mounted: [effects list]); update scorecard.
    - "approved — cut [effect]" (e.g. "approved — cut DOF") — ship stack minus the named effect;
      record the cut in TP6_OPERATOR_GATE.md; update scorecard for shipped effects.
    - "reduce [effect]" + reason (e.g. "reduce DOF — reads gimmicky"): executor adjusts the named
      effect's primary strength param by the minimum correction; recaptures; re-presents that shot.
    - "cut [effect]" + reason: executor removes the named effect from EffectComposer; recaptures;
      re-presents; iterates until approval or stack is depleted.
    - "stop" — DEFAULT STOP; ship CenterGameState + fog tune; keep ?fx-off as default; record
      the stop reason and which effects were rejected.
    Do NOT auto-approve.
  </resume-signal>
  <done>
    The operator has reviewed all 3 frozen money shots (?fx-off vs ?fx-on) with the HARD gates
    (M1/M6/M7/M8/M9/M11) restated from 07-06-SUMMARY. The verdict + per-effect disposition (N8AO:
    shipped/reduced/cut; DOF: shipped/cut; BrightnessContrast: shipped; Vignette: shipped/reduced;
    Noise: shipped/cut) + HARD-gate confirmation + ?fx flag map + non-blocking rollback are
    recorded in docs/table-3d/TP6_OPERATOR_GATE.md. SCORECARD_TABLE_3D.md depth/AO/composition
    rows updated if shipped (must not regress from TP5 column; note if AAA(5) awaits TP9).
    No new lights. No geometry/camera changes. LOCAL only — no push/deploy/merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, no untrusted input, not in prod build. /table-lab.html is dev-server-only.
At this gate the boundaries are the perceptual judgment seam, the HARD metric gates (M1/M6/M7/M8/M11),
and the non-blocking rollback disposition (?fx absent = TP5-identical default).

| Boundary | Description |
|----------|-------------|
| automated pre-gate metrics -> human perceptual gate | M1/M6/M7/M8/M9/M11/+A and grep-checks are necessary-not-sufficient; the operator judges "cinematic-premium honest depth, hero tack-sharp, no glow/gimmick" |
| ?fx-on stack -> ?fx-off default (non-blocking split) | The entire postprocessing stack is behind ?fx; if rejected, the table ships at the TP5 exact look; CenterGameState + fog tune are unconditional (always ship) |
| TP6 changes -> cameras/geometry/lights | This gate changes only the postprocessing disposition; no cameras, no new lights, no geometry beyond the CenterGameState (already committed in 07-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07a | Tampering | HARD gates (M1/M6/M7/M8/M11) — any failure | mitigate | Restated at gate from 07-06-SUMMARY; operator cannot approve if any HARD gate FAILS; if any fail, execute the correction before re-presenting |
| T-07-07b | Tampering | Bloom added as "just one more effect" during gate | mitigate | M7 HARD gate: no Bloom mounted (M7=0); grep-check-tp6-07 CHECK 5 enforces; any Bloom addition is explicitly deferred to never (permanent ban) |
| T-07-07c | Tampering | Auto-approve circumventing the operator gate | mitigate | autonomous:false; this checkpoint cannot be auto-approved; auto_advance does not apply |
| T-07-07d | Repudiation | Gate decision not recorded | mitigate | docs/table-3d/TP6_OPERATOR_GATE.md records verdict + per-effect disposition + HARD-gate confirmation + flag map (non-auto-approvable; operator-signed) |
| T-07-07e | Tampering | Stop-on-ambiguous correction adds a new effect (scope creep) | mitigate | Stop-on-ambiguous corrections ONLY reduce or cut effects — no new effects may be added at the gate; only param reductions or removals |
| T-07-07f | Tampering | CenterGameState scope expanded at gate | mitigate | CenterGameState is locked (deck stub + dealer button only); no new objects may be added; any "add a chair / add chips" request is deferred to TP8 |
| T-07-SC | Tampering | npm installs | accept | No package installs in the TP6 gate plan |
</threat_model>

<verification>
- The operator has reviewed HERO (?cam=hero default + ?cam=hero&fx) + CARD (?cam=card default + ?cam=card&fx) + MACRO (?cam=macro default + ?cam=macro&fx), with the ?fx-off vs ?fx-on A/B, the flag map, and the HARD gates presented.
- The HARD gates from 07-06-SUMMARY are restated: M1 PASS (hero razor-sharp); M6 PASS; M7=0 (no Bloom); M8 PASS (8-20%); M9 PASS (or Noise cut); M11 PASS (< 8ms). Non-negotiable.
- docs/table-3d/TP6_OPERATOR_GATE.md records: verdict, A/B basis (tp6-gate/ captures), per-effect disposition, HARD-gate confirmation, ?fx flag map, non-blocking rollback disposition.
- docs/table-3d/SCORECARD_TABLE_3D.md depth/AO/composition rows updated if shipped (must not regress from TP5 column; note if AAA(5) awaits TP9).
- No Bloom. No new lights. No geometry/camera changes.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Operator A/B complete at HERO + CARD + MACRO (?fx-off vs ?fx-on); verdict recorded.
- Unambiguous yes: TP6 ships (full stack or sub-stack; scorecard not regressed).
- OR: partial ship (some effects cut at gate; CenterGameState + fog tune always ship; record cuts).
- OR: DEFAULT STOP (postprocessing rejected; ?fx-off ships as default; CenterGameState ships; explicit reason documented).
- The non-blocking split is explicit in TP6_OPERATOR_GATE.md (?fx absent = TP5 look; CenterGameState unconditional).
- LOCAL only — no push/deploy/merge.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-07-SUMMARY.md` when done,
and write the verdict to `docs/table-3d/TP6_OPERATOR_GATE.md`. Update the depth/AO/composition
rows in docs/table-3d/SCORECARD_TABLE_3D.md with the post-TP6 scores if shipped (must not regress;
note if AAA(5) awaits TP9 final scorecard sign-off). The SUMMARY records the full gate disposition:
verdict, which effects shipped / were reduced / were cut, the ?fx default path as confirmed, the
final flag map, and any stop-on-ambiguous corrections applied at the gate.
</output>
