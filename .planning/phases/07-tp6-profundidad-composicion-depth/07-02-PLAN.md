---
phase: 07-tp6-profundidad-composicion-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP6 — N8AO: aoRadius 0.5-1.5 (world), intensity 1.5-3, distanceFalloff 0.5-1; honest crevice darkening under cards/chips/rail; no halos; not no-effect"
  - "SSOT §TP6 — M6/crevice: under-object luma >= 12% darker than adjacent open felt (N8AO is the primary satisfier)"
  - "SSOT §TP6 — M7=0 (no Bloom); M11 within floor (measure N8AO cost in isolation; halfRes if perf breaches)"
  - "SSOT §TP6 — everything behind ?fx; N8AO lives inside the EffectComposer scaffold from 07-01"

must_haves:
  truths:
    - "With ?fx, crevice darkening is visible under hole cards, chip stacks, and at the felt-to-rail seam (honest — not halos, not zero effect)"
    - "M6 passes: pixel sample directly under each hole card and each chip stack is >= 12% darker (luma) than adjacent open felt at hero shot with ?fx"
    - "M7 passes: grep-check-tp5-06.cjs exits 0 (Bloom still absent); no Bloom token in lab source"
    - "M11 measured: N8AO delta recorded (baseline ?fx-no-effects vs ?fx-N8AO-only); halfRes={true} applied if delta breaches M11 floor"
    - "?fx-off path renders identically to TP5 (N8AO not active when ?fx absent)"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "N8AO added inside EffectComposer; aoRadius/intensity/distanceFalloff/halfRes props set; starting params from RESEARCH Pattern 2"
      contains: "N8AO"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx N8AO"
      to: "EffectComposer scaffold (07-01)"
      via: "N8AO added as first child of EffectComposer inside the qp('fx') guard; declaration order: N8AO first (crevice-first stack)"
      pattern: "N8AO"
---

<objective>
Add N8AO (screen-space ambient occlusion with temporal denoising) as the first effect inside
the EffectComposer scaffold. This is the primary satisfier of the M6 crevice-darkening metric
that has been owed since TP5. Tune aoRadius/distanceFalloff to eliminate halos while preserving
honest darkening under cards/chips/rail. Measure M11 perf impact in isolation.

Purpose: N8AO grounds every surface element — hole cards, chip stacks, felt-to-rail seam — with
honest contact/crevice AO that SoftShadows + ContactShadows do not produce (those are directional
shadow maps; N8AO is screen-space depth-buffer sampling). This is red-team #2 satisfied.

Output: TableLab.tsx with N8AO at starting params; M6 PASS confirmed at hero shot with ?fx;
M11 N8AO delta recorded; halfRes applied if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add N8AO inside EffectComposer with starting params; verify prop names from installed source</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - .planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md — the prop name
      verification findings from Task 1 of 07-01. If aoRadius, distanceFalloff, halfRes were
      confirmed present in the installed dist, use those prop names exactly. If any prop name
      differs, use the verified alternative from that SUMMARY.
    - 07-RESEARCH.md "Pattern 2: N8AO — honest crevice darkening" — the halo-vs-no-effect axis
      explanation, the aoRadius 0.8 starting value, distanceFalloff 0.7, intensity 2.0, halfRes
      false, screenSpaceRadius false. Also the M6 threshold (>=12% luma delta).
    - 07-RESEARCH.md "Pitfall 3: N8AO halos" — what to watch for and the fix direction.
    - 07-RESEARCH.md "Pitfall 4: Postprocessing fps drop" — per-effect isolation measurement.
    - frontend/src/lab/TableLab.tsx — locate the EffectComposer scaffold added in 07-01 (the
      qp("fx") block); N8AO goes as the FIRST child inside EffectComposer.
  </read_first>
  <action>
    STEP 1 — Verify N8AO prop names from installed source (before writing JSX):
      grep -r "aoRadius\|distanceFalloff\|halfRes\|screenSpaceRadius" frontend/node_modules/@react-three/postprocessing/dist/ 2>/dev/null | head -10

    If prop names confirmed: proceed with the RESEARCH pattern. If any prop name differs from
    the RESEARCH, use the confirmed name and note the discrepancy in the SUMMARY.

    STEP 2 — Update the import line added in 07-01:
      FROM: import { EffectComposer } from "@react-three/postprocessing";
      TO:   import { EffectComposer, N8AO } from "@react-three/postprocessing";

    STEP 3 — Add N8AO as the FIRST child inside the EffectComposer scaffold:

      {qp("fx") !== null && (
        <EffectComposer multisampling={4} enableNormalPass={false}>
          <N8AO
            aoRadius={0.8}
            intensity={2.0}
            distanceFalloff={0.7}
            halfRes={false}
            screenSpaceRadius={false}
          />
          {/* DOF — added in 07-03 */}
          {/* Vignette / Grade / Noise — added in 07-04 */}
        </EffectComposer>
      )}

    Starting params (per RESEARCH Pattern 2 + SSOT §TP6 range):
    - aoRadius={0.8} — mid-range of SSOT 0.5-1.5; targets crevice scale without halo
    - intensity={2.0} — mid-range of SSOT 1.5-3; artistic darkening strength
    - distanceFalloff={0.7} — reduces halos; RESEARCH recommends 0.5-0.7 sweet spot
    - halfRes={false} — full-res first; flip to true only if M11 breaches (perf fallback)
    - screenSpaceRadius={false} — world-space radius (matches SSOT "world units" spec)

    STEP 4 — Capture and evaluate at hero shot with ?fx:
      Ensure dev server is running (cd frontend && npm run dev if not already up).
      Take a capture:
        LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/n8ao-hero.png
      Visually inspect the capture: honest crevice darkening under hole cards, chip stacks, and
      the felt-to-rail seam? Any halos around large objects (rail, table body)?
      - If halos visible: lower aoRadius to 0.6 or lower distanceFalloff to 0.5.
      - If no visible effect: raise intensity to 2.5 or raise aoRadius to 1.0.
      Capture all 3 shots for the SUMMARY record:
        LAB_URL="http://localhost:5173/table-lab.html?cam=card&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/n8ao-card.png
        LAB_URL="http://localhost:5173/table-lab.html?cam=macro&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/n8ao-macro.png

    STEP 5 — M6 measurement (pixel luma check):
      Run M6 checker against the n8ao-hero.png capture:
        node tools/table-3d/run-metrics.mjs .dev-stack/diag/tp6/n8ao-hero.png
      (Or whichever M6-capable tool is in tools/table-3d/ that accepts a file path; if run-metrics
      requires a directory, place the capture in a staging dir or inspect the M6 luma manually:
      sample a 4x4px region directly under a hole card vs. adjacent felt in the capture.)
      Confirm M6 >= 12% luma delta (under object vs. adjacent open felt).

    STEP 6 — M11 N8AO isolation measurement (per-effect perf gate defense):
      With the dev server showing the hero shot with N8AO active (?fx&cam=hero):
        node tools/table-3d/stats-read.mjs http://localhost:5173 hero
      Compare against the TP5 baseline draw count from 06-05-SUMMARY. Record:
        - Baseline (no ?fx): [value from TP5 / or re-measure without ?fx]
        - + EffectComposer only (no effects, scaffold from 07-01): [delta]
        - + N8AO at fullRes: [total and delta]
      If N8AO delta breaches M11 floor (median frame-time > 8ms, or draw count suggests): flip
      halfRes={true} and re-measure. Record the halfRes decision in the SUMMARY.

    Commit: "feat(07-02): add N8AO aoRadius=0.8 inside EffectComposer ?fx (M6 crevice AO)"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(!/<N8AO/.test(s)){console.error('FAIL: N8AO not in non-comment code');process.exit(1);} console.log('OK — N8AO present in TableLab.tsx');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(/Bloom/.test(s)){console.error('FAIL: Bloom detected — M7 HARD gate');process.exit(1);} console.log('OK — no Bloom in TableLab.tsx');"</automated>
  </verify>
  <done>N8AO present in TableLab.tsx non-comment code inside EffectComposer; M6 PASS (>=12% luma delta under hole cards at hero shot with ?fx); M7 PASS (no Bloom); M11 N8AO delta recorded (halfRes decision documented); tsc clean; vitest green. Captures at .dev-stack/diag/tp6/n8ao-{hero,card,macro}.png committed. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only.

| Boundary | Description |
|----------|-------------|
| N8AO prop names (A4 assumption) | Prop names verified from installed source before JSX is written; if different, use confirmed names |
| M6 threshold | Luma comparison run after capture; >= 12% delta is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02a | Tampering | N8AO halos (aoRadius too large) | mitigate | Start at 0.8; visually inspect capture; lower to 0.6 + distanceFalloff to 0.5 if halos visible; M6 checker as secondary confirmation |
| T-07-02b | Tampering | N8AO no-effect (aoRadius too small or distanceFalloff too low) | mitigate | Start at 0.8; if M6 fails (< 12% delta), raise intensity to 2.5 or aoRadius to 1.0; SSOT range top is 1.5 |
| T-07-02c | Tampering | M11 breach from full-res N8AO | mitigate | Measure N8AO delta in isolation per RESEARCH Pitfall 4 protocol; flip halfRes={true} if frame-time > 8ms; record the decision |
| T-07-02d | Tampering | Bloom accidentally imported | mitigate | grep-check-tp5-06.cjs CHECK 5 catches Bloom; tsc would error on unused import; explicit verify step |
| T-07-SC | Tampering | npm installs | accept | No new packages installed in this plan; packages from 07-01 already installed |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp5-06.cjs exits 0 (Bloom still absent, CHECK 5 relaxed).
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- N8AO present in TableLab.tsx non-comment code (verified by inline node check).
- M6 luma delta >= 12% at hero shot with ?fx confirmed from n8ao-hero.png capture.
- M11 N8AO isolation delta recorded (halfRes decision documented in SUMMARY).
- ?fx-off path visually identical to TP5 (N8AO not active).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- N8AO mounted inside EffectComposer behind ?fx; starting params from RESEARCH; prop names verified from installed source.
- M6 PASS at hero shot with ?fx (>= 12% luma delta under objects vs adjacent felt).
- M7 PASS (no Bloom anywhere in lab source). M11 delta recorded; halfRes decision documented.
- tsc clean; vitest green. Captures committed. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-02-SUMMARY.md` when done.
Include: final N8AO params (aoRadius/intensity/distanceFalloff/halfRes after tuning), M6 measurement
(luma delta value and which regions sampled), M11 N8AO delta, halfRes decision, capture paths.
These values are the input contract for 07-06 (full metric suite) and the operator gate (07-07).
</output>
