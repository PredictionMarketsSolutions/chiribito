---
phase: 07-tp6-profundidad-composicion-depth
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - frontend/src/lab/TableLab.tsx
autonomous: true
requirements:
  - "SSOT §TP6 — Vignette: offset 0.3-0.5, darkness 0.5-0.8; M8 band 8-20% (corner luma 8-20% below center luma)"
  - "SSOT §TP6 — tune EXISTING fog args (near only, within frozen backdrop rule); far rail reads as air"
  - "SSOT §TP6 — filmic grade + grain: BrightnessContrast brightness 0.03/contrast 0.05 (+A warm-lifted shadows); Noise opacity 0.02-0.05; M9 determinism verified"
  - "SSOT §TP6 — NO Bloom mounted (M7 HARD gate); +A warm corner floor PASS; Noise is cut-first candidate if M9 determinism fails"

must_haves:
  truths:
    - "With ?fx, corners are measurably darker than center luma — delta 8-20% (M8 PASS)"
    - "+A passes: corner luma > 0 (not crushed black), corner hue warm (not neutral grey)"
    - "M9 determinism: two consecutive hero captures with ?fx&spin=off are byte-identical (md5 match)"
    - "M7 PASS: no Bloom mounted anywhere in lab source (grep-check exits 0)"
    - "Existing fog near distance tuned if far rail reads sharp (unchanged far value; unchanged color #141009)"
    - "If M9 fails (Noise is time-seeded): Noise opacity lowered to 0.02 first; if still fails, Noise cut and disposition recorded"
  artifacts:
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "Vignette + BrightnessContrast + Noise added inside EffectComposer; fog near possibly tuned"
      contains: "Vignette"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx Vignette"
      to: "M8 metric (corner vs center luma)"
      via: "Vignette offset=0.4, darkness=0.6 starting values; M8 checker confirms 8-20% delta"
      pattern: "Vignette"
    - from: "frontend/src/lab/TableLab.tsx BrightnessContrast"
      to: "+A metric (warm shadow floor)"
      via: "BrightnessContrast brightness=0.03 lifts shadow floor before Vignette darkens corners; reconciles M8 + +A"
      pattern: "BrightnessContrast"
---

<objective>
Add the remaining three postprocessing effects — Vignette (frame darkening, M8), BrightnessContrast
(warm grade, +A), and Noise (grain, M9 determinism check) — and tune the existing fog near
distance if needed. This completes the postprocessing stack inside the EffectComposer.

Purpose: Vignette frames the table action; BrightnessContrast provides the warm +A lift; Noise
adds the faint filmic grain that makes the scene feel captured rather than rendered. Tune order
matters: BrightnessContrast first (warm shadow floor), then Vignette (corner darkening), then
verify M8 + +A pass simultaneously. Noise is the weakest perceptual effect and the first to cut
if M9 determinism fails.

Output: TableLab.tsx with full stack (N8AO + DOF-or-cut + Vignette + BrightnessContrast + Noise);
M8 PASS; +A PASS; M9 determinism PASS; M7 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vignette + BrightnessContrast + Noise; tune fog; verify M8 + +A + M9</name>
  <files>frontend/src/lab/TableLab.tsx</files>
  <read_first>
    - 07-RESEARCH.md "Pattern 4: Vignette + Fog tune" — Vignette props (offset/darkness/eskil),
      M8 metric description (corner vs center luma delta 8-20%), fog tuning rule (near inward if
      far rail reads sharp; DO NOT change far or color), M8 + +A tension resolution order
      (BrightnessContrast first, then Vignette, verify both simultaneously).
    - 07-RESEARCH.md "Pattern 5: Filmic Grade (BrightnessContrast + optional HueSaturation)" —
      BrightnessContrast props (brightness=0.03, contrast=0.05); Noise props (opacity=0.03,
      premultiplied=false); M9 determinism note (UV-seeded vs time-seeded; verify empirically;
      fallback: lower opacity to 0.02; cut if still fails).
    - frontend/src/lab/TableLab.tsx — locate: (a) the existing fog element (confirm args are
      ["#141009",20,60] — near=20 is the value to potentially tune inward to 18 or 16); (b) the
      EffectComposer block with N8AO + DOF from 07-02/07-03; effects order is N8AO → DOF →
      Vignette → BrightnessContrast → Noise.
    - tools/table-3d/m9-determinism.mjs — first 30 lines to confirm the invocation pattern for M9
      (two captures + md5 compare), or infer from the mjs filename what it does.
    - 07-RESEARCH.md "Metric-Gate Assertions" — M8 threshold (delta 8-20%), M9 method (two
      consecutive captures md5 must match), +A threshold (corner luma > 0 + warm hue).
  </read_first>
  <action>
    STEP 1 — Update import line to add Vignette, BrightnessContrast, Noise:
      import { EffectComposer, N8AO, DepthOfField, Vignette, BrightnessContrast, Noise }
        from "@react-three/postprocessing";
      (If DepthOfField was cut in 07-03, omit it from this import.)

    STEP 2 — Add Vignette, BrightnessContrast, Noise inside EffectComposer in this declaration order
    (BrightnessContrast before Vignette per the M8+A tuning order from Pattern 4):

      {qp("fx") !== null && (
        <EffectComposer multisampling={4} enableNormalPass={false}>
          <N8AO {/* params from 07-02 */} />
          {/* DOF here if not cut in 07-03 */}
          <BrightnessContrast
            brightness={0.03}
            contrast={0.05}
          />
          <Vignette
            offset={0.4}
            darkness={0.6}
            eskil={false}
          />
          <Noise
            opacity={0.03}
            premultiplied={false}
          />
        </EffectComposer>
      )}

    Starting params (per RESEARCH Pattern 4 + Pattern 5):
    - BrightnessContrast: brightness=0.03 (faint warm shadow lift; +A), contrast=0.05 (gentle
      highlight roll-off). Tune brightness UP to 0.05 if +A corner hue reads neutral; do NOT
      exceed 0.1 (Instagram filter threshold).
    - Vignette: offset=0.4 (where darkening begins; 0=center, 1=edge), darkness=0.6 (how dark
      corners get). If M8 delta < 8%: raise darkness to 0.7. If M8 delta > 20%: lower to 0.5.
      eskil=false (standard radial vignette).
    - Noise: opacity=0.03 (mid-range of SSOT 0.02-0.05). If M9 fails: lower to 0.02 first.
      premultiplied=false.

    STEP 3 — Fog tune (if needed):
    Capture hero shot with ?fx and inspect the far rail area. If the far rail reads "hard/sharp":
      Change fog args from ["#141009",20,60] to ["#141009",18,60].
      (Only change near; keep far=60 and color="#141009" unchanged — frozen backdrop rule.)
    If far rail already reads as air (soft, warm distance) at near=20: leave unchanged.

    STEP 4 — Capture and measure M8:
      LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/grade-hero.png
      node tools/table-3d/run-metrics.mjs .dev-stack/diag/tp6/grade-hero.png
    M8 check: mean corner luma vs center luma delta must be 8-20%.
    M8 < 8%: raise Vignette darkness to 0.7.
    M8 > 20%: lower Vignette darkness to 0.5.
    +A check: corner luma > 0 (not crushed), corner hue warm. If corners read neutral grey:
      raise BrightnessContrast brightness to 0.05 and re-check.
    Repeat M8 + +A tuning until both pass simultaneously (max 2 iterations; if cannot reconcile
    at legal params, prefer M8 and note +A near-miss in SUMMARY — Vignette M8 is the harder gate).

    STEP 5 — M9 determinism (Noise grain freeze):
      Two consecutive captures:
        LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/m9-a.png
        LAB_URL="http://localhost:5173/table-lab.html?cam=hero&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/m9-b.png
      If tools/table-3d/m9-determinism.mjs exists, run it:
        node tools/table-3d/m9-determinism.mjs .dev-stack/diag/tp6/m9-a.png .dev-stack/diag/tp6/m9-b.png
      Otherwise compare md5 hashes directly:
        node -e "const c=require('crypto'),f=require('fs'); const ha=c.createHash('md5').update(f.readFileSync('.dev-stack/diag/tp6/m9-a.png')).digest('hex'); const hb=c.createHash('md5').update(f.readFileSync('.dev-stack/diag/tp6/m9-b.png')).digest('hex'); if(ha!==hb){console.error('FAIL: M9 — captures differ (md5: '+ha+' vs '+hb+')');process.exit(1);} console.log('OK — M9 determinism PASS');"
      If M9 FAILS (md5 mismatch): lower Noise opacity to 0.02 and re-test M9.
      If still fails at 0.02: CUT Noise from the compositor. Remove Noise from the EffectComposer
      children and from the import line. Record disposition "Noise cut — M9 determinism failure
      (time-seeded noise not deterministic at opacity 0.02-0.03 in this version)." The table ships
      without Noise; the other effects remain.

    STEP 6 — M7 verify (no Bloom):
      node tools/table-3d/grep-check-tp5-06.cjs

    Capture all 3 shots for the full suite in 07-06:
      LAB_URL="http://localhost:5173/table-lab.html?cam=card&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/grade-card.png
      LAB_URL="http://localhost:5173/table-lab.html?cam=macro&fx&spin=off" node .dev-stack/lab-shot.mjs .dev-stack/diag/tp6/grade-macro.png

    Commit: "feat(07-04): add Vignette+BrightnessContrast+Noise; tune fog; M8+A+M9 PASS"
    OR if Noise cut: "feat(07-04): add Vignette+BrightnessContrast; Noise cut (M9); M8+A PASS"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n'); if(!/<Vignette/.test(s)){console.error('FAIL: Vignette not in non-comment code');process.exit(1);} if(/Bloom/.test(s)){console.error('FAIL: Bloom detected — M7 HARD gate');process.exit(1);} console.log('OK — Vignette present, no Bloom');"</automated>
  </verify>
  <done>Vignette + BrightnessContrast present in TableLab.tsx non-comment code inside EffectComposer; Noise present (or cut with explicit disposition); M8 PASS (delta 8-20%); +A PASS (corner luma > 0, warm hue); M9 determinism PASS (or Noise cut with reason); M7 PASS (no Bloom); fog near tuned if needed; tsc clean; vitest green. Captures at .dev-stack/diag/tp6/grade-{hero,card,macro}.png. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only.

| Boundary | Description |
|----------|-------------|
| M8 + +A tension | BrightnessContrast must lift shadow floor before Vignette darkens corners; tune order is critical |
| M9 Noise determinism (A3 assumption) | UV-seeded per RESEARCH; must be verified empirically; Noise is the cut-first candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04a | Tampering | M8 delta > 20% (over-vignetted / casino-vignette) | mitigate | Start at darkness=0.6; lower to 0.5 if M8 > 20%; M8 checker confirms; the 8-20% band is absolute |
| T-07-04b | Tampering | +A corners crushed black (vignette too dark + no warm lift) | mitigate | BrightnessContrast before Vignette in composition stack; brightness=0.03 lifts shadow floor first; raise to 0.05 if corner hue reads neutral |
| T-07-04c | Tampering | Noise M9 determinism failure (time-seeded grain) | mitigate | Verify M9 empirically with two consecutive captures; lower opacity to 0.02 first; cut Noise if still fails; Noise is the weakest perceptual effect — cutting it does not affect M6/M7/M8/M1 |
| T-07-04d | Tampering | Fog far distance or color changed (frozen backdrop violation) | mitigate | Only fog near may be tuned (20→18 at most); far=60 and color="#141009" are FROZEN; explicit read_first confirms current args |
| T-07-04e | Tampering | Bloom accidentally imported alongside Vignette/BrightnessContrast/Noise | mitigate | Import line adds only Vignette, BrightnessContrast, Noise; grep-check-tp5-06 CHECK 5 catches Bloom |
| T-07-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- node tools/table-3d/grep-check-tp5-06.cjs exits 0 (M7 PASS — Bloom absent).
- cd frontend && npx tsc --noEmit clean.
- cd frontend && npx vitest run src/lab/ green.
- Vignette present in TableLab.tsx non-comment code.
- M8 PASS: corner vs center luma delta 8-20% at hero shot with ?fx.
- +A PASS: corner luma > 0 + warm hue at hero shot with ?fx.
- M9 PASS: two consecutive hero captures with ?fx md5-identical (or Noise cut with disposition).
- Fog near tuned if needed (far and color unchanged).
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- Full postprocessing stack present in EffectComposer: N8AO → (DOF-or-cut) → BrightnessContrast → Vignette → (Noise-or-cut).
- M8 PASS (8-20% delta). +A PASS. M9 PASS (or Noise cut). M7 PASS.
- tsc clean. vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-04-SUMMARY.md` when done.
Include: final Vignette params (offset/darkness), BrightnessContrast params, Noise status (present
at opacity / or cut with reason), fog near value (changed or unchanged), M8 measurement (delta %),
+A measurement (corner luma + hue), M9 verdict (PASS or cut), captures committed. This is the
input contract for 07-06 (full metric suite) and the operator gate (07-07).
</output>
