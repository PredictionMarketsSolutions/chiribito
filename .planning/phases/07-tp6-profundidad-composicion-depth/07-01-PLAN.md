---
phase: 07-tp6-profundidad-composicion-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/lab/TableLab.tsx
  - tools/table-3d/grep-check-tp5-06.cjs
autonomous: true
requirements:
  - "SSOT §TP6 — install @react-three/postprocessing@2.19.1 + postprocessing@^6.39.1 (first time; neither currently present)"
  - "SSOT §TP6 — EffectComposer multisampling={4} enableNormalPass={false} behind ?fx guard (qp('fx') !== null)"
  - "SSOT §TP6 — grep-check transition: relax tp5-06 CHECK 5 from no-EffectComposer to no-Bloom only (TP6 supersedes)"
  - "SSOT §TP6 — ?fx default OFF; off-path must restore exact pre-TP6 look; LOCAL only"

must_haves:
  truths:
    - "npm install succeeds; @react-three/postprocessing@2.19.1 and postprocessing present in frontend/node_modules"
    - "TableLab.tsx renders without console errors at http://localhost:5173/table-lab.html (no ?fx param — default path, pre-TP6 look unchanged)"
    - "EffectComposer is mounted (transparent pass-through, no effects yet) when ?fx is present in the URL"
    - "grep-check-tp5-06.cjs exits 0 after the Check 5 relaxation (EffectComposer no longer causes false failure)"
    - "cd frontend && npx tsc --noEmit clean under src/lab; cd frontend && npx vitest run src/lab/ green"
  artifacts:
    - path: "frontend/package.json"
      provides: "@react-three/postprocessing@2.19.1 and postprocessing@^6.39.1 in dependencies"
      contains: "@react-three/postprocessing"
    - path: "frontend/src/lab/TableLab.tsx"
      provides: "EffectComposer scaffold behind qp('fx') !== null; no effects mounted yet"
      contains: "EffectComposer"
    - path: "tools/table-3d/grep-check-tp5-06.cjs"
      provides: "Relaxed CHECK 5 — checks no-Bloom only (not no-EffectComposer)"
      contains: "Bloom"
  key_links:
    - from: "frontend/src/lab/TableLab.tsx"
      to: "@react-three/postprocessing"
      via: "import { EffectComposer } from '@react-three/postprocessing'; guarded by qp('fx') !== null"
      pattern: "EffectComposer"
    - from: "tools/table-3d/grep-check-tp5-06.cjs CHECK 5"
      to: "frontend/src/lab/TableLab.tsx"
      via: "relaxed regex: /Bloom/.test() only — EffectComposer presence no longer triggers fail"
      pattern: "Bloom"
---

<objective>
Install the two postprocessing packages for the first time and wire the EffectComposer scaffold
behind the ?fx URL flag — no effects yet, just the transparent pass-through. Simultaneously
relax the TP5 grep-check CHECK 5 so it no longer false-fails when EffectComposer is present.

Purpose: TP6 needs the compositor present before any effect can be added. This plan establishes
the ?fx conditional mount, verifies the default (no ?fx) path is byte-identical to TP5, and
transitions the TP5 invariant check so it only guards against Bloom (not EffectComposer).

Output: frontend/package.json + lock updated; TableLab.tsx with EffectComposer scaffold; relaxed
grep-check-tp5-06.cjs; confirmed scene renders cleanly in both ?fx-off (default) and ?fx-on paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-CONTEXT.md
@.planning/phases/07-tp6-profundidad-composicion-depth/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @react-three/postprocessing@2.19.1 + postprocessing@^6.39.1</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json — confirm current deps: @react-three/fiber@^8.17.10, three@^0.169.0,
      react@^18; confirm @react-three/postprocessing and postprocessing are ABSENT.
    - 07-RESEARCH.md "Package Legitimacy Audit" — both packages slopcheck [OK]; no postinstall
      scripts; N8AO is bundled inside @react-three/postprocessing (do NOT install n8ao separately).
    - 07-RESEARCH.md "Standard Stack" — peer dep verification commands.
  </read_first>
  <action>
    Run the install from the frontend/ directory (exact command, pinned versions as researched):

      cd frontend && npm install @react-three/postprocessing@2.19.1 postprocessing@^6.39.1

    After install, verify peer deps resolved without conflicts:
      npm view @react-three/postprocessing@2.19.1 peerDependencies
      (expected: react ^18.0, three >= 0.138.0, @react-three/fiber ^8.0 — all satisfied)

    Verify N8AO is bundled (do NOT install separately):
      ls frontend/node_modules/@react-three/postprocessing/node_modules/n8ao 2>/dev/null || echo "N8AO bundled at root node_modules"

    Verify prop names from installed source before writing any JSX in Task 2:
      grep -r "worldFocusDistance" frontend/node_modules/@react-three/postprocessing/dist/ 2>/dev/null | head -3
      grep -r "aoRadius\|distanceFalloff" frontend/node_modules/@react-three/postprocessing/dist/ 2>/dev/null | head -3
      grep -r "halfRes" frontend/node_modules/@react-three/postprocessing/dist/ 2>/dev/null | head -3

    Record the prop name findings in the SUMMARY (A4/A5/A1/A2 assumption verification from
    RESEARCH.md Assumptions Log). If worldFocusDistance is absent, note the fallback prop name
    (focusDistance with computed normalized value). Do NOT block on this — proceed with the
    verified prop names; the effect JSX is authored in 07-02 and 07-03, not here.

    This is the ONLY npm install in the TP6 program. LOCAL only — no push/deploy/merge.
  </action>
  <verify>
    <automated>cd frontend && node -e "require('@react-three/postprocessing'); console.log('OK — @r3f/postprocessing importable')"</automated>
    <automated>cd frontend && node -e "require('postprocessing'); console.log('OK — postprocessing importable')"</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Both packages importable from frontend/node_modules; package.json lists @react-three/postprocessing@2.19.1 and postprocessing entries; tsc --noEmit clean. Prop name findings (worldFocusDistance / aoRadius / halfRes presence) recorded in SUMMARY for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Wire EffectComposer scaffold behind ?fx in TableLab.tsx + relax grep-check-tp5-06 CHECK 5</name>
  <files>frontend/src/lab/TableLab.tsx, tools/table-3d/grep-check-tp5-06.cjs</files>
  <read_first>
    - frontend/src/lab/TableLab.tsx — read the FULL file. Locate: (a) the existing qp() helper
      invocations (?chips, ?card, ?light, ?rail, ?spin precedents); (b) the fog element (args
      confirm ["#141009",20,60] to leave unchanged); (c) where scene content ends (the last
      geometry group closing tag) — EffectComposer must be placed AFTER all scene content, as a
      sibling; (d) existing imports block at top.
    - tools/table-3d/grep-check-tp5-06.cjs — read the FULL file; locate CHECK 5 exactly (the
      Bloom|EffectComposer regex and its fail message) — must change ONLY that regex.
    - 07-RESEARCH.md "Pattern 1: EffectComposer mount behind ?fx" — the exact JSX pattern and
      placement rule. Note: EffectComposer MUST be AFTER all scene content in JSX tree; MUST NOT
      be inside a per-frame conditional.
    - 07-RESEARCH.md "Grep-Check Transition" — the exact old regex to replace and the new one.
  </read_first>
  <action>
    PART A — TableLab.tsx: add EffectComposer scaffold.

    1. Add to the import block at the top:
         import { EffectComposer } from "@react-three/postprocessing";
       Do NOT import any effect components yet (N8AO, DepthOfField, etc. are added in 07-02/03/04).

    2. Inside the Scene component (or whatever the root R3F component is), AFTER all geometry
       groups and lights, add the conditional mount:

         {qp("fx") !== null && (
           <EffectComposer
             multisampling={4}
             enableNormalPass={false}
           >
             {/* Effects added in 07-02 through 07-04 */}
           </EffectComposer>
         )}

       Placement rule: the EffectComposer must be the LAST element in the JSX tree, after all
       scene content — it is a sibling of Table/Cards/Chips/Lights, never a parent. This matches
       the structural note in RESEARCH "Key structural rule" and Pitfall 7 (no per-frame toggle).

    3. ?fx flag semantics (Claude's discretion, per RESEARCH Open Question 3):
       - Default: ?fx ABSENT = composer NOT mounted (exact pre-TP6 look, byte-identical to TP5).
       - ?fx present (any value): composer mounted (effects added in subsequent plans).
       - Document the flag in a comment near the conditional.

    4. Do NOT change any existing fog, SoftShadows, ContactShadows, lights, geometry, or
       camera/control params. The ?fx=off path must be identical to the pre-TP6 render.

    PART B — tools/table-3d/grep-check-tp5-06.cjs: relax CHECK 5.

    Find the existing CHECK 5 block (the line containing /Bloom|EffectComposer/.test). Change
    ONLY the regex from /Bloom|EffectComposer/ to /Bloom/ (remove EffectComposer from the
    banned list). Update ONLY the fail message text to reflect the relaxation:

      OLD regex: /Bloom|EffectComposer/.test(nonComment)
      NEW regex: /Bloom/.test(nonComment)

      OLD fail msg: "...all postprocessing deferred to TP6..."
      NEW fail msg: "CHECK 5 FAILED — Bloom detected in lab source. M7 HARD gate: Bloom is
        permanently banned (casino trap + perf sink). EffectComposer is now permitted (TP6 adds
        it). Only Bloom remains forbidden."

    Update the one-line OK message for CHECK 5 to note the relaxation:
      "(5) No Bloom in frontend/src/lab/ (M7 PASS — Bloom banned; EffectComposer now permitted per TP6)"

    Do NOT change CHECK 1-4 or CHECK 6. The overall exit-0 ok-line may mention "relaxed in TP6"
    for check 5. No other changes to grep-check-tp5-06.cjs.

    Commit: "feat(07-01): wire EffectComposer scaffold behind ?fx; relax tp5-06 check-5 to no-bloom"
  </action>
  <verify>
    <automated>node tools/table-3d/grep-check-tp5-06.cjs</automated>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run src/lab/</automated>
    <automated>node -e "const s=require('fs').readFileSync('frontend/src/lab/TableLab.tsx','utf8'); if(!/EffectComposer/.test(s)) { console.error('FAIL: EffectComposer not found in TableLab.tsx'); process.exit(1); } console.log('OK — EffectComposer present in TableLab.tsx');"</automated>
  </verify>
  <done>grep-check-tp5-06.cjs exits 0 with relaxed CHECK 5 (EffectComposer present does not fail; only Bloom still fails). TableLab.tsx imports EffectComposer; the ?fx conditional mount is in place AFTER all scene geometry. tsc clean; vitest green. The ?fx-off path (no ?fx param) renders identically to TP5 — no effects, no visual change. LOCAL only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — isolated lab render, dev-server-only. No untrusted input, no auth, no API.

| Boundary | Description |
|----------|-------------|
| npm install supply chain | Two new packages installed; slopcheck [OK] both; no postinstall scripts |
| EffectComposer conditional placement | Must be AFTER all scene content; must not be per-frame toggled (shader recompile storm) |
| grep-check CHECK 5 relaxation | Must only relax EffectComposer from the banned list; Bloom must remain banned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01a | Tampering | npm install supply chain | mitigate | Both packages [OK] per slopcheck 0.6.1 at RESEARCH time (2026-06-12); no postinstall scripts confirmed; blocking human checkpoint not required for [OK] packages |
| T-07-01b | Tampering | EffectComposer per-frame toggle (shader recompile storm) | mitigate | qp("fx") reads window.location.search at page load — static value, not per-frame state; EffectComposer mounts once at load or not at all; mirrors the safe ?chips/?light/?card/?rail pattern already in codebase |
| T-07-01c | Tampering | N8AO installed separately (Shader Error 0 regression) | mitigate | N8AO is bundled inside @react-three/postprocessing@2.19.1 as n8ao@^1.6.6; installer must NOT run npm install n8ao separately |
| T-07-01d | Tampering | CHECK 5 relaxation too broad (all postprocessing banned regex removed) | mitigate | Only /Bloom/ stays banned; /EffectComposer/ removed from CHECK 5 regex; Bloom ban is the M7 HARD gate and remains intact in both grep-check-tp5-06 (CHECK 5) and grep-check-tp6-07 (CHECK 5) |
| T-07-SC | Tampering | npm installs | mitigate | slopcheck + Package Legitimacy Audit verified [OK] — no blocking checkpoint required; recorded in RESEARCH Legitimacy Audit table |
</threat_model>

<verification>
- cd frontend && npm list @react-three/postprocessing postprocessing — both present at correct versions.
- node tools/table-3d/grep-check-tp5-06.cjs — exits 0 (CHECK 5 relaxed; Bloom still banned).
- cd frontend && npx tsc --noEmit — clean (no type errors under src/lab/).
- cd frontend && npx vitest run src/lab/ — all tests green.
- Dev server renders http://localhost:5173/table-lab.html (no ?fx) identically to TP5 — no visual change.
- Dev server renders http://localhost:5173/table-lab.html?fx (with ?fx) without console errors — EffectComposer mounted, transparent pass-through.
- LOCAL only — no push/deploy/merge.
</verification>

<success_criteria>
- @react-three/postprocessing@2.19.1 and postprocessing present in frontend/node_modules.
- TableLab.tsx has EffectComposer scaffold (no effects) behind qp("fx") !== null.
- grep-check-tp5-06.cjs exits 0 after CHECK 5 relaxation.
- tsc clean; vitest green. LOCAL only.
</success_criteria>

<output>
Create `.planning/phases/07-tp6-profundidad-composicion-depth/07-01-SUMMARY.md` when done.
Include: exact installed versions (from package.json post-install), prop name verification
findings (worldFocusDistance / aoRadius / halfRes — confirmed or fallback noted), grep-check-tp5-06
CHECK 5 old vs new regex diff, and confirmation that ?fx-off path is visually identical to TP5.
These findings are the input contract for 07-02 (N8AO) and 07-03 (DOF).
</output>
